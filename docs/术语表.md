# 术语表

> **AI 修改说明**：修改本文档前先读 `docs/AI协作/文档维护指南.md`。
> 本文档是项目术语、别名和中英文检索词的权威索引；新增系统、玩法名、工具名、缩写或英文别名时，必须同步 `docs/_kb_index.json` 的相关关键词，必要时同步 `docs/词表与契约.md`。

---

## 1. 用法

- AI 搜索前先用本表扩展关键词，避免只搜中文或只搜英文导致漏结果。
- 本文档不登记约定字符串白名单；正式 id 仍以 `docs/词表与契约.md` 为准。
- 本文档允许记录同义词、缩写、口语说法和英文 API 语境。

## 2. 核心玩法术语

| 标准术语 | 常见别名 / 英文 | 说明 | 权威来源 |
|----------|----------------|------|----------|
| 俯视角射击刷宝生存 | top-down looter shooter survival, top-down action looter, 射击刷宝游戏, 俯视刷子射击 | 当前游戏类型定位；核心操作是移动、瞄准和按住 `fire` action 持续射击，装备、词条、技能和遗物优先服务枪线与射击构筑；玩法判定与显示都按 2D 平面处理，`Camera2D` 保持水平、等比缩放和玩家居中，不再用 `Camera3D` 正交投影、低模 3D 视觉层、相机变形或菱形 / 等距地图格模拟斜俯视 | GDD, ADR #125, ADR #124, ADR #109, ADR #98 |
| 量化矩形地图格 | rectangular map grid, square top-down grid, quantized rectangular grid | 当前地图的地面度量：`map_layouts.json.grid.cell_width/cell_height` 定义单个矩形格的水平宽度 / 垂直高度；bounds 是格尺寸整数倍的轴对齐矩形，出生点吸附到格心，出生安全区可见提示贴住矩形格线，机关按 `radius_tiles` 奇偶吸附到合法锚点 | GDD §5, ADR #125 / #106, `docs/代码/map_manager.md` |
| 俯视资产落地规则 | top-down asset readability, top-down footprint, asset anchor point | 俯视角下的资产处理规则：贴地范围默认用矩形 / 方形俯视格或清晰俯视轮廓，机关和规则型地面 footprint 尺寸应为格子整数倍；角色、敌人、拾取物、子弹、障碍物和特效不强制矩形，但必须声明 footprint、中心 / 交互点、朝向读法、排序层和真实碰撞 / 触发形状 | GDD §8.2-A, ADR #125, ADR #124, `docs/代码/gameplay_runtime.md` |
| IP 美术风格 | art direction, IP art style, faction color language, 阵营色, 功能色, 兴趣点色彩 | 《破巢者》首个敌巢视觉方向：被异宇宙规则钙化出来的活体建筑；敌巢 / 虫族使用骨白、蜡黄、干肉粉、深红、黑紫和少量毒蓝，青 / 红 / 白归属虫族与敌巢，玩家和玩家子弹默认避开青 / 红 / 白，宝箱与地图兴趣点按功能色区分 | `docs/IP美术风格.md`, GDD §8.1, ADR #111 |
| 矩形机关 / 矩形危险地块 | rectangular hazard tile, top-down hazard tile | 通用 `Hazard` 的当前表现和触发区域：矩形是机关自己的危险地块，`hazards.csv.radius_tiles` 表示占用地图矩形格的整数倍；背景网格、机关绘制和触发判定共享同一份 grid cell size | GDD, ADR #125, ADR #103, `docs/代码/hazard_system.md` |
| 固定斜俯视 2.5D 刷宝动作生存 / 固定斜俯视 2.5D 射击刷宝生存 | fixed oblique 2.5D looter action survival, fixed oblique 2.5D looter shooter survival, isometric-like looter shooter | 历史视角口径，ADR #124 后不再作为当前正式视角；除引用 ADR #99~#104 等历史上下文外，后续面向当前设计应使用“俯视角射击刷宝生存” | ADR #99, ADR #100, ADR #101, ADR #102, ADR #104, ADR #109, ADR #124 |
| 破巢者 | Nestbreakers, 破巢行动 | 项目 IP 方向：多英雄主动突入跨宇宙敌方核心据点，在怪潮中夺取遗物并尝试打穿源头 | `docs/IP设定.md`, GDD §1.2, ADR #88 |
| 巢 / 敌巢 | enemy nest, stronghold, lair, core domain | 跨宇宙敌方在本宇宙建立或拖拽进来的核心据点、生产源头、通道锚点或意志中枢；不限定为虫巢，可表现为魔城、母厂、王庭、星舰核心、界门堡垒等 | `docs/IP设定.md`, GDD §1.2, ADR #88 |
| 巢核 | nest core, enemy core | 一局或一个敌巢主题的核心目标，可作为 Boss / 终点 / 局外资源来源或通道锚点的包装 | `docs/IP设定.md`, GDD §1.2, ADR #88 |
| 首都星域 | capital sector, capital star sector | 我方银河系星际文明被打散后仍相对稳定、能组织破巢行动的最后中心 | `docs/IP设定.md`, GDD §1.2, ADR #88 |
| 界门 | dimensional gate, inter-universe gate | 其他宇宙与本宇宙之间突然打开的通道；敌巢可作为界门锚点、补给门或扩张源 | `docs/IP设定.md`, GDD §1.2, ADR #88 |
| 遗物 | relic, passive item, 被动道具 | 被动构筑内容，数据驱动 | GDD §6, 词表 |
| 装备 Mod | gear mod, loadout mod, Warframe-like mod, 装备模组 | ADR #115 后的跨局构筑件：玩家刷取、升级、分解并装配到英雄 / 武器 loadout；首片替代旧永久升级轨道，不等同于 `ModLoader` 的玩家本地数据包 mod | GDD §7.2, ADR #115, `docs/代码/gear_mod_system.md` |
| 英雄 Mod配置 | hero mod loadout, hero loadout | 装备 Mod 的一套配置槽，影响英雄 / 玩家本体属性或能力；首片与武器 Mod 配置分开保存、校验 capacity / drain | GDD §7.2, ADR #115 |
| 武器 Mod配置 | weapon mod loadout, weapon loadout | 装备 Mod 的一套配置槽，影响武器基础属性或射击行为；首个测试 Mod 是提升武器基础伤害的武器 Mod | GDD §7.2, ADR #115 |
| 主动道具 | active item, usable item | 可充能释放的道具 | GDD §6 |
| 消耗品 | consumable | 一次性使用物品 | GDD §6 |
| 机关 | hazard, trap | 环境伤害或控制对象 | GDD §5.4 |
| 构筑 | build, synergy | 遗物 / 道具叠加形成的玩法组合 | GDD |
| 经验 | XP, experience, exp | 击杀掉落并用于升级的成长资源 | GDD §7.1 |
| 升级选择 | level-up choice, upgrade choice | 启用成长池的模式升级时弹出的候选奖励选择；常规为 3 选 1，`luck` 可概率 4 选 1；ADR #120 后默认标准模式暂不启用 | GDD §7.1, ADR #43, ADR #120 |
| 候选数量 | choice count, option count | 升级面板本次提供的选项数量，必须可回放确定 | GDD §7.1 |
| 局外成长 | meta progression, metaprogression, 元进度, 跨局成长 | 泛指跨局成长；ADR #115 后当前主路线改为装备 Mod / loadout，旧货币 + 永久升级轨道已按 ADR #117/#118 退役 | GDD §7.2, ADR #46, ADR #115, ADR #118 |
| 局外货币 | meta currency, persistent currency | 历史 F6 结算获得并用于购买永久升级的跨局资源；项目未上线，ADR #118 后旧测试档迁移 / 补偿不再维护 | ADR #46, ADR #118 |
| 永久升级轨道 | upgrade track, meta upgrade, progression node | 历史 F6 可逐级购买的局外成长节点；F11 后不再作为未来下一局属性来源，旧数据已按 ADR #118 删除 | ADR #46, ADR #118 |
| 账号等级 | account level, profile level | 跨局累计经验等级，授予解锁或开启新轨道 | GDD §7.2 |
| 解锁项 | unlock, unlock id | 角色、遗物池、模式、难度或开局选项的跨局解锁 | 词表 §13 |
| 鼠标瞄准 | mouse aim, cursor aim | 默认键鼠瞄准方式：按鼠标相对玩家 / 视口中心方向射击 | GDD, ADR #82 |
| 四方向瞄准 | 4-direction aim, cardinal aim, 4dir | 历史默认 / 当前兜底输入：上 / 下 / 左 / 右瞄准 | GDD, ADR #4, ADR #39, ADR #82 |
| 按住开火 | held fire, hold-to-fire, manual fire, trigger held | 当前主武器开火方式：按住 `fire` action 持续射击，默认鼠标左键 / 手柄右扳机；松开停火 | GDD, ADR #109, 词表 §7 |
| 自动开火 | auto fire, autofire | 历史开火口径：玩家不手动控制是否开火；已由按住开火覆盖，除历史 ADR / 日志外不要作为当前默认玩法描述 | ADR #109 |
| 玩家居中 | centered player, camera lock | 玩家固定在屏幕中心，世界滚动 | GDD, ADR #3 |

## 3. 输入与设备术语

| 标准术语 | 常见别名 / 英文 | 说明 | 权威来源 |
|----------|----------------|------|----------|
| 手柄 | gamepad, controller, joypad | 泛指 Xbox / PlayStation / Switch 类输入设备 | ADR #39 |
| D-pad | 十字键, directional pad | 手柄方向键，用于兜底瞄准与 UI 导航 | 词表 §7 |
| 左摇杆 | left stick, left analog | 正式项目默认移动；MVP 临时可瞄准 | GDD, MVP 文档 |
| 右摇杆 | right stick, right analog | 手柄兜底瞄准 | GDD, ADR #39, ADR #82 |
| 手柄 A / South | A button, south button, bottom face button | 默认主动道具 / MVP 重开 | 词表 §7, MVP 文档 |
| Start / Menu | start button, menu button | 默认暂停 | 词表 §7 |
| G.U.I.D.E / GUIDE | Godot Unified Input Detection Engine, 输入引擎 | 固定版本 vendored 物理输入、context、remapping 与 prompt 引擎；业务不得直接访问 | `docs/代码/guide.md`、ADR #151 |
| InputService | input service, 输入服务, 输入门面 | 项目唯一输入业务边界；输出生成 action 与归一化 bool / Vector2 intent | `docs/代码/input_service.md`、ADR #151 |
| input action | action, 输入动作 | 词表登记并生成常量的稳定业务语义；由 InputService 解释，不能等同物理键 | 词表 §7 |
| binding id | 重绑定 id, binding slot id | 设置页与 InputService 用来标识稳定绑定槽的 `input.*` id；例如 `input.move_stick`、`input.aim_stick`、`input.fire`，不等同 GUIDE action，也不供业务读取 | 词表 §7.1、`docs/代码/input_service.md` |
| mapping context | input context, 映射上下文 | GUIDE action 映射集合；正式为 gameplay / ui / debug，由 InputService 管生命周期 | `docs/代码/input_service.md` |
| input intent | normalized intent, 归一化输入意图 | 业务消费的 bool 或 Vector2 最终值；本地设备、回放、未来 AI / 网络使用同形边界 | GDD §6.7、`docs/代码/input_service.md` |
| 重绑定 | rebinding, remap | 通过 InputService / GUIDE 捕获并保存键鼠或手柄 slot | GDD §9.5 |

## 4. 架构与基础设施术语

| 标准术语 | 常见别名 / 英文 | 说明 | 权威来源 |
|----------|----------------|------|----------|
| autoload | singleton, 单例 | Godot 全局单例 | GDD §9 |
| RNG | random, 随机源 | 种子化随机与子流 | ADR #20 |
| GameClock | clock, time source, 时间源 | 玩法时间、暂停冻结、慢动作 | ADR #26 |
| Replay | replay recorder, 回放 | 录制 seed + 输入序列 | ADR #16 |
| GameState | state machine, 状态机 | 全局流程状态 | ADR #21 |
| UIManager | UI stack, 界面栈 | 弹窗、焦点、暂停联动 | ADR #23 |
| PoolManager | object pool, 对象池 | 高频实体复用 | ADR #22 |
| 视觉效果目录 | visual effects catalog, VFX catalog | `visual_effects.json` 中以稳定 effect id 描述资源、空间、生命周期、质量变体与 reduced-motion 策略的正式效果目录；不是业务事件表 | GDD §9.24、ADR #158、`docs/代码/visual_effects.md` |
| 表现 Profile | presentation profile, feedback profile | `presentation_profiles.json` 中把稳定 cue 映射为视觉、音频、震屏、屏幕反馈与挂点参数的可继承内容绑定；角色、敌人、技能、武器和机关通过 `presentation_profile_id` 选择 | GDD §9.24、ADR #158、`docs/代码/visual_effects.md` |
| Reduced Motion | 减少动态效果, 低动态模式 | 无障碍表现策略：去除平移、弹性、stagger、视差和持续循环，使用瞬时状态或不超过 100 ms 的淡入淡出；不暗改玩家的震屏开关，也不能隐藏攻击边界或状态读法 | GDD §9.24、ADR #158、`docs/代码/ui_effects.md` |
| VFX Library | 视觉效果库, VFX editor | 只在 Godot 编辑器启用的项目自有预览、筛选、选择、验证与建效向导；正式运行时和发布包不得依赖它 | ADR #158、`docs/代码/visual_effects.md` |
| Combat | damage pipeline, 伤害管线 | 单入口伤害结算 | ADR #24 |
| StatusEffect | buff, debuff, DoT, 状态效果 | 持续效果资源与组件 | ADR #24 |
| SaveManager | save system, 存档 | 存档、版本、迁移、损坏备份 | ADR #25 |
| PlatformServices | platform services, 平台服务, Steam provider | 当前已有的统一平台 SDK 门面；未来以 GodotSteam adapter 承接 Steam 身份票据、成就、富状态、Overlay、Lobby / 邀请，正式客户端当前仍使用 `none` 后端 | GDD §9.22, ADR #84 / #150, `docs/代码/platform_services.md` |
| OnlineServices | online services, backend services, 在线服务, 后端服务 | ADR #150 规划但尚未实现的通用在线门面；未来以 Talo provider 承接跨平台身份、排行榜 / 统计、Live Config、事件上传和轻量社交，不等于 `PlatformServices` 或权威战斗服务器 | `docs/在线服务规划.md`, GDD §9.23, ADR #150 |
| GodotSteam | Steamworks adapter, Steam SDK plugin | 未来正式 `PlatformServices` 的 Steamworks SDK adapter；只负责平台能力，不是通用在线后端。现有 Steamworks Lab 的接入不等于正式 `client` 已安装 | `docs/在线服务规划.md`, ADR #150, `output/steamworks_lab/README.md` |
| Talo | Talo Game Services, game backend, 游戏后端 | 未来 `OnlineServices` 的通用在线服务供应商，可选 Talo Cloud 或官方自托管发行版；项目不开发自有通用后端，当前不安装且未批准具体在线功能 | `docs/在线服务规划.md`, ADR #150 |
| run 存档 | run save, suspend save, resume run, 续局存档 | 暂停保存退出后可恢复当前一局的档案 | GDD §9.16, ADR #48 |
| meta 存档 | meta save, profile save, metaprogression save | 局外长期档案；F11 后保存装备 Mod 库存、等级、资源、英雄 / 武器 loadout、解锁和统计，旧 F6 永久升级字段仅作迁移兼容 | GDD §9.16, ADR #48, ADR #115 |
| AudioManager | audio system, 音频 | SFX / BGM 播放与 bus 路由 | ADR #27 |
| ModifierEngine | stat modifier, 修正器 | 属性加法 / 乘法叠加 | GDD §6.3 |
| DataLoader | data loader, 数据加载 | 加载 JSON 与校验数据 | GDD |
| 数值配置手册 | numeric config manual, tuning guide, 调参手册 | `client/data/README.md`，说明数据文件、字段、单位、范围和调参流程 | ADR #45 |
| 多语言文案手册 | localization manual, copy config, locale guide | `client/locale/README.md`，说明 `strings.csv`、语言列、key 命名和占位符规则 | ADR #45 |
| 契约同步 | contract sync, sync contracts | `tools/sync_contracts.py` 从 `docs/词表与契约.md` 生成 `_contracts.json` 与 GDScript 常量 | ADR #51 |
| 数据校验 | data validation, schema check | `tools/validate_data.py` 校验正式数据、locale 与词表 / locale key 引用 | ADR #51 / #75 |
| Godot Bridge | bridge, lightweight bridge, headless bridge | `tools/godot_bridge.py` 封装场景树导出、数据校验、Godot 版本和 headless boot | ADR #51 |
| GrowthSystem | XP system, level-up system, 成长系统 | 经验累计、等级阈值、升级候选数量和奖励应用 | GDD §7.1 |
| GearModSystem | gear mod system, 装备 Mod 系统, loadout system | 规划中的 F11 装备 Mod 入口，负责 Mod 定义、库存、等级、资源、英雄 / 武器 loadout、掉落、升级、分解和开局 modifier snapshot | GDD §7.2, ADR #115, `docs/代码/gear_mod_system.md` |
| MetaProgressionSystem | meta progression system, 元进度系统, 局外成长系统 | Legacy：F6 结算奖励、账号等级、永久升级、解锁和 meta 存档校验系统；ADR #117 后运行时 / UI 删除，ADR #118 后旧测试档迁移 / 补偿也删除 | GDD §7.2, ADR #46, ADR #118 |
| 本地数据包 Mod | local mod, data package mod, ModLoader mod, 创意工坊 mod | 玩家或创作者提供的声明式数据 patch 包，由 `ModLoader` + `DataLoader` 加载；与装备 Mod 是不同系统，不给玩家局内装备槽或数值升级 | GDD §9.21, ADR #83 |

## 5. AI 协作与文档术语

| 标准术语 | 常见别名 / 英文 | 说明 | 权威来源 |
|----------|----------------|------|----------|
| AI 导航 | project map, navigation | 项目地图、扩展点、依赖图 | `docs/AI导航.md` |
| 项目记忆 | project memory, long-term memory | 跨会话长期索引 | `docs/AI记忆/项目记忆.md` |
| 当前状态 | current state, machine state | 机器可读当前阶段、下一步、最近验证 | `docs/AI记忆/current_state.json` |
| TODO | todo list, backlog, future tasks, 未来任务清单 | 人工可读后续任务清单，按 P0~P3 维护 | `docs/TODO.md` |
| 会话日志 | session log | 当日对话与变更摘要 | `docs/AI记忆/会话日志/` |
| 任务模板 | task template | 高频任务执行清单 | `docs/AI协作/任务模板/` |
| 上下文预算 | context budget | 每类任务读取范围 | `docs/AI协作/上下文预算.md` |
| 详细模块文档 | detailed module doc | 完整项目代码文档标准 | ADR #40 |
| 代码文档索引 | module docs index | `docs/代码/README.md` | ADR #35, #40 |
| 知识库索引 | KB index, knowledge base index | 人工与机器可读文档索引 | ADR #41 |
| 健康检查 | health check, docs check | 文档结构、链接、索引一致性检查 | `docs/AI协作/文档健康检查.md` |

## 6. 常见误用

| 误用 | 正确说法 | 原因 |
|------|----------|------|
| 道具泛指所有内容 | 区分主动道具、消耗品、遗物 | 数据结构和扩展点不同 |
| 手柄轴直接写代码里 | GUIDE mapping + InputService intent | 设备差异必须停留在绑定 / 适配层 |
| 自动生成文档 | 自动创建/维护详细模块文档 | 不能用简短摘要替代维护文档 |
| MVP 代码经验直接进正式项目 | 先复盘，再 ADR / GDD / 词表同步 | MVP 是隔离实验区 |
| 词表当术语表 | 词表登记正式 id，术语表登记别名 | 权威范围不同 |
