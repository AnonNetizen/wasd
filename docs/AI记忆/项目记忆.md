# 项目记忆（AI 协作长期索引）

> 本文件用于**跨会话、跨机器**续接 AI 对话，定位为长期索引。
> 机器可读的当前阶段、下一步、最近验证见 `docs/AI记忆/current_state.json`；每日细节见 `docs/AI记忆/会话日志/`。
>
> 维护方式：选项 **(a) 主动更新** —— 每次重要对话/决策结束后，AI 主动追加/修订本文件，无需用户提醒（成本可控：仅写关键变更，不复述细节）。
> 触发约定见文末「更新约定」一节。
>
> **AI 修改说明**：修改本文档前先读 `docs/AI协作/文档维护指南.md`。本文档是跨会话长期索引；改项目快照、ADR 摘要或记忆结构时，必须同步 `docs/AI记忆/current_state.json`、对应源文档（如 `docs/决策记录.md` / 规则 / GDD / AI导航）和当日会话日志。

## 0. 快速续接清单（新环境必读）

新机器 / 新会话第一次开工先读：`README.md`、`docs/AI协作/快速开工.md`、`docs/AI记忆/current_state.json`、当前平台编码规则入口、`docs/AI导航.md` 相关段落；再按任务读词表 / GDD / ADR / 测试策略等权威文档。

**本文件**是长期冷存储，需要长期背景、ADR 摘要或历史脉络时再读。

> 看完热路径就能"接得上"，无需翻历史聊天；完整长期背景按任务需要再读本文件相关节。

## 1. 项目快照

- **名称**：wasd
- **作者署名**：中文 `伦敦阿农`；英文 `Anon London`
- **类型**：俯视角射击刷宝生存（手动按住开火的射击身份 + 《星际战甲》/《暗黑》刷装备与词条长期追求 + 手工房间串联短刷图 + 以撒式道具 / 机关 / 构筑组合；玩法判定与显示都保持 2D 平面）
- **IP 方向**：ADR #88 已定为 **《破巢者》**（英文暂定 `Nestbreakers`），独立权威文档为 `docs/IP设定.md`：未知原因导致其他宇宙与本宇宙的通道突然打开，原本拥有银河系星际文明的人类被打散，只剩首都星域相对稳定；多英雄主动突入敌方“巢”，在怪潮中夺取遗物、升级构筑并尝试打穿巢核、切断通道或削弱敌方源头；“巢”是敌方核心据点 / 生产源头 / 通道锚点 / 意志中枢的泛称，不限定为虫巢。ADR #111 新增 `docs/IP美术风格.md` 作为 IP 美术权威：首个敌巢视觉方向是“被异宇宙规则钙化出来的活体建筑”，敌巢 / 虫族占用青 / 红 / 白和骨白 / 蜡黄 / 干肉粉 / 黑紫，玩家与玩家子弹默认避开青 / 红 / 白，敌方远程攻击可用红色，宝箱和地图兴趣点按功能色区分。
- **引擎**：Godot 4.7.1 stable + GDScript（强制类型化；新写 / 修改脚本以 Godot 4.7 官方 GDScript style guide 为基础风格）
- **分辨率与宽高比**：ADR #131 后，正式客户端当前只设计 / 验收固定 16:9 分辨率，默认逻辑画布为 1920×1080；非 16:9 屏幕通过 `canvas_items + keep` 等比缩放并补黑边，不拉伸、不裁切、不扩大玩法视野。其他比例是 P3 优化，未来也按独立固定预设接入，不做连续任意比例适配。
- **F12 结果清单补充**：`GameOverPanel` 已在撤离成功时列出带回的 dust / Gear Mod、击杀数和用时，并在死亡失败时列出丢失的暂存战利品；`runtime-smoke` 覆盖成功 / 失败两条结果清单路径。
- **F12 真实缓存箱交互补充**：资源缓存 / Mod 缓存已从“可伤害或进圈领取”改为 `requires_interaction` 交互缓存箱；`MapManager` 会为无目标交互缓存生成独立 `interest_point_cache_position` 格心 anchor，并让关联 director 陷阱避开缓存 footprint；`GameplayRunLoop` 会按该 anchor 生成贴合矩形地图格的 `InterestPointCache`，玩家进入领取半径后 HUD 显示 `interact` 提示并按键打开，奖励仍进入 `run.pending_loot`。`input.interact` 已进入词表、Settings、SettingsPanel 和 smoke 覆盖；守卫清空条件和掉落爆出表现仍待后续。
- **当前视角补充**：ADR #124 后当前正式视角改回俯视角 2D；正式玩家场景删除 `Player3DVisual` / `SubViewport + Camera3D` 正交视觉层，`Player._draw()` 直接绘制 2D 俯视占位并按完整 `aim_direction` 显示朝向标记。ADR #125 后地图格、边界、机关、兴趣点 footprint 和撤离区都改为水平 / 垂直矩形俯视格。ADR #148 后 Player 子场景内以 `Phantom Camera 0.11.0.3` GLUED PCam 维持原有严格居中 / 水平 / 等比口径，玩家有效受伤按 `camera_feedback.json` 产生可关闭位移震屏。
- **F12 撤离首片补充**：ADR #123 后，小巢核击破不再直接完成本局；它只开启贴合矩形地图格的撤离区，玩家站进撤离区完成短读条后才提交 `run.pending_loot`、删除 `run` 并显示完成面板。`extraction` 状态进入 run snapshot；死亡、重开、回标题或主动放弃仍丢失未结算战利品。
- **当前状态**：详见 `docs/AI记忆/current_state.json`。F13 已完成 9×9 模块连续大地图、3×3 流式、模块迷雾、目标撤离和 run v4；F12 open-warzone 只保留为显式非默认回归。ADR #145 / #146 的 F14 已在完整 99×99 静态地形上增加 `ModuleNavigationField`：完整 mask、地形视线和守家 / 最后已知位置 AStar 保持全图；玩家跨格时只在最大视觉范围推导的半径 8 窗口内重算确定性八方向共享流场，单次最多 289 格并只清理上次触达索引。EnemyAI schema v3 按地形视线、路径距离和 1.5 秒记忆依次感知，畅通直追、受阻绕行，冲锋 / 远程受墙体门禁。导航 / 感知缓存不进 run，run 仍为 v4；开放战区无导航 provider 时保留直线兜底。ADR #144 的玩家唯一战斗目标、敌方友伤拒绝和无伤害中心分离保持不变。ADR #143 后性能测试只由用户当次明确触发。后续工作以用户最后明确指令为准。
- **Steamworks Slime Lab 单人 AI 大招 / 自主游击 / 本地多人 / 纪录 / 网络 wire**：ADR #135 后玩家可见本地多人是单进程同屏，P1 固定键鼠，P2–P4 由独立手柄自动加入；ADR #136 后快照 wire 改为 FastLZ + 最多 900 字节分片，Lobby `lab_version=2`；ADR #137 后最长存活纪录拆为单人 / 多人两类，旧混合纪录清空。ADR #138 后所有自动 smoke 统一走 `py -3 tools\steamworks_lab_toolchain.py smoke`：每进程隔离 `user://`，精确检查成功 / 致命日志，battle 默认五轮，ENet 使用动态端口、host `READY` 和轻量 client 副本，并保护真实设置 / 存档与源码 `steam_appid.txt=4955670`。ADR #139 确立仅在 `PlayMode.SINGLE` 启用的 AI 队友大招：P1 子弹命中 / 普通击杀 / Boss 击杀合计充能 `+1 / +6 / +21`，100 点按 `E` 召唤持续 10 秒且不可受伤、不吸引火力、自动射击的 AI；松开再按住 `E` 可发起一次 AI 自动同意的合体，合体计入 10 秒。ADR #140 已把 AI 改为确定性自主游击 / 预判闪避：避开敌弹、普通敌人、Boss 和障碍物，常规距 P1 使用 210 px 硬限、超过 220 px 复位；按 `E` 后高速归队至 92 px 并停靠 0.8 秒再合体。目标 battle 1/1、local-couch 与权威 `smoke --suite all` 已通过，all 含 battle 5/5、动态端口 ENet、最大分片仍不超过 900 字节且受保护文件未改变。AI 不进入真人 roster、强化、纪录、玩家卡或网络快照，多人、wire、存档与正式 `client` 边界不变。测试不得备份真实玩家文件；Steam 仍一设备一玩家，ENet 只保留内部协议回归。详细入口为 `output/steamworks_lab/README.md`。
- **F5 存档续局**：暂停菜单、保存并退出、标题继续游戏、F4 run payload、`RNG` / `GameClock` 快照恢复已落地；`SaveManager` 的 `run` kind 已提升到 version 2，注册 v1 -> v2 迁移，并新增 `python tools/godot_bridge.py --project client save-smoke` 覆盖 run roundtrip、`.bak` 回退、双坏档隔离和迁移链；用户已手动试玩暂停 / 保存 / 续局并反馈无问题，最终 F5 手动存档 checklist 也已确认通过；坏 run 存档点击继续会回标题、隐藏继续按钮并显示本局存档重置提示；`ui_restore` 已覆盖普通游玩、暂停菜单、升级选择面板以及暂停菜单叠在升级面板上的状态，升级续局会保留同一组已掷出的候选，升级界面按 Esc 可打开暂停菜单并关闭后回到升级选择。F5 收口后已进入并落地 F6 局外成长首切片。
- **F6 局外成长历史**：F6 曾落地 `MetaProgressionSystem` autoload、`SaveManager` 的 `meta` profile roundtrip、死亡结算奖励、账号经验 / 等级奖励、升级购买、解锁持久化、标题菜单“局外升级”入口、`MetaProgressionPanel` 升级列表、下一局永久 modifiers 和 `meta-smoke`。ADR #115~#117 后该运行时路线已退役：旧 autoload、旧面板、标题旧入口、死亡旧货币 / 账号经验结算和 `meta-smoke` 已删除；项目尚未上线，ADR #118 后旧 `meta_progression.json`、旧 meta 契约、旧文案和旧测试档迁移 / 补偿逻辑也已删除。
- **F7 设置持久化、设置面板、运行时语言刷新、输入绑定与 UI 栈首片**：`Settings` 已从内存态推进到 `user://settings.cfg` 持久化，`SettingsPanel` 已作为正式 `.tscn` 场景接入标题菜单和暂停菜单；ADR #148 已接线并显示 `gameplay.screen_shake`，关闭时立即停止当前震动并抑制后续玩家受伤震屏。`video.*`、松开瞄准停火、瞄准模式和失焦暂停等其他未接线 key 仍保留但隐藏。核心运行时 UI 已覆盖既有实例语言刷新，键盘主输入绑定首片已覆盖移动、兜底瞄准、主动道具、暂停、确认 / 返回，收尾 polish 已补输入绑定保存反馈、共用键位提示和一键恢复输入默认。`UIManager` 已统一栈顶 `ui_back` 和 push 后默认焦点兜底。`settings-smoke` 覆盖持久化、恢复、可见 / 隐藏面板控件、输入绑定写入、输入反馈 / 恢复默认、入口 signal 和核心 UI 语言刷新，`runtime-smoke` 覆盖真实标题 / 暂停设置面板开关、局外成长面板返回与焦点落点。F7 当前验收范围已收口；后续完整按键捕获、完整手柄重绑定、焦点环和手柄导航作为后续 polish 维护。
- **F8 回放 / 测试 / 平衡基线当前状态**：`docs/AI协作/工作包/F8-ReplayTestingBalance.md` 是回放 / 测试 / 平衡维护入口；已落地临时 `l1-smoke`（`RNG` / `GameClock` / `GameState` / `SaveManager` / `Combat`）、`Replay` `.replay` 文件 envelope 与 `replay-smoke` roundtrip、`replay-runner` summary diff / `--rerun-runtime-summary`、runner 输入播放首片、runtime event 播放首片、扩展稳定帧样本 diff、`replay-input-smoke` gameplay 输入录制首片、四条 checked-in golden、`client/tools/rng_audit.gd`，以及 `perf-probe` schema v2 可比较性能基线。ADR #120 后默认标准模式不启用升级三选一；`golden_level_up_choice` 由测试 harness 显式启用成长池，`RNG` 子流 seed 派生使用域隔离 SHA-256 mixer。ADR #143 后性能 probe 仅保留为用户明确触发的按需工具，不再作为 F9 内容扩展或 AI 交付的默认回归护栏。
- **F9 内容扩展 / Demo 打磨当前状态**：`docs/AI协作/工作包/F9-ContentDemoPolish.md` 已建立为当前阶段入口。F9.0~F9.5 第一轮 Demo 已收口：首个数据内容包、`f9-demo-smoke`、占位轮廓 / 命中反馈、自动回归 / 性能守门和完整人工 checklist 均通过。后续用户又点名推进有限地图 + PCG + 人工调整能力，已落地 ADR #93、`client/data/map_layouts.json`、`MapManager`、通用 `Hazard` / `HazardSystem`、FEA-12 测试机关、run payload schema v2 的 `map` / `hazards` 快照、DataLoader schema 校验和 runtime / save / F9 demo / perf smoke；ADR #125 后地图使用量化矩形格，`Hazard` 的 `radius_tiles` 按矩形格整数倍定义危险地块大小。`docs/功能建议池.md` 是后续手动点名的新功能菜单，AI 不从其中自行推进。
- **F10 战区导演当前状态**：`docs/AI协作/工作包/F10-WarzoneDirector.md` 是当前战区导演入口。`client/data/warzone_directors.json` schema v2 和 `WarzoneDirector` 由 `GameplayRunLoop` 在刷怪前按固定阶段判断 `spawn_waves.csv` 的 wave 是否启用；匹配当前 layout 的 `interest_points` 会传给 `MapManager`，用通用 PCG / 矩形格锚点规则生成 `source="director"` 初始机关，debug summary 暴露当前 director、phase、mutation、wave、interest point 与 map hazard source counts。ADR #144 已删除导演的旧敌人组合元数据。DataLoader / `tools/validate_data.py` 校验 director 与 mode、wave、hazard、map layout 的跨文件引用，并要求兴趣点 `hazard_ids` 非空；runtime-smoke 和 f9-demo-smoke 覆盖初始 warmup、后期 guarded phase 与 director-sourced hazard。设计红线：当前不读玩家状态、不做隐藏 DDA、不接运行时 LLM。
- **F12 短刷图开放战区已落地状态**：`docs/AI协作/工作包/F12-ShortLootRuns.md` 是开放战区短刷图的已落地入口；ADR #127 后，默认下一阶段改由 F13 手工房间制承接。F12 默认标准模式放弃 25 分钟长局，先做暗黑式 8-12 分钟短刷图：地图由多个 POI、精英 / 宝箱 / 材料点和小巢核目标组成，Gear Mod、材料和词条价值承担局内 / 局外主要追求；默认模式暂时屏蔽经验升级 3 选 1，`mode_standard_survival` 不声明 `growth_pools`，`GameplayRunLoop` 在无 growth pool 时不生成 XP pickup、不升级、不进入 `LEVEL_UP`。F12 数据首片已落地：`map_standard_nest` 偏外侧投放，`warzone_directors.json` 阶段为 0-1 / 1-4 / 4-7 / 7-9 / 9+ 分钟，9+ 是软加压不是硬倒计时，`spawn_waves.csv` 分别在 0:00 / 1:00 / 4:00 / 5:00 / 7:00 打开 chaser / swarm / stalker / spitter / bulwark，四个 director 兴趣点占位为精英巢点、Mod 缓存、资源缓存和小巢核。兴趣点奖励、真实缓存箱交互、可伤害目标、撤离与结果清单首片已落地：资源缓存可暂存 `gear_mod_dust`，Mod 缓存可暂存测试 Gear Mod，小巢核可暂存 dust + Mod，资源缓存 / Mod 缓存会生成独立格心 `InterestPointCache`，矩形 footprint + 低矮俯视箱体贴合地图格并避开关联陷阱 footprint，玩家进入领取半径后按 `interact` 打开；`interest_points` claimed 状态、`run.pending_loot` 和 `extraction` 都进入 run snapshot 并可续局恢复；击破小巢核只开启矩形撤离区，撤离读条成功才把 pending loot 写入 `meta.gear_mods` 并显示带回清单；死亡、重开、回标题或主动放弃会丢失未结算战利品并显示丢失清单。精英巢点和小巢核可用 `target_hp` / `target_hit_radius` 生成 `InterestPointTarget`，子弹命中后走 `Combat.apply_damage()`，目标一生成即可受伤；视觉 footprint 按地图矩形格整数尺寸绘制，MapManager 会为目标生成独立格心 anchor 并让关联机关 / 陷阱避开目标 footprint；摧毁目标触发原有奖励 / 撤离逻辑，目标生命 / 摧毁状态进入 run snapshot。正式核心实体行为 / 美术、多出口撤离、精英守卫语义、更正式 Result UI、缓存箱守卫 / 爆出表现尚未接线。GrowthSystem、`growth.csv`、`growth_pools.json` 和 `LevelUpPanel` 保留给未来其他模式。
- **EnemyAI 对玩家行为 / F14 导航感知**：ADR #144 固定玩家唯一战斗目标、`team_enemy` 伤害拒绝与无伤害中心分离；ADR #145 在此边界内增加共享流场 + 混合感知，ADR #146 把活动目标流场从全图收敛为感知范围驱动的局部窗口。`enemy_ai_profiles.json` schema v3 使用 `perception.sight_radius / path_awareness_radius / memory_duration`；`ModuleNavigationField` 从完整模块 assignment 构建 99×99 mask，提供半径 8 / 最多 289 格的确定性八方向活动 Dijkstra、全图 `AStarGrid2D`、supercover 地形视线和敌人半径走廊。`Enemy` 畅通时连续直追，受阻时读共享流场；记忆只追最后已知位置，结束后普通敌人停止、守家敌人绕路回出生点；冲锋必须当前感知且走廊清晰，远程必须当前地形视线。导航 / 感知为派生状态，不写 run v4；开放战区使用无 provider 直线兜底。当前五个 profile 不变，module-world / runtime smoke 覆盖局部上限、连续跨格、真实绕路、感知顺序、记忆、守家、攻击墙体门禁与 ADR #144 护栏。
- **本地 Mod 接口首片**：ADR #83 已预留玩家 mod 数据包入口：`ModLoader` 扫描 `user://mods/<mod_id>/mod.json`，`DataLoader` 合并声明式 JSON / CSV append patch 和少量动态契约扩展；当前不接创意工坊、不联网、不执行玩家脚本，未来创意工坊只作为分发层。
- **平台服务接口首片**：ADR #84 已预留 Steam 优先的平台服务入口：`PlatformServices` 统一承接成就、统计、富状态 / 状态显示、overlay、Lobby / 联机入口和用户身份；当前不接 Steamworks SDK，默认 `none` 空后端，其他平台后续走 provider adapter。
- **DebugTools / GM 指令首片**：`DebugConsole` 与 `GMCommandRegistry` 只在 debug/dev_tools 构建启用；当前支持 help/stats/spawn/xp/hp/damage/heal/dust/kill/clear/seed，命令通过 `GameplayRunLoop.debug_*`、`Player.debug_*`、`GearModSystem.debug_grant_resource()` 和 `Combat` / `PoolManager` 正式 API 生效；`debug-tools-release-smoke` 模拟 release guard，真实导出 preset 后还需排除调试资源。
- **Godot 4.7 迁移**：ADR #85 已将 `client/project.godot` 的 `config/features`、README / GDD、三平台规则和三平台 Godot skills 迁到 Godot 4.7；按官方 4.7 迁移指南扫描后，当前未发现项目使用需要改写的 4.6-only 高风险 API。ADR #141 进一步将当前开发、验证和推荐编辑器补丁基线精确到 Godot 4.7.1 stable；`project.godot` 的 4.7 特性标识、官方 4.7 文档路径与 Steam 工具 4.7 系列兼容检查保持不变。
- **正式客户端 Godot 插件**：ADR #147 固定版本入库并共享启用 `@icons 1.4.0` 与 `Script-IDE 2.2.3`；ADR #148 以同一维护型 fork 策略入库 `Phantom Camera 0.11.0.3`，仅保留 addon 并排除 examples。三者都保留 MIT 许可、不设 lint 豁免、不自动更新；Phantom Camera 运行时管理器由项目固定 autoload，详细哈希和本地补丁见 `client/addons/README.md`，Runtime / Resource / Editor / C# 边界、项目 2D 接入与故障排查统一见 `docs/代码/phantom_camera.md`。
- **Gameplay runtime 正式化**：用户要求先把 F4 临时框架改成正式框架；已完成正式命名收口和 `.tscn` 场景资源化，不改玩法行为：`f4_*` 脚本和 UI 类改为 `GameplayRunLoop`、`Player`、`WeaponSystem`、`Bullet`、`Enemy`、`PickupOrb`、`GameplayHud`、`TitleMenu`、`PauseMenu`、`GameOverPanel` 等正式名，活跃组改为 `active_enemies` / `active_bullets` / `active_pickups`，`runtime_smoke.gd` / `runtime-smoke` 成为正式 smoke 入口并保留 `f4-smoke` 兼容别名；稳定 runtime/UI 结构已迁到 `client/scenes/gameplay/*.tscn` 与 `client/scenes/ui/*.tscn`。后续稳定 gameplay/UI 结构默认改场景资源，脚本只做节点绑定、配置、数据刷新和 signal 编排；动态创建保留给 `PoolManager` 对象池工厂和数据驱动重复 UI。
- **设计文档版本**：v1.24
- **仓库结构**：`docs/`（文档 + AI 知识库）、`client/`（正式 Godot 客户端）、`server/`（暂占位）、`draft/` / `DRAFT/`（AI 禁区）
- **远端**：`https://github.com/AnonNetizen/wasd.git`
- **License**：MIT

## 2. 核心设计支柱（一眼回忆）

| 支柱 | 一句话 |
|------|-------|
| 数据驱动 | 所有可调数值进 `client/data/`，平表数值优先 CSV、复杂配置优先 JSON；字段说明进 `client/data/README.md`，零代码调参 + 热重载 |
| 双语本地化 | 玩家可见文本进 `client/locale/strings.csv`，当前首批维护 `zh_CN` / `en`；AI 自动补另一语言首版译文，人工最终复核；UI 尺寸以英文 `en` 文案长度作为设计和验收基准 |
| 固定 16:9 显示基线 | 当前只为经过验证的 16:9 固定分辨率预设设计 UI / 玩法视野，默认 1920×1080；非 16:9 屏幕等比加黑边，其他比例留作未来逐项验收的 P3 优化 |
| HUD 详细数值 | 按住 `show_stats_panel` action 显示详细数值面板、松开隐藏，不暂停、不入 UI 栈；默认 Tab，可通过 Settings 重绑定 |
| 短刷图默认循环 | ADR #120 后默认标准模式先做暗黑式 8-12 分钟短刷图；F12 已有开放战区兴趣点 / 缓存 / 小巢核 / 撤离首片，ADR #127 后下一阶段转向手工房间串联：清房开门、进入下一个房间，仍保留 Gear Mod / dust 暂存和成功完成 / 撤离后结算；不做默认 25 分钟长局 |
| 装备 Mod 跨局成长 | ADR #115 后跨局成长主路线是参考《星际战甲》的装备 Mod；数据 / 契约首片已建立（`gear_mods.json`、掉落表、升级成本、`gear_mod_dust`、测试武器伤害 Mod、DataLoader schema），`GearModSystem` 已管理 Mod 库存、等级、资源、英雄 / 武器 loadout、掉落、升级、分解、可延迟提交的掉落结果和开局 modifier snapshot；标题 `GearModPanel` 已支持切换英雄 / 武器、查看资源 / 容量 / 效果并执行装备、卸下、升级、分解；玩家归因掉落会显示 HUD 暂存提示；旧 `MetaProgressionSystem` 运行时 / UI / smoke 已按 ADR #117 删除，旧测试档迁移 / 补偿路径已按 ADR #118 删除 |
| 强玩家存档 | `SaveManager` 同时支持 `meta` 局外成长和 `run` 暂停退出续局，要求迁移、原子写入、备份回退和损坏隔离 |
| 遗物 = 数据 | `modifiers`（数值）+ `behaviors`（行为）描述，**严禁为每个遗物写硬编码分支** |
| 技能 = 射击强化优先的轻量 GAS 数据 | `skills.json` 描述 ability tags、activation 条件、成本、目标、效果和冷却；角色 / 道具 / 模式只引用 skill id，资源池可扩展；当前默认技能为 `skill_overdrive_rounds`，通过 `skill_effect_weapon_modifiers` 临时强化主武器，伤害技能走 `Combat`，持续状态走 Player / Enemy / SkillSystem 自身的 `StatusEffectComponent` |
| 敌人对玩家行为 = 数据 | 敌人基础数值在 `enemies.csv`，行为在 `enemy_ai_profiles.json`；用 action profile 表达追击、快速近战、冲锋、守家和远程，玩家是唯一战斗目标，敌方友伤被拒绝，禁止按 enemy id 写 AI 分支 |
| 有限地图 + 可调 PCG | 地图不再无限扩展；`map_layouts.json` 声明轴对齐矩形 bounds、矩形格尺寸、出生点、安全半径、刷怪边距、PCG 机关规则和人工摆点，`MapManager` 负责可复现生成 / 保存，并约束玩家 / 敌人中心移动不离开贴格矩形边界；出生点吸附到格心，出生安全区可见提示是贴格矩形，人工机关 / PCG 机关按 `radius_tiles` 奇偶吸附到合法锚点，通用机关是 `radius_tiles` 整数占格的矩形危险地块 |
| 战区导演 = 固定阶段 / 非隐藏 DDA | `warzone_directors.json` schema v2 声明 mode 对应的固定阶段、巢变异、wave gating 和兴趣点；`WarzoneDirector` 按 `GameClock` 时间门控 wave，并把 layout 匹配的兴趣点交给 `MapManager` 生成 `source="director"` 初始机关；不读玩家状态、不动态降难 / 升难、不在运行时接 LLM |
| 扩展优先 | 默认玩法规则不是硬编码上限；破限角色 / 道具必须用 capability、tag、primitive 或可复用 strategy 声明式扩展 |
| 模式友好资源复用 | 角色、遗物、道具、敌人、成长奖励等资源本体默认模式无关；游戏模式只通过资源池、权重、条件、禁用列表和轻量覆盖组合资源。默认标准模式当前不声明 `growth_pools`，局内升级 3 选 1 只给未来显式声明该池的模式使用 |
| 本地 Mod 接口 | 玩家 mod 先作为 `user://mods/<mod_id>/mod.json` 数据包进入，走 `ModLoader` + `DataLoader` 声明式 patch；创意工坊未来只负责分发，不执行玩家脚本 |
| 平台服务接口 | Steam API 优先通过 `PlatformServices` 预留：成就、统计、状态显示、overlay、Lobby / 邀请和用户身份统一进门面；其他平台后续走 provider adapter |
| 未来多人友好边界 | 当前只做单人，不实现网络；输入、伤害、阵营、回放、存档和埋点预留 actor / participant / team / intent 概念 |
| **完整 autoload 矩阵** | 3 协作（`Localization`/`Settings`/`Analytics`）+ 2 确定性（`RNG`/`GameClock`）+ 1 回放（`Replay`）+ 1 平台服务（`PlatformServices`）+ 3 协调（`GameState`/`UIManager`/`PoolManager`）+ 2 资源（`SaveManager`/`AudioManager`）+ AI 协作工程（`docs/AI协作/`） |
| 玩家恒居中 | 相机挂玩家下，**关 limit 与 drag margin** |
| 词表契约 | 所有约定字符串走 `docs/词表与契约.md` 白名单，**禁止裸字符串/编造 id** |
| 代码-文档同步 | 长期代码模块 / 公共 API / 数据 schema / 依赖变化必须同步详细的 `docs/代码/` 模块文档 |
| AI 知识库 | `docs/AI知识库索引.md` + `docs/_kb_index.json` + `docs/术语表.md` + `tools/docs_health_check.py` |
| 可验证数据闭环 | `tools/sync_contracts.py` 生成 `_contracts.json` 与 `client/scripts/contracts/*.gd`；`tools/validate_data.py` 校验正式数据 / locale；`tools/test_data_loader_schema.py` 固定坏数据回归；`tools/lint_gdscript_rules.py` 固定第一档 GDScript 项目 lint；`tools/lint_project_rules.py` 固定第二档项目规则 lint；`tools/lint_semantic_rules.py` 固定第三档非阻塞语义 advisory；CI 已接入 |
| 轻量 Godot Bridge | `tools/godot_bridge.py` 默认操作正式 `client/`，支持 `export-tree`、`validate-data`、`godot-version`、`headless-boot`、`runtime-smoke`、`f9-demo-smoke`、`settings-smoke`、`gear-mod-smoke`、`save-smoke`、`debug-tools-smoke`、`debug-tools-release-smoke`、F8 `l1-smoke`、`replay-smoke`、`replay-runner`、`replay-runner --rerun-runtime-summary`、`replay-input-smoke`、`capture-golden-replay`、`rng-audit`；`startup-probe` / `perf-probe` 仅由用户明确触发 |
| 默认中文沟通 | AI 面向用户的回复 / 计划 / 总结默认中文，引用代码、API、日志等原文时保留原语言 |
| 事实审查 / 需求评估 / 澄清优先 | 用户问有没有问题 / 风险时不硬找问题；用户提需求后先反馈价值、性价比、复杂度和主要风险；需求、术语、验收标准、授权边界或上下文含义不清时先问，不自行脑补高风险假设 |
| AI 友好工程 | 项目索引、词表、数据 / 文案配置手册、数据校验、黄金样例、ADR、模板齐备；**任务模板 + 上下文预算 + 角色分工 + 实时验证回路 + 工具先行代码审核流程** |
| 低 token 开工 | 日常接手先读 `docs/AI协作/快速开工.md` + `current_state.json`，完整长期记忆 / GDD / ADR / 测试策略按任务触发读取 |
| 阶段工作包 | 正式项目阶段任务优先读 `docs/AI协作/工作包/`，代码阶段优先模块文档 + 目标源码 |
| 暂停 | `get_tree().paused` + 菜单节点 `process_mode=ALWAYS`，键走 InputMap action `pause` |
| 确定性 | 随机走 `RNG`、时间走 `GameClock`；普通新局 / 重开由 `RNG.set_random_run_seed()` 生成新主 seed，继续 / replay / smoke / golden 走恢复或固定 seed；禁止裸 `randi()` / `Time.get_ticks_msec()`（回放回归的硬约束） |

## 3. 既定决策（ADR 摘要，详见 `docs/决策记录.md`）

148 条已落地决策（详见 `docs/决策记录.md`）。摘要：

| 范围 | 摘要 |
|------|------|
| #1~#12 | 初始玩法定位、Godot/GDScript、摄像机、输入、数据驱动遗物、数值外置、i18n、Settings、Analytics、AI 友好工程、元规则 |
| #13~#19 | 暂停、三段仓库、AI 记忆、回放、平衡接口、AI 协作工程、记忆自动瘦身 |
| #20~#28 | RNG、GameState、PoolManager、UIManager、Combat/StatusEffect、SaveManager、GameClock、AudioManager、词表→代码常量 |
| #29~#33 | 测试金字塔、首批 3 subagents + 5 commands、多平台适配、通用入口、OpenCode 配置 |
| #34~#148 | 扩展优先、代码-文档同步、默认中文、`draft/` 禁区、MVP 隔离与验证完成后删除、手柄支持、详细模块文档、AI 知识库 v2、经验升级选择、AI 记忆三层结构、完整项目数值 / 文案配置手册、Stage 1 CI、强玩家存档、创意 / 策略 subagents、事实审查与需求评估规则、需求不明先问、数据校验 / 契约同步 / 轻量 Bridge、AI Git 提交策略、项目级 skills、上下文压缩后的任务恢复规则、外部 AI 工具历史收敛、GM 指令 / 调试工具构建隔离、正式项目阶段工作规划、CSV / JSON 数值分流、双语本地化、多模式资源复用、未来多人边界、Godot 4.7 迁移与 4.7.1 补丁基线、RNG SHA-256 mixer、《破巢者》IP、EnemyAI、轻量 GAS / StatusEffect / DoT、俯视角 2D 矩形格射击刷宝、F10 战区导演、F11 装备 Mod、F12 短刷图与撤离、F13 模块大地图、F14 敌人共享流场与混合感知、Steamworks Slime Lab、PowerShell 稳定执行、固定 16:9、性能测试按需、敌人种间生态删除、固定版本 Godot 插件维护型 fork 与 Phantom Camera 玩家跟随 / 受伤震屏 |

## 4. 待决策项（详见 `docs/修改建议.md`）

当前 D~E 待决策（A、B、C 与 J~R 已采纳归档）：
- D. 子弹手感（初期提速 / 弹道宽容）
- E. 升级选项池内容（遗物 / 属性 / 主动强化 / 回血 / 刷新等取舍）

## 5. 工具链与基础设施

- **Git 配置**：`core.quotepath=false`、`commit.template=.gitmessage`，`.gitattributes` 锁 LF + LFS 预设
- **提交规范**：Conventional Commits（详见 `.gitmessage`）；AI 大更改默认自动 commit，大型代码改动提交前自动事实型 review，细微改动不提交也不触发正式 review，且只 stage 本次任务文件
- **CI/CD**：Stage 1 基础 CI 已启用（`.github/workflows/docs-check.yml`：contract sync / data validate / DataLoader schema regression / GDScript project lint / project rules lint / semantic advisory lint / docs health / whitespace）；本地 `.pre-commit-config.yaml` 复用 Stage 1 门禁，并增加 Steamworks Lab toolchain 单元回归与 staged whitespace fix/check；路线图见 `docs/CICD规划.md`
- **AI 协作工程**：`docs/AI协作/` 已就位（快速开工 + 阶段工作包 + 7 份任务模板 + 上下文预算含 S / M / L / XL 复杂度分级 + 角色分工 + 引擎集成 + 实时验证回路 + 代码审核流程 + 文档健康检查 + AI 技能资源评估 + 10 个项目级 subagents + 9 个四平台同步项目级 skills）
- **AI 知识库**：`docs/AI知识库索引.md`、`docs/_kb_index.json` v2、`docs/术语表.md` 与 `tools/docs_health_check.py` 已落地并接入三平台 `/health-check`（ADR #41~#42）
- **AI 平台入口**：`AGENTS.md` 通用；`.codebuddy/` 服务 CodeBuddy；`CLAUDE.md` + `.claude/` 服务 Claude Code（ADR #87）；`CODEX.md` / `.codex/` 服务 Codex；`OPENCODE.md` / `.opencode/` 服务 OpenCode；规则 21 要求核心语义一致，规则 29 / ADR #130 统一 PowerShell 稳定执行；当前有 10 个 subagents 与 9 个四平台同步 skills；上下文压缩后以用户最后明确指令为准
- **词表→代码常量**：脚本生成流水线已落地（GDD 9.19 / ADR #28 / ADR #51），`tools/sync_contracts.py` 生成 `_contracts.json` 与 `client/scripts/contracts/*.gd`
- **Steamworks Lab Windows 工具链**：当前开发与发布验证标准为 Godot 4.7.1 stable + GodotSteam 4.20 官方 GDExtension + Steamworks 1.64；`steam_toolchain.lock.json` 的 `godot_version: "4.7"` 与 `tools/steamworks_lab_toolchain.py` 的检查继续兼容整个 4.7 补丁系列，工具统一 setup / verify / export-release / smoke，直接走 `--godot` / `GODOT_PATH`，templates 按所选 editor 模式从标准用户目录或 self-contained `editor_data/` 精确匹配，smoke 隔离用户环境并统一多进程生命周期；SteamPipe / Depot / 双账号 smoke 仍是外部步骤
- **正式客户端 Godot 插件**：`client/addons/` 固定 `@icons 1.4.0`、`Script-IDE 2.2.3` 与 `Phantom Camera 0.11.0.3`，发布包哈希、许可、本地补丁和手工升级 SOP 见 `client/addons/README.md`；AI 理解 Phantom Camera 源码时先读 `docs/代码/phantom_camera.md`，再按目标打开少量源码，不默认扫描整个 addon
- **PR / Issue 模板**：`.github/` 下已配齐（PR 模板嵌入了规则自检清单）

## 6. 近期对话脉络（按时间倒序）

> 详细每日记录见 `docs/AI记忆/会话日志/YYYY-MM-DD.md`。这里只放"足够续接"的概要。

| 日期 | 摘要 | 详见 |
|------|------|------|
| 2026-07-22 | ADR #148：固定版本入库 Phantom Camera 0.11.0.3，用 Player 子场景内的 GLUED PCam 替换直接 Camera2D 配置，接入数据化玩家受伤震屏、`RNG.camera_fx` 和可见 Settings 开关；随后新增完整插件模块文档、机器索引、AI 路由和核心源码文档头，使后续维护无需重扫整个 addon | [`会话日志/2026-07-22.md`](会话日志/2026-07-22.md) |
| 2026-07-21 | ADR #143~#147：性能测试改为按需；F14 敌人交互、导航感知与局部流场收口；正式客户端固定版本启用 `@icons 1.4.0` 与 `Script-IDE 2.2.3`，插件作为保留 MIT 署名的维护型 fork 且不设 lint 豁免 | [`会话日志/2026-07-21.md`](会话日志/2026-07-21.md) |
| 2026-07-18 | Steamworks Slime Lab export-release 模板定位补齐 self-contained `editor_data/`，并清除长期文档中的单机绝对路径；配置的 Godot 4.7.1 official 已通过工具回归、verify、all smoke 与 Windows release 导出；ADR #141 随后将当前开发、验证与推荐编辑器基线精确同步为 Godot 4.7.1 stable | [`会话日志/2026-07-18.md`](会话日志/2026-07-18.md) |
| 2026-07-17 | ADR #139 / #140 的 Steamworks Slime Lab 单人 AI 大招与自主游击已落地；后续修复软体驱动倍率、归队预测过冲、近距离移动跟随和单人离线主循环漏掉合体 tick 的问题，并用真实 E 事件 + 真实软体物理覆盖移动 P1 的 92 px + 0.8 秒闭环。目标 battle、local-couch 与权威 all-suite 全绿，网络 / 存档 / 正式 `client` 边界不变 | [`会话日志/2026-07-17.md`](会话日志/2026-07-17.md) |
| 2026-07-16 | ADR #138：Steamworks Lab smoke 统一为隔离 Python runner，固化成功协议、动态 ENet、真实 `user://` 与源码 App ID 保护、pre-commit 和四平台 skill 入口 | [`会话日志/2026-07-16.md`](会话日志/2026-07-16.md) |
| 2026-07-15 | ADR #134 / #135：Steamworks Slime Lab 直接使用配置 Godot 工具链，并把玩家可见本地多人从 ENet 双开改为 P1 键鼠 + P2–P4 手柄的单进程同屏；Steam 与内部协议回归边界保持分离 | [`会话日志/2026-07-15.md`](会话日志/2026-07-15.md) |
| 2026-07-12 | ADR #132 / #133：Steamworks Slime Lab 先落地 module 工具链，核对 4.20 当前源码后改为已内含 `SteamMultiplayerPeer` 的官方 GDExtension；保留可复现 setup / verify / export-release、显式 App ID 初始化和 release 产物门禁 | [`会话日志/2026-07-12.md`](会话日志/2026-07-12.md) |
| 2026-07-11 | ADR #129 / #130：Steamworks Slime Lab 切换专属 App ID `4955670` 并收口初始化 / Lobby / offline 边界；AI 工作流新增 PowerShell 稳定执行规则与可复制模板，不引入包装器 | [`会话日志/2026-07-11.md`](会话日志/2026-07-11.md) |
| 2026-06~07-02 | F1~F13 基础阶段、正式客户端框架、数据 / 契约 / 测试、存档 / 回放、Gear Mod、短刷图与 Steamworks Lab 首轮能力集中落地 | [`会话日志/`](会话日志/) |

## 7. 当前状态入口

> 给下次会话/新机器接入时的明确指引。

当前阻塞、下一步、最近验证的机器可读来源是 `docs/AI记忆/current_state.json`。

摘要：没有阻塞；正式项目当前阶段为 F13 模块大地图。标准模式固定 9×9 模块、每模块 11×11 方格，中心起点与关键路线骨架固定，普通模块按 seed 组合 approved 模板；ModuleWorldManager 维护 assignment/hash/迷雾/逐槽位动态状态，ModuleChunk 只实例化最多 3×3 邻域且不逐格建 Node。玩家与敌人均受封锁格物理边界约束，模块敌人只允许在 floor 格生成或恢复。目标完成后开启撤离，run v4 恢复同一地图并校验 hash；旧 v3 run 重置，meta 不变。线性 RoomManager 已删除，open-warzone 仅为回归路径。ADR #143 后性能测试仅由用户当次明确触发；正式视角仍是俯视角 2D 和矩形格，不读玩家状态、不做隐藏 DDA、不接运行时 LLM；下一步以用户最后明确指令为准。

## 8. 文件位置速查（避免新会话找不到）

| 你想找 | 路径 |
|--------|------|
| AI 开工入口 | `AGENTS.md`（通用） / `docs/AI协作/快速开工.md`（低 token 热路径） / `CLAUDE.md`（Claude Code） / `CODEX.md`（Codex） / `OPENCODE.md`（OpenCode） |
| 编码规则入口 | `AGENTS.md` 第 3 步（CodeBuddy: `.codebuddy/rules/game-coding-rules.md`；Codex: `.codex/rules/game-coding-rules.md`；OpenCode: `.opencode/rules/game-coding-rules.md`；Claude: `.claude/rules/game-coding-rules.md`） |
| PowerShell 稳定执行 | 平台规则第 29 节 + `docs/AI协作/工具适配指南.md`「Windows PowerShell 稳定执行」 |
| 项目地图 / 知识库 | `docs/AI导航.md` + `docs/AI知识库索引.md` + `docs/_kb_index.json` + `docs/术语表.md` |
| GDD / ADR / 待决策 | `docs/游戏设计文档.md` + `docs/决策记录.md` + `docs/修改建议.md` |
| IP / 美术风格 | `docs/IP设定.md` + `docs/IP美术风格.md` |
| 路线与候选 | `docs/正式项目工作规划.md` + `docs/TODO.md` + `docs/功能建议池.md` + `docs/AI辅助开发机会清单.md` |
| 独立 Steamworks Slime Lab | `output/steamworks_lab/README.md`（App ID `4955670`；本地单进程同屏、Steam 联机、GodotSteam 4.20 Windows 工具链 / 导出权威；不等同于正式 `client/PlatformServices`） |
| 正式客户端 Godot 编辑器插件 | `client/addons/README.md`（固定版本、官方来源、SHA-256、许可、本地补丁与手工升级流程） |
| 当前 F13 / 历史工作包 | `docs/AI协作/工作包/F13-HandcraftedRooms.md`；其余阶段见 `docs/AI协作/工作包/` |
| 代码文档 | `docs/代码文档规范.md` + `docs/代码/README.md` + 对应模块文档 |
| 测试 / CI | `docs/测试策略.md` + `docs/CICD规划.md` + `docs/AI协作/实时验证回路.md` |
| 数据 / 文案 / 字符串契约 | `client/data/README.md` + `client/locale/README.md` + `docs/词表与契约.md` |
| AI 协作工程 | `docs/AI协作/README.md` + `docs/AI协作/工具适配指南.md` + `docs/AI协作/上下文预算.md` |
| **本文件（长期索引）** | `docs/AI记忆/项目记忆.md` |
| 机器当前状态 | `docs/AI记忆/current_state.json` |
| 每日会话日志 | `docs/AI记忆/会话日志/YYYY-MM-DD.md` |
| 验证工具 | `tools/docs_health_check.py` + `tools/sync_contracts.py` + `tools/validate_data.py` + `tools/lint_*.py` + `tools/godot_bridge.py` |

## 9. 更新约定（重要）

**触发条件（满足任一即更新）**：
- 任何 ADR 新增 / 修改
- 任何编码规则新增 / 修改
- 任何"待决策项"被采纳或新增
- 任何重要文件结构调整
- 任何工具链变更（CI、git 配置、新模板）
- 重要里程碑达成

**更新动作**（AI 主动执行，无需用户提醒）：
1. 修订本文件第 1~5 节（项目快照 / 决策 / 待决策 / 工具链）以反映长期事实
2. 更新 `docs/AI记忆/current_state.json`（当前阶段、最新 ADR、待决策项、下一步、最近验证）
3. 在第 6 节保留 / 覆盖当日一行摘要（**一行表格条目 + 日志链接**，不展开细节）
4. 在 `会话日志/YYYY-MM-DD.md` 写当日详细摘要（输入/产出/决策/验证）

**节制原则**：
- 只记**结论 + 关键参数 + 文件指针**，不复述完整对话
- 单次会话总更新量目标 < 1KB
- 历史细节归入会话日志，短期状态归入 `current_state.json`，长期索引保持精简（< 200 行最佳）

**自动瘦身（无需用户提醒）**：
- 每次更新本文件前先检查总行数；**临近 200 行立即瘦身**——不等用户问。
- 第 6 节单日条目超过 1 行 → 折叠为「日期 + 一句话摘要 + 日志链接」格式。
- 7 天前的条目 → 压缩为「YYYY-MM-W（周）：一句话」周聚合。
- 30 天前的条目 → 整体折叠为「月份 + 该月主要里程碑」月聚合。
- 决策清单（第 3 节）只保留编号 + 一句话；详情永远去 `决策记录.md` 查。
- 同一位置的累积修订 → **覆盖**而非追加（如 ADR 数量、CICD 项数）。
- `current_state.json` 用覆盖更新，不追加历史；历史永远进会话日志。
- 触发瘦身时本身**不算重大变更**，无需新加 ADR；仅在会话日志记一笔。
**不触发条件**（避免噪声）：
- 单纯的代码风格修复 / typo
- 不涉及决策的纯实现工作（在会话日志记一笔即可）

**示例格式**：`| 2026-06-13 | 一句话摘要 | 链接 |`；旧条目按周 / 月聚合即可。

> 维护要求：本文件是 AI 协作的"长期记忆主控"。任何与本约定不一致的更新，应在 `docs/决策记录.md` 中以新 ADR 改写约定，再同步修订本文件。
