# 2026-06-25 会话日志

> 本文件记录当天 AI 协作的关键变更摘要；长期索引见 `docs/AI记忆/项目记忆.md`，机器可读当前状态见 `docs/AI记忆/current_state.json`。

## 摘要

- 按 `AGENTS.md` 完成 Codex 快速开工，读取快速开工、current_state、Codex 编码规则、AI 导航与 F10 相关工作包 / 模块文档。
- 用户问 F10 下一步后，确认推进 F10.2：把 `warzone_directors.json.interest_points[]` 真正接入 `MapManager` 初始地图机关生成。
- 新增 ADR #113：`WarzoneDirector` 只过滤当前 layout 的兴趣点，`MapManager` 使用通用 PCG / 菱形锚点规则生成 `source="director"` hazards；不读玩家状态、不按 `poi_id` / `hazard_id` 特判、不提升 run 存档 schema。
- 实现 `WarzoneDirector.interest_points_for_layout(layout_id)`，`GameplayRunLoop` 在新局和旧快照缺机关 fallback 时把导演兴趣点传给 `MapManager.generate_hazard_placements()`。
- `MapManager` 生成顺序为 manual -> pcg -> director interest points；director placement 保留 `source="director"` 与 `interest_point_id`，`debug_summary().hazard_sources` 统计 source 数量。
- DataLoader / `tools/validate_data.py` 要求兴趣点 `hazard_ids[]` 非空，`tools/test_data_loader_schema.py` 增加空列表和缺失机关引用坏样例。
- `runtime-smoke` 与 `f9-demo-smoke` 增加 director-sourced hazard 覆盖，确认 FEA-12 兴趣点进入初始地图。
- 同步 F10 工作包、WarzoneDirector / MapManager / GameplayRuntime / DataLoader 模块文档、数值手册、GDD、AI 导航、AI 知识库索引、机器索引、项目记忆和 current_state。
- 用户追问“每局游戏的种子是不是一样的”后确认普通新局路径仍在用默认 seed；新增 ADR #114，让标题开始新局和局内重开调用 `RNG.set_random_run_seed()`，继续游戏从 run snapshot 恢复，replay / smoke / golden 工具仍走固定 seed 路径。
- `RNG` 新增 `set_random_run_seed()` 公共入口，生成的 seed 保证不同于调用前当前主 seed；`FormalClientBoot` 新增 `_start_new_gameplay_run()` 只服务人工开始 / 重开；`l1-smoke` 增加新 API 激活当前 run seed 的断言，`runtime-smoke` 增加重开按钮后 seed 改变的断言。
- 同步 RNG、FormalClientBoot、Gameplay Runtime、GDD、AI 导航、AI 知识库索引、机器索引、项目记忆和 current_state 的普通新局 seed 口径。
- 按用户反馈清空 `map_standard_nest.manual_hazards`，移除两个默认固定陷阱；手摆功能和 schema 保留，后续可用于非陷阱或关键场景点。
- 修复 `tools/test_data_loader_schema.py` 的 manual hazard 坏样例构造，让默认 `manual_hazards` 为空时仍能测试偶数 `radius_tiles` 机关必须使用网格顶点。

## 验证

- 已通过：`python tools/sync_contracts.py --check`、`python tools/validate_data.py`、`python tools/test_data_loader_schema.py`、`python tools/lint_gdscript_rules.py`、`python tools/lint_project_rules.py`、`python -m json.tool docs/AI记忆/current_state.json`、`python -m json.tool docs/_kb_index.json`、`python tools/docs_health_check.py`、`git diff --check -- . ":(exclude)draft/**" ":(exclude)DRAFT/**"`。
- 已通过：`python tools/lint_semantic_rules.py` 退出 0；仍报告 30 条既有 autoload-bypass-pool-ui advisory，本轮未新增相关分支风险。
- 已通过：`python tools/godot_bridge.py --project client headless-boot`、`runtime-smoke`、`f9-demo-smoke`、`save-smoke`、`l1-smoke`、`perf-probe`。一次并行 `runtime-smoke` 出现 Godot 原生 signal 11，单独重跑通过，未复现为脚本断言失败。
- 已通过：因 data fingerprint / runtime summary 变化，重录并 rerun 四条 checked-in golden replay：`golden_basic_run`、`golden_pause_resume`、`golden_full_death`、`golden_level_up_choice`。
- 已通过：ADR #114 普通新局 seed 改动的 `python tools/lint_gdscript_rules.py`、`python tools/lint_project_rules.py`、`python tools/lint_semantic_rules.py`、`python -m json.tool docs/AI记忆/current_state.json`、`python -m json.tool docs/_kb_index.json`、`python tools/docs_health_check.py`、`git diff --check -- . ":(exclude)draft/**" ":(exclude)DRAFT/**"`。
- 已通过：`python tools/godot_bridge.py --project client headless-boot`、`l1-smoke`、`runtime-smoke`、`save-smoke`；`runtime-smoke` 和 `save-smoke` 仍输出预期的坏档解析 / 隔离诊断但退出 0。
- 已通过：四条 checked-in golden replay rerun：`golden_basic_run`、`golden_pause_resume`、`golden_full_death`、`golden_level_up_choice`，均保持 replay 内固定 `run_seed=20260619`。
- 已通过：默认手摆陷阱清空后的 `python tools/sync_contracts.py --check`、`python tools/validate_data.py`、`python tools/test_data_loader_schema.py`、`python tools/lint_gdscript_rules.py`、`python tools/lint_project_rules.py`、`headless-boot`、`runtime-smoke`、`f9-demo-smoke`、`save-smoke`、`perf-probe`；`perf-probe` 显示当前 active hazards 为 8，即 PCG + director，不再包含两个 manual hazards。
- 已通过：默认手摆陷阱清空后的四条 checked-in golden replay rerun：`golden_basic_run`、`golden_pause_resume`、`golden_full_death`、`golden_level_up_choice`，无需重录。

## F11 装备 Mod / 局外装配规划

- 用户明确选择参考《星际战甲》的 Mod 系统，要求暂时提供英雄 / 武器两套 Mod 配置，删除现有局外成长系统，并先规划 / 写文档再开工。
- 新增 ADR #115：跨局成长从 F6 永久升级轨道改为 Gear Mod loadout；旧 `MetaProgressionSystem` / `meta_progression.json` 进入 legacy 迁移 / 补偿范围，不再作为未来下一局属性来源。
- 新增 `docs/AI协作/工作包/F11-GearModLoadout.md`，规划首片范围：`GearModSystem`、`gear_mods.json`、`gear_mod_drop_tables.csv`、`gear_mod_fusion_costs.csv`、英雄 / 武器两套 loadout、capacity / drain、Mod 升级消耗资源、分解返还资源。
- 新增 `docs/代码/gear_mod_system.md`，记录 Gear Mod 系统职责、API 草案、SaveManager meta payload、测试义务和旧 MetaProgression 迁移边界。
- 首个测试 Mod 规划为武器 Mod：提升武器基础 `damage`，玩家击杀最普通小怪 `enemy_chaser` 时有 1% 概率掉落；非玩家击杀、环境击杀或怪物互杀不应产出该 Mod。
- 同步 GDD §7.2 / §9.16、数据手册、测试策略、AI 导航、代码文档索引、术语表、知识库索引、机器索引、正式项目工作规划、TODO、项目记忆和 current_state；后续真正实现前仍需先登记 Gear Mod 相关契约 id 和数据 schema。
- 验证通过：`python -m json.tool docs/AI记忆/current_state.json`、`python -m json.tool docs/_kb_index.json`、`python tools/docs_health_check.py`、`git diff --check -- . ":(exclude)draft/**" ":(exclude)DRAFT/**"`。

## F11 装备 Mod 数据 / 契约首片

- 按用户确认“就这么干”，先落地 F11 第一片：登记 Gear Mod id / slot / rarity / resource / stack rule 契约，生成 `_contracts.json` 与 `client/scripts/contracts/gear_mod_*.gd`。
- 新增 `client/data/gear_mods.json`、`gear_mod_drop_tables.csv`、`gear_mod_fusion_costs.csv`：首个测试武器 Mod 为 `gear_mod_weapon_damage_test`，rank 0 提供 `damage mult 1.10`，每 rank `+0.05`，最大 rank 5；`enemy_chaser` 玩家击杀掉落率 `0.01`；升级消耗 `gear_mod_dust`，分解返还 10。
- `DataLoader` 与 `tools/validate_data.py` 新增 Gear Mod schema：校验 id / locale / slot / rarity / rank / drain / modifier stat / stack rule / dismantle resource、掉落表敌人和 Mod 引用、概率范围、等级区间、升级成本资源和 `1..max_rank` 覆盖。
- `tools/test_data_loader_schema.py` 新增坏样例：未登记 Mod id、缺 locale key、modifier stat 未登记、掉落敌人缺失、掉落概率越界、升级资源未登记、升级成本缺 rank。
- 同步 GDD、F11 工作包、GearModSystem / DataLoader 模块文档、数据手册、测试策略、AI 导航、正式项目工作规划、TODO、快速开工、项目记忆和 current_state；F11 下一步改为实现 GearModSystem 运行时与 SaveManager meta 迁移。
- 已通过：`python tools/sync_contracts.py --check`、`python tools/validate_data.py`、`python tools/test_data_loader_schema.py`、`python tools/lint_gdscript_rules.py`、`python tools/lint_project_rules.py`、`python tools/test_project_rules_lint.py`、`python tools/lint_semantic_rules.py`、`python tools/test_semantic_rules_lint.py`、`python -m json.tool docs/AI记忆/current_state.json`、`python -m json.tool docs/_kb_index.json`、`python tools/docs_health_check.py`、`git diff --check -- . ":(exclude)draft/**" ":(exclude)DRAFT/**"`、`python tools/godot_bridge.py --project client headless-boot`。
- `lint_semantic_rules.py` 仍只报告既有 30 条 autoload-bypass-pool-ui advisory，退出 0；本轮未新增相关业务分支风险。

## F11 GearModSystem 运行时首片

- 新增 `client/scripts/autoload/gear_mod_system.gd` 并注册 autoload；Gear Mod 状态写入 `SaveManager` 的 `meta` payload 子字段 `gear_mods`，保留旧 meta 字段作为 legacy 兼容数据。
- `GearModSystem` 支持 profile roundtrip、资源、库存、稳定 `instance_id`、英雄 / 武器两套 loadout、capacity / drain、`unique_by_id` 唯一装备、授予、升级、分解、`enemy_chaser` 掉落解释和 hero / weapon modifier snapshot。
- `GameplayRunLoop` 开局已停止调用 `MetaProgressionSystem.current_modifiers()`，改为分别把 `GearModSystem.current_modifiers("hero")` 应用到 Player、把 `current_modifiers("weapon")` 应用到 WeaponSystem；玩家归因击杀敌人时调用 `GearModSystem.roll_drop_for_enemy(enemy_id)`。
- `Enemy` 新增 `enemy_id()` 只读 API，供击杀掉落路径读取数据驱动敌人 id。
- 新增 `client/tools/gear_mod_smoke.gd` 与 `python tools/godot_bridge.py --project client gear-mod-smoke`，覆盖授予、槽位拒绝、装备、重复拒绝、分解返还、容量阻止升级、资源升级、modifier 数值变化和强制掉落。
- `tools/godot_bridge.py` 输出解码改为 UTF-8 + replacement，避免 Windows 控制台编码吞掉 Godot 诊断。
- 同步 GearModSystem、Gameplay Runtime、MetaProgression legacy、FormalClientBoot、F11 工作包、测试策略、AI 导航、代码文档索引、知识库索引、机器索引、正式项目工作规划、TODO、项目记忆和 current_state；下一步从“实现运行时”转为 Gear Mod UI、获得提示和旧 purchased_upgrades 补偿迁移。
- 已通过：`python tools/sync_contracts.py --check`、`python tools/validate_data.py`、`python tools/test_data_loader_schema.py`、`python tools/lint_gdscript_rules.py`、`python tools/godot_bridge.py --project client headless-boot`、`python tools/godot_bridge.py --project client gear-mod-smoke`、`python tools/godot_bridge.py --project client runtime-smoke`、`python tools/godot_bridge.py --project client meta-smoke`。
- 注意：`runtime-smoke` 和 `meta-smoke` 都会操作默认 `slot_0`，不要并行跑；本轮一次并行运行互相干扰后，顺序重跑均通过。`runtime-smoke` 仍输出预期的坏档解析 / 隔离诊断但退出 0。

## F11 Gear Mod 最小 UI

- 标题菜单新增“装备 Mod”入口与 `gear_mod_requested` signal；`FormalClientBoot` 通过 `UIManager.push()` 打开 `GearModPanel`，关闭后回到标题菜单。
- 新增 `client/scenes/ui/gear_mod_panel.tscn` 与 `client/scripts/ui/gear_mod_panel.gd`：默认显示武器 Mod，可切换英雄 / 武器 loadout，展示 `gear_mod_dust`、capacity / used drain、Mod 列表、rank、drain、效果、下一阶升级成本和分解返还。
- Gear Mod 面板按钮流已接入 `GearModSystem` 公共 API：装备 / 卸下、升级、分解都会刷新资源、容量、列表、详情和本地化反馈；已装备 Mod 禁止分解，满级禁用升级。
- `GearModSystem.mod_summaries()` 增补 `upgrade_cost` 与 `dismantle` 摘要字段，避免 UI 反查数据表。
- 新增 `ui_gear_mod_*` 双语文案并重新导入 `strings.zh_CN.translation` / `strings.en.translation`；`gear-mod-smoke` 扩展为实例化面板并点击装备、升级、卸下、分解按钮。
- 同步 GearModSystem、Gameplay Runtime、FormalClientBoot、F11 工作包、GDD、测试策略、AI 导航、词表、本地化 README、知识库索引、项目记忆和 current_state；下一步转为获得 Mod 提示和旧 `purchased_upgrades` 补偿迁移。

## F11 Gear Mod 掉落获得提示

- `GearModSystem.roll_drop_for_enemy()` 的掉落结果新增 `name_key`，供 UI 直接显示掉落 Mod 名称。
- `GameplayHud` 新增 `show_gear_mod_drop_feedback(name_key)`，复用升级获得提示的金色 HUD 样式，但使用独立文案 `ui_gear_mod_drop_obtained`。
- `GameplayRunLoop._roll_gear_mod_drop()` 在玩家归因击杀掉落命中后转发 HUD 获得提示；`runtime-smoke` 新增受控 debug roll，固定下一次 `enemy_chaser` 掉落来覆盖击杀路径。
- `gear-mod-smoke` 覆盖强制掉落 payload、HUD 获得提示、导入翻译 key 解析和语言切换刷新；`strings.csv` 与 `.translation` 产物已同步。
- 同步 F11 工作包、GearModSystem / Gameplay Runtime 模块文档、GDD、测试策略、AI 导航、TODO、项目记忆和 current_state；下一步收窄为旧 `purchased_upgrades` 补偿迁移。

### 本轮获得提示验证

- 已通过：`python tools/validate_data.py`、`python tools/lint_gdscript_rules.py`、`python tools/lint_project_rules.py`。
- 已通过：`python tools/godot_bridge.py --project client headless-boot`、`python tools/godot_bridge.py --project client gear-mod-smoke`、`python tools/godot_bridge.py --project client runtime-smoke`；`runtime-smoke` 仍输出预期的坏 `run.save` 解析 / 重置诊断但退出 0。

## F11 旧 purchased_upgrades 成本补偿

- 新增 ADR #116：旧 `purchased_upgrades` 不再提供下一局永久属性，而是按 `meta_progression.json.upgrade_tracks[].costs` 的已购等级历史花费 1:1 折算为 `gear_mod_dust`。
- `GearModSystem._normalize_profile()` 新增 legacy 补偿归一化：只补偿尚未记录的等级，并把进度写到 `gear_mods.legacy_migration.purchased_upgrades_compensation.compensated_levels`，保留旧字段作为 legacy 诊断数据。
- `gear-mod-smoke` 新增合成旧档样例：购买旧 damage 2 级与 fire_rate 1 级时应补偿 70 `gear_mod_dust`，二次读取不重复发放。
- 同步 F11 工作包、GearModSystem / MetaProgression / SaveManager / Gameplay Runtime 模块文档、GDD、测试策略、AI 导航、知识库索引、机器索引、正式项目工作规划、TODO、项目记忆和 current_state；下一步转为更多 Gear Mod 内容或正式手动迁移 checklist。
