# 2026-07-15 会话日志

- 用户要求 Steamworks Slime Lab 完全停止使用 `output/steamworks_lab/.toolchain/`，直接使用本机 `GODOT_PATH=G:\Godot\Godot.exe`；新增 ADR #134，保留 GodotSteam 4.20 GDExtension、Steamworks 1.64、插件版本锁、release 门禁和外部 Steam 验收边界。
- `tools/steamworks_lab_toolchain.py` 保持 `setup / verify / export-release` CLI：editor 解析顺序改为 `--godot` → `GODOT_PATH` → PATH / 常见安装，显式无效路径 fail-fast；setup 用系统临时目录下载 / 校验 / 解压插件，通过 `addons/godotsteam.tmp` / `.bak` 同盘替换并清理遗留 `.toolchain`，不复制 editor/templates、不保留下载缓存；export-release 按 editor 输出定位精确版本的 Godot 用户 templates。
- 新增 `tools/test_steamworks_lab_toolchain.py` 9 条回归，覆盖路径优先级、无效目录、相邻 `steam_api64.dll` 冲突、临时安装与遗留清理、安装替换失败回滚、无 `.toolchain`、精确 templates 目录和 export preset 不再要求 `.toolchain` 排除。
- 从 Godot 官方 4.7.1-stable 发布下载约 1.19 GiB 标准 templates，按官方 `SHA512-SUMS.txt` 校验为 `AFCC83D8D3D298038F19C58744A0D660FA75DD4BAA33CB55D1011BB2565A2A8C2381728924564CB909E37C205A23F21B521B23BD057993AFD43AE4DA0B2F9D47`，安装到 `%APPDATA%\Godot\export_templates\4.7.1.stable` 后删除临时下载目录。
- 自动验证通过：Python compile + 9 regressions、setup、verify、runtime-presence、export-release、导出程序离线启动、Steam 配置 smoke、Lab headless boot、release 必需文件 / 禁入内容和 `.toolchain` / 模板下载缓存不存在检查；release 包含 exe / PCK / `libgodotsteam.windows.template_release.x86_64.dll` / `steam_api64.dll` / `THIRD_PARTY_NOTICES.txt`。
- README、测试策略、AI导航、ADR、知识库索引、项目记忆和 `current_state.json` 已同步；历史 ADR #132/#133 与 2026-07-12 日志保持原样。SteamPipe / Depot / overlay / 双账号真实 P2P 仍是外部后续步骤。
- 本轮按 L 级工具链任务读取并核对 `tools/steamworks_lab_toolchain.py`、Lab README / lock / export preset、文档维护与代码审核流程、测试策略、AI导航、ADR、项目记忆和 current_state；未进入 `draft/` / `DRAFT/`。
