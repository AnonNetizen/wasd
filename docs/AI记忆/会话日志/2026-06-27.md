# 2026-06-27 会话日志

## AI 上下文预算复杂度分级

- 输入：用户询问流行 AI 代码工作流与本项目自建工作流的差异，并确认“应该分一下不同复杂度的任务怎么做”。
- 产出：新增 ADR #121；`docs/AI协作/上下文预算.md` 增加 S / M / L / XL 任务复杂度分级，要求先判流程深度，再按任务类型读取文件。
- 同步：更新 `docs/AI协作/README.md`、`docs/AI导航.md`、`docs/AI知识库索引.md`、`docs/AI记忆/项目记忆.md` 和 `docs/AI记忆/current_state.json`；顺手补齐知识库 ADR 矩阵中的 #120 条目。
- 验证：计划执行 JSON 校验、docs health 和 diff whitespace 检查。

## F12 短刷图数据首片

- 输入：用户确认默认标准局不做硬性 8 分钟倒计时，而是通过 8-12 分钟目标窗口和 9 分钟后软加压收束，并要求继续 F12 下一步。
- 产出：`map_standard_nest` 玩家出生点改为偏外侧格心；`warzone_directors.json` 标准局阶段改为 0-1 / 1-4 / 4-7 / 7-9 / 9+ 分钟，9+ 仅为软加压；`spawn_waves.csv` 改为 0:00 chaser、1:00 swarm、4:00 stalker、7:00 bulwark 持续窗口；新增 `poi_elite_nest`、`poi_mod_cache`、`poi_resource_cache`、`poi_minor_nest_core` 四个 director 兴趣点占位；`runtime-smoke` 和 `f9-demo-smoke` 改为断言四点位与 7 分钟小巢核压力。
- 同步：更新 F12 工作包、GDD、AI 导航、数据手册、WarzoneDirector / MapManager / Gameplay Runtime 模块文档、项目记忆和 `current_state.json`。
- 验证：`sync_contracts --check`、`validate_data`、`test_data_loader_schema`、`lint_gdscript_rules`、`lint_project_rules`、`lint_semantic_rules`、JSON、`docs_health_check`、`diff --check`、`headless-boot`、`runtime-smoke`、`f9-demo-smoke`、`gear-mod-smoke`、`perf-probe`、`save-smoke` 和四条 checked-in golden replay rerun 均通过；`save-smoke` 的坏档错误日志为预期探针，semantic lint 仍为既有 advisory。
