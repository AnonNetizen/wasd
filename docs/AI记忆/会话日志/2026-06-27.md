# 2026-06-27 会话日志

## 局内刷取参考研究文档

- 输入：用户要求把 2D 射击局内刷取玩法的参考游戏和建议写成文档。
- 产出：新增 `docs/局内刷取参考研究.md`，按枪感 / 带回式刷取 / 兴趣点路线 / 局内构筑 / 敌潮压力拆解 `Escape From Duckov`、`SYNTHETIK`、`HELLDIVERS`、`ZERO Sievert`、`Enter the Gungeon`、`Nuclear Throne`、`20 Minutes Till Dawn`、`Nova Drift`、`Hades` 等参考对象，并明确该文档是设计参考，不是已采纳路线图。
- 同步：更新 `docs/AI导航.md`、`docs/AI知识库索引.md`、`docs/_kb_index.json`、`docs/AI记忆/current_state.json` 和 `docs/AI记忆/项目记忆.md` 的入口。
- 验证：计划执行 JSON 校验、docs health 和 diff whitespace 检查。

## AI 上下文预算复杂度分级

- 输入：用户询问流行 AI 代码工作流与本项目自建工作流的差异，并确认“应该分一下不同复杂度的任务怎么做”。
- 产出：新增 ADR #121；`docs/AI协作/上下文预算.md` 增加 S / M / L / XL 任务复杂度分级，要求先判流程深度，再按任务类型读取文件。
- 同步：更新 `docs/AI协作/README.md`、`docs/AI导航.md`、`docs/AI知识库索引.md`、`docs/AI记忆/项目记忆.md` 和 `docs/AI记忆/current_state.json`；顺手补齐知识库 ADR 矩阵中的 #120 条目。
- 验证：计划执行 JSON 校验、docs health 和 diff whitespace 检查。

## F12 短刷图数据首片

- 输入：用户确认默认标准局不做硬性 8 分钟倒计时，而是通过 8-12 分钟目标窗口和 9 分钟后软加压收束，并要求继续 F12 下一步。
- 产出：`map_standard_nest` 玩家出生点改为偏外侧格心；`warzone_directors.json` 标准局阶段改为 0-1 / 1-4 / 4-7 / 7-9 / 9+ 分钟，9+ 仅为软加压；`spawn_waves.csv` 改为 0:00 chaser、1:00 swarm、4:00 stalker、7:00 bulwark 持续窗口；新增 `poi_elite_nest`、`poi_mod_cache`、`poi_resource_cache`、`poi_minor_nest_core` 四个 director 兴趣点占位；`runtime-smoke` 和 `f9-demo-smoke` 改为断言四点位与 7 分钟小巢核压力。
- 同步：更新 F12 工作包、GDD、AI 导航、数据手册、WarzoneDirector / MapManager / Gameplay Runtime 模块文档、项目记忆和 `current_state.json`。
- 验证：`sync_contracts --check`、`validate_data`、`test_data_loader_schema`、`lint_gdscript_rules`、`lint_project_rules`、`lint_semantic_rules`、JSON、`docs_health_check`、`diff --check`、`headless-boot`、`runtime-smoke`、`f9-demo-smoke`、`gear-mod-smoke`、`perf-probe`、`save-smoke` 和四条 checked-in golden replay rerun 均通过；`save-smoke` 的坏档错误日志为预期探针，semantic lint 仍为既有 advisory。

## F12 兴趣点奖励首片

- 输入：用户确认“继续推进”，沿着 F12 下一步把兴趣点从占位推进到首版奖励 / 完成语义；仍保持 8-12 分钟目标窗口，不加硬性一局倒计时。
- 产出：`warzone_directors.json` 兴趣点新增领取半径、解锁时间、资源奖励、Gear Mod 奖励和小巢核完成标记；`GameplayRunLoop` 可一次性领取兴趣点奖励，保存 / 续局恢复 `interest_points` claimed 状态，资源缓存给 dust，Mod 缓存给测试 Gear Mod，小巢核给 dust + Mod 并进入完成面板；`GearModSystem` 新增正式资源授予 API，HUD 新增资源获得反馈，DataLoader / `validate_data` / schema 回归校验奖励引用。
- 同步：更新 F12 工作包、GDD、AI 导航、数据手册、WarzoneDirector / MapManager / GearModSystem / Gameplay Runtime / SaveManager 模块文档、测试策略、项目记忆和 `current_state.json`。
- 验证：`sync_contracts --check`、`validate_data`、`test_data_loader_schema`、`lint_gdscript_rules`、`lint_project_rules`、`test_project_rules_lint`、`lint_semantic_rules`、`test_semantic_rules_lint`、JSON、`docs_health_check`、`diff --check`、`headless-boot`、`runtime-smoke`、`gear-mod-smoke`、`save-smoke`、`f9-demo-smoke`、`perf-probe` 和四条 checked-in golden replay 重录 + 串行 rerun 均通过；golden 重录原因是数据指纹有意变化，semantic lint 仍为既有 23 条 advisory。
- 当时待做：真实宝箱交互、真实小巢核实体、撤离或正式 Result UI、完整战利品清单、精英巢点奖励语义和 Gear Mod / 材料掉落频率；其中真实宝箱交互已在本日后续“F12 真实缓存箱交互首片”完成首片。

## F12 可伤害兴趣点目标首片

- 输入：用户要求继续推进 F12；本步选择把 Mod 缓存、精英巢点和小巢核从纯领取元数据推进到可见、可被子弹打掉的目标占位。
- 产出：新增 `InterestPointTarget` 低频节点和 `active_interest_point_targets` 命中组；`Bullet` 可同时命中敌人与兴趣点目标，伤害仍走 `Combat.apply_damage()`；`warzone_directors.json` 兴趣点新增 `target_hp` / `target_hit_radius`；`GameplayRunLoop` 生成 / 保存 / 恢复目标生命和摧毁状态，目标摧毁后触发原有奖励 / 完成逻辑；runtime-smoke 改为通过目标伤害覆盖 Mod 缓存和小巢核完成路径。
- 同步：更新数据手册、F12 工作包、GDD、AI 导航、WarzoneDirector / MapManager / Gameplay Runtime / SaveManager 模块文档、测试策略、项目记忆和 `current_state.json`。
- 验证：`sync_contracts --check`、`validate_data`、`test_data_loader_schema`、`lint_gdscript_rules`、`lint_project_rules`、`test_project_rules_lint`、`lint_semantic_rules`、`test_semantic_rules_lint`、JSON、`docs_health_check`、`diff --check`、`headless-boot`、`runtime-smoke`、`gear-mod-smoke`、`save-smoke`、`f9-demo-smoke`、`perf-probe` 和四条 checked-in golden replay rerun 均通过；本次不需要重录 golden；semantic lint 仍为非阻塞 advisory，新增兴趣点目标动态挂节点归类为低频 POI 合理例外。
- 当时待做：真实宝箱交互、正式核心实体行为 / 美术、撤离或正式 Result UI、完整战利品清单、精英守卫语义和 Gear Mod / 材料掉落频率；其中真实宝箱交互已在本日后续“F12 真实缓存箱交互首片”完成首片。

## F12 兴趣点目标可读性修复

- 输入：用户反馈三个问题：兴趣点不能被子弹打、视觉上不符合地图格、会和陷阱重合。
- 产出：`InterestPointTarget` 改为生成后立即可被子弹 / `Combat` 伤害，`claim_start_time` 只继续限制无目标兴趣点的进圈领取；目标视觉改为贴合量化地图格的菱形 footprint；`MapManager` 为带 `target_hp` 的兴趣点先生成独立格心 target anchor，再把 director 机关放到目标附近并避开目标 footprint；`GameplayRunLoop` 使用该目标 anchor 作为兴趣点位置。
- 同步：更新数据手册、F12 工作包、WarzoneDirector / MapManager / Gameplay Runtime 模块文档、测试策略、项目记忆和 `current_state.json`。
- 验证：计划追加 `runtime-smoke` 覆盖真实 Bullet 在 `claim_start_time` 前命中兴趣点目标、目标格点对齐和目标避开 active hazards；最终命令以本次提交前验证记录为准。

## F12 战利品暂存后结算

- 输入：用户明确不要“已拿到就带回”，参考《星际战甲》撤离逻辑；死亡不带回任何未结算奖励，其余按前一轮建议推进。
- 产出：新增 ADR #122；`GameplayRunLoop` 增加 `run.pending_loot`，兴趣点奖励和玩家归因 Gear Mod 掉落先暂存，保存退出会保存 pending，击破小巢核会统一调用 `GearModSystem.grant_resource()` / `grant_mod()` 写入 `meta.gear_mods`；死亡、重开、回标题或主动放弃只删除 run、不提交 pending；`GameOverPanel` 可显示带回 / 丢失战利品摘要。
- 同步：更新 F12 工作包、GDD、Gameplay Runtime / GearModSystem 文档、数据与 locale 手册、测试策略、AI 导航、知识库索引、项目记忆和 `current_state.json`；HUD 文案从“获得”改为“暂存”。
- 验证：计划执行数据 / 文档 / GDScript lint、headless、runtime / gear-mod / save / f9 / perf smoke 和四条 checked-in golden replay；最终命令以本次提交前验证记录为准。

## F12 小巢核后撤离首片

- 输入：用户要求继续推进 F12；本轮按前序讨论把“战利品必须带回”落实为首版撤离点，而不是击破小巢核立刻入账。
- 产出：新增 ADR #123；`warzone_directors.json` 小巢核新增 `extraction_radius` / `extraction_hold_time`；`GameplayRunLoop` 击破小巢核后开启贴合菱形地图格的撤离区，玩家站进撤离区完成短读条后才提交 `run.pending_loot`、删除 `run` 并显示完成面板；撤离状态进入 run snapshot；HUD 新增撤离点开启提示；MapManager 目标摆位在严格 `min_spacing` 失败时降级为不重叠摆位，避免 POI 目标语义消失。
- 同步：更新 F12 工作包、GDD、Gameplay Runtime / WarzoneDirector / MapManager 文档、数据手册、测试策略、AI 导航、知识库索引、项目记忆和 `current_state.json`。
- 验证：计划执行数据 / schema / GDScript / 项目规则 / 语义 lint、headless、runtime / gear-mod / save / f9 / perf smoke、docs health、JSON、diff whitespace 和 checked-in golden replay；最终命令以本次提交前验证记录为准。

## F12 结果清单首片

- 输入：用户确认继续做“F12 结算结果清单”，让撤离成功后能清楚看到本局带回了什么。
- 产出：`GameOverPanel` 从只显示摘要升级为逐项清单：撤离成功显示带回的 `gear_mod_dust` / Gear Mod、击杀数和用时；死亡失败显示丢失的暂存 Gear Mod。结果清单行格式走 `ui_result_resource_line` / `ui_result_gear_mod_line` 本地化 key，`runtime-smoke` 覆盖成功和失败两条面板文本路径，`strings.csv` 已重新导入 `.translation`。
- 同步：更新 F12 工作包、GDD、AI 导航、Gameplay Runtime 模块文档、locale 手册、测试策略、项目记忆和 `current_state.json`。
- 验证：已通过 `validate_data`、`lint_gdscript_rules`、`lint_project_rules` 和 `runtime-smoke`；完整提交前验证以最终记录为准。

## F12 真实缓存箱交互首片

- 输入：用户确认先做真实宝箱交互，目标是让资源缓存 / Mod 缓存不再被子弹打或进圈自动领取，而是进入半径后按交互键打开。
- 产出：新增 `input.interact` / `input.interact` 设置项，默认 E、手柄 X；资源缓存 / Mod 缓存在 `warzone_directors.json` 使用 `requires_interaction=true`，运行时生成贴合地图菱形格的 `InterestPointCache`；HUD 显示“按 {binding} 打开缓存箱”，按 `interact` 后领取奖励并继续暂存到 `run.pending_loot`。`runtime-smoke` 覆盖资源缓存和 Mod 缓存交互领取，`settings-smoke` 覆盖交互键重绑定和设置面板控件。
- 同步：更新词表、设置文档、F12 工作包、GDD、AI 导航、WarzoneDirector / MapManager / Gameplay Runtime 模块文档、数据与 locale 手册、测试策略、项目记忆和 `current_state.json`。
- 验证：计划执行数据 / schema / GDScript / 项目规则 / 语义 lint、Godot import、headless、runtime / settings / gear-mod / save / f9 / perf smoke、docs health、JSON、diff whitespace 和 checked-in golden replay；最终命令以本次提交前验证记录为准。

## F12 交互缓存箱贴格与避让修复

- 输入：用户反馈真实缓存箱有两个问题：视觉上不符合菱形地图格风格，并且会和陷阱重叠。
- 产出：`MapManager` 把 POI blocker 从目标专用泛化为 POI anchor；`requires_interaction=true` 且无 `target_hp` 的资源缓存 / Mod 缓存会先生成独立 `interest_point_cache_position` 格心 marker，关联 director 陷阱围绕该 anchor 摆放但避开缓存 footprint；`GameplayRunLoop` 优先用该缓存 anchor 生成 `InterestPointCache`；`InterestPointCache` 从矩形箱体占位改为菱形 footprint / inner diamond / core glyph / opened shard 视觉，并加入 `active_interest_point_caches` 组；`runtime-smoke` 新增缓存箱格点对齐和不与 `active_hazards` 重叠断言。
- 同步：更新 GDD、AI 导航、F12 工作包、MapManager / Gameplay Runtime 模块文档、测试策略、项目记忆和 `current_state.json`。
- 验证：已通过 `lint_gdscript_rules`、`validate_data`、`lint_project_rules`、`headless-boot` 和 `runtime-smoke`；完整提交前验证以最终记录为准。

## F12 缓存箱层级与玩家 3D 朝向修复

- 输入：用户反馈缓存箱会显示在玩家模型之上；玩家模型既然已经是 3D，就不应再只做左右两个角度。
- 产出：`InterestPointCache` 场景层级降到玩家 `Player3DVisual/VisualSprite` 下方，避免贴地缓存箱压住玩家模型；`Player3DVisual` 新增完整 `set_facing_direction()` / `facing_direction()`，按 `Player.aim_direction` 计算模型 Y 轴 yaw，保留 `set_facing_sign()` 兼容 2D fallback；`Player` 优先把完整瞄准方向传给 3D 视觉；`runtime-smoke` 新增玩家 3D 视觉跟随完整瞄准方向和缓存箱低于玩家模型的断言。
- 同步：更新 F12 工作包、GDD、AI 导航、Gameplay Runtime 模块文档、项目记忆和 `current_state.json`。
- 验证：已通过 `json current_state`、`lint_gdscript_rules`、`validate_data`、`lint_project_rules`、`docs_health_check`、`diff --check`、`headless-boot`、`runtime-smoke` 和 `lint_semantic_rules`；runtime-smoke 覆盖 3D 视觉跟随完整瞄准方向和缓存箱低于玩家模型，semantic lint 仍为既有 advisory。

## F12 缓存箱正式表现首片

- 输入：用户确认把缓存箱从抽象菱形标记推进为正式表现首片，并指出显示层级仍有问题。
- 产出：`InterestPointCache` 从大面积蓝色 / 金色菱形 glyph 改为淡菱形 footprint + 低矮箱体，Mod / 资源缓存只用小嵌片做功能色区分；打开后显示抬起的箱盖状态；场景 `z_index` 调整为 `-1`，让缓存箱位于地图背景 / MapManager 之上、机关 / 敌人 / 玩家模型之下；`runtime-smoke` 把层级断言收紧为地面层级。
- 同步：更新 F12 工作包、GDD、AI 导航、Gameplay Runtime 模块文档、项目记忆和 `current_state.json`。
- 验证：已通过 `lint_gdscript_rules`、`validate_data`、`lint_project_rules`、`json current_state`、`docs_health_check`、`diff --check`、`headless-boot`、`runtime-smoke` 和 `lint_semantic_rules`；runtime-smoke 覆盖缓存箱格点对齐、避开陷阱、交互暂存和地面层级，semantic lint 仍为既有 advisory。
