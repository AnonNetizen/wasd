# 2026-07-18 会话日志

- 用户要求在 `output/test_lab` 落地一个原创、类 Nova Drift 视觉原则的纯暗空霓虹几何战斗美术实验，并明确选择“纯暗空霓虹 + 可玩战斗沙盒 + 抽象战斗体 + 轻量生命循环”；该实验只验证高对比矢量轮廓、弹幕分型和构筑可视化，不复制参考作品的具体资产，也不修改正式 `client/`、GDD、ADR、AI 导航、`current_state.json` 或项目记忆。
- 新增 `neon_geometry_combat_test.tscn`：琥珀 / 紫色裂锥体玩家、品红断环猎体、青色三轴炮体、实心玩家裂针、敌方楔弹与空心断环弹全部使用 typed GDScript 和 `CanvasItem` draw API；包含 WASD / 方向键移动、鼠标瞄准、按住左键射击、受击闪烁、预警、命中碎片、死亡崩散、自动重生、48 + 48 发固定弹池与 64 槽 VFX 池。场中央折射棱芯会展开玩家侧模块并把单发切换为 `-10° / 0° / +10°` 三向弹幕，玩家重生保留升级，`R` 完整重置，`Esc` 返回实验索引。
- 新增 `neon_geometry_combat_smoke.gd` 与确定性截图工具：smoke 覆盖索引入口、1 玩家 + 2 敌人、可玩开火 action、三类弹体、1→3 发构筑切换、玩家 / 敌人死亡重生、重生无敌、升级保留、重置清理和固定池容量；Godot 4.7.1 Test Lab headless boot、显式场景加载、相关 GDScript `--check-only` 与专用 smoke 通过。预览截图 `screenshots/neon_geometry_combat_test.png` 已人工复核，玩家、两类敌人和三类弹体在暗背景上轮廓分明，实心玩家弹与红色楔弹 / 空心断环弹不混淆，辉光未遮蔽预警主体；场景未使用位图、HDR Bloom、嵌入式 `Image` 或 `PackedByteArray`。
- Steamworks Slime Lab 工具链补齐 portable/self-contained Godot：`export-release` 在 editor 同目录存在 `_sc_` 时只检查 `editor_data/export_templates/<精确版本>.stable`，普通安装仍检查 `%APPDATA%/Godot/export_templates/<精确版本>.stable`，禁止跨模式误用模板；Python 回归覆盖两种目录、self-contained 不回退和缺失目录报错。仓库 README、ADR #134、AI 导航、测试策略和 AI 记忆移除单机绝对路径，统一以 `--godot` / `GODOT_PATH` 或环境变量路径表述。
- 用户确认霓虹几何实验基础成立但表现空间仍大；在不引入图片、HDR Bloom、额外实体节点或扩大固定池的前提下，重做玩家双前叉负形、断环朝向缺口、三轴锁向主炮、分级辉光材质、三层深空背景、弹体残像、四类分层池化 VFX、后坐 / 命中 / 死亡 / 重生节奏、折射棱芯部署与升级准星、几何 HUD 和确定性交火截图。扩展 smoke 继续门禁 `48 + 48 + 64` 固定容量，并新增世界层冲击、升级部署波、VFX 类型覆盖与 reset 清理断言。
- 用户进一步要求至少三轮“截图 → 六维视觉批评 → 修改 → 回归”，并明确指出现有效果仍缺材质、特效与 juice。保留 Round 0–3 四张确定性对比图及 `NEON_GEOMETRY_VISUAL_ITERATIONS.md`：Round 1 建立暗壳、不透明主面、方向性分面、热芯与局部环境染色；Round 2 加入 hit-stop、枪口爆闪、冲击星芒、折射涟漪和分阶段危险预警；Round 3 放大提亮玩家、加入移动残影、收敛命中遮挡、重做开放式升级准星、递减敌弹残影和三类单位专属崩解。VFX 仍固定为 64 槽但扩展为六类复用表现，专项 smoke 新增 hit-stop、五类以上并发 VFX 与 reset 清理断言。
- 用户随后指定 Nova Drift 官方实机截图作为 Round 4 参考，并选择“全时高强霓虹”。本轮只提炼白热核心 / 饱和色层 / 宽外晕、长渐细轨迹、体量差和单一事件焦点，不复制飞船、Boss、HUD 或资产：玩家、断环猎体与三轴炮体在确定性构图中约放大 15% / 20% / 28%，背景新增程序化宽星云与右下画外环状星体；玩家运动带固定 14 点，三类弹体各用固定 10 点环形历史形成金紫枪线、红色燕尾短迹和空心断环列；预警移到近黑隔离底的最上层，64 个 VFX 槽继续复用但加入宽能量盘与白热接触核。新增 `debug_prepare_capture_phase()` 的 CHARGE / CONTACT / AFTERMATH 三状态，捕获工具一次输出三张 Round 4 对比图并让 CONTACT 覆盖 canonical 预览；专项 smoke 覆盖阶段、轨迹上限、reset 清空、48/48/64 容量与零子节点，连续两次捕获 SHA-256 一致。Round 4 在 100% / 50% / 25% 检查下仍可区分三单位和三弹种，未修改正式 `client/`、GDD、ADR、AI 导航、项目记忆或 `current_state.json`。
- 用户确认项目当前引擎补丁基线为 Godot 4.7.1 stable，并要求只同步说明、规则与 AI 协作资料，不修改运行时代码、工具代码或项目配置。新增 ADR #141，GDD 升至 v1.19；README、正式客户端、Test Lab、Steamworks Lab、AI 导航、CI / 启动模块文档、三份完整规则及 `.agents` / 四平台五份 `godot-gdscript` skill 已同步。`project.godot` 的 `config/features="4.7"`、官方 `/en/4.7/` 文档和 Steam lock 的 `godot_version: "4.7"` 继续保留 minor-series 语义。配置编辑器输出 `4.7.1.stable.official.a13da4feb`，三个项目 headless boot、Steamworks Lab `verify`、JSON / ADR / GDD / 项目记忆一致性、规则与 skill 同步哈希、文档健康、diff 与目标文件 pre-commit 均通过；未处理现有未跟踪 `.uid` / `.png.import`。
