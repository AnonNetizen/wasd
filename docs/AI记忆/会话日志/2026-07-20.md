# 2026-07-20 会话日志

- 用户要求完整实现 F13 九宫模块大地图计划。ADR #142 取代 ADR #127 的线性手工房间默认方向：正式标准模式现为 9×9 个无缝模块、每模块 11×11 格、单格默认 160 px 且世界级可配置，总计 99×99 格；完整 9×9 已成为默认，`--module-world-technical-slice` 保留中心 3×3 / 外圈 72 槽封锁的 opt-in 首片，`--open-warzone` 仅作非默认回归。
- 新增世界配置、模块注册表、16 个独立模块 JSON（15 个 approved AI 正式模板 + 1 个 candidate 封锁模板）、AI 模块模板和五组生成契约；DataLoader 与 Python validator 双端门禁 9×9 / 11×11、正格尺寸、固定锚点唯一角色、approved 池、旋转 / 通道 / 外圈、footprint / 引用 / 占用、安全区、角色预算、全局可达性、路线与可选探索不超过 14，并为坏样例补回归。
- 新增 `ModuleWorldManager`、合并绘制 / 碰撞的 `ModuleChunk` 和 9×9 模块迷雾 HUD；默认按 run seed / `RNG.world` 组合普通槽，失败使用随版本提交的安全布局，只保持玩家周围最多 3×3 chunk。GameplayRunLoop 在模块模式停用旧 PCG / WarzoneDirector 摆点，按世界槽位生成、保存、释放和恢复敌人、机关、子弹与掉落；子弹 / 掉落按卸载时实际位置归槽，离开 / 返回不重复刷怪或领奖。
- run schema / SaveManager run kind 升至 v4，保存 81 槽 assignment / rotation、迷雾、目标 / 撤离和逐槽动态状态。map hash 现在覆盖世界配置、seed、assignment 与本局引用的完整模块 JSON，地形 / 通道 / 摆放 / 格尺寸 / 锚点改变都会拒绝旧续局；GameplayRunLoop 在 hash / assignment 失败时于玩家和实体恢复前 fail closed。旧 v3 run 使用专用“不兼容”提示并只删除 run，`meta` roundtrip 保持不变。
- 删除 RoomManager、线性序列数据、房间 marker / demo 场景、旧门 / 清房契约、旧启动参数和 `room-switch-smoke`；旧 F13 手工房间工作包标记 superseded，F12 开放战区仍以 `runtime-smoke` / `f9-demo-smoke` 保留回归。
- 事实型审查发现并推动修复：内容未纳入 map hash、恢复未完全 fail closed、子弹 / 掉落未流式保存、v3 提示误报损坏、fixed_slots 锚点角色缺门禁、cell_size 被错误锁死 160、接缝 smoke 仅瞬移、optional exploration 未限制 14。默认与技术首片 smoke 现用真实 CharacterBody2D 移动跨缝，并覆盖内容变更 hash、瞬态池化实体和坏 hash 拒绝。
- 四条 checked-in golden replay 因新增内容敏感 `module_map_hash` 有意重录并逐条 `replay-runner --rerun-runtime-summary` 通过。新增 `startup-probe` 用 Bridge 单调时钟硬门禁正式主场景首帧到 `PLAYING` ≤2 秒，本机结果约 1.17 秒；`perf-probe` 通过，p99 / 跨模块最大帧约 6.94 ms、静态内存约 59.5 MiB、敌人峰值 46、子弹峰值 0、活跃 chunk 9。最终完整静态、headless、runtime、save、F9、性能、文档健康与预提交结果见 `current_state.json.last_verified` 和本任务提交。
