# 2026-06-17 会话日志

> 本文件记录当天重要 AI 协作对话摘要。长期事实同步到 `docs/AI记忆/项目记忆.md`，机器当前状态同步到 `docs/AI记忆/current_state.json`。

## F3 DataLoader characters schema 边界

- 输入：用户要求继续推进 F3。
- 评估：在 `game_modes.json` 已能引用角色池后，补 `characters.json` 能让默认角色从“只有词表 id”变成正式数据源，价值高；主要风险是过早实现角色选择、起始武器或破限能力运行时，因此本次只落数据、契约、schema 和 smoke 计数。
- 产出：新增 `client/data/characters.json` 默认角色黄金样例；新增 `character_` 本地化前缀与默认角色中英文文案；`DataLoader` 与 `tools/validate_data.py` 校验角色 id、locale key、默认解锁、tags、capabilities、control profile 和 base stats，并要求 `game_modes` 角色池与 `meta_progression` 角色解锁指向 `characters.json` 中真实存在的角色；`FormalClientBoot` 输出 `characters` 计数；同步数据手册、本地化手册、GDD、DataLoader / FormalClientBoot 模块文档、AI 导航、测试策略、知识库索引、项目记忆与 `current_state.json`。
- 结论：F3 已覆盖 `player.json`、`characters.json`、`game_modes.json`、`meta_progression.json`、`strings.csv`、`growth.csv` 与 `growth_pools.json`；下一小步优先补 DataLoader L1 schema 错误测试，或继续 weapons / relics / enemies 等后续内容数据 schema。

## F3 DataLoader schema 回归测试

- 输入：用户继续要求推进 F3。
- 评估：当前 F3 已有正向 `validate_data.py` 与 headless smoke，但缺少坏样例回归；新增轻量 Python 测试比立刻引入完整 GUT runner 成本更低，能先守住 DataLoader-facing schema 错误格式。
- 产出：新增 `tools/test_data_loader_schema.py`，用临时数据副本验证黄金数据通过，并断言未登记 character / mode id、缺失 locale key、错误 stat 类型、非法 stat 范围、game_modes 角色 / growth_pool 引用缺失、meta unlock 角色目标缺失都会 fail-fast；`.github/workflows/docs-check.yml` 已接入该测试；同步 ADR #70、CI / 实时验证 / 测试策略 / DataLoader 文档 / F3 工作包 / AI 导航 / 知识库索引 / 记忆。
- 结论：DataLoader schema 的 L1 等价坏样例回归已补齐；F3 下一步可继续 weapons / relics / enemies 等后续内容数据 schema，或等 Godot test runner 接入后迁入 / 补齐 GUT 侧 DataLoader 单测。

## F3 DataLoader weapons schema 边界

- 输入：用户继续要求推进 F3。
- 评估：在角色和游戏模式数据边界已落地后，补 `weapons.json` 能让 `characters.starting_weapon_id` 与 `game_modes.resource_pools.weapons` 形成真实跨文件引用闭环，价值高；主要风险是过早实现 WeaponSystem、子弹实例化、命中判定或武器选择 UI，因此本次只落数据、locale、schema、回归测试和 smoke 计数。
- 产出：新增 `client/data/weapons.json` 默认基础武器黄金样例；新增 `weapon_` 本地化前缀与基础武器中英文文案；`characters.json` 默认角色引用 `starting_weapon_id`；`game_modes.json` 标准模式声明武器池；`DataLoader` 与 `tools/validate_data.py` 校验武器 id、locale key、默认解锁、fire_mode、fire_audio_id、base_stats、projectile pool、damage_type、弹体数值、角色起始武器引用和模式武器池引用；`tools/test_data_loader_schema.py` 补武器坏样例；`FormalClientBoot` 输出 `weapons` 计数；同步数据手册、本地化手册、词表、DataLoader 文档、AI 导航、测试策略、知识库索引、F3 工作包、TODO、正式规划与记忆。
- 结论：F3 已覆盖 `player.json`、`characters.json`、`weapons.json`、`game_modes.json`、`meta_progression.json`、`strings.csv`、`growth.csv` 与 `growth_pools.json`；下一小步优先继续 relics / enemies 等后续内容数据 schema，仍不实现对应玩法运行时。

## F3 DataLoader relics schema 边界

- 输入：用户继续要求推进 F3。
- 评估：`relics.json` 与已有 `modifiers` / `behaviors` 数据规则直接相连，先落被动遗物 schema 能提升后续升级池、掉落、局外解锁和内容生产的性价比；主要风险是提前实现遗物拾取、`ModifierEngine` 应用或 UI，因此本次只落数据、locale、schema、回归测试和 smoke 计数。
- 产出：新增 `client/data/relics.json` 默认遗物 `relic_sharp_rounds` 黄金样例；新增 `relic_sharp_rounds_name` / `relic_sharp_rounds_desc` 中英文文案；`game_modes.json` 标准模式声明遗物池；`DataLoader` 与 `tools/validate_data.py` 校验遗物 id、locale key、默认解锁、`tag_relic`、modifiers、behaviors、至少一个 modifier / behavior 和模式遗物池引用；`tools/test_data_loader_schema.py` 补遗物坏样例；`FormalClientBoot` 输出 `relics` 计数；同步数据手册、本地化手册、DataLoader 文档、AI 导航、测试策略、知识库索引、F3 工作包、TODO、正式规划与记忆。
- 结论：F3 已覆盖 `player.json`、`characters.json`、`weapons.json`、`relics.json`、`game_modes.json`、`meta_progression.json`、`strings.csv`、`growth.csv` 与 `growth_pools.json`；下一小步优先继续 enemies 等后续内容数据 schema，仍不实现敌人运行时或刷怪系统。

## F3 DataLoader enemies schema 边界

- 输入：用户继续要求推进 F3。
- 评估：`enemies.csv` 是 F4 敌人与刷怪前最关键的平表数据边界，先落 schema 能让模式敌人池、对象池 id、伤害类型和基础数值提前闭环；主要风险是提前实现 Enemy/Spawner/Combat 运行时，因此本次只落数据、locale、词表、schema、回归测试和 smoke 计数。
- 产出：新增 `client/data/enemies.csv` 默认敌人 `enemy_chaser` 黄金样例；新增 `enemy_chaser_name` 中英文文案；新增 `tag_enemy` content tag 并同步生成契约；`game_modes.json` 标准模式声明敌人池；`DataLoader` 与 `tools/validate_data.py` 校验敌人 id、locale key、`tag_enemy`、pool id、生命、移速、接触伤害、伤害类型、经验奖励、命中半径和模式敌人池引用；`tools/test_data_loader_schema.py` 补敌人坏样例；`FormalClientBoot` 输出 `enemies` 计数；同步数据手册、本地化手册、词表、DataLoader 文档、AI 导航、测试策略、知识库索引、F3 工作包、TODO、正式规划与记忆。
- 结论：F3 已覆盖 `player.json`、`characters.json`、`weapons.json`、`enemies.csv`、`relics.json`、`game_modes.json`、`meta_progression.json`、`strings.csv`、`growth.csv` 与 `growth_pools.json`；下一小步可继续 hazards / spawn_waves / active_items 等后续内容数据 schema，仍不实现对应玩法运行时。

## F3 DataLoader hazards schema 边界

- 输入：用户继续要求推进 F3。
- 评估：`hazards.csv` 是后续地图机关、波次事件和 HazardSystem 前的低风险数据边界；先落 schema 能让模式机关池、对象池 id、伤害类型和触发数值提前闭环。主要风险是提前实现机关放置、碰撞、预警表现或伤害结算，因此本次只落数据、locale、词表、schema、回归测试和 smoke 计数。
- 产出：新增 `client/data/hazards.csv` 默认机关 `hazard_spike_trap` 黄金样例；新增 `hazard_spike_trap_name` 中英文文案；新增 `hazard_` locale 前缀、`tag_hazard` content tag 与 `hazard_spike` pool id 并同步生成契约；`game_modes.json` 标准模式声明机关池；`DataLoader` 与 `tools/validate_data.py` 校验机关 id、locale key、`tag_hazard`、pool id、伤害、伤害类型、触发间隔、范围、持续时间和模式机关池引用；`tools/test_data_loader_schema.py` 补机关坏样例；`FormalClientBoot` 输出 `hazards` 计数；同步数据手册、本地化手册、词表、DataLoader 文档、AI 导航、测试策略、知识库索引、F3 工作包、TODO、正式规划与记忆。
- 结论：F3 已覆盖 `player.json`、`characters.json`、`weapons.json`、`enemies.csv`、`hazards.csv`、`relics.json`、`game_modes.json`、`meta_progression.json`、`strings.csv`、`growth.csv` 与 `growth_pools.json`；下一小步可继续 spawn_waves / active_items / consumables 等后续内容数据 schema，仍不实现对应玩法运行时。
