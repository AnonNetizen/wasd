# 2026-06-16 会话日志

> 本文件记录当天重要 AI 协作对话摘要。长期事实同步到 `docs/AI记忆/项目记忆.md`，机器当前状态同步到 `docs/AI记忆/current_state.json`。

## 多模式资源复用原则

- 输入：用户提出后续可能会有多种游戏模式，角色、遗物等资源不要做得强耦合，应易于复用。
- 评估：该需求与既有“扩展优先 / 数据驱动 / 禁止按 id 写特殊分支”一致，价值高；主要风险是过早做重模式系统，因此本次只落设计约束和数据边界，不实现模式系统。
- 产出：新增 ADR #65；同步 GDD、AI 导航、数据手册、三平台规则入口、项目记忆与 `current_state.json`。
- 结论：资源本体默认模式无关；后续 `game_modes.json` 等模式配置只负责组合资源池、权重、条件、禁用列表和轻量覆盖，禁止为某个模式复制资源或写 `if mode_id == ...` 内容分支。
- 实际读取：`AGENTS.md` 初始化上下文、`project-doc-sync` skill、`docs/AI协作/文档维护指南.md`、`docs/游戏设计文档.md`、`docs/决策记录.md`、`docs/AI导航.md`、`client/data/README.md`、三平台规则入口、`docs/AI记忆/项目记忆.md`、`docs/AI记忆/current_state.json`。

## 未来多人 PvE / PvP 预留边界

- 输入：用户确认当前不做多人，但未来可能做多人 PvE 甚至 PvP，要求现在预留空间。
- 评估：多人会影响输入、阵营、伤害、模式、回放、存档和埋点边界；现在实现网络、同步协议、服务器权威或反作弊性价比低，因此只预留轻量架构边界。
- 产出：新增 ADR #66；同步 GDD §6.7、数据手册、AI 导航、三平台规则、AI 知识库索引、项目记忆与 `current_state.json`。
- 结论：当前仍单人离线优先；业务逻辑避免写死唯一玩家 / 唯一队伍 / 玩家只打敌人的假设，统一保留 actor、participant、team、intent 与 Combat source/target 边界。
- 实际读取：`project-doc-sync` skill、`docs/AI协作/文档维护指南.md`、`docs/游戏设计文档.md`、`docs/AI导航.md`、`client/data/README.md`、三平台规则入口、`docs/决策记录.md`、`docs/AI知识库索引.md`、`docs/AI记忆/项目记忆.md`、`docs/AI记忆/current_state.json`。

## 正式项目 F1 工程骨架

- 输入：用户要求根据 `AGENTS.md` 初始化后，按 `docs/正式项目工作规划.md` 开工。
- 评估：F1 是当前最靠前且阻塞后续阶段的任务，价值高；本次只建立可验证 Godot 工程骨架，不提前迁移 MVP 临时代码或实现 F2 autoload。
- 产出：新增 `client/project.godot`、`client/scenes/boot/main.tscn`、`client/scripts/boot/formal_client_boot.gd`、`client/README.md`、`docs/代码/formal_client_boot.md`，补齐 `client/assets/` 与 `client/templates/` 占位目录，并同步 README、AI 导航、代码文档索引、TODO、知识库索引与 `current_state.json`。
- 结论：正式 `client/` 已成为可启动 Godot 4.6.3 项目根；下一步进入 F2，优先做 `DataLoader`、`RNG`、`GameClock`、`GameState` 最小可验证 autoload 骨架。
- 实际读取：`AGENTS.md` 初始化上下文、`docs/正式项目工作规划.md`、`godot-gdscript` / `godot-scene-validation` / `project-doc-sync` / `safe-git-commit` skills、`docs/代码文档规范.md`、`docs/测试策略.md`、GDD 项目结构 / 本地化 / 设置 / autoload / 输入相关段落、`docs/AI协作/文档维护指南.md`、`docs/代码/README.md`、`docs/_kb_index.json`、`docs/TODO.md`、`tools/godot_bridge.py`。

## 正式项目 F2 第一片 autoload 骨架

- 输入：用户要求继续下一步，按 `current_state.json` 候选推进 F2。
- 评估：先落 `DataLoader` / `RNG` / `GameState` / `GameClock` 能给后续数据、确定性、流程和暂停打底；本次不提前做 UI、存档、音频或玩法内容。
- 产出：新增 `client/scripts/autoload/data_loader.gd`、`rng.gd`、`game_state.gd`、`game_clock.gd` 并注册到 `client/project.godot`；启动脚本加入 autoload smoke 输出；新增四份模块文档并同步客户端 README、AI 导航、代码索引、TODO、知识库索引和 `current_state.json`。
- 结论：F2 第一片可 headless 启动，启动日志能验证契约缓存、RNG 子流、初始状态和 seed；下一步建议补 `Settings`、`Localization`、`UIManager` 最小 API。
- 实际读取：`godot-gdscript` / `godot-scene-validation` / `project-doc-sync` / `safe-git-commit` skills、`docs/AI记忆/current_state.json`、`docs/正式项目工作规划.md`、`docs/AI导航.md`、`docs/代码文档规范.md`、`docs/测试策略.md`、`docs/AI协作/文档维护指南.md`、GDD §9.3~§9.19、`client/project.godot`、生成契约文件与 `docs/代码/README.md`。
## 正式项目 F2 第二片 autoload 骨架

- 输入：用户要求继续下一步，按 `current_state.json` 候选继续 F2。
- 评估：补 `Settings`、`Localization`、`UIManager` 能建立设置、本地化和 UI 栈边界，价值高且可独立验证；本次只做最小内存态 API，不提前实现设置持久化、正式翻译导入、具体 UI 场景或完整焦点管理。
- 产出：新增 `client/scripts/autoload/settings.gd`、`localization.gd`、`ui_manager.gd` 并注册到 `client/project.godot`；启动 smoke 输出新增 settings 数量、当前 locale 和 UI 栈深度；新增 `docs/代码/settings.md`、`localization.md`、`ui_manager.md`，同步客户端 README、代码文档索引、AI 导航、TODO、知识库索引、项目记忆与 `current_state.json`。
- 结论：F2 前两片 autoload 可 headless 启动，当前已覆盖 `DataLoader`、`RNG`、`GameState`、`GameClock`、`Settings`、`Localization`、`UIManager`；下一步继续小切片补 `Analytics`、`Replay`、`PoolManager`、`SaveManager`、`AudioManager`。
- 验证：`tools/godot_bridge.py --project client headless-boot` 通过，启动日志包含 `settings=12 locale=zh_CN ui_stack=0`；其余 L0 / 文档 / JSON 验证记录见 `current_state.json`。

## 正式项目 F2 Analytics autoload 骨架

- 输入：用户继续要求推进正式项目下一步，按 F2 剩余 autoload 候选选择小切片落地 `Analytics`。
- 评估：`Analytics` 可复用既有事件契约和 `Settings.privacy.analytics_enabled`，价值高且体量可控；主要风险是过早引入真实上报或扩大隐私边界，因此本次只做本地内存事件队列、契约校验和隐私开关联动。
- 产出：新增 `client/scripts/autoload/analytics.gd` 并注册到 `client/project.godot`；启动 smoke 输出新增 analytics 事件数量与启用状态；新增 `docs/代码/analytics.md`，同步客户端 README、代码文档索引、AI 导航、TODO、知识库索引、项目记忆与 `current_state.json`。
- 结论：F2 当前已覆盖 `DataLoader`、`RNG`、`GameState`、`GameClock`、`Settings`、`Analytics`、`Localization`、`UIManager`；下一步继续小切片补 `Replay`、`PoolManager`、`SaveManager`、`AudioManager`。
- 验证：`tools/godot_bridge.py --project client headless-boot` 通过，启动日志包含 `analytics_events=24 analytics_enabled=true`；其余 L0 / 文档 / JSON 验证记录见 `current_state.json`。
