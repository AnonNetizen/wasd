# 2026-07-11 会话日志

- 用户提供 Steam App ID `4955670` 并要求正式化 `output/steamworks_lab`。新增 ADR #129：该 Lab 切换为长期维护的独立 Steam 应用身份，但仍与正式 `client/PlatformServices` provider 接入分离；App ID 是公开标识，不作为凭据处理。
- App ID 从 Spacewar `480` 切换为 `4955670`：`project.godot` 新增 `steam/app_id` 与 Godot export-template Steam 客户端重启开关，`steam_appid.txt` 仅保留为本地开发文件；`TransportAdapter` 解析 `steamInit` 结果、核对 Steam runtime App ID、支持 `restartAppIfNecessary` 与 `--disable-steam`、严格校验 Lobby marker / protocol version，并在 peer 创建失败时离开 Lobby；`NetworkSession` 在 adapter 入树前连接信号，Steam 异步连接失败会清理 pending 状态并回到 offline。
- 新增 `tests/steam_config_smoke.gd`，覆盖 App ID 双来源一致、初始化返回值、Lobby 兼容、显式离线诊断和连接失败回退；Lab README 补 AI 修改说明、专属 App ID、本地开发 / Depot 边界、真实 Steam 双账号 smoke 清单，并同步 AI导航、测试策略、AI知识库索引、机器索引与 AI 记忆。按记忆瘦身约定把 2026-07-02 的 7 条 Lab 摘要折叠为 1 条，详细历史仍保留在原日志。
- 自动验证：Godot `4.7.stable.steam` 的 `headless-boot` / `export-tree` 通过；隔离 APPDATA/LOCALAPPDATA 的 `steam_config_smoke -- --disable-steam` 全绿；不同项目副本 + 独立用户目录的双进程本地 ENet host/client smoke 全绿；JSON、docs health、diff check 通过，`output/steamworks_lab` 已无 `480` / `Spacewar` 残留。完整 `battle_smoke` 的新增 Steam 断言通过，但整套仍有 4 个既有失败（world rect、multiplayer panel 两项、merge gunner shot count）；用 `git archive HEAD` 的未修改临时副本复跑得到完全相同失败，确认不是本次回归。真实 GodotSteam / Steam Multiplayer Peer、两个账号 / 设备、overlay / Lobby / invite / cold-start 与 Depot 内容未验证，因为仓库未安装 / 固定相关二进制，也未配置 export preset / SteamPipe VDF。
- 本轮按 L 级预算读取开工热路径、Godot / 文档同步 / 测试 / review / 提交 skills、Lab README / project / Steam transport / network / smoke、文档维护指南、测试策略、ADR / AI导航 / 知识库 / AI记忆相关段和 Steamworks / GodotSteam 官方资料；未进入 `draft/` / `DRAFT/`。
- 用户随后明确授权修改 `AGENTS.md` 并同步各平台规则，采用“规则 + 可复制模板”而不新增包装器，新增 ADR #130。通用入口与 CodeBuddy / Codex / Claude 完整规则新增 PowerShell 稳定执行条款，OpenCode 保持转发式入口；固定字符串优先 `rg -F`，全部 `rg` 选项置于 `--` 前，路径走 `-LiteralPath`，cmdlet / 原生错误分开处理，合法非零退出码先归一化再并行或进入 fail-fast。
- `docs/AI协作/工具适配指南.md` 新增可复制的固定搜索、正则、中文路径、外部可执行文件、pre-commit 参数数组和 `git diff --no-index` 模板；后者先以 `Get-Item -LiteralPath` 校验文件输入，避免把缺失路径同样返回的 `1` 误判成差异。修正代码审核 / 实时验证 / 四平台 data-author、Codex TOML prompt、Steamworks Lab 与 F13 工作包中的尖括号 PowerShell 命令占位符。同步快速开工、AI 协作 README、AI 导航、知识库索引与机器索引；按记忆自动瘦身规则把近期脉络合并为每个日期一行并压缩文件速查，详细历史仍指向每日会话日志。
- 自动验证：`rg` 有匹配 / 无匹配 / 正则语法错误分别得到 0 / 1 / 2 并按语义处理；`git diff --no-index` 相同 / 不同文件得到 0 / 1，缺失输入在调用 Git 前被拒绝；中文路径 `Get-Content -LiteralPath`、27 个非草稿 PowerShell fenced block 语法解析、三份完整规则同哈希与 OpenCode 继承、JSON / TOML、docs health、排除草稿的 diff check 和针对 27 个任务文件的 pre-commit 全绿。事实复核发现并关闭了“缺失输入也可能返回 1”的 P2，未发现其他 P0~P2；未修改包装器、hook、CI、运行时代码或全局配置，也未运行无关 Godot 测试。
- 用户提供《AI驱动的通用Tile场景生成系统设计文档》PDF，要求评估并落地一个含树、木柜和大理石地板的 2D 方块场景实验。产出定位为 `output/test_lab` 独立 PoC：废弃大理石温室 Style Pack、三张原子素材、数据 manifest、6×4 确定性组合场景、分层显示 / metadata 检视、专用 smoke 与截图入口。
- AI 美术工作流采用内置 `imagegen` 分别生成地板、树和木柜；对象经纯 `#ff00ff` chroma-key、soft matte / despill、112px 安全框适配和 Lanczos 缩放，精确提示词与后处理记录写入 `style_pack.json`。已完成素材规格验证：三张最终 PNG 均为 128×128；大理石完全不透明，edge metric 为 `0.02484170751633981`（阈值 `0.08`），2×2 人工预览未见明显硬接缝；树 alpha coverage / partial 分别为 `0.5372314453125` / `0.03204345703125`，木柜为 `0.734619140625` / `0.02459716796875`，两者四角 alpha 全为 0、magenta residual 均为 0。
- 边界锁定：实验不修改正式 `client/`，不加入玩家、开柜 / loot 结算、批量变体、完整 EditorPlugin 或运行时 AI，也不把本次 PoC 写入 GDD、ADR、AI 导航、`current_state.json` 或项目记忆。Godot `4.7.stable.steam` 的 Test Lab headless boot、export-tree、显式场景启动与 7 个任务 GDScript `--check-only` 均通过；专用 `ai_universal_tile_smoke` 验证同 seed 签名一致、下一 seed 不同、24 个 Base / 3 棵树 / 3 个木柜、外围无重叠、6 个碰撞与五层显隐后输出 `AI_UNIVERSAL_TILE_SMOKE_OK`。场景与索引截图均重新生成并人工复核，素材风格统一、木柜可读、对象无裁切、地板无明显硬接缝、悬停 metadata 与滚动入口完整；新场景未发现嵌入式 `Image` 或 `PackedByteArray`。
- 提交前事实型 review 关闭 3 个 P2：布局签名移除 seed，确保 next-seed 断言比较真实布局；Style Pack 补齐 GDD 强制的 `asset_type` / `footprint_shape` 并由运行时与 smoke 双检；删除未被运行时使用的 `tile_types` 重复权威，场景组合只由 Style Pack、`base_fill_asset_id` 与 `generation_rules.placements` 决定。复核后未发现 P0–P2；语义 lint 的 33 条 advisory 全部来自未改动的正式 `client/` 既有文件，归类为 out-of-scope/defer。
