# 2026-07-21 会话日志

- 用户明确要求性能测试不再随每次实现或回归运行，需要时由用户主动提出。
- ADR #143 固化按需策略：`startup-probe`、`perf-probe`、Godot Profiler 和同类性能采样退出日常修改、提交前检查、常规回归、默认 CI 与 AI 交付义务；现有 probe 保留，避免未来需要时重建能力。
- 同步测试策略、CICD 规划、AI 导航、知识库索引、当前工作包 / 模块文档、项目记忆与 `current_state.json`；历史性能结果和历史会话记录保持不变。
- 本次只做文档政策同步，不修改 Godot / Python 运行时代码，也不运行任何性能测试。
- 修复 F13 模块地形实体规则：玩家与敌人改为使用模块封锁边界碰撞；敌人模板摆放、运行时生成和 run v4 快照恢复都拒绝封锁格，只允许落在 `module_cell_floor`。
- `module-world-smoke` 增加玩家 / 敌人撞墙、封锁格生成和非法快照恢复回归；数据 schema 增加敌人 footprint 必须全为 floor 的负例。静态规则、契约、数据 / schema、文档健康、Godot 场景解析、headless boot、module-world、runtime、save 与 L1 smoke 均通过；按用户要求未运行性能测试。
- 用户明确要求删除敌人种间交互生态，但保留五种敌人对玩家的追击、快速近战、冲锋、守家、远程行为和无伤害中心分离。
- 新增 ADR #144：EnemyAI 只把玩家作为战斗目标，`team_enemy` 来源伤害被拒绝；删除猎食 / 逃跑关系、旧生态 tag / action、其他敌人扫描和导演敌人组合元数据。`enemy_ai_profiles.json` 与 `warzone_directors.json` 升 schema v2，run 保持 v4，旧快照非法动作恢复时清空并在下一决策 tick 重选。
- 三个 profile 改名为 `enemy_ai_fast_chase`、`enemy_ai_charge_stalker`、`enemy_ai_home_guard`；五种正式敌人只保留 `tag_enemy`。Godot / Python schema 和负例测试明确拒绝旧字段与旧 id，避免未来误加回来。
- `runtime-smoke` 改为覆盖五类敌人只选择玩家、快速接近 / 冲锋 / 守家 / 远程行为、敌方友伤不生效、两敌近距共存时不互伤但继续中心分离，以及旧 v4 非法动作恢复后重新选择合法动作。
- GDD 升 v1.21，并同步词表、数据手册、EnemyAI / WarzoneDirector / GameplayRuntime / DataLoader 模块文档、F10 工作包、测试策略、AI 导航、知识库索引、AI 机会清单和三层记忆；AID-01 标记为已退役，后续 AI 不得从候选清单或历史 ADR 自动恢复种间交互方向。
- contracts/data/schema、GDScript/project/semantic lint、headless、完整 9×9、中心 3×3 技术首片、runtime、F9、L1、save 均通过；四条 golden 已按数据指纹 `5a65b531e81e104b12532662f353a5d768307272b373690aab07d30f9911ca9b` 重录并通过真实运行时摘要复跑，结果见 `docs/reports/2026-07-21-replay-regression.md`。按用户策略未运行性能测试。
- 用户将敌人寻路与感知独立为 F14：F13 模块大地图保持完成，只提供完整 99×99 静态地形；敌人采用共享流场 + 地形视线 / 路径距离 / 最后已知位置混合感知。
- 新增 ADR #145 与 `F14-EnemyNavigationAndPerception.md`。`ModuleNavigationField` 从完整 assignment 构建 walkability mask，玩家跨格时运行确定性八方向 Dijkstra，禁止斜穿墙角；守家 / 记忆目标在决策 tick 使用 `AStarGrid2D`，并提供 supercover 视线和敌人半径扩张走廊查询。
- F14 最终非性能回归通过：contracts/data/schema、GDScript/project/semantic lint、headless、完整 9×9 / 中心 3×3 module-world、runtime、F9、L1、save 以及四条 golden capture + runtime-summary 全绿。技术首片 smoke 的 Enemy 断言改为等待确定数量的 physics frame，避免 process frame 未跨过决策 tick 的不稳定判断。现有 replay data fingerprint 仍为 `5a65b531e81e104b12532662f353a5d768307272b373690aab07d30f9911ca9b`；按 ADR #143 未运行性能测试。
- `Enemy.configure()` 接受可选导航提供者：模块模式由 `GameplayRunLoop` 注入 `ModuleWorldManager`，开放战区保留直线兜底。Enemy 畅通直追、受阻读取共享流场；混合感知按视线、路径、1.5 秒记忆排序，记忆不读取玩家实时位置；冲锋要求清晰走廊，远程开火要求当前地形视线。玩家唯一目标、敌方友伤拒绝和中心分离保持不变。
- `enemy_ai_profiles.json` 升 schema v3，以 `perception.sight_radius / path_awareness_radius / memory_duration` 取代并拒绝旧 `sense_radius`；Godot / Python validator 与坏样例覆盖旧 schema、缺失 / 非法 perception、路径半径越界和负记忆时间。run 仍为 v4，导航 / 感知缓存不保存。
- `module-world-smoke` 增加确定性流场、真实模块绕路、路径距离、无斜穿、封锁 / 越界、技术首片外圈和同格精确目标覆盖；`runtime-smoke` 增加直追、流场 waypoint、视线 / 路径 / 记忆、守家回位与冲锋 / 远程墙体门禁。按用户策略不运行任何性能测试。
- 用户反馈 F14 后玩家每走一格都会规律性卡顿；定位为玩家跨 160 px 全局格时主线程同步重算完整 99×99 Dijkstra，并为堆节点创建临时字典，不是 3×3 chunk 流式加载。
- 新增 ADR #146 / F14.1：完整 99×99 walkability mask、地形视线和守家 / 记忆 AStar 保持全图；活动玩家流场半径从已加载 EnemyAI profile 最大 `sight_radius` 自动推导，当前 `ceil(860 / 160) + 2 = 8`，每次最多处理 17×17 = 289 格。流场只清理上次实际触达索引，最小堆改为并行距离 / 格索引数组；活动窗口外查询不可达，全图 AStar 不截断。
- `module-world-smoke` 增加运行时半径推导、289 格上限、连续 20 次跨格不退化、局部范围外不可达但全图 AStar 仍可查询；run 保持 v4，EnemyAI schema 保持 v3，不新增数据或词表。按 ADR #143 不运行性能 probe，玩家仍需在实际游戏中连续行走 20 格并跨两个模块接缝确认体感停顿消失。
- F14.1 完整非性能回归通过：contracts/data/schema、GDScript/project/semantic lint 与 lint 自测、JSON / docs health / diff check、headless、完整 9×9 / 中心 3×3、runtime、F9、L1、save 均通过；四条现有 golden 直接复跑真实运行时摘要全部通过，数据指纹仍为 `5a65b531e81e104b12532662f353a5d768307272b373690aab07d30f9911ca9b`，无需重录。semantic lint 保持 40 条已审阅 advisory；按用户计划未运行任何性能测试。
