# 2026-08-11 会话日志

## ADR #197：HazardCatalogValidator

- 新增纯 `HazardCatalogValidator` 与 typed `ValidationResult { is_valid, row_count }`；只接收已加载 rows、locale / contract callbacks 与错误 sink，不读文件 / Mod、不建引用索引、不写 schema count。
- 精确保留 id→locale→tags→pool→damage→element→trigger→radius→duration 的 source-order、id 不 trim、tag trim / 去空、CSV String 解析 / finite / 数值下限，以及未知 tag “有诊断但不拉低总 bool、也不参与 duplicate” 的历史兼容缺口。
- `DataLoader` 保留表现 profile 引用→schema validator→reference builder 三次独立读取、Gear Mod 掉落后 / active items 前调用、资源路径 / Mod-aware locale / 契约 adapters，以及无条件写 raw row count。
- 专项 GUT 9/9、40 asserts，共享 full GUT 238/238、1934 asserts；contracts、`validate_data`、串行 schema、ModLoader、runtime、F9、module-world full / technical、正式 headless boot 与四条 Replay v9 golden 均通过，boot count 为 `hazards=2`。data fingerprint / map hash 与 golden 文件不变、未重录；独立事实审查未发现 Critical / High / Medium，未运行性能 probe，人工体验项保持待用户验收。

## ADR #197：EnemyExplosionAttackHandler

- 新增无状态、无 Node 所有权的 `EnemyExplosionAttackHandler`，只借用 `EnemyActionRuntime` 与 typed ports；`Enemy` 继续持有 SceneTree / Node targets、Combat、生命 / 奖励归因、signal/context 与 Run v19 23-key façade。
- 精确保留 fresh arm 的 current action→clear focus→armed flags→windup/timer→停速 / 关碰撞→windup signal→visual refresh，zero windup 同步 detonate；armed restore 使用独立入口，只强制 action/state、关碰撞并发一次 windup，不复用 arm、不清 flags/focus/velocity、不因 timer0 同步爆炸。
- detonate 继续先写 exploded / hit 再发 committed；direct targets 在 signal 后按 live source position 结算，完成后才收集 active enemies，并用爆炸前 frozen origin 过滤、仅按 runtime spawn serial 排序。跨 direct / group 不去重，连锁目标保持 `life=0 / defeated=false` 并延迟引爆，源 self defeat 最后执行。`runtime_smoke` 的 5 处 arm / 2 处 advance 私有调用迁至 debug-build typed seams。
- 专项 GUT 9/9、127 asserts，共享 full GUT 229/229、1894 asserts；L1、actor-scene、runtime、save、loading、module-world full / technical、正式 headless boot 与四条 Replay v9 golden 均通过。data fingerprint / map hash 与 golden 文件不变、未重录；独立事实审查未发现 Critical / High / Medium，未运行性能 probe，人工体验项保持待用户验收。

## ADR #197：PlayerDataValidator

- 新增纯 `PlayerDataValidator` 与 typed `ValidationResult { is_valid, stat_count, has_stat_count }`；只接收已加载 root、stat / pool callbacks 与错误 sink，不读文件 / Mod、不写 schema count。
- 精确保留 root 与非法 / 空 `base_stats` 两处硬返回、schema→body→base stats→gold→energy 的 source-order、body required→extra→radius、raw stat source-order / count、遗留 stat 诊断、金币池 callback 空返回后的双诊断，以及 root / defense / dash / energy 的既有 under-validation。
- `DataLoader` 保留 locale 后 / camera feedback 前的单次 `player.json` 读取、通用 Mod overlay、资源路径包装、stat / pool 契约 adapters，以及仅到达合法非空 `base_stats` 后写 `player_stats` count。
- 专项 GUT 10/10、53 asserts，共享 full GUT 229/229、1894 asserts；contracts、`validate_data`、串行 schema、L1、actor-scene、runtime、save、loading、正式 headless boot 与四条 Replay v9 golden 均通过。data fingerprint / map hash 与 golden 文件不变、未重录；独立事实审查未发现 Critical / High / Medium，未运行性能 probe，人工体验项保持待用户验收。

## ADR #197：RewardChoicePoolValidator

- 新增纯 `RewardChoicePoolValidator` 与 typed `ValidationResult { is_valid, pool_count }`；只接收已加载 root、locale / modifier callbacks 与错误 sink，不读文件 / Mod / 契约、不写 schema count。
- 保持 schema→pools→pool→entries→entry 的 source-order、int-like exact schema 1、合法 String pool / entry 重复范围和 locale / modifier callback 顺序；机械保留 pools / entries / modifiers 非 Array，以及 modifier 缺失 / 未登记 stat 时“已有诊断但整体 bool 可能仍成功”的历史兼容缺口，不在架构提取中顺手收紧。
- `DataLoader` 保留等级曲线后 / 难度 profile 前的物理读取、资源路径包装、Mod-aware locale / stat modifier adapters，以及仅 Dictionary root 写规范化 pool count。数据手册把旧 `schema_version >= 1` 漂移同步为代码 / Python 的固定 `1`。
- 专项 GUT 11/11、45 asserts，共享 full GUT 217/217、1773 asserts；`validate_data`、串行 schema、`mod-loader-smoke`、正式 headless boot 与四条 Replay v9 golden 均通过。data fingerprint / map hash 与 golden 文件不变、未重录；独立事实审查未发现 Critical / High / Medium，未运行性能 probe，人工体验项保持待用户验收。

## ADR #197：EnemyChargeAttackHandler

- 新增无状态、无 Node 所有权的 `EnemyChargeAttackHandler`，只借用 `EnemyActionRuntime` 与 typed ports；`Enemy` 继续持有 CharacterBody movement / bounds、Combat、signal/context、focus 与 Run v19 23-key façade。
- 精确保留缺 focus 原样返回、零方向只更新 lock 且不重置命中位、windup 越界先进入 release 并 emit committed，以及 release 每帧完整 delta 只推进一次；Actor port 顺序保持 face→no-slide move→bounds→primary→distinct player sweep。
- 几何命中位仍在 Combat 前写入；damage rejected 仍按几何命中触发 stop_on_hit，只有 player target 且伤害 applied / amount > 0 / 距离与时长有效时才 knockback。finish 继续 clear action/state/timer→写 cooldown→恢复 primary focus；`runtime_smoke` 的 charge 私有白盒调用已迁至 debug-build typed seams。
- 专项 unit / Actor integration 8/8、111 asserts，共享 full GUT 199/199、1669 asserts；L1、actor-scene、runtime、save、loading、module-world full / technical、正式 headless boot 与四条 Replay v9 golden 均通过。data fingerprint / map hash 与 golden 文件不变、未重录；独立事实审查未发现 Critical / High / Medium，未运行性能 probe，人工体验项保持待用户验收。

## ADR #197：DifficultyProfileValidator

- 新增纯 `DifficultyProfileValidator` 与 typed `ValidationResult { is_valid, profile_count }`；只接收已加载 root、locale callback 与错误 sink，不读文件 / Mod、不建引用索引、不写 schema count。
- 保持 root/profile required→extra、id→name→五数值字段→9 段 stage locale / 重复的 source-order、短路、int-like / finite / 正数 / 范围语义；生产继续保留旧 `String(...)` 取值路径，但不把测试字面量跨类型转换声明成非 fatal 兼容契约。
- `DataLoader` 保留奖励池后首次读取、资源路径包装、Mod-aware locale adapter、仅 Dictionary root 写规范化 profile count、第二次物理读取后构建 difficulty ID 索引，再进入 game mode 校验。`difficulty_progression.md` 的旧 Replay v8 事实同步为当前 v9 / 拒绝 v8。
- 专项 GUT 11/11，共享 full GUT 199/199、1669 asserts；`validate_data`、串行 schema、正式 headless boot 与四条 Replay v9 golden 均通过。data fingerprint / map hash 与 golden 文件不变、未重录；独立事实审查最终 Critical / High / Medium 为 0，未运行性能 probe，人工体验项保持待用户验收。

## ADR #197：EnemyMeleeAttackHandler

- 新增无状态、无 Node 所有权的 `EnemyMeleeAttackHandler`，只借用 `EnemyActionRuntime` 与 typed ports；`Enemy` 继续持有 target、Combat、LOS、signal/context、focus 与 Run v19 23-key façade。
- 精确保留缺 focus 原样返回、零方向在重置 hit flags 前返回、windup / 零 windup 同步提交，以及提交时 primary→distinct player 的目标构造。每目标继续按 available/alive→零距离直接命中或 range→arc→terrain LOS 判断。
- 所有目标 damage 完成后才 emit committed；signal 时 Runtime 仍是提交前状态，随后才写 cooldown、clear action/state/timer 并恢复 primary focus。`runtime_smoke` 的三处近战私有白盒调用已迁至 debug-build typed seams。
- full GUT 180/180、1515 asserts（Handler unit 6/6、Actor integration 2/2）；L1、actor-scene、runtime、save、loading、正式 headless boot 与四条 Replay v9 golden 均通过。data fingerprint / map hash 与 golden 文件不变、未重录；独立事实审查未发现 Critical / High / Medium，未运行性能 probe，人工体验项保持待用户验收。

## ADR #197：DataSourceReader

- 新增无缓存、无 autoload / Mod 依赖的纯 `DataSourceReader`，以 typed `JsonReadResult` / `CsvReadResult` 承接官方 JSON / CSV 的单次打开与解析，并返回 `ok`、payload 与失败 field / expected；不输出错误、不保存跨调用状态。
- `DataLoader` 继续保留公开 `load_json()` / `load_csv()`、路径诊断、失败 fallback、成功后才应用 Mod overlay、`_contracts.json` 禁止覆盖及每次调用重新读取的外部契约。JSON `null` 与解析失败元数据、CSV header / no-header、短行补空、长行截断、单列空白行、空文件语义均保持。
- 新增 Reader unit 与 DataLoader adapter integration；后者真实经过 public load façade，锁定失败不 overlay、成功后 overlay、contracts bypass 与同路径重复物理读取。独立事实审查补齐该接线覆盖后未发现 Critical / High / Medium。
- 目标 GUT 12/12、81 asserts，共享 full GUT 180/180、1515 asserts；`validate_data`、串行 DataLoader schema、contracts / project lint、ModLoader、L1、runtime、replay smoke、正式 headless boot 与四条 Replay v9 golden 均通过。data fingerprint / map hash 与 golden 文件不变、未重录；未运行性能 probe，人工体验项保持待用户验收。

## ADR #197：CreditsValidator

- 新增纯 `RefCounted` `CreditsValidator` 与 typed `ValidationResult`，只接收已加载 root、locale 校验 Callable 与 `(field_path, expected)` 错误 sink；不读文件、autoload、Mod、fingerprint、缓存或 `user://`。
- 精确保留 root → schema → sections → section → entries → entry 的 source-order 诊断与局部类型短路；额外 key 继续允许，非法 `external_*` 仍校验 URL、license 与三个 bool，copyright 最后。section id / entry kind 保持旧 `String(Variant)` 行为，不用 `str()` 扩宽异常输入。
- `DataLoader` 保留 `credits.json` 读取、现有 Mod-aware `_require_locale_key()` adapter，以及仅 Dictionary root 写 `credit_sections` / `credits` 的计数语义。
- validator GUT 10/10、52 asserts，full GUT 160/160、1340 asserts；`validate_data`、串行 DataLoader schema、ModLoader、正式 headless boot 与四条 Replay v9 golden 均通过，golden SHA-256 不变、未重录。独立事实审查未发现 Critical / High / Medium；未运行性能 probe，人工体验项保持待用户验收。

## ADR #197：EnemyRangedAttackHandler / EnemyProjectileMaterializer

- 新增无状态 `EnemyRangedAttackHandler`，只借用 `EnemyActionRuntime` 与 typed ports 推进 windup、burst、scheduled shot、interval 和 cooldown；不持有 Run v19 字段。`Enemy` 继续持有目标 / 导航 / movement / LOS / range / cooldown 起手门禁、signal/context 与 service adapter。
- 新增无状态 `EnemyProjectileMaterializer`，严格保持 `PoolManager.acquire` → capability → normalize / muzzle → `global_position` → reparent → `Bullet.configure()` 顺序；零方向不 acquire，null parent 不 reparent 但仍 configure，失败不擅自 release。
- 每发继续先尝试材化，再发 committed signal，随后才扣 burst，并进入 cooldown 或 shot interval；pool / capability / configure 失败仍按旧语义提交并消耗 scheduled shot。L1/runtime 的 projectile、burst start 和 ranged advance 私有调用已迁至 debug-build typed seams。
- full GUT 160/160、1340 asserts（Materializer 5/5、Handler 5/5、Actor integration 2/2）；L1、actor-scene、runtime、save、loading、正式 headless boot 与四条 Replay v9 golden 均通过，golden SHA-256 不变、未重录。独立事实审查补齐 null-parent 单测后未发现 Critical / High / Medium；未运行性能 probe，人工体验项保持待用户验收。

## ADR #197：CameraFeedbackValidator

- 新增纯 `RefCounted` `CameraFeedbackValidator`，只接收已加载 root 与 `(field_path, expected)` 错误 sink；不读文件、autoload、Mod、fingerprint、缓存或 `user://`。
- 精确保留 schema → aim 4 字段 → player shake 7 字段 → weapon shake 7 字段 → weapon exponent 的旧诊断顺序和文本；profile 非 Dictionary 时按原边界短路，额外 key 继续允许，int-like / finite / 正数 / 非负 / 比例上界语义不变。
- `DataLoader` 保留 player 后校验、第二次读取 `camera_feedback.json`、`DataReferenceIndexBuilder` 收集 ID、再进入 visual effects 的调用顺序，以及仅 Dictionary root 写 `camera_feedback_profiles=2` 的计数语义。
- validator GUT 10/10、31 asserts，full GUT 138/138、1119 asserts；`validate_data`、串行 DataLoader schema、VFX、runtime、正式 headless boot 与四条 Replay v9 golden 均通过。四条 golden SHA-256 与 map hash 不变、未重录；独立事实审查未发现 Critical / High / Medium，未运行性能 probe，人工体验项保持待用户验收。

## ADR #197：EnemyActionRuntime

- 新增纯 `RefCounted` `EnemyActionRuntime`，单一持有 current action、phase / timer、cooldown、命中位、burst / lock direction 与 armed / exploded 共 11 个字段；Runtime 不依赖 Node、Combat、movement、projectile、signals、autoload、RNG 或 GameClock。
- `Enemy` Actor 继续显式组装 Run v19 23-key snapshot、执行 handler / 副作用与 restore 信号。legacy / 非法 ranged 状态保持旧 fallback / cooldown；合法 windup 与 burst 均先停速并按 normalized lock 更新朝向，仅 windup emit；armed restore 恰好发一次。
- L1 与 runtime smoke 的 10 处旧 `_current_action` 私有写入迁至 debug-build、Brain-action 校验的 typed 测试门面，不恢复双所有权或字符串反射。
- full GUT 128/128、1088 asserts（ActionRuntime unit 8/8、Actor integration 2/2）；L1、actor-scene、runtime、save、正式 headless boot 与四条 Replay v9 golden 均通过，golden 未重录，map hash 保持 `2a7ebcdf…c200a`。独立事实审查发现并修复合法 burst 恢复漏停速 / 朝向及一处测试前置假绿，最终 Critical / High / Medium 均为 0；未运行性能 probe，人工体验项保持待用户验收。

## ADR #197：EnemyRewardModelValidator

- 新增纯 `RefCounted` `EnemyRewardModelValidator`，只接收已加载 root 与 `(field_path, expected)` 错误 sink；不读文件、autoload、Mod、fingerprint、缓存或 `user://`。
- 精确保留 required → extra → schema → `base_coefficient` → `time_growth_per_tier` → `random_multiplier_min` → `random_multiplier_max` → 上下界关系的旧诊断顺序和文本；integral float 继续视为 int-like，数值上下界在有限性 / 正数前置失败后仍按旧语义比较。
- `DataLoader` 保留 `ENEMY_REWARDS_PATH` 读取、EnemyAI profile 后 / `enemies.csv` 与 Mod 隔离前的调用位置，以及仅 Dictionary root 写 `enemy_reward_models=1` 的计数语义。
- validator GUT 12/12、35 asserts，`validate_data`、串行 DataLoader schema、L1、ModLoader、正式 headless boot 与四条 Replay v9 golden 均通过；golden 未重录。独立事实审查未发现 Critical / High / Medium；未运行性能 probe，人工体验项保持待用户验收。

## ADR #197：EnemyBrain

- 新增纯 `RefCounted` `EnemyBrain`，集中已校验 profile / action source order、决策计时、visible → path-aware → memory → unaware 四级感知、Utility 分数和 movement intent；Brain 不持有 Node、导航 provider、RNG、GameClock、autoload 或字符串反射。
- `Enemy` Actor 继续负责主目标 / 地形视线 / route / corridor 查询，构造 typed `SenseInput`，并单一持有当前 action、攻击 FSM、Combat / Pool / clock / RNG、信号和 Run v19 23-key 快照。保持只在 `score > best + 0.001` 时替换候选、近战 / 爆炸 / 冲锋 cooldown gate、ranged 候选例外，以及决策提交后才刷新 waypoint。
- full GUT 89/89、793 asserts（Brain unit 8/8、Actor integration 2/2）；`actor-scene-smoke`、`runtime-smoke`、正式 headless / save / loading / module-world full + technical 门禁与四条 Replay v9 golden 均通过，golden 未重录。独立事实审查未发现 Critical / High / Medium。
- Meta v4、Run v19、Replay v9、game v1.18、data / map hash 与玩法结果不变；未运行性能 probe，视觉、听觉与手感仍待用户人工验收。

## ADR #197：ModuleWorldLayout

- 新增纯 `RefCounted` `ModuleWorldLayout`，集中 world / registry / template 只读副本、run seed、assignment、坐标 / 旋转 / footprint / 邻接、terrain / mask / flood-fill、placement / 空地查询、content-sensitive map hash 与事务式 restore candidate；不依赖 Node、autoload、文件或 `user://`。
- `ModuleWorldManager` 保留 `Node2D` 公共 facade，通过 typed `RandomPort` 注入 `RNG.world`，继续拥有场景候选提交、导航、streaming、动态 state 编排和 Run v19 九字段显式组装。fixed / pool precheck 后才触碰 RNG，保持 snapshot/configure → objective → limited → ordinary → restore 的消费顺序；restore 继续在 assignment / scene / hash 全部通过后才提交 layout、导航和动态 state。
- Layout unit 覆盖 RNG 次数 / 顺序、assignment 插入序、fallback 原错误文本、restore/hash、坐标 / row-major / 旋转 / footprint / terrain；full GUT 96/96、873 asserts（Layout 7/7），module-world full + technical、runtime/save、正式 headless boot 与四条 Replay v9 golden 均通过，golden 未重录。事实审查未发现 Critical / High / Medium。
- Meta v4、Run v19、Replay v9、game v1.18、data / map hash 与玩法结果不变；未运行性能 probe，视觉、听觉与手感仍待用户人工验收。

## ADR #197：LevelProgressionValidator

- 新增纯 `RefCounted` `LevelProgressionValidator`，只接收已加载 root 与 `(field_path, expected)` 错误 sink；不读文件、autoload、Mod、缓存或 `user://`。
- 保持 root、schema version、首级成本、倍率分子 / 分母及关系的旧错误顺序和文本；integral float 继续视为 int-like，下限失败后仍检查分子 / 分母关系，额外 root key 继续允许。
- `DataLoader` 保留 `LEVEL_PROGRESSION_PATH` 读取、错误路径包装、角色后 / 奖励池前的调用位置，以及仅 Dictionary root 写 `level_progression_profiles=1` 的计数语义。
- validator GUT 10/10、25 asserts，`validate_data`、串行 DataLoader schema、正式 headless boot 与四条 Replay v9 golden 均通过；golden 未重录，map hash 保持 `2a7ebcdf…c200a`。独立事实审查未发现 Critical / High / Medium；未运行性能 probe，人工体验项保持待用户验收。
