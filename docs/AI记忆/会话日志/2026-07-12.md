# 2026-07-12 会话日志

- 用户要求实现 Steamworks Slime Lab 的 GodotSteam 安装锁定与 Windows 正式导出。新增 ADR #132：Windows 工具链固定为官方 Godot 4.7 + GodotSteam 4.20 + Steamworks SDK 1.64 module editor / templates；不采用已停止维护的第三方 Peer，也不提交外部二进制。
- 新增 `output/steamworks_lab/steam_toolchain.lock.json` 与 `tools/steamworks_lab_toolchain.py`：`setup` 下载、校验 SHA-256 并解压到忽略的 `.toolchain/`，`verify` 检查 editor、GodotSteam 4.20、`Steam` singleton 和 `SteamMultiplayerPeer.host_with_lobby/connect_to_lobby/add_peer`，`export-release` 生成 Windows x64 exe / PCK、复制同版 `steam_api64.dll` 与 `THIRD_PARTY_NOTICES.txt`、排除开发 / 测试 / 工具链文件并执行导出程序离线启动。
- `project.godot` 的 App ID 权威迁移到 GodotSteam 4.20 `steam/initialization/app_data/app_id=4955670`，关闭模块自动初始化与 embedded callbacks；`TransportAdapter` 优先显式调用 `steamInitEx(4955670, false)`，仅在旧接口存在时回退 `steamInit(4955670, false)`。`steam_config_smoke` 用 fake Steam 对象覆盖参数与 fallback，新增 `steam_runtime_presence_smoke` 验证真实锁定运行时。
- 新增 tracked Windows `export_presets.cfg`；根 `.gitignore` 仅为 Lab preset 开例外，并忽略 `.toolchain/`。README、AI导航、测试策略、ADR、项目记忆和 `current_state.json` 同步；项目记忆按自动瘦身规则把 6 月逐日热历史聚合并移除冗余分隔，从 228 行降到 193 行。
- 自动验证已通过：官方 editor / templates 下载大小与 SHA-256、重复 `setup`、`verify`、普通 Godot `steam_config_smoke -- --disable-steam`、普通 / GodotSteam headless boot、Windows `export-release` 与导出程序离线启动；release 同时带 Godot / GodotSteam 许可声明，本地 ENet host/client 双进程全部断言通过。SteamPipe / Depot / overlay / 两个授权账号真实 P2P 仍是外部后续步骤。
- ENet smoke 仍输出既有风险：host 的 1424-byte unreliable snapshot 超过 1392 MTU；隔离 client 副本首次加载用户已有 UID 化 `main.tscn` 时回退 text path。两者均不由本轮工具链 / 初始化改动引入，事实 review 中记录为 out-of-scope residual risk；现有用户改动 `project.godot` 的 `resizable` 删除、`main.tscn` UID 化和未跟踪 `steam_config_smoke.gd.uid` 不纳入本任务提交。
