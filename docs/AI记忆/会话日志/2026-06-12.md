# 2026-06-12 会话日志

> 本文件由 AI 主动归档（首日初始化）。

## 输入（用户主要请求）

1. 加暂停功能
2. 评估框架可优化点
3. 把建议写进 `修改建议.md`
4. 新建 `README.md`
5. 新建 `.gitignore`
6. 评估 Git 上还能优化什么
7. 1~9 项 + MIT license + 提交
8. 评估 CI/CD 优化点
9. 把 CI/CD 写在一份文档里
10. 仓库重组为 `docs/client/server` + 建 AI 记忆体系（按当前对话）

## 主要产出

### 设计 / 文档
- 设计文档升级 v1.2 → v1.3，新增 9.8 暂停与游戏状态管理小节
- `docs/词表与契约.md` 新增第 7 节「输入动作 action」，并补 `pause` action / `game_pause` & `game_resume` 埋点 / `gameplay.pause_on_focus_loss` 设置 / 本地化 `ui_pause` & `ui_quit`
- `docs/AI导航.md` 增加暂停扩展点条目，模块清单加 `PauseMenu`
- `docs/决策记录.md` 新增 ADR #13（暂停功能）
- 编码规则第 8 条扩展暂停约定，自检清单补对应项
- `docs/修改建议.md` 新增 J~R 共 9 项框架结构性建议（含 RNG / GameState / PoolManager / UIManager / 伤害管线 / SaveManager / GameClock / AudioManager / 词表→常量单一来源）
- 新增 `docs/CICD规划.md`（17 项候选 + 4 阶段路线图）

### 工具链 / 仓库基础设施
- 新建 `README.md`（项目入口 + 必读文档表 + 红线 + 仓库结构）
- 新建 `.gitignore`（Godot 4 + 三大 OS + 编辑器临时文件，保留 `.codebuddy/`）
- 新建 `LICENSE`（MIT，Copyright 2026 wasd contributors）
- 新建 `.gitattributes`（统一 LF + 二进制资源 + linguist + LFS 预设）
- 新建 `.editorconfig`
- 新建 `.gitmessage`（Conventional Commits 模板）
- 新建 `CONTRIBUTING.md`
- 新建 `.github/pull_request_template.md`（嵌入规则自检清单）
- 新建 `.github/ISSUE_TEMPLATE/`（bug / feature / design 三模板）
- Git 配置：仓库级 `core.quotepath=false` + `commit.template=.gitmessage`

### 仓库重组
- 7 份设计文档 → `docs/`（rename 保留历史）
- 新建 `client/`（Godot 项目根占位）
- 新建 `server/`（联机/后端预留）
- 同步修订 README / CONTRIBUTING / .github 模板的相对链接

### AI 记忆体系
- 新建 `docs/AI记忆/项目记忆.md`（主索引）
- 新建 `docs/AI记忆/README.md`（用法说明）
- 新建本日志文件
- 维护方式确定为：**选项 (a) AI 主动更新**，触发条件与节制原则写在主索引第 9 节

## 关键决策

| 决策 | 理由 |
|------|------|
| 暂停 = `get_tree().paused` + 菜单节点 `PROCESS_MODE_ALWAYS` + 可重绑定 `pause` action | 状态控制集中，不散落硬编码 |
| 仓库分 docs/client/server + 工具链留根 | 文档与实现物理隔离，未来可扩展联机 |
| 客户端目录用 `client/`（不是 `godot/`） | 更通用，未来加桌面包装/启动器也合适 |
| `server/` 占位但暂不设计 | 当前单机项目，为防"为何有空目录"的疑问，加 ADR 说明 |
| AI 记忆采用「主索引 + 会话日志」二级结构 | 主索引精简续接 + 日志可回溯 |
| AI 记忆主动更新模式 (a) | 用户成本最低，靠节制原则控 token |

## 提交（Git）

| commit | 内容 |
|--------|------|
| `8919ebd` | chore: init repo with license, gitignore/attributes and contribution scaffolding |
| `79da1ba` | docs: 初始设计文档与编码规则 |
| `0793d16` | docs(proposal): 待决策项 A~D 与 J~R |
| `58bf6e1` | chore: restructure into docs/client/server layout |
| 后续 | docs(memory): add AI 记忆体系；docs(adr): 同步重组 ADR + 规则更新 |

## 未决 / 下一步

- 评审 J/L/M（⭐⭐⭐）后转 ADR 入设计文档
- 代码启动：`client/project.godot` 与 M1 框架地基
- CI 阶段 1：docs-check / contract-check / commitlint 三个 workflow
- A~D 玩法手感平衡讨论
