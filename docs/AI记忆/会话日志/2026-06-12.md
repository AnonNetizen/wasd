# 2026-06-12 会话日志

> 本文件由 AI 主动归档。当日工作量大，记录从粗到细。

## 用户主要请求（按时间）

1. 加暂停功能
2. 评估框架优化方向
3. 把建议写进 `修改建议.md`
4. 新建 `README.md` / `.gitignore`
5. 评估 Git 工程化优化（采纳 1~9 + MIT + 提交）
6. 评估 CI/CD（写进单独文档）
7. 仓库重组三段结构 + AI 记忆体系
8. 把对话整理成 `ai-indie-game-framework` skill（user scope）
9. 评估业界 AI 工作流差距，采纳 1~8 改进
10. 自动瘦身规则
11. 评估美术协作改进方向（待落地）
12. **`docs/修改建议.md` J~R 全部采纳**

## 主要产出

### 阶段 A：起步（暂停 / 工程化 / 仓库结构）
- 暂停功能（ADR #13）
- README / LICENSE(MIT) / CONTRIBUTING / .gitignore / .gitattributes / .editorconfig / .gitmessage / .github/ 模板
- CICD 规划文档（17 项，4 阶段）
- 仓库重组：`docs/` + `client/` + `server/`（ADR #14）
- AI 记忆体系：`docs/AI记忆/` 主索引 + 会话日志（ADR #15）

### 阶段 B：业界对标改进 1~8（GDD v1.4）
- 回放系统第 4 条基础设施（GDD 9.9，ADR #16）
- 平衡测试接口预留（GDD 9.10，ADR #17）
- AI 协作工程第 5 条基础设施（GDD 9.11，ADR #18）
- `docs/AI协作/`：7 份任务模板 + 上下文预算 + 角色分工 + 引擎集成 + 实时验证回路
- AI 导航 5.2 节 Mermaid 系统依赖图
- CICD 阶段 4 加 4.L 健康度 / 4.M 回放回归 / 4.N 平衡 sim
- 自动瘦身规则（ADR #19）

### 阶段 C：J~R 全采纳（GDD v1.4 → v1.5，ADR #20~#28）
9 条结构性增强一次性落地：

| ADR | 主题 | GDD | 规则 | 词表 |
|-----|------|-----|------|------|
| 20 | J. RNG 子流 | 9.18.1 | 12-A 扩展 | §11 |
| 21 | K. GameState | 9.12 | 12-C | §4（state_changed 等） |
| 22 | L. PoolManager | 9.13 | 12-C | §8 |
| 23 | M. UIManager | 9.14 | 12-C | — |
| 24 | N. 伤害管线 + StatusEffect | 9.15 | 12-D | §9 / §9-A |
| 25 | O. SaveManager | 9.16 | 12-D | §4（save_*） |
| 26 | P. GameClock | 9.18.2 | 12-A 扩展 | — |
| 27 | Q. AudioManager | 9.17 | 12-D | §10 |
| 28 | R. 词表→代码常量 | 9.19 | 15 扩展 | 末尾 |

伴随更新：
- 模块清单（`Combat` / `StatusEffectComponent` / 8 个 autoload 全部进表）
- 系统依赖图新增"协调中枢"与"资源管理"两组分类
- AI 导航扩展点表新增 7 行（UI 弹窗 / 高频实体 / 伤害逻辑 / 持续效果 / 存档 / 音效 / AI 任务）
- 红线新增 6 条禁区（`get_tree().paused` 直接读写 / `add_child` UI / `target.hp -=` / 各自 DoT / 无 version 存档 / 直接 play() / 手改生成文件）
- 自检清单加 9 条
- M1 里程碑改为"完整 autoload 矩阵"
- `docs/修改建议.md` 中 J~R 整体改为已采纳归档表

### 阶段 D：skill 创建（与项目无关）
- `~/.codebuddy/skills/ai-indie-game-framework/` user scope
- 26 个文件（SKILL.md + 3 references + 10 templates + 4 github-templates + 8 assets）

## 关键决策与权衡

- **J 用子流分流而非单一 RNG**：避免局部调用偏移影响全局序列；`luck` 通过 `weighted_pick` 注入。
- **K 把 PauseMenu/Replay/Analytics 改为 GameState 消费者**：消除散落判断，状态机做协调中枢。
- **N effect 原语薄包装**：`ignite`/`chain` 等不再各自实现 tick，统一注入 `StatusEffect`。
- **O 强制 version + 迁移链**：踩过最多坑的地方，第一天就立。
- **P 不依赖 `Engine.time_scale`**：避免影响物理与 UI；`GameClock.set_time_scale` 只作用于业务时间。
- **R 选脚本生成 (b) 而非手写 (a)**：单一来源 + hook 兜底 = 漂移不可能。
- **AI 记忆按"自动瘦身"规则**：本日完整摘要进会话日志，主索引第 6 节只保留一行。

## 提交（Git，本会话累计）

```
8919ebd chore: init repo with license, gitignore/attributes and contribution scaffolding
79da1ba docs: 初始设计文档与编码规则
0793d16 docs(proposal): 待决策项 A~D 与 J~R
58bf6e1 chore: restructure into docs/client/server layout
2965703 docs(memory): add AI 记忆体系
8d2e4c9 docs(adr): 同步规则与 ADR 至新仓库结构
71b2705 docs(framework): 引入回放/平衡/AI协作工程三条基础设施 (GDD v1.4)
3d15198 docs(workflow): 引擎集成、依赖图、健康度
61f433e docs(rules+memory): AI 记忆自动瘦身规则 (ADR #19)
478969a docs(memory): 自动瘦身落地
b9299fb docs(framework): K/L/M (GDD 9.12-9.14, v1.5)
422079c docs(framework): N/O/Q (GDD 9.15-9.17)
6dc29b7 docs(rules+framework): J/P/R (GDD 9.18-9.19)
（待）   docs(adr+memory): J~R 归档 + ADR #20~#28 + 词表 + AI导航 + 项目记忆
```

## 未决 / 下一步

- **代码启动**：M1 完整 autoload 矩阵（11 个），最小骨架先行
- **CI 阶段 1**：含 `tools/sync_contracts.py`（GDD 9.19）
- **任务模板试用**：跑真实任务回收反馈
- **A~D 玩法平衡**：等核心循环跑通再讨论
- **美术 / 音效协作**（后续话题）：等真接美术时启动
