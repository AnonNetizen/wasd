# 2026-07-01 会话日志

- Steamworks Slime Lab 首片：按用户要求新建独立 Godot 4.7 测试项目 `output/steamworks_lab`，不改正式 `client`；复用无骨骼软体细胞思路实现 `slime_body.gd` 史莱姆外观，新增 `SlimePlayer`、`NetworkSession`、`TransportAdapter` 和主场景 UI。第一版支持 WASD / 方向键移动、host authoritative 位置同步、本机 ENet host / join，以及可选 GodotSteam / `SteamMultiplayerPeer` Steam Spacewar `480` lobby/P2P adapter；GodotSteam / Steamworks 二进制不入仓库，README 记录本地安装、双开测试和 Steam 手动 smoke。验证：`py -3 tools/godot_bridge.py --project output/steamworks_lab headless-boot`、`export-tree`、`git diff --check` 通过；未执行真实 Steam 手动 smoke（本机未安装 / 登录对应 GodotSteam 测试环境）。
