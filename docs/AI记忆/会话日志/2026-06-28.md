# 2026-06-28 会话日志

- 远程敌人首片：新增 ADR #126、`ai_action_ranged_attack`、`enemy_ai_ranged_spitter` 和 `enemy_spitter` 喷棘者；`Bullet` 支持可配置 source / target team 与目标组，`Player` 暴露 `active_player`、`is_alive()` 和 `hit_radius()`，`GameplayRunLoop` 注册 `enemy_ranged` 池并在 5:00 接入远程波次；runtime-smoke 覆盖远程敌弹伤害玩家。读取范围：AGENTS/CODEX 快速开工、Codex 规则、AI导航、上下文预算、加敌人模板、godot-gdscript / project-doc-sync skill、EnemyAI / Gameplay Runtime / DataLoader / locale / 数据手册相关段。
- F13 手工房间制规划：按用户提出“类似挺进地牢的一个个房间、出入口、手工制作、需要关卡编辑器支持门 / 陷阱 / 敌人生成点”的方向，新增 ADR #127 和 `docs/AI协作/工作包/F13-HandcraftedRooms.md`；默认短刷图下一阶段转向 Godot `.tscn` + marker 的手工房间串联，首片只做线性清房开门 / 进入下一房间，暂不自研完整 EditorPlugin UI；同步 GDD v1.17、AI导航、正式项目工作规划、TODO、测试策略、AI知识库索引、`docs/_kb_index.json`、快速开工、项目记忆和 current_state，并关闭 C 待决策。该轮为文档规划更新，未改运行时代码。
- F13 手工房间制首片实现（ADR #128）：用户点名“先出 plan 再写代码，直接开始推进 F13”。先用并行勘探 workflow 摸清 run loop / MapManager / SaveManager / DataLoader / 场景契约 / smoke 工具链 / 实体系统集成点，再实现首片。
  - 契约：词表新增 §15-A door_direction（dir_north/south/east/west）、§15-B door_unlock_rule（unlock_on_clear/unlock_open）、§15-C room_clear_condition（room_clear_all_enemies/room_clear_none），接入 `sync_contracts.py` 生成 `door_directions.gd` / `door_unlock_rules.gd` / `room_clear_conditions.gd` 与 `_contracts.json`。
  - marker / 场景：新增 `client/scripts/gameplay/rooms/` 下 `RoomRoot` + 四类 marker（`extends Marker2D` + 类型化 `@export` + `to_data()`，项目首次用 @export 编辑器数据节点）；用一次性 headless 生成器产出 `room_demo_entry.tscn` / `room_demo_arena.tscn` 后删除生成器。
  - 数据：新增 `rooms.json` / `room_sequences.json`，`DataLoader.gd` + `tools/validate_data.py` 双校验（scene_path 存在性、跨文件 room/mode 引用），`tools/test_data_loader_schema.py` 加 5 个坏样例（含拷贝房间场景目录到临时仓库），`client/data/README.md` 记字段。
  - 运行时：新增 `RoomManager`（`Node2D`，房间 carrier 下由 `GameplayRunLoop` 挂在 `ActiveWorld` 创建 / 驱动，对象池生成仍在 run loop）；`GameplayRunLoop` 加 carrier 分支（opt-in `debug_enable_room_carrier()` 或存档 room 块）、`_spawn_enemy_at`、`_update_room`、清房计数、`create_run_snapshot` room 块、`_restore_room_carrier`、`RUN_SNAPSHOT_SCHEMA_VERSION` 2→3；`SaveManager` `CURRENT_KIND_VERSIONS[RUN]` 2→3 + `_migrate_run_v2_to_v3`。
  - smoke：新增 `room-switch-smoke`（runner + `formal_client_boot` 分发 + `godot_bridge` 命令），覆盖房间加载 / 清房开门 / 切房 / pause-save-continue 恢复；`save_manager_smoke` 更新为两步迁移链 + v2→v3 room 回填断言。
  - 验证：sync_contracts --check / validate_data / test_data_loader_schema / lint_gdscript / lint_project_rules / lint_semantic(advisory) / headless-boot / runtime-smoke / save-smoke / room-switch-smoke / f9-demo-smoke / gear-mod-smoke / settings-smoke 全过；四条黄金回放因 data_fingerprint（新契约 + rooms/room_sequences schema_counts）变化重录，diff 仅 fingerprint / recording_hash / 单个 UI 点击时间戳亚帧抖动，行为 summary 未变。
  - 文档：新增 `docs/代码/room_manager.md`，ADR #128，并同步 `gameplay_runtime.md` / `save_manager.md` / `map_manager.md` / AI导航 / 测试策略 / 引擎集成 / GDD / F13 工作包 / 词表 / 数据 README / 项目记忆 / current_state。
  - 边界：默认 `mode_standard_survival` 仍是 open-warzone（F12 不变）；把房间设为默认可玩模式需重写 F12 runtime/f9 smoke + 重录黄金，留作后续切片，已写入 current_state next_actions。
