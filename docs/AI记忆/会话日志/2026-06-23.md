# 2026-06-23 会话日志

> **AI 修改说明**：修改本文档前先读 `docs/AI协作/文档维护指南.md`。本文档记录当天 AI 协作细节；长期结论同步到 `docs/AI记忆/项目记忆.md` 与 `docs/AI记忆/current_state.json`。

## 摘要

- 用户先要求按 `AGENTS.md` 初始化对话，已完成快速开工热路径读取。
- 用户询问现有技能系统后，确认当前 `SkillSystem` 是可复用主动技能首片：`skills.json` 定义技能，角色通过 `starting_loadout.skill_ids` 引用，资源池来自 `skill_resources`，旋风斩经 `Combat` 造成 AOE 伤害。
- 用户要求吸收 Unreal GAS 思想后，新增 ADR #94：项目采用 Godot 适配版轻量 GAS，只吸收 ability / tag / cost / cooldown / targeting / effect 管线，不照搬 Unreal 网络预测、replication 或 Blueprint 类型系统。
- 用户问下一步后，继续推进 ADR #95：状态效果首片接入轻量 GAS，`StatusEffectComponent` 管理沉默状态授予 / 移除 `ability_tag_silenced`、按 `GameClock` 过期并进入 SkillSystem 快照。
- 用户同意继续后，推进 ADR #96：Player 与 Enemy 正式接入 `StatusEffectComponent`，实体状态效果、owned ability tags 和状态快照进入真实目标与对象池复用边界。
- 用户确认“先做 burn”后，推进 ADR #97：burn 作为首个真实 DoT 状态接入项目版轻量 GAS，经 `StatusEffectComponent` 计时并通过 `Combat.apply_damage()` 结算 `fire + is_dot` 伤害。
- 用户随后确认“先把 burn 变成可玩内容，不做 poison”，新增 `skill_ignite_slash` 点燃斩作为真实数据技能；默认主键仍释放旋风斩，点燃斩作为第二个已加载技能和模式池内容存在。
- 用户指出敌人能跑到地图外，这是有限地图边界漏接；已修复为敌人生成 / 续局恢复时注入 `MapManager.bounds()`，Enemy 普通移动、冲锋、中心分离和快照恢复后统一 clamp 到有限地图内。

## 产出

- `docs/词表与契约.md` 新增 §12-G `ability_tag`，并通过 `tools/sync_contracts.py` 生成 `client/scripts/contracts/ability_tags.gd` 与更新 `_contracts.json`。
- `client/data/skills.json` 为 `skill_whirlwind_slash` 增加 `ability_tags` 与 `activation.required_tags` / `blocked_tags` / `granted_tags`。
- `client/scripts/gameplay/skill_system.gd` 增加 owned ability tag 计数、`add_owned_tag` / `remove_owned_tag` / `has_owned_tag` / `owned_tags` API、activation tag gating、激活期临时 tag 和 run 快照恢复。
- 提交前 review 发现旧快照仅含 `owned_tags` 时会被 `owned_tag_counts` 默认空字典短路；已修复为字段存在时才走新计数格式，并用 L1 smoke 覆盖旧数组与新计数两条恢复路径。
- `DataLoader`、`tools/validate_data.py` 与 `tools/test_data_loader_schema.py` 增加 ability tag / activation schema 校验。
- `client/tools/l1_smoke.gd` 增加 `ability_tag_silenced` 阻断释放和 `ability_tag_activating` 临时 tag 清理覆盖。
- 同步更新 GDD、ADR、AI 导航、知识库索引、数据手册、DataLoader / Gameplay Runtime / SkillSystem 模块文档、测试策略与 AI 记忆。
- `docs/词表与契约.md` 新增 §9-B `status_stack_rule`，并生成 `client/scripts/contracts/status_stack_rules.gd`；`status_effects.gd` 新增 `silence`，`skill_effects.gd` 新增 `skill_effect_apply_status`。
- 新增 `client/scripts/combat/status_effect.gd` 与 `status_effect_component.gd`，并新增 `docs/代码/status_effect_component.md` 作为状态效果模块文档。
- `SkillSystem` 支持 `apply_status_effect()`、`skill_effect_apply_status`、状态效果快照 / 恢复，以及已有 owned tag 快照下避免状态 tag 双计数。
- `DataLoader`、`tools/validate_data.py` 与 `tools/test_data_loader_schema.py` 增加 `skill_effect_apply_status` 的 status、duration、stack_rule 和 granted ability tag schema 校验。
- `client/tools/l1_smoke.gd` 增加自我沉默：施加 `silence` 后阻断释放，快照恢复后仍阻断，过期后释放 `ability_tag_silenced` 并恢复可释放。
- 调试过程中曾遇到 GDScript 编译顺序和 typed array 返回问题：避免直接用未稳定解析的 `StatusEffect` 类型注解，并让 `TARGET_ALLY` 返回真正的 `Array[Node]`。
- `client/scripts/gameplay/player.gd` 与 `client/scripts/gameplay/enemy.gd` 新增 `apply_status_effect()`、`active_statuses()`、owned ability tag API，以及 `status_effects` / `owned_tag_counts` run 快照恢复。
- `Enemy.configure()`、`_pool_release()` 与 `_pool_reset()` 现在清理状态组件和 owned tags，避免对象池复用时把上一只敌人的状态带到下一只。
- `client/tools/l1_smoke.gd` 增加真实 Player / Enemy 状态宿主覆盖：技能向 Enemy 施加沉默、Enemy configure 复用清理、Player / Enemy 快照恢复和状态过期释放。
- 提交前事实 review 重点复核 `StatusEffectComponent.clear(false)` 语义：组件会“可选择不释放 tags，但始终清空状态表”；已在文档说明该语义，并在 L1 smoke 增加 configure 后 active statuses 为空的断言。
- 同步更新 ADR #96、Gameplay Runtime、SkillSystem、EnemyAI、StatusEffectComponent、GDD、AI 导航、测试策略、知识库索引与 AI 记忆。
- `StatusEffect` 新增 `damage_type`、`tick_remaining`、`source_team`、`target_team` 运行时字段，快照 / 恢复同步保存 DoT 结算上下文。
- `StatusEffectComponent` 新增 DoT tick 循环：只在 `GameState.PLAYING` 下推进，暂停期间冻结，通过 `Combat.apply_damage()` 输出带 `is_dot` flag 的 `DamageInfo`。
- `Player` / `Enemy` 新增 `combat_team_id()`，burn DoT 快照会保留来源队伍与目标队伍，便于恢复后仍能正确归因。
- `DataLoader`、`tools/validate_data.py` 与 `tools/test_data_loader_schema.py` 增加 `skill_effect_apply_status` DoT schema 校验：正数 `magnitude` + 正数 `tick_interval` 必须提供已登记 `damage_type`。
- `client/tools/l1_smoke.gd` 新增真实 Player 对 Enemy 施加 burn 的覆盖：校验 `fire`、`is_dot`、敌人扣血、暂停冻结、快照恢复继续 tick 与最终过期。
- 同步更新 ADR #97、StatusEffectComponent、SkillSystem、DataLoader、Combat、Gameplay Runtime、EnemyAI、GDD、AI 导航、测试策略、知识库索引、数据手册与 AI 记忆；ADR #97 首次提交时未改默认技能数据，后续本轮点燃斩内容见下方。
- `docs/词表与契约.md` 新增 `skill_ignite_slash`，并通过 `tools/sync_contracts.py` 生成 `SkillIds.SKILL_IGNITE_SLASH` 与更新 `_contracts.json`。
- `client/data/skills.json` 新增 `skill_ignite_slash`：`target_enemy` 锁定最近敌人，消耗 `mana`，用 `skill_effect_apply_status` 施加 `burn`，参数为 `damage_type=fire`、`magnitude=1.5`、`tick_interval=0.2`、`duration=1.2`、`stack_rule=REFRESH`。
- `characters.json.starting_loadout.skill_ids` 保持 `skill_whirlwind_slash` 第一项并追加 `skill_ignite_slash` 第二项；`game_modes.json.resource_pools.skills` 加入点燃斩权重 60，因此默认主键行为不替换旋风斩。
- `client/locale/strings.csv` 新增点燃斩中英文名称 / 描述；数据手册、SkillSystem / Gameplay Runtime 模块文档、GDD、AI 导航、测试策略和 AI 记忆同步更新。
- `client/tools/l1_smoke.gd` 的 burn 覆盖改为从真实 `skills.json` 读取 `skill_ignite_slash`，验证新增数据技能通过正式 SkillSystem / StatusEffectComponent / Combat 链路生效。
- `client/scripts/gameplay/enemy.gd` 新增 `set_movement_bounds()` / `clear_movement_bounds()`，并在普通移动、冲锋移动、中心分离、`configure()` 和 `restore_snapshot()` 后应用有限地图 bounds clamp。
- `client/scripts/gameplay/gameplay_run_loop.gd` 在 `_spawn_enemy()` 与 `_restore_enemy_snapshots()` 中为池化敌人注入 `MapManager.bounds()`；同时新增 `_connect_enemy_defeated()`，复用池化敌人前清理指向当前 run loop 的旧 `defeated` signal 连接，避免重复连接错误。
- `client/tools/runtime_smoke.gd` 新增敌人边界覆盖：把猎物放到地图右边缘并让其逃离掠食者，断言移动仍在 bounds 内；再用越界 snapshot 恢复验证旧存档 / 异常位置会被夹回地图内。既有生态 AI 测试坐标也从地图外调整到 bounds 内。
- 同步更新 GDD、AI 导航、MapManager / EnemyAI / Gameplay Runtime 模块文档、测试策略和 AI 记忆，明确有限地图边界约束玩家与敌人中心点。

## 验证

- 已通过：`python tools/sync_contracts.py --check`
- 已通过：`python tools/validate_data.py`
- 已通过：`python tools/test_data_loader_schema.py`
- 已通过：`python tools/lint_gdscript_rules.py`
- 已通过：`python tools/lint_project_rules.py`
- 已通过：`python tools/test_project_rules_lint.py`
- 已通过：`python tools/lint_semantic_rules.py`（退出 0；保留既有动态 UI / 节点 advisory）
- 已通过：`python tools/test_semantic_rules_lint.py`
- 已通过：`python tools/godot_bridge.py --project client l1-smoke`
- 已通过：修复旧 / 新 owned ability tag 快照恢复兼容后重跑 `python tools/godot_bridge.py --project client l1-smoke`
- 已通过：`python tools/godot_bridge.py --project client runtime-smoke`（命令通过；本机已有坏 run 存档会触发预期诊断）
- 已通过：修复旧 / 新 owned ability tag 快照恢复兼容后重跑 `python tools/godot_bridge.py --project client runtime-smoke`（命令通过；本机已有坏 run 存档会触发预期诊断）
- 已通过：`python tools/godot_bridge.py --project client save-smoke`（命令通过；smoke 会刻意制造坏档验证回退 / 隔离诊断）
- 已通过：`python tools/godot_bridge.py --project client headless-boot`
- 已通过：`python tools/godot_bridge.py --project client replay-smoke`
- 已通过：`python tools/godot_bridge.py --project client replay-input-smoke`
- 已通过：ADR #95 状态效果首片重跑 `python tools/sync_contracts.py --check`
- 已通过：ADR #95 状态效果首片重跑 `python tools/validate_data.py`
- 已通过：ADR #95 状态效果首片重跑 `python tools/test_data_loader_schema.py`
- 已通过：ADR #95 状态效果首片重跑 `python tools/lint_gdscript_rules.py`
- 已通过：ADR #95 状态效果首片重跑 `python tools/lint_project_rules.py`
- 已通过：ADR #95 状态效果首片重跑 `python tools/test_project_rules_lint.py`
- 已通过：ADR #95 状态效果首片重跑 `python tools/lint_semantic_rules.py`（退出 0；现有动态 UI / 节点 advisory 外，`SkillSystem` 一次性挂载 `StatusEffectComponent` 被通用 add_child 规则提示，人工判断为低频内部组件 false positive）
- 已通过：ADR #95 状态效果首片重跑 `python tools/test_semantic_rules_lint.py`
- 已通过：ADR #95 状态效果首片重跑 `python tools/godot_bridge.py --project client l1-smoke`
- 已通过：ADR #95 状态效果首片重跑 `python tools/godot_bridge.py --project client runtime-smoke`（命令通过；本机已有坏 run 存档会触发预期诊断）
- 已通过：ADR #95 状态效果首片重跑 `python tools/godot_bridge.py --project client save-smoke`（命令通过；smoke 会刻意制造坏档验证回退 / 隔离诊断）
- 已通过：ADR #95 状态效果首片重跑 `python tools/godot_bridge.py --project client headless-boot`
- 已通过：ADR #95 状态效果首片重跑 `python tools/godot_bridge.py --project client replay-smoke`
- 已通过：ADR #95 状态效果首片重跑 `python tools/godot_bridge.py --project client replay-input-smoke`
- 已通过：ADR #95 状态效果首片重录四条 checked-in golden replay（`golden_basic_run` / `golden_pause_resume` / `golden_full_death` / `golden_level_up_choice`）
- 已通过：ADR #95 状态效果首片重跑四条 checked-in golden replay rerun（`golden_basic_run` / `golden_pause_resume` / `golden_full_death` / `golden_level_up_choice`）
- 已通过：ADR #95 状态效果首片重跑 `python tools/docs_health_check.py`
- 已通过：ADR #95 状态效果首片重跑 `python -m json.tool docs/_kb_index.json` 与 `python -m json.tool docs/AI记忆/current_state.json`
- 已通过：ADR #95 状态效果首片重跑 `git diff --check -- . ':(exclude)DRAFT/**' ':(exclude)draft/**'`
- 已通过：ADR #95 状态效果首片最终 `pre-commit run --all-files`
- 首次 `replay-runner --replay-file client/tests/replays/golden_basic_run.replay --rerun-runtime-summary` 因 `data_fingerprint` 变化触发预期 mismatch；随后已重录四条 checked-in golden replay。
- 已通过：四条 checked-in golden replay capture（`golden_basic_run` / `golden_pause_resume` / `golden_full_death` / `golden_level_up_choice`）
- 已通过：四条 checked-in golden replay rerun（`golden_basic_run` / `golden_pause_resume` / `golden_full_death` / `golden_level_up_choice`）
- 已通过：`python tools/docs_health_check.py`
- 已通过：`git diff --check -- . ':(exclude)DRAFT/**' ':(exclude)draft/**'`
- 已通过：ADR #96 Player / Enemy 状态宿主重跑 `python tools/sync_contracts.py --check`
- 已通过：ADR #96 Player / Enemy 状态宿主重跑 `python tools/validate_data.py`
- 已通过：ADR #96 Player / Enemy 状态宿主重跑 `python tools/test_data_loader_schema.py`
- 已通过：ADR #96 Player / Enemy 状态宿主重跑 `python tools/lint_gdscript_rules.py`
- 已通过：ADR #96 Player / Enemy 状态宿主重跑 `python tools/lint_project_rules.py`
- 已通过：ADR #96 Player / Enemy 状态宿主重跑 `python tools/test_project_rules_lint.py`
- 已通过：ADR #96 Player / Enemy 状态宿主重跑 `python tools/lint_semantic_rules.py`（退出 0；新增 Player / Enemy 内部挂载 `StatusEffectComponent` 与既有 SkillSystem 一样触发通用 add_child advisory，人工判断为状态宿主内部组件 false positive）
- 已通过：ADR #96 Player / Enemy 状态宿主重跑 `python tools/test_semantic_rules_lint.py`
- 已通过：ADR #96 Player / Enemy 状态宿主重跑 `python -m json.tool docs/_kb_index.json` 与 `python -m json.tool docs/AI记忆/current_state.json`
- 已通过：ADR #96 Player / Enemy 状态宿主重跑 `python tools/docs_health_check.py`
- 已通过：ADR #96 Player / Enemy 状态宿主重跑 `git diff --check -- . ':(exclude)DRAFT/**' ':(exclude)draft/**'`
- 已通过：ADR #96 Player / Enemy 状态宿主重跑 `python tools/godot_bridge.py --project client l1-smoke`
- 已通过：ADR #96 Player / Enemy 状态宿主重跑 `python tools/godot_bridge.py --project client runtime-smoke`（命令通过；本机已有坏 run 存档会触发预期诊断）
- 已通过：ADR #96 Player / Enemy 状态宿主重跑 `python tools/godot_bridge.py --project client save-smoke`（命令通过；smoke 会刻意制造坏档验证回退 / 隔离诊断）
- 已通过：ADR #96 Player / Enemy 状态宿主重跑 `python tools/godot_bridge.py --project client headless-boot`
- 已通过：ADR #96 Player / Enemy 状态宿主重跑 `python tools/godot_bridge.py --project client replay-smoke`
- 已通过：ADR #96 Player / Enemy 状态宿主重跑 `python tools/godot_bridge.py --project client replay-input-smoke`
- 已通过：ADR #96 Player / Enemy 状态宿主重跑四条 checked-in golden replay rerun（`golden_basic_run` / `golden_pause_resume` / `golden_full_death` / `golden_level_up_choice`）
- 已通过：ADR #96 Player / Enemy 状态宿主最终 `pre-commit run --all-files`
- 已通过：ADR #97 burn DoT 重跑 `python tools/sync_contracts.py --check`
- 已通过：ADR #97 burn DoT 重跑 `python tools/validate_data.py`
- 已通过：ADR #97 burn DoT 重跑 `python tools/test_data_loader_schema.py`（新增覆盖未登记 `damage_type` 与 DoT 缺 `damage_type` 两条失败用例）
- 已通过：ADR #97 burn DoT 重跑 `python tools/lint_gdscript_rules.py`
- 已通过：ADR #97 burn DoT 重跑 `python tools/lint_project_rules.py`
- 已通过：ADR #97 burn DoT 重跑 `python tools/test_project_rules_lint.py`
- 已通过：ADR #97 burn DoT 重跑 `python tools/lint_semantic_rules.py`（退出 0；新增相关 Player / Enemy / SkillSystem 内部挂载 `StatusEffectComponent` 属状态宿主内部组件 false positive，其余为既有动态 UI / 节点 advisory）
- 已通过：ADR #97 burn DoT 重跑 `python tools/test_semantic_rules_lint.py`
- 已通过：ADR #97 burn DoT 重跑 `python -m json.tool docs/_kb_index.json` 与 `python -m json.tool docs/AI记忆/current_state.json`
- 已通过：ADR #97 burn DoT 重跑 `python tools/docs_health_check.py`
- 已通过：ADR #97 burn DoT 重跑 `git diff --check -- . ':(exclude)DRAFT/**' ':(exclude)draft/**'`
- 已通过：ADR #97 burn DoT 重跑 `python tools/godot_bridge.py --project client l1-smoke`
- 已通过：ADR #97 burn DoT 重跑 `python tools/godot_bridge.py --project client runtime-smoke`（命令通过；本机已有坏 run 存档会触发预期诊断）
- 已通过：ADR #97 burn DoT 重跑 `python tools/godot_bridge.py --project client save-smoke`（命令通过；smoke 会刻意制造坏档验证回退 / 隔离诊断）
- 已通过：ADR #97 burn DoT 重跑 `python tools/godot_bridge.py --project client headless-boot`
- 已通过：ADR #97 burn DoT 重跑 `python tools/godot_bridge.py --project client replay-smoke`
- 已通过：ADR #97 burn DoT 重跑 `python tools/godot_bridge.py --project client replay-input-smoke`
- 已通过：ADR #97 burn DoT 重跑四条 checked-in golden replay rerun（`golden_basic_run` / `golden_pause_resume` / `golden_full_death` / `golden_level_up_choice`）；该阶段默认 `skills.json` 未挂载 burn，因此无需重录 golden
- 点燃斩首轮 `python tools/validate_data.py` 与 `python tools/test_data_loader_schema.py` 曾因 `stack_rule=refresh` 小写失败；已改为词表登记值 `REFRESH` 后通过。
- 已通过：点燃斩重跑 `python tools/sync_contracts.py --check`
- 已通过：点燃斩重跑 `python tools/validate_data.py`
- 已通过：点燃斩重跑 `python tools/test_data_loader_schema.py`
- 已通过：点燃斩重跑 `python tools/lint_gdscript_rules.py`
- 已通过：点燃斩重跑 `python tools/lint_project_rules.py`
- 已通过：点燃斩重跑 `python tools/test_project_rules_lint.py`
- 已通过：点燃斩重跑 `python tools/lint_semantic_rules.py`（退出 0；既有动态 UI / 节点 advisory 与 Player / Enemy / SkillSystem 内部 `StatusEffectComponent` 挂载提示已人工接受为现有内部组件 / 动态节点边界）
- 已通过：点燃斩重跑 `python tools/test_semantic_rules_lint.py`
- 已通过：点燃斩重跑 `python tools/godot_bridge.py --project client l1-smoke`（从真实 `skills.json` 读取 `skill_ignite_slash` 覆盖 burn DoT 链路）
- 已通过：点燃斩重跑 `python tools/godot_bridge.py --project client runtime-smoke`（命令通过；本机已有坏 run 存档会触发预期诊断，默认主键仍释放 `skill_whirlwind_slash`）
- 已通过：点燃斩重跑 `python tools/godot_bridge.py --project client save-smoke`（命令通过；smoke 会刻意制造坏档验证回退 / 隔离诊断）
- 已通过：点燃斩重跑 `python tools/godot_bridge.py --project client headless-boot`
- 已通过：点燃斩重跑 `python tools/godot_bridge.py --project client replay-smoke`
- 已通过：点燃斩重跑 `python tools/godot_bridge.py --project client replay-input-smoke`
- 点燃斩首次 rerun `golden_basic_run.replay` 因 `data_fingerprint` 从新增技能数据后变化而预期失败；随后已重录四条 checked-in golden replay。
- 已通过：点燃斩重录四条 checked-in golden replay（`golden_basic_run` / `golden_pause_resume` / `golden_full_death` / `golden_level_up_choice`）
- 已通过：点燃斩重跑四条 checked-in golden replay rerun（`golden_basic_run` / `golden_pause_resume` / `golden_full_death` / `golden_level_up_choice`）
- 已通过：敌人边界修复重跑 `python tools/lint_gdscript_rules.py`
- 已通过：敌人边界修复重跑 `python tools/lint_semantic_rules.py`（退出 0；保留既有动态 UI / 节点 advisory，`Enemy` 内部 `StatusEffectComponent` 挂载提示仍按既有状态宿主边界接受）
- 已通过：敌人边界修复重跑 `python tools/godot_bridge.py --project client runtime-smoke`；新增敌人边界断言通过，首次暴露的池化敌人 `defeated` signal 重复连接错误已修复并重跑通过，剩余 SaveManager 坏档日志为 smoke 预期诊断
- 已通过：敌人边界修复重跑 `python tools/godot_bridge.py --project client headless-boot`
- 已通过：敌人边界修复重跑 `python tools/godot_bridge.py --project client save-smoke`（命令通过；smoke 会刻意制造坏档验证回退 / 隔离诊断）
- 已通过：敌人边界修复重跑 `python tools/godot_bridge.py --project client replay-smoke`
- 已通过：敌人边界修复重跑 `python tools/godot_bridge.py --project client replay-input-smoke`
- 已通过：敌人边界修复重跑四条 checked-in golden replay rerun（`golden_basic_run` / `golden_pause_resume` / `golden_full_death` / `golden_level_up_choice`），无需重录

## 注意

- `DRAFT/暂存.md` 在本轮开始前已有用户侧修改；AI 未读取、未修改、不会提交。
- 后续若要继续扩展轻量 GAS，优先从 `client/data/skills.json` 与 `docs/词表与契约.md` §12-C~12-G 入手；不要把运行时状态塞进 `content_tags`。
- 后续若要继续状态效果，优先看 `docs/代码/status_effect_component.md`；新状态登记词表 §9-A，新叠加规则登记 §9-B，通过 `skill_effect_apply_status` 或未来 on-hit primitive 注入。
