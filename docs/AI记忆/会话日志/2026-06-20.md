# 2026-06-20 会话日志

## 产出

- 继续推进 F8 perf / balance baseline：`client/tools/perf_probe.gd` 从单次 180 帧 JSON 扩展为 schema v2 可比较基线，新增 `baseline_id`、固定场景 / seed、30 帧 warmup 后 180 帧采样、avg / p95 / p99 / max 帧时间、active / peak entity counts、pool final stats / peak active 与 `budget_status`。warmup 用于排除 headless 启动尖峰，避免早期单帧卡顿污染基线。
- 同步 F8 工作包、测试策略、CI 规划、AI 导航、Gameplay Runtime 文档、正式项目规划、TODO、项目记忆和 `current_state.json`，把 perf-probe 从“基础 JSON”更新为“schema v2 可比较性能 / 平衡基线”。
- 继续推进 F8 replay baseline：新增 `golden_level_up_choice.replay`，通过真实经验球触发升级选择，再由工具层调用 `LevelUpPanel.choose_index()`，覆盖 `Replay.record_decision(level_up, ...)`、候选 id 顺序、选中 id 和修正应用。
- 为回放确定性收紧 `RNG` 子流 seed 派生：从 Godot `hash()` 改为项目内稳定字符串滚动算法；升级候选抽中后按 id 稳定排序，避免同一候选集合因随机选择顺序影响回放中“选择第 N 项”的语义。同步 ADR #81、RNG / Replay / Gameplay Runtime / FormalClientBoot 文档、F8 工作包、测试策略、CI / AI 导航 / 知识库索引、TODO、项目记忆和 `current_state.json`。
- 完成 F8 小收口审计：确认 L1 smoke、四条 checked-in golden replay、runtime summary rerun、schema v2 perf / balance baseline 与当前文档状态一致；F8 当前验收基线可收口，但阶段切换等待用户确认。下一步可选 F9 内容扩展 / Demo 打磨，或先把四条 golden replay regression 接入更正式的 CI / 显式本地命令组合。
- 完成 F9 准备：新增 `docs/AI协作/工作包/F9-ContentDemoPolish.md`，明确 F9 目标、必读文件、拆分建议、可改文件、禁止事项、验收命令和手动 Demo checklist；同步 AI 协作 README、上下文预算、AI 导航、知识库索引、机器索引、TODO、项目记忆和 `current_state.json`，把当前阶段切到 F9，并保留 F8 golden replay / perf-probe 作为回归护栏。
- 设置面板收尾改为只展示已接线即时生效项：保留语言、音量、自动录制回放、数据收集和基础输入绑定；隐藏当前未接线的 fullscreen / vsync、松开瞄准停火、自动瞄准、屏幕震动和失焦暂停预留项，并同步 settings-smoke、模块文档、F7 工作包、AI 导航、测试 checklist 和项目记忆口径。
- 采纳瞄准方式决策 B 并新增 ADR #82：默认射击从 4 方向改为鼠标相对玩家 / 视口中心方向瞄准，方向键 / 手柄右摇杆 / D-pad 作为兜底输入；玩家和敌人占位表现只做左 / 右朝向。同步 GDD、词表、规则、数据默认 `control_profile` / `fire_mode`、locale、Gameplay Runtime / Replay / Settings 文档、测试策略、AI 导航、TODO、项目记忆和 `current_state.json`。
- 预留玩家 mod 接口并新增 ADR #83：新增 `ModLoader` autoload 扫描 `user://mods/<mod_id>/mod.json`，只支持声明式 JSON / CSV append patch 与少量动态契约扩展；`DataLoader` 合并 mod 数据并继续校验 schema；创意工坊暂不接入，未来只作为分发层。同步 GDD、三平台规则、DataLoader / ModLoader 模块文档、数据手册、测试策略、AI 导航、项目记忆和 `current_state.json`。
- 预留 Steam 优先的平台服务接口并新增 ADR #84：新增 `PlatformServices` autoload，统一承接 Steam 成就、统计、富状态 / 状态显示、overlay、Lobby / 联机入口和用户身份；当前只提供 `none` 空后端，不接 Steamworks SDK、不联网，其他平台后续走 provider adapter。同步 GDD、三平台规则、PlatformServices 模块文档、测试策略、AI 导航、知识库索引、项目记忆和 `current_state.json`。
- 完成 Godot 4.7 迁移并新增 ADR #85：按官方 4.7 release 与迁移指南核对 breaking changes，项目未命中需改写的高风险 API；`client/project.godot` 的 `config/features` 改为 `4.7`，README / GDD / AI 导航 / CI 规划 / 三平台规则与 Godot skills / 项目记忆统一更新为 Godot 4.7 基线。
- 用 Godot 4.7 重跑四条 checked-in golden replay 后，仅更新 `.replay` envelope 的 `data_fingerprint` 到 `92b2a5487f50b4e06955fdd66a64b087ebf03955c2a795fe9bba5615ea1c5e65`；四条 runtime summary 重新比较通过，未改 gameplay 预期摘要。
- 修正活跃 Godot style guide 来源链接：知识库任务路由和 `client/data/credits.json` 均指向 Godot 4.7 固定文档路径；历史 ADR / 会话日志中的 4.6 记录保留为历史事实。
- 针对 STS2 式 RNG 跨子流相关性风险新增 ADR #86：`RNG` 子流 seed 派生从早前稳定字符串滚动算法升级为带域隔离的 SHA-256 mixer，并新增 `client/tools/rng_audit.gd` / `python tools/godot_bridge.py --project client rng-audit`，用 10,000 个 run seed、6 个子流、每流前 4 次 `randf()` 审计 Pearson 相关性；修改 seed mixer / 子流集合时必须同时跑 `rng-audit` 和四条 checked-in replay runner。
- 为 Claude Code 建立平台原生配置并新增 ADR #87：仓库根新增项目自有的 `.claude/agents/`（10）、`.claude/commands/`（5）、`.claude/skills/`（9）、`.claude/rules/game-coding-rules.md` 与 `.claude/settings.json`（仅 `draft/` / `DRAFT/` deny），与 `.codebuddy/` / `.codex/` / `.opencode/` 同源、核心语义一致；agents 工具名从 codex 风格映射为 Claude（Read/Grep/Glob/Edit/Write/Bash），commands 转 Claude slash-command frontmatter，skills / rules 按 verbatim 复制。项目级 skills 同步面由三平台扩为四平台。明确此 `.claude/` 是项目自建配置，不复活 ADR #56~#59 / #72 拒绝的外部 `.claude/` 整包，不引入活跃 hooks；权限 allow 白名单（放行 `tools/*.py` 等）因 auto-mode 分类器判定为自我修改被拦，留待用户单独授权。同步 `CLAUDE.md`、四平台 `game-coding-rules.md`（rule 2 / rule 21）、`docs/AI协作/工具适配指南.md`、`docs/AI协作/AI技能资源评估.md`、`docs/AI导航.md`、`AGENTS.md`、项目记忆和 `current_state.json`。

## 验证

- `py -3 tools/lint_gdscript_rules.py`：通过。
- `py -3 tools/godot_bridge.py --project client perf-probe`：通过；输出 `schema_version=2`、`baseline_id=f8_perf_probe_standard_180f`、`warmup_frames=30`、`sample_frames=180`、`budget_status.status=pass`，并包含帧时间分布、实体峰值和池峰值。
- F8 perf / balance baseline v2 最终验证：`py -3 tools/lint_gdscript_rules.py`、`py -3 tools/lint_semantic_rules.py`、`py -3 -m json.tool docs/AI记忆/current_state.json`、`py -3 -m json.tool docs/_kb_index.json`、`py -3 tools/docs_health_check.py`、`git diff --check -- . ":(exclude)draft/**" ":(exclude)DRAFT/**"`、`py -3 tools/godot_bridge.py --project client perf-probe`、`py -3 tools/godot_bridge.py --project client headless-boot` 和 Python 3.12 路径前置后的 `pre-commit run --all-files` 均通过；`perf-probe` 输出 `schema_version=2`、`budget_status.status=pass`。semantic lint 仍为既有 19 条 advisory。
- F8 level-up choice golden 中途验证：`py -3 tools/lint_gdscript_rules.py`、`py -3 tools/lint_semantic_rules.py`、`py -3 tools/godot_bridge.py --project client l1-smoke`、`py -3 tools/godot_bridge.py --project client replay-smoke`、四条 `capture-golden-replay` 和四条 checked-in replay 的 `replay-runner --replay-file ... --rerun-runtime-summary` 均通过；`golden_level_up_choice` 摘要包含 1 条 `level_up` decision，候选 id 为 `growth_damage_small` / `growth_fire_rate_small` / `growth_pickup_range_small`，选中 `growth_damage_small`，且 `level_up_choice_applied=true`。semantic lint 仍为既有 19 条 advisory。
- F8 level-up choice golden 最终验证：Stage 1 等价脚本、JSON 校验（`current_state.json`、`_kb_index.json` 与四条 golden replay）、`docs_health_check`、`git diff --check`、`l1-smoke`、`replay-smoke`、`replay-runner`、`replay-runner --rerun-runtime-summary`、`replay-input-smoke`、四条 `capture-golden-replay`、四条 checked-in replay 的 `replay-runner --replay-file ... --rerun-runtime-summary`、`perf-probe`、`headless-boot`、`runtime-smoke`、`settings-smoke`、`save-smoke`、`meta-smoke` 和 Python 3.12 路径前置后的 `pre-commit run --all-files` 均通过。`git diff --check` 只提示 `current_state.json` 的既有 CRLF 规范化 warning；`runtime-smoke` / `settings-smoke` / `save-smoke` 日志里的坏 JSON / 非法设置错误仍是既有恢复探针。
- F8 小收口审计验证：四条 golden replay JSON 校验、`docs_health_check`、四条 `replay-runner --replay-file ... --rerun-runtime-summary`、`l1-smoke` 和 `perf-probe` 均通过；`golden_level_up_choice` 仍报告 `decision_events=1`、`level=2`、`xp=20`、`level_up_choice_applied=true`，`perf-probe` 仍报告 `schema_version=2` 与 `budget_status.status=pass`。
- F9 准备验证：`py -3 -m json.tool docs/AI记忆/current_state.json`、`py -3 -m json.tool docs/_kb_index.json`、`py -3 tools/docs_health_check.py`、`git diff --check -- . ":(exclude)draft/**" ":(exclude)DRAFT/**"` 通过后记录。
- F9.0 盘点验证：`py -3 -m json.tool docs/AI记忆/current_state.json`、`py -3 -m json.tool docs/_kb_index.json`、`py -3 tools/sync_contracts.py --check`、`py -3 tools/validate_data.py`、`py -3 tools/lint_project_rules.py`、`py -3 tools/docs_health_check.py`、`git diff --check -- . ":(exclude)draft/**" ":(exclude)DRAFT/**"` 均通过。本轮只改文档 / 机器状态，未改 `client/**/*.gd` 或 gameplay 数据值，因此未跑 GDScript lint、Godot smoke、golden replay runner 或 `perf-probe`；这些留给 F9.1 内容数据切片。
- F9.1 小内容首片验证：`py -3 tools/sync_contracts.py --check`、`py -3 tools/validate_data.py`、`py -3 tools/test_data_loader_schema.py`、`py -3 tools/lint_project_rules.py`、`py -3 tools/test_project_rules_lint.py`、`py -3 tools/godot_bridge.py --project client headless-boot`、`py -3 tools/godot_bridge.py --project client runtime-smoke`、`py -3 tools/godot_bridge.py --project client l1-smoke`、`py -3 tools/godot_bridge.py --project client replay-smoke`、四条 checked-in replay 的 `replay-runner --rerun-runtime-summary` 和 `py -3 tools/godot_bridge.py --project client perf-probe` 均通过。首次 replay runner 因 data fingerprint 从 `92b2a5487f50b4e06955fdd66a64b087ebf03955c2a795fe9bba5615ea1c5e65` 变为 `b4138b1f767764bbafd3ef56d731e329567435c076b7a75dec498e15802dd302` 被拦截，更新四条 replay envelope 后重跑通过；`golden_level_up_choice` 仍只包含二级旧候选，确认新增三级候选未扰动短 golden。
- F9.2 自动化 Demo 前置探针验证：`py -3 tools/lint_gdscript_rules.py` 与 `py -3 tools/godot_bridge.py --project client f9-demo-smoke` 均通过；`f9-demo-smoke` 用 `GameClock.set_time_scale(20.0)` 快进到 `game_time=55.32`，确认 `enemy_bulwark` 从 `wave_standard_mid_bulwarks` 生成，覆盖 run snapshot / `SaveManager` roundtrip、三级成长候选加载和死亡结算记忆余烬链路。该验证不替代 F9.2 人工 3~5 分钟手感试玩。
- F9.2 手动试玩结论与 F9.3 表现首片验证：用户反馈 F9.1 内容试玩“没有什么问题”，因此未调 `enemy_bulwark` / 波次 / 三级成长候选数值；随后新增敌人占位统一暗色轮廓和眼睛描边。`py -3 tools/lint_gdscript_rules.py`、`py -3 -m json.tool docs/AI记忆/current_state.json`、`py -3 tools/docs_health_check.py`、`runtime-smoke`、`f9-demo-smoke`、`sync_contracts --check`、`validate_data`、`lint_project_rules`、`test_project_rules_lint`、`l1-smoke`、`perf-probe`、`lint_semantic_rules`、`test_semantic_rules_lint` 与 `git diff --check -- . ":(exclude)draft/**" ":(exclude)DRAFT/**"` 均通过；semantic lint 仍为既有 19 条 advisory，runtime-smoke 的坏 JSON 日志仍是既有恢复探针。
- F9.3 Demo 音频 cue 计划验证：在 `docs/代码/audio_manager.md` 和 F9 工作包补充 F9 Demo cue 矩阵，列出 `sfx_player_shoot`、`sfx_player_hurt`、`sfx_enemy_hit`、`sfx_enemy_die`、`sfx_pickup_orb`、`sfx_ui_click`、`sfx_ui_levelup`、`music_run_loop` 与预留 `music_boss`，并明确具体 cue id 不写入 `docs/词表与契约.md` 的前缀表，避免污染 `AudioIds.PREFIXES`。`py -3 -m json.tool docs/AI记忆/current_state.json`、`py -3 -m json.tool docs/_kb_index.json`、`py -3 tools/docs_health_check.py`、`py -3 tools/sync_contracts.py --check`、`py -3 tools/validate_data.py`、`py -3 tools/lint_project_rules.py` 与 `git diff --check -- . ":(exclude)draft/**" ":(exclude)DRAFT/**"` 均通过。
- F9.3 无音频资源视觉反馈验证：`py -3 tools/lint_gdscript_rules.py`、`py -3 -m json.tool docs/AI记忆/current_state.json`、`py -3 tools/docs_health_check.py`、`runtime-smoke`、`f9-demo-smoke`、`l1-smoke`、`perf-probe`、`sync_contracts --check`、`validate_data`、`lint_project_rules`、`test_project_rules_lint`、`lint_semantic_rules`、`test_semantic_rules_lint` 与 `git diff --check -- . ":(exclude)draft/**" ":(exclude)DRAFT/**"` 均通过；`f9-demo-smoke` 仍到达 55 秒 bulwark 路径，`perf-probe` 仍为 `schema_version=2` / budget pass，semantic lint 仍为既有 19 条 advisory，runtime-smoke 的坏 JSON 日志仍是既有恢复探针。
- F9.3 反馈颜色与时长一致性验证：`py -3 tools/lint_gdscript_rules.py`、`py -3 -m json.tool docs/AI记忆/current_state.json`、`py -3 tools/docs_health_check.py`、`runtime-smoke`、`f9-demo-smoke`、`l1-smoke`、`perf-probe`、`sync_contracts --check`、`validate_data`、`lint_project_rules`、`test_project_rules_lint`、`lint_semantic_rules`、`test_semantic_rules_lint` 与 `git diff --check -- . ":(exclude)draft/**" ":(exclude)DRAFT/**"` 均通过；`runtime-smoke` 覆盖升级获得反馈、死亡反馈和玩家伤害硬路径，坏 JSON 日志仍是既有恢复探针；`perf-probe` 仍为 `schema_version=2` / budget pass，semantic lint 仍为既有 19 条 advisory。
- F9.3 命中火花 / 伤害数字池验证：`py -3 tools/lint_gdscript_rules.py`、`py -3 -m json.tool docs/AI记忆/current_state.json`、`py -3 tools/docs_health_check.py`、`runtime-smoke`、`f9-demo-smoke`、`l1-smoke`、`perf-probe`、`sync_contracts --check`、`validate_data`、`lint_project_rules`、`test_project_rules_lint`、`lint_semantic_rules`、`test_semantic_rules_lint` 与 `git diff --check -- . ":(exclude)draft/**" ":(exclude)DRAFT/**"` 均通过；`runtime-smoke` 新增断言确认敌人受伤会 acquire `hit_spark` 和 `damage_number`。semantic lint 当前为 21 条 advisory：既有提示加两个反馈池 PoolManager 工厂 `instantiate()` 提示，已人工判定为合理非阻塞；runtime-smoke 的坏 JSON 日志仍是既有恢复探针，`perf-probe` 仍为 `schema_version=2` / budget pass。
- F9.3 命中反馈人工试玩记录验证：`py -3 -m json.tool docs/AI记忆/current_state.json`、`py -3 tools/docs_health_check.py`、`py -3 tools/lint_gdscript_rules.py` 与 `git diff --check -- . ":(exclude)draft/**" ":(exclude)DRAFT/**"` 均通过。本轮只记录用户人工观察结论并补交 Godot 为新脚本生成的 `.gd.uid` sidecar，没有改 gameplay 逻辑。
- F9.4 自动回归与性能守门审计验证：`sync_contracts --check`、`validate_data`、`test_data_loader_schema`、`lint_gdscript_rules`、`lint_project_rules`、`test_project_rules_lint`、`lint_semantic_rules`、`test_semantic_rules_lint`、`current_state.json` / `_kb_index.json` JSON 校验、`docs_health_check`、`git diff --check`、`headless-boot`、`runtime-smoke`、`f9-demo-smoke`、`settings-smoke`、`meta-smoke`、`save-smoke`、`l1-smoke`、`replay-smoke`、四条 checked-in replay 的 `replay-runner --rerun-runtime-summary` 与 `perf-probe` 均通过。`perf-probe` 输出 `schema_version=2`、`budget_status.status=pass`、`p99=9.09ms`；semantic lint 仍为已接受的 21 条 advisory，坏 JSON / 非法配置日志仍是既有恢复探针。
- 设置面板 wired-only 收尾验证：`py -3 tools/lint_gdscript_rules.py`、`py -3 tools/lint_semantic_rules.py`、`py -3 -m json.tool docs/AI记忆/current_state.json`、`py -3 -m json.tool docs/_kb_index.json`、`py -3 tools/docs_health_check.py`、`py -3 tools/godot_bridge.py --project client settings-smoke`、`py -3 tools/godot_bridge.py --project client runtime-smoke`、`git diff --check -- . ":(exclude)draft/**" ":(exclude)DRAFT/**"` 和 Python 3.12 路径前置后的 `pre-commit run --all-files` 均通过；semantic lint 仍为既有 19 条 advisory，settings/runtime smoke 中的非法配置 / 坏存档日志仍是恢复探针。
- Mod 接口首片中途验证：`py -3 tools/lint_gdscript_rules.py` 通过；`py -3 tools/lint_semantic_rules.py` 仍为既有 19 条 advisory，未指向本次新增 `ModLoader` / `DataLoader`；`py -3 tools/godot_bridge.py --project client headless-boot` 通过并输出 `mods=0`；`py -3 tools/godot_bridge.py --project client l1-smoke` 通过，覆盖临时 mod manifest、动态 tag 和 JSON append。
- Mod 接口首片最终验证：`py -3 -m json.tool docs/AI记忆/current_state.json`、`py -3 -m json.tool docs/_kb_index.json`、`py -3 tools/docs_health_check.py`、`py -3 tools/sync_contracts.py --check`、`py -3 tools/validate_data.py`、`py -3 tools/test_data_loader_schema.py`、`py -3 tools/lint_gdscript_rules.py`、`py -3 tools/lint_project_rules.py`、`py -3 tools/test_project_rules_lint.py`、`py -3 tools/lint_semantic_rules.py`、`py -3 tools/test_semantic_rules_lint.py`、`py -3 tools/godot_bridge.py --project client l1-smoke`、`py -3 tools/godot_bridge.py --project client headless-boot` 与 `git diff --check -- . ":(exclude)draft/**" ":(exclude)DRAFT/**"` 均通过；L1 smoke 已覆盖临时 mod 的 manifest、动态 `content_tags` / `locale_prefixes`、JSON append、CSV append 与合并后 schema 校验。`pre-commit run --all-files` 因本机 hook 调用 `python` 返回 Windows `9009`，未实际执行；已用等价 `py -3` 脚本补齐。semantic lint 仍为既有 19 条 advisory，未指向本次新增 `ModLoader` / `DataLoader`。
- PlatformServices 首片中途验证：`py -3 tools/lint_gdscript_rules.py` 通过；`py -3 tools/lint_semantic_rules.py` 仍为既有 19 条 advisory，未指向本次新增 `PlatformServices`；`py -3 tools/godot_bridge.py --project client headless-boot` 通过并输出 `platform_provider=none platform_available=false`；`py -3 tools/godot_bridge.py --project client l1-smoke` 通过，覆盖 Steam 优先预留、空后端安全退化、成就 / 富状态 / overlay / Lobby 请求记录。
- Godot 4.7 迁移验证：使用临时下载的官方 `Godot_v4.7-stable_win64.exe`，`godot-version` 输出 `4.7.stable.official.5b4e0cb0f`；`headless-boot`、`l1-smoke`、`runtime-smoke`、`settings-smoke`、`save-smoke`、`meta-smoke`、`replay-smoke`、`replay-input-smoke`、四条 checked-in golden replay 的 `replay-runner --rerun-runtime-summary` 和 `perf-probe` 均通过。runtime / settings / save smoke 中的坏 JSON / 非法配置日志仍是既有恢复探针；本机 Steam 默认 Godot 仍是 4.6.3，后续本机 / CI 需指向 4.7。
- Godot 4.7 迁移收尾验证：`py -3 -m json.tool docs/AI记忆/current_state.json`、`py -3 -m json.tool docs/_kb_index.json`、`py -3 tools/docs_health_check.py`、`py -3 tools/sync_contracts.py --check`、`py -3 tools/validate_data.py`、`py -3 tools/test_data_loader_schema.py`、`py -3 tools/lint_gdscript_rules.py`、`py -3 tools/lint_project_rules.py`、`py -3 tools/test_project_rules_lint.py`、`py -3 tools/lint_semantic_rules.py`、`py -3 tools/test_semantic_rules_lint.py`、`git diff --check -- . ":(exclude)draft/**" ":(exclude)DRAFT/**"` 均通过；临时将 Python 3.12 目录加入 `PATH` 后，`pre-commit run --all-files` 也通过。semantic lint 仍为既有 19 条 advisory。
- 活跃链接修正后复验：`py -3 tools/validate_data.py`、`py -3 -m json.tool client/data/credits.json` 通过；四条 checked-in golden replay 在 Godot 4.7 下重新执行 `replay-runner --rerun-runtime-summary` 均通过，确认 style guide URL 数据更新没有改变 replay fingerprint 或 gameplay summary。
- RNG 相关性中途验证：`py -3 tools/lint_gdscript_rules.py` 通过；`py -3 tools/godot_bridge.py --godot %TEMP%/godot-4.7-stable/Godot_v4.7-stable_win64.exe --project client rng-audit` 通过，报告 `max_abs_correlation=0.02290`，低于阈值 0.06；四条 checked-in replay 的 `replay-runner --replay-file ... --rerun-runtime-summary` 均通过，确认 SHA-256 seed mixer 没有改变当前 golden runtime summary。
- 落地正式项目 DebugTools / GM 指令首片：新增 debug/dev_tools 专用 `DebugConsole` 和 `GMCommandRegistry`，由 `FormalClientBoot` 在 runtime guard 通过后动态加载；命令覆盖 `help`、`stats`、`spawn`、`xp`、`heal`、`hp`、`damage`、`kill_player`、`kill_enemies`、`clear_enemies`、`meta`、`seed`，并通过 `GameplayRunLoop.debug_*`、`Player.debug_*`、`MetaProgressionSystem.debug_grant_currency()`、`Combat` 和 `PoolManager` 等正式 API 生效；新增 `client/tools/debug_tools_smoke.gd` 与 `godot_bridge.py` 的 `debug-tools-smoke` / `debug-tools-release-smoke`。
- 推进 F9.0 内容扩展 / Demo 打磨盘点：在 `docs/AI协作/工作包/F9-ContentDemoPolish.md` 补充当前可复用 runtime / 数据内容、暂不适合作为首片的内容类型、F9.1 推荐小内容包和 Demo 手动 checklist 首版；同步 `docs/TODO.md`、`docs/AI记忆/current_state.json` 与项目记忆，把下一步切到 F9.1 数据优先小内容首片。本轮实际读取热路径、F9 工作包、上下文预算、文档维护指南、正式项目 F9 段、测试策略 L3/L4/L5 与 §7、Gameplay Runtime 文档、数据 / locale 手册、当前内容数据、词表相关段和 TODO；未读取或触碰 `draft/` / `DRAFT/`。
- 推进 F9.1 小内容首片：新增 `enemy_bulwark` 慢速高血量压迫敌人（复用 `enemy_chaser` pool/runtime）、`wave_standard_mid_bulwarks` 55 秒后中段刷怪波次、`growth_move_speed_small` / `growth_max_hp_small` 两个三级 `stat_modifier` 升级候选，以及对应 `zh_CN` / `en` 文案。同步 F9 工作包、TODO、项目记忆和 `current_state.json`；四条 checked-in golden replay 仅更新 data fingerprint 到 `b4138b1f767764bbafd3ef56d731e329567435c076b7a75dec498e15802dd302`，运行时摘要保持通过。
- 推进 F9.2 自动化 Demo 前置探针：新增 `client/tools/f9_demo_smoke.gd`、`tools/godot_bridge.py f9-demo-smoke` 命令和 `FormalClientBoot` 启动分支；探针 headless 跑正式 runtime，确认 55 秒后新中段敌人、存档 roundtrip、三级候选加载和死亡结算链路，为后续人工手感试玩提供硬路径护栏。同步 F9 工作包、TODO、项目记忆和 `current_state.json`；工作区中既有 `DRAFT/暂存.md` 与 Godot 自动生成的 `.translation` / `.import` 改动未触碰。
- 推进 F9.2 / F9.3：记录用户 F9.2 手动试玩通过，保持 F9.1 数值不变；启动 F9.3 Demo 表现占位规范首片，在 `Enemy._draw()` 为所有敌人统一增加暗色轮廓和眼睛描边，仍由 `enemies.csv.visual_color` 提供填充色，不按敌人 id 写分支，也不接入正式美术资源。同步 `docs/代码/gameplay_runtime.md`、F9 工作包、TODO、项目记忆和 `current_state.json`。
- 推进 F9.3 音频 cue 计划：补 `docs/代码/audio_manager.md` 的 Demo cue 矩阵和接入顺序，补 F9 工作包、TODO、项目记忆与 `current_state.json`；本轮仅规划 id 和边界，不新增音频资源、不注册 stream、不接入 gameplay 播放代码，不触碰 `docs/词表与契约.md` 前缀契约。
- 推进 F9.3 无音频资源视觉反馈切片：用户确认当前没有临时音频资源，因此不接 `AudioManager` 注册 / 播放，改为先统一核心实体占位可读性；`Bullet._draw()` 绘制黄色圆点 + 暗色轮廓，`PickupOrb._draw()` 绘制绿色圆点 + 暗色轮廓并保留吸附短弧与收集放大淡出。同步 Gameplay Runtime 文档、F9 工作包、TODO、项目记忆和 `current_state.json`，下一步继续升级 HUD / 玩家受伤 / 敌人命中死亡视觉反馈一致性。
- 推进 F9.3 反馈颜色与时长一致性切片：`Player._draw()` 增加暗色轮廓并将受伤反馈改为 0.16 秒红闪；`Enemy._enemy_color()` 将命中反馈明确为 0.16 秒暖白闪，死亡反馈为 0.18 秒橙色放大淡出；`GameplayHud.show_upgrade_feedback()` 的升级获得提示改为金色文字、暗色阴影和 1.35 秒淡出。同步 Gameplay Runtime 文档、F9 工作包、TODO、项目记忆和 `current_state.json`；下一步可继续命中火花 / 伤害数字池，或等临时音频资源可用后回到 `AudioManager` 注册 / smoke。
- 推进 F9.3 命中火花 / 伤害数字池切片：新增 `client/scenes/gameplay/hit_spark.tscn`、`damage_number.tscn` 与对应脚本，`GameplayRunLoop` 注册 / 预热 / 清理 `hit_spark` 和 `damage_number` 池，并监听 `Combat.damage_applied`，仅在伤害实际应用时生成短命火花和漂浮伤害数字；反馈节点不进入 run 快照。`runtime_smoke.gd` 增加池 acquire 断言，同步 Gameplay Runtime 文档、F9 工作包、TODO、项目记忆和 `current_state.json`。
- 记录 F9.3 命中反馈人工试玩结论：用户观察后确认飘字密度 / 遮挡没有问题，因此不调火花 / 伤害数字数值；同步 F9 工作包、TODO、项目记忆和 `current_state.json`，把下一步推进到 F9 收束清单 / F9.4 回归守门复核。
- 完成 F9.4 自动回归与性能守门审计：基础门禁、核心 smoke、四条 checked-in golden replay runtime rerun 和 `perf-probe` 均通过；本轮不重录 replay、不更新期望摘要，下一步转入 F9.5 完整手动 Demo checklist / 下一小内容选择。
- 记录 F9.5 完整 Demo 手动 checklist 通过与 F9 第一轮收口：用户确认“没什么问题，已经可以收口”，因此不继续默认追加第二个小内容包；下一步转为中型系统决策，候选包括武器选择 / 第二把武器可见获取路径、角色选择、遗物 runtime、hazard runtime 或音频资源接入。
- DebugTools 验证：`py tools/lint_gdscript_rules.py`、`py tools/lint_project_rules.py`、`py tools/godot_bridge.py --project client debug-tools-smoke` 和 `py tools/godot_bridge.py --project client debug-tools-release-smoke` 均通过。首次并行跑两个 debug smoke 时因共享 `user://saves/slot_0/meta.save` 出现 SaveManager 文件替换竞争日志，顺序重跑后干净通过；本机默认 `python` 是 Windows Store stub，验证使用 `py`。
- DebugTools 最终复验：`py -m json.tool docs/AI记忆/current_state.json`、`py -m json.tool docs/_kb_index.json`、`py tools/lint_semantic_rules.py`、`py tools/test_semantic_rules_lint.py`、`py tools/docs_health_check.py`、`py tools/sync_contracts.py --check`、`py tools/validate_data.py`、`headless-boot`、`debug-tools-smoke`、`debug-tools-release-smoke`、`runtime-smoke`、`meta-smoke` 均通过；semantic lint 仍为既有 19 条 advisory，runtime-smoke 中坏 run 存档日志仍是恢复探针。
