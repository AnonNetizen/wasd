# 2026-07-22 会话日志

- 用户批准将 Phantom Camera 用于正式玩家摄像机，并明确开始执行安装、跟随、受伤震屏、Settings、Credits、ADR、验证与提交计划。
- 官方发布包 `Phantom Camera 0.11.0.3` 以 SHA-256 `42B4BBD7CE6EFA726ABC6E927328D349887C9343FE53900A5083860C1848749B` 固定；仅入库 `addons/phantom_camera/`，排除 `examples/`，保留 Marcus Skov 的 MIT 许可与署名。
- 新增 ADR #148。Phantom Camera 作为项目维护型 fork：补齐强类型与成员顺序，适配 Godot 4.7.1 `Object.is_class(StringName)` 签名，移除上游 `Engine.physics_jitter_fix=0` 覆写，修正 `stop(false)` 立即停止，禁用编辑器插件自动改 autoload / 重启编辑器。项目显式保持 `physics_jitter_fix=0.5`、固定注册 `PhantomCameraManager`，并关闭插件 updater。
- 玩家场景仍遵守 Camera2D 属于 Player 子树的项目红线：新增 `GameplayCameraController` 子场景，内含 `CenteredCamera + PhantomCameraHost`、优先级 10 的 `PlayerCamera` GLUED PCam 和 `PlayerDamageShake` noise emitter。相机保持原有严格居中、水平、等比 `Vector2.ONE`、无 smoothing / damping / lookahead / dead zone / auto zoom / load tween。
- 新增 `camera_feedback.json` 和 Godot / Python 双端 schema，玩家有效受伤从 `Combat.damage_applied` 触发位移震屏；敌人受伤和玩家无敌拦截不震屏。噪声 seed 走新的 `RNG.camera_fx` 纯表现子流，不偏移 spawn / drop / combat 序列。
- `gameplay.screen_shake` 从隐藏预留设置改为可见并即时生效：关闭会 `stop(false)` 当前发射、归零 Camera2D offset，并抑制后续玩家受伤震屏。
- Credits、数据手册、客户端 / addon 说明、GDD v1.24、Gameplay Runtime 模块文档、测试策略、AI 导航与三层记忆按同一口径同步。Phantom Camera 保守记录 `included_in_build=true` / `requires_notice=true`，源码树保留其 `LICENSE`。
- 定向 `runtime-smoke` 覆盖管理器 / host / PCam 激活、移动跟随、严格相机参数、敌人伤害不震、有效 / 致死玩家伤害震动、无敌拦截不震、关闭设置即停与抑制震动；`settings-smoke` 覆盖可见开关和 Settings 写入。
- 事实型 review 发现全新 clone 尚无 `.godot/global_script_class_cache.cfg` 时，Godot 会先于全局类型扫描解析 Phantom Camera 的交叉类型；`headless-boot` 因此收紧为 `--headless --editor` 预扫描后再正式启动，并把脚本解析错误标记转成非零失败。排除 `.godot` 的临时正式项目副本已验证通过，临时目录已清理。
- 完整 `pre-commit run --all-files` 通过；`headless-boot`、`l1-smoke`、`runtime-smoke`、`settings-smoke`、`save-smoke`、`f9-demo-smoke` 与 7 子流 `rng-audit` 通过。新增数据 / 契约使 replay data fingerprint 更新为 `00b4467330f41b1fed1b99b00a77c38d6e8672ce35a4af56da3d4281b8bbb113`，四条 golden replay 已重录并逐条通过真实运行时摘要对照。官方 zip SHA-256 与文档一致，目标缺失项仅为排除的 62 个 `examples/` 文件，额外项仅为项目 seed helper 及其 UID。
- Godot 编辑器扫描和插件加载无脚本 / 解析错误；保留一条既有 `spawn_waves.end.translation` / `spawn_waves.en.translation` UID 重复警告。震屏幅度对真实操作瞄准可读性的体感仍属于人工试玩验收。
- 本次按 ADR #143 不运行 `startup-probe`、`perf-probe` 或同类性能测试。
