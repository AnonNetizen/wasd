# 2026-07-29 会话日志

## ADR #171 突击枪手点射与常见敌人调整

- 用户要求现有远程怪更贴近弹幕地狱，并把它改成最常见的基础怪。内部 `enemy_spitter`、`enemy_ai_ranged_spitter`、专属对象池和场景路径保持稳定，玩家可见名称改为“突击枪手 / Assault Gunner”。
- `enemy_ai_profiles.json` 升 schema v5。突击枪手在 600 px 射程、280 px 保持距离内，经过 0.65 秒首次攻击延迟后，只在起手视线合法时锁定玩家当时方向并停止移动；显示 0.32 秒直线预警，再以 0.12 秒间隔沿固定方向点射 4 发，整轮完成后冷却 0.95 秒。每弹 18 中性伤害、440 px/s、720 px 射程、5 px 半径、1.8 秒寿命；提交后不追踪、不因失去视线中断，墙体继续挡弹，难度只缩放每弹伤害。
- `Enemy` 新增 `ranged_windup` / `ranged_burst` 状态和 `burst_shots_remaining` 诊断。每轮只发一次 `attack_windup_started`，每弹发一次 `attack_committed`。Run 保持 v8，只增量保存剩余弹数；旧 v8 缺字段按空闲恢复，非法 action、阶段、弹数、方向或计时快照清空攻击并进入一次完整冷却。
- 新增 `presentation_enemy_rifle`，复用直线预警和枪口闪光；`enemy_spitter.tscn` 增加枪管轮廓。共享 `bullet_basic` 根据 `source_team` 切换玩家黄弹与敌方红弹 / 拖尾，并在配置、恢复、释放和复用时完整重置；子弹预热由 24 升至 64，容量保持 192。
- 模式与模块世界完整敌池统一为突击枪手 100、爆猎者 55、群袭者 30、壁垒者 20、伏击者 15，突击枪手从 0 秒解锁并占 100/220（约 45.5%），不设每房保底。开放战区的枪手 wave 从 0 秒起进入所有导演阶段，`spawn_interval=1.35s`、`max_alive=16`；其他波次下调后完整生成率由约 2.0745/s 调为约 2.0732/s，枪手上限占 16/37（约 43.2%）。
- 敌人预热重分配为突击枪手 12、爆猎者 6、群袭者 4、伏击者 3、壁垒者 3，总数仍为 28。突击枪手金币由 4 降至 3，“弹道稳定 Mod”单体掉率由 1% 降至 0.2%。
- 新增 ADR #171，并同步 GDD v1.41、F12 / F14 工作包、Data / Locale 手册、EnemyAI / Gameplay Runtime / PoolManager 文档、测试策略、AI 导航、知识库双索引和三层 AI 记忆；历史 ADR #126 / #170 与旧会话日志保留原文。
- 自动验证通过 contracts、data/schema、三档 lint、VFX bake、actor / VFX / runtime / L1 / Gear Mod / save / loading / 完整与技术模块世界 / replay / replay-input smoke、场景树导出、Godot 4.7.1 headless editor/boot、文档 / JSON 健康、`git diff --check` 和完整 pre-commit。四条 Replay v3 黄金回放按最终数据指纹重录，并串行以运行时摘要重跑通过。本任务按 ADR #143 不运行 startup/performance probe 或 Profiler。
- 事实型 review 发现并修复两处 P2：一是导演前两阶段未启用已改为 0 秒的枪手 wave，导致开放战区实际仍延迟到 240 秒；二是 `ranged_burst` 非法快照可携带完整 4 发或负计时恢复，现均清空并冷却，同时补充损坏 action 的冷却兜底。无剩余 P0 / P1 / P2 发现。
- 1920×1080 多枪手交叉枪线、预警可读性、红色敌弹、静域屏障、墙角、暂停 / 续局和中英文名称仍保留为人工视觉 / 手感验收项。

## ADR #172 突击枪手局部蓄光与慢速敌弹

- 用户取消突击枪手的提前直线弹道，但保留 0.32 秒锁向停步前摇；`presentation_enemy_rifle` 改在 `muzzle` 挂点复用 `weapon_muzzle_flash_default`，以 `#ff291fff` tint 和 0.35 缩放显示局部红色蓄光。远程 windup context 不再提供轨迹 `scale`，每轮一次 windup 与每发一次 impact cue 保持。
- 敌弹速度由 440 降至 280 px/s，寿命由 1.8 延长至 2.6 秒，继续由 720 px 最大射程先行回收；伤害、半径、四发点射、发射间隔、冷却、视线、锁向、墙体、难度、出现率与经济不变。EnemyAI schema v5、Run v8 和 Replay v3 均不升级。
- 自动验证通过 contracts、data/schema、三档 lint及其回归、actor / VFX / runtime / L1 / save / loading / 完整与技术模块世界 / replay / replay-input / UIManager / settings smoke、场景树导出、Godot 4.7.1 headless editor/boot、文档 / JSON 健康、`git diff --check` 和完整 pre-commit。四条 Replay v3 黄金回放按最终数据重录，并串行以运行时摘要重跑通过。
- 事实型 review 发现并修复一处 P2 越界：相邻 `presentation_enemy_charge` 曾被误改为枪口蓄光；现已恢复专用地面冲撞预警并增加 VFX 回归断言。无剩余 P0 / P1 / P2 发现。
- 本任务按 ADR #143 不运行性能 probe；1920×1080 多枪手枪口蓄光、无提前弹道线、慢速红弹、静域屏障、墙角、暂停 / 续局及中英文名称仍保留为人工视觉 / 手感验收项。

## ADR #173 世界事件系统与五类交互模块

- 新增 `world_events.json` schema v1、`RNG.world_event` 和世界事件 id / 类型 / 状态 / 奖励 / Mod 池 / 防御伤害目标组契约；落地防御、生存、占点、金币祭坛、血量祭坛五类交互规则、双语 HUD、固定波次计划、隐藏完成奖励和原子血量献祭。
- 模块 JSON 升 schema v4，增加严格 `{world_event_id}` placement 与 `module_role_world_event`；模块世界升 schema v3，每层从五种 approved 纯平原事件模板中等权无放回选三种、各一次，其余非固定槽由 `module_flat_ground` 填充。模块编辑器、校验、烘焙场景和默认生成池同步完成。
- `WorldEventController` 独立管理持续事件全局互斥、祭坛并行、120 秒占点超时、后台模块 pin、事件敌人归属、奖励与事务幂等。持续事件结束后残敌转为玩家目标，离开事件模块或死亡后才解除后台 pin；普通首次进入遭遇不在事件模块叠加生成。
- 防御事件敌人通过显式 spawn context 锁定 1200 HP 防御目标，近战、冲撞、远程和爆炸均可伤害目标，同时仍可命中路径上的玩家；普通环境敌人继续只攻击玩家。共享子弹按最近扫掠交点选择伤害目标，池复用不泄漏事件上下文。
- Run 升 v9，保存事件状态、pin、目标生命、计时、占点双进度、固定波次计划、事件敌人归属、隐藏奖励和祭坛事务；旧 Run v8 明确不兼容并只清理 run，Meta v2 保留，Replay 继续使用 v3。
- 自动验证通过 contracts、data/schema、三档 lint、world-event / actor / VFX / runtime / L1 / Gear Mod / save / loading / 完整与技术模块世界 / replay smoke、模块烘焙 check / smoke、场景树导出、Godot 4.7.1 headless editor/boot、文档 / JSON 健康、`git diff --check` 和完整 pre-commit。四条 Replay v3 黄金回放按最终数据指纹重录，并以运行时摘要逐条重跑通过。
- 事实型 review 发现并修复一处 P2：事件结束切换残敌为玩家目标时曾清空已提交攻击，导致武装爆猎者立即空爆、突击枪手中断剩余点射；现只切换目标与伤害组，保留武装 / 前摇 / 释放 / 点射状态和计时，并增加 runtime 回归。无剩余 P0 / P1 / P2 发现。
- 本任务按 ADR #143 不运行性能 probe。用户明确要求不由 AI 执行人工检查；1920×1080 五种轮廓、灰度 / 色弱、弹幕遮挡、后台跨模块、暂停 / 续局与中英文即时切换由用户验收。

## ADR #174 人工检查专属边界

- 用户要求把“写明了人工检查的地方 AI 都不要检查，让人工检查”固化为项目规则，避免 AI 通过 GUI 自动化、截图自审或模拟输入越权替代人工验收。
- 任务说明、计划、ADR、GDD、工作包、测试策略、checklist 或验收项中明确标为人工检查 / 人工验收 / 手动验收 / L5 / 真实设备 / 视觉、听觉或手感验收的项目，统一只由人工执行；AI 不执行、不替代、不自行宣称通过或失败。
- AI 仍必须完成同一任务中的非人工自动化验证；人工项只交付前置条件、步骤、预期现象、已知风险和“待人工验收”状态。人工回传结果后，AI 可整理、分析和修复，但结论归属于人工，修复后仍交回人工复验。
- 普通“手动运行命令”不自动等同于人工验收，避免影响既有脚本、smoke、headless、lint、schema 和 Replay 验证责任。
- 规则同步至通用入口、四平台规则 / 入口、快速开工、AI 协作 README、工具适配指南、测试策略、AI 导航、知识库索引和三层 AI 记忆。
- 自动验证通过四平台完整规则哈希对照、`current_state.json` / `_kb_index.json` 解析、文档健康、`git diff --check` 和完整 pre-commit；未执行任何人工、GUI、截图、录屏、模拟输入、真实设备、视觉、听觉或手感验收。
