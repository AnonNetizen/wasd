# 2026-06-26 会话日志

> 本文件记录当天 AI 协作的关键变更摘要；长期索引见 `docs/AI记忆/项目记忆.md`，机器可读当前状态见 `docs/AI记忆/current_state.json`。

## 摘要

- 按 `AGENTS.md` 完成 Codex 快速开工，读取快速开工、current_state、Codex 编码规则、AI 导航与玩法设计相关工作包 / 模块文档。
- 用户围绕“关卡到底做成什么样”“局内成长意味着什么”“一局时长”连续讨论后，确认默认模式先不做 25 分钟长局，改为参考暗黑类短循环。
- 新增 ADR #120：默认标准模式改为暗黑式 8-12 分钟短刷图循环，局内经验升级 3 选 1 暂不在默认模式启用；GrowthSystem / `growth.csv` / `growth_pools.json` / `LevelUpPanel` 保留为未来非默认模式能力。
- 新增 `docs/AI协作/工作包/F12-ShortLootRuns.md`：记录默认循环目标、时间盒、POI / 小巢核 / 精英 / 宝箱结构、Gear Mod 掉落主轴、默认成长屏蔽边界、实现顺序和验收命令。
- `mode_standard_survival` 移除 `resource_pools.growth_pools`；`GameplayRunLoop` 在当前模式没有 growth pool 时不生成 XP pickup、不触发升级、不进入 `LEVEL_UP`，并在 debug summary 暴露 `level_up_growth_enabled=false`。
- `runtime-smoke` 与 `f9-demo-smoke` 从默认验证升级 3 选 1 改为验证默认模式无局内升级；DataLoader schema 坏样例辅助函数改为在需要时主动注入测试用 `growth_pools`。
- 为保留升级 3 选 1 的自动回归，`GameplayRunLoop` 新增 `debug_enable_level_up_growth()`，`golden_replay_capture.gd` / `replay_runner.gd` 只在 `golden_level_up_choice` 场景显式调用；默认标准模式仍由 `game_modes.json` 无 `growth_pools` 决定关闭升级。
- 重录 `client/tests/replays/golden_level_up_choice.replay`，让该 golden 继续验证 `LevelUpPanel`、`Replay.record_decision(level_up, ...)` 和选择后修正应用。
- 同步 GDD、数据手册、Gameplay Runtime / Replay 模块文档、F8 工作包、测试策略、CI/CD 规划、AI 导航、AI 知识库索引、机器索引、AI 协作 README、项目记忆和 current_state。

## 验证

- 已通过：`python tools/sync_contracts.py --check`、`python tools/validate_data.py`、`python tools/test_data_loader_schema.py`、`python tools/lint_gdscript_rules.py`、`python tools/lint_project_rules.py`、`python -m json.tool docs/AI记忆/current_state.json`、`python -m json.tool docs/_kb_index.json`、`python -m json.tool client/data/game_modes.json`、`python tools/docs_health_check.py`、`git diff --check -- . ":(exclude)draft/**" ":(exclude)DRAFT/**"`。
- 已通过：`python tools/lint_semantic_rules.py` 退出 0；仍报告 23 条既有 autoload-bypass-pool-ui advisory，本轮未新增阻塞项。
- 已通过：`python tools/godot_bridge.py --project client headless-boot`、`runtime-smoke`、`f9-demo-smoke`、`gear-mod-smoke`、`save-smoke`；`runtime-smoke` / `save-smoke` 仍输出预期坏档解析 / 隔离诊断但退出 0。
- 已通过：`python tools/godot_bridge.py --project client capture-golden-replay --golden-scenario golden_level_up_choice` 重录升级选择 golden。
- 已通过：四条 checked-in golden replay 顺序 rerun：`golden_basic_run`、`golden_pause_resume`、`golden_full_death`、`golden_level_up_choice`；其中 `golden_level_up_choice` 通过测试 harness 显式启用 growth pool，默认标准模式仍由 runtime / F9 smoke 验证不进 `LEVEL_UP`。
