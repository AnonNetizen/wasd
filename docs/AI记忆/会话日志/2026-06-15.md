# 2026-06-15 会话日志

## 输入
- 用户指出项目 skill 布局混乱：有 `.agents/skills`、各平台目录和 `.opencode/vendor/` 外部 skill / subagent / reference 跳转。
- 用户要求整理为不重复、不冲突、删除对项目无用内容；冲突时以本项目为主；所有 skill 应直接可用，不再通过 reference；vendor 内容应整合进项目后删除 vendor。
- 用户进一步指出当前执行者是 Codex，不能只把能力收进 OpenCode；项目级 skills 应同步到 `.codex/` 和 `.codebuddy/`。
- 用户要求实现 GM 指令、调试工具，并确保这些能力只存在于调试版本，正式导出版没有。

## 产出
- 将 OpenCode skill 加载路径收敛为 `.opencode/skills`，从 `.opencode/opencode.json` 移除 `.agents/skills`。
- 删除 `.agents/skills/game-ai-reference/SKILL.md`、`.opencode/vendor/ai-resources/` 三个外部 submodule、vendor README 与 `.gitmodules`。
- 将外部库中对本项目有用的流程吸收到项目级 skills：
  - `godot-gdscript` 吸收 typed GDScript、scene organization、Resource 注意事项等 GodotPrompter 可用经验。
  - `godot-scene-validation` / `godot-test-diagnostics` 吸收 headless Godot CLI、日志、XDG 与 smoke/test 分离规则。
  - 新增 `playtest-review`，吸收 CCGS 中试玩报告、里程碑复盘和 QA 计划的轻量流程。
- 将 9 个项目级 skills 同步复制到 `.codebuddy/skills/`、`.codex/skills/` 与 `.opencode/skills/`，并把 skill frontmatter 改为通用 `agent-skills` 兼容口径。
- 新增 MVP `scripts/debug_tools.gd`，由 `main.gd` 仅在 `OS.is_debug_build()` 或 `dev_tools` feature 下动态加载；支持 `help`、`stats`、`heal`、`hp`、`damage`、`spawn`、`clear`、`kill`、`spawner on/off`、`reset`。
- `spawner.gd` 新增 `spawn_enemy_now()`，`main.gd` 新增少量 debug API；GM 指令通过现有刷怪、扣血和清怪流程改状态。
- GDD 新增 9.20，要求完整项目 GM / 调试工具只在 debug/dev_tools 构建启用，release preset 不启用 `dev_tools` 且排除调试资源。
- 词表新增 `debug_toggle_console`、`debug_submit_command`、`debug_close_console` action，并重新生成 `_contracts.json` 与 `client/scripts/contracts/actions.gd`。
- 同步 `AGENTS.md`、`CLAUDE.md`、`CODEX.md`、`OPENCODE.md`、三平台规则、README、AI导航、AI协作 README、工具适配指南、AI技能资源评估、知识库索引、机器索引与 AI 记忆。

## 决策
- **ADR #59**：项目级 skills 统一收敛到 `.opencode/skills/` 并删除 vendor/reference 层；外部有用经验必须本地化进项目自有 skill。
- **ADR #60**：项目级 skills 三平台同步安装；CodeBuddy、Codex 与 OpenCode 均读取各自目录下同名 `SKILL.md`，不再跨平台 reference。
- **ADR #61**：GM 指令与调试工具只在 debug/dev_tools 构建启用；release preset 不启用 `dev_tools` 且应排除调试资源。

## 验证
- `python -m json.tool .opencode/opencode.json` 通过（使用 Codex 内置 Python）。
- `python -m json.tool docs/_kb_index.json` 通过（使用 Codex 内置 Python）。
- `python -m json.tool docs/AI记忆/current_state.json` 通过（使用 Codex 内置 Python）。
- `python tools/docs_health_check.py` 通过，输出 `docs health check passed`。
- `python tools/sync_contracts.py --check` 通过，输出 `sync contracts check passed`。
- `python tools/validate_data.py` 通过，输出 `data validation passed`。
- `git submodule status` 输出为空，确认仓库不再登记外部 submodule。
- `git diff --check -- . ":(exclude)draft/**" ":(exclude)DRAFT/**"` 通过。
