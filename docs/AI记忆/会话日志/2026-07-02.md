# 2026-07-02 会话日志

- Steamworks Slime Lab 修复"按鼠标发射不了子弹"：S5 把 UI 挂上 `CanvasLayer` 后 `_ui_root` / 三个页面 / 菜单 `CenterContainer` 第一次真正全屏，而 Control 默认 `MOUSE_FILTER_STOP`，整屏鼠标点击被 UI 层吞掉进不了 `_unhandled_input`（修居中时引入的回归；截图与 headless smoke 都测不到鼠标拦截所以漏网）。修复：`_ui_root` / `_make_page` / `_make_centered_panel` 的全屏容器统一 `MOUSE_FILTER_IGNORE`，实际面板 / 按钮保持默认 STOP。新增 `tests/input_fire_probe.gd`（带窗口注入 `InputEventMouseButton` 实测按下开火 / 持续生成子弹 / 松开停火，全 PASS）；battle_smoke 增加 ui_root / game_page mouse_filter 静态断言防回归；顺手修 smoke 脆弱点——boss / 障碍注入击杀子弹加 `pierce_remaining=999`，避免被路过敌人先消耗（连跑 3 次稳定全 PASS）。

- Steamworks Slime Lab 卷轴射击改造提交前事实型 review：工具面 pre-commit（4 个切片 commit）全过、battle_smoke / net 双进程 smoke / headless-boot 全绿、semantic lint 不适用（output/ 不在 lint 范围）；发现并修复 1 个中危视觉 bug——client 端玩家子弹未 `register_player_bullet`，导致 `client_tick` 视觉消隐空转（子弹穿过敌人不消失）；记录 1 个低危已知限制不修——联机 20s 超时由 host 自动选 buff 时 client 本地记账缺该层，本地开火冷却略保守（弹速不受影响，以 host 广播 speed 为准）；未发现其他正确性问题，改动全部限于 output/ 测试项目、未触碰正式 client/ 与项目红线。
- Steamworks Slime Lab 卷轴射击改造 S5（打磨 / UI 修复 / 文档）：竖屏下旧状态面板缩窄移到血心下方避免重叠；截图验收发现 buff 面板不居中，定位出**原项目遗留 bug**——`_ui_root`（Control）挂在 Node2D 主场景下，anchors 参照 Node2D 的 anchorable rect（0×0），全套页面 UI 其实一直靠子容器 min size 贴左上展开；修复为 UI 挂 `CanvasLayer`（`UiLayer` → `UiRoot` full rect），开始菜单从此真正居中，同时删除实体 move_child 到 UiRoot 前的旧 hack（CanvasLayer 恒在世界层之上）；`buff_panel` / `battle_hud` 统一改用入树后 `set_anchors_and_offsets_preset`。新增 `tests/capture_battle.gd` 截图脚本（开始菜单 / 战斗 / buff 面板三张，沿用 test_lab 经验：不能加 --headless 否则 gl_compatibility 黑图）；README 重写为竖版卷轴射击玩法说明 + 双开测试清单 + headless 回归命令 + 新脚本清单 + 故障提示（含 headless 双实例 `.godot` 缓存锁需项目副本）。验证：battle_smoke / net_host_smoke + net_client_smoke / headless-boot 全绿；三张截图目视验收通过（竖屏布局、血心 / 计时 / Tier / 敌人敌弹 / 缩小史莱姆 / 居中 buff 面板与时停悬停敌机）。

- Steamworks Slime Lab 卷轴射击改造 S4（联机同步）：`network_session` 新增 5 组 RPC（`broadcast_phase` / `send_buff_options` rpc_id 单发 / `send_buff_choice_to_host` / `broadcast_enemy_volley` PackedVector2Array 一次齐射一条 / `broadcast_battle_reset`），并给所有 client→host 发送加 `_client_connection_ready()` 连接状态 gate（修复原有的连接握手期 RPC 报错刷屏）；director 新增 `battle_snapshot()`（phase / tier / time / boss_kills / boss{x,y,hp,max_hp,index} / enemies 扁平数组[id,kind,x,y,hp,max_hp] / obstacles[id,x,y,hp,max_hp,radius]）与 `apply_snapshot_battle()` client 按 id 调和重建（新建 remote 插值实体、屏内消失爆散、障碍 shape_seed=id 两端同形状）、`apply_phase()`（统一 emit `phase_changed`，冻结仅 CHOOSING_BUFF）、`spawn_volley_visual`、`client_tick` 玩家弹视觉消隐；main 侧 host 广播 phase / options / volley / reset，client 收 RPC 转 director、选 buff 本地记账（射速 / 弹速本地冷却生效）再上报，快照带玩家 hp/alive/inv → `apply_snapshot_extras`，`_on_peer_left` 通知 director 防选择死锁。验证：battle_smoke 单机回归全 PASS、headless-boot 无错误；新增 `tests/net_host_smoke.gd` + `tests/net_client_smoke.gd` 双进程 ENet localhost 实测全 PASS（client 连接、玩家镜像、非权威、敌人快照重建、时钟同步；host 峰值 2 玩家、敌人活跃）。注意：Windows 下两个 headless Godot 实例共用同一项目目录会因 `.godot` 缓存锁读不到脚本，client 端需用项目副本跑（已在 README 记录）。

- Steamworks Slime Lab 卷轴射击改造 S3（boss 与障碍物）：新增 `boss.gd`（第 n 个 boss hp=60n×(1+0.2·tier)、从上方入场滑到悬停点横摆、瞄准 5 扇 ±12° + 环形 16 颗两种弹幕、血量 <30% enrage 射速 ×0.7、接触 1 心）与 `obstacle.gd`（seed 不规则多边形岩块 + 按损伤显示裂纹、下落 + 正弦漂移、挡玩家弹可打碎、碰玩家扣 1 心不自毁、敌弹不交互）；director 每 300s 生成 boss、boss 存活期普通刷怪间隔 ×2、障碍 8-14s 随机最多 5 个；玩家弹判伤扩展为敌人 → boss → 障碍（boss / 障碍命中即消耗不穿透），boss 击杀大爆散 + boss_kills 计数进 HUD 血条 / 结算。验证：battle_smoke 追加 boss 准时生成、障碍生成、子弹注入真实判伤路径击杀 boss / 打碎障碍，全 PASS。

- Steamworks Slime Lab 卷轴射击改造 S2（30 秒三选一 buff 与时停状态机）：`battle_director` 拆分 `_battle_tick` / `_choice_tick`，battle_clock 跨 30s×k 进入 CHOOSING_BUFF——tier+1、全实体 `set_battle_frozen`（`set_physics_process(false)` 显式冻结，不用 `get_tree().paused`）、为每个存活玩家 roll 3 个互异 buff（满血剔除回血）、`phase_changed` / `buff_options_ready` signal 通知 main；`submit_buff_choice` 校验 pending / 下标后记账生效，pending 清空恢复 BATTLE；联机 20s 超时自动随机选（单机不催），`notify_peer_left` 剔除防卡死。新增 `buff_panel.gd`（全屏遮罩 + 竖排三卡片 + 等待态 + 倒计时）；main 冻结期输入清零（host 全员 / offline 本地）、`set_player_bullets_frozen` 冻玩家弹、面板选择回调单机直连 director。7 种可叠 buff：射速 / 伤害 / 多发 / 移速 / 回血 / 弹速 / 穿透。验证：headless-boot 无 SCRIPT ERROR；battle_smoke 扩展 buff 流程（30s 进 CHOOSING、tier=1、3 选项、单机无超时、时停时钟冻结、选择恢复、buff 记账）全 PASS。
- Steamworks Slime Lab 雷电式竖版卷轴射击改造 S0+S1（竖屏与单机战斗核心）：窗口 / `VIEWPORT_SIZE` 改 540×960、`WORLD_RECT` 改竖版战场、出生点改底部横排、Multiplayer 页改竖排布局、背景改滚动网格 + 双层视差星点；史莱姆整体缩小（base_radius 56→32、core 38→21、max_speed 360→340）并新增 `movement_bounds` 边界 clamp；新增 `battle_director.gd`（权威战斗模拟：刷怪 / 波次 / tier 缩放 / 圆-圆判伤 / buff 记账接口 / GAME_OVER 判定，权威判定统一 `is_authority()`=host 或 offline）、`enemy.gd`（DART 直冲 / GUNNER 悬停瞄准弹 / STRAFER 斜穿垂直弹）、`enemy_bullet.gd`、`burst_effect.gd`、`battle_hud.gd`（血心 / 计时 / Tier / boss 条骨架 / 观战角标 / GameOver 面板）；玩家 3 血 + 1.2s 无敌帧 + 死亡观战半透明；玩家子弹带 owner / damage / pierce / speed 并由 director 判伤（膜锚定期不判伤）；射击链路改为本地 peer_id 瞄准 + 多发扇形 + buff 冷却接口，`network_session` shot 协议加 speed 参数。验证：`headless-boot` 无 SCRIPT ERROR（注意 headless-boot exit code 恒 0，必须 grep 错误）、新增 `tests/battle_smoke.gd` headless smoke 全 PASS（敌人生成、战斗时钟、受击 / 无敌帧、全灭 GAME_OVER、重开复位清场）。
