# 2026-06-22 会话日志

## 产出

- 完成对话初始化：按 `AGENTS.md` 热路径读取 `CODEX.md`、`docs/AI协作/快速开工.md`、`docs/AI记忆/current_state.json`、`.codex/rules/game-coding-rules.md` 与 `docs/AI导航.md`；确认工作区初始干净，F9 第一轮 Demo 已收口，后续任务需用户明确点名。
- 讨论并确立 IP 方向：从“残响档案”“裂隙 / 怪潮 / 余烬”等较抽象或常见包装，收束为 **《破巢者》**（英文暂定 `Nestbreakers`）。核心设定是多英雄主动突入敌方“巢”，在怪潮中夺取遗物、升级构筑并尝试打穿巢核；“巢”泛指敌方核心据点 / 生产源头 / 意志中枢，不限定为虫巢。
- 做了轻量公开检索，未发现“破巢者 / Nestbreakers”作为同类游戏主标题的明显红旗；但英文侧存在若干 `Nestbreaker` / `Nest Breaker` 的任务、活动、物品或社区用法，正式商用前仍需做 USPTO、CNIPA、WIPO、EUIPO、Steam / App Store / 域名 / 社媒等正式商标和商店名清检。
- 将《破巢者》IP 写入 GDD §1.1 / §1.2，明确核心句、世界规则、失败推进、多英雄动机和主题关键词，并把核心循环改为“选择英雄 → 突入敌巢 → ... → 打穿巢核 / 死亡 → 结算”。
- 新增 ADR #88，记录《破巢者》IP 决策与理由；同步 `docs/术语表.md` 的 `破巢者` / `巢` / `巢核` 检索词、`docs/AI导航.md` 项目概述、`docs/AI知识库索引.md` ADR 矩阵、`docs/_kb_index.json` 关键词、`docs/AI记忆/项目记忆.md` 和 `docs/AI记忆/current_state.json`。
- 按用户要求将 IP 细节从 GDD 概述中独立出来：新增 `docs/IP设定.md` 作为《破巢者》IP / 世界观 / 英雄与敌方势力包装 / 遗物命名 / 局外成长包装 / 宣发语 / 商标注意的权威入口；GDD §1.2 保留短摘要并指向该文档。同步术语表、ADR #88、AI 导航、知识库索引、机器索引、项目记忆和 `current_state.json`。
- 按用户新方向调整世界观历史：未知原因导致其他宇宙与本宇宙的通道突然打开，原本拥有银河系星际文明的人类被跨宇宙敌群打散，只剩首都星域和少数核心据点影响较轻；“巢”改为敌方在本宇宙建立或拖拽进来的核心据点、生产源头、通道锚点或意志中枢，英雄深入敌营是因为只有破坏巢核、切断通道或削弱源头才能真正影响敌方。
- 按用户明确点名实现怪物生态 AI 首片：新增 `client/data/enemy_ai_profiles.json`，在 `enemies.csv` 增加 `ai_profile_id` 并加入猎物 / 掠食者 / 领地 tags；新增 `enemy_stalker` 潜猎者和中段刷怪波次，让疾行者可逃离掠食者、潜猎者可狩猎猎物、壁垒可表现领地倾向。
- 扩展词表与生成常量：新增 `tag_enemy_prey`、`tag_enemy_predator`、`tag_enemy_territorial`，新增 `enemy_ai_actions` 契约和 `client/scripts/contracts/enemy_ai_actions.gd`，并让 `DataLoader` / `tools/validate_data.py` / schema regression 校验 AI profile action、生态 tag 与 `enemies.csv.ai_profile_id` 引用。
- 重写 `Enemy` 行为为“感知 + Utility 评分 + FSM + Steering”分工：敌人可感知玩家和 `active_enemies`，按 profile 选择接近、逃跑、环绕、冲锋或守家；怪物互相接触伤害统一走 `Combat`，非玩家击杀不计玩家 kills / XP；run 快照保存 home、当前 action、冲锋状态和最后伤害来源。
- 在 `runtime-smoke` 增加生态断言：生成猎物和掠食者，确认猎物选择 `ai_action_flee_threat`、掠食者选择狩猎目标；并新增 `docs/代码/enemy_ai.md`、ADR #89、GDD / 数据手册 / AI导航 / 测试策略 / 代码模块索引 / AI记忆同步。
- 按用户反馈将 `docs/AI记忆/current_state.json` 从历史流水账收缩为接手导航牌：只保留当前阶段、最近完成、候选下一步、最近热验证、阻塞和注意事项；详细历史继续留在本会话日志，长期事实留在 `项目记忆.md`。
- 按用户要求新增可复用主动技能系统首片：新增 `client/scripts/gameplay/skill_system.gd`，技能定义进入 `client/data/skills.json`，默认角色用 `starting_loadout.skill_ids` 引用 `skill_whirlwind_slash` 并声明 `skill_resources.mana`；`SkillSystem` 管理资源消耗 / 回复、冷却、目标筛选、释放结果和 run 快照。
- 新增技能契约与文案：词表新增 `skill_ids`、`skill_resources`、`skill_targeting`、`skill_effects`，生成对应 GDScript 常量；新增 `skill_` locale 前缀、`tag_skill`、旋风斩中英文名称 / 描述。`game_modes.json` 新增技能资源池，DataLoader / `tools/validate_data.py` 校验技能引用、资源、targeting、effect 和模式池。
- 旋风斩首片行为：`skill_whirlwind_slash` 消耗 25 `mana`，3 秒冷却，使用 `aoe_enemies_around_caster` 选取施法者半径内敌人，并用 `skill_effect_damage` 经 `Combat.apply_damage()` 造成物理 AOE 伤害；当前 `target_enemy` / `target_ally` 作为后续可扩展 targeting primitive 预留。
- 同步长期文档与决策：新增 `docs/代码/skill_system.md`，同步 Gameplay Runtime、DataLoader、GDD §6.1-A、AI 导航、测试策略、知识库索引 / `_kb_index.json`、项目记忆和 `current_state.json`；新增 ADR #90，确认技能定义与英雄解耦、资源池可扩展、targeting/effect primitive 化。
- 按用户反馈修正 UI 按钮高亮：`UIManager` 新增输入设备感知的导航焦点门控，鼠标 / 键盘 / 触摸模式会释放按钮焦点，手柄输入后才给当前 UI 抓默认焦点；标题、暂停、设置和结算菜单改为走 `UIManager.grab_focus_for_navigation()`，避免非手柄操作时常驻按钮高亮。
- 同步 UI 焦点契约文档与 runtime smoke：`docs/代码/ui_manager.md` / `docs/代码/gameplay_runtime.md` 记录新焦点策略，`runtime-smoke` 覆盖鼠标打开面板无焦点、手柄导航后补焦点。
- 按用户要求确立多语言 UI 英文长度基准：新增 ADR #91，规定 UI 布局、按钮宽度、面板、HUD、升级选择和结算默认以英文 `en` 文案长度设计和验收，不能用中文短文本密度作为唯一尺寸依据。
- `settings-smoke` 增加英文按钮类控件适配检查：在英文 locale 下遍历标题、暂停、设置、升级、失败结算和局外成长面板的可见按钮，断言文本测量宽度能放进控件；同步 `client/locale/README.md`、GDD、测试策略、四平台规则、AI导航、知识库索引、项目记忆和 `current_state.json`。
- 按用户要求新增详细数值面板：登记 `input.show_stats_panel` 设置项与 `show_stats_panel` action，默认 Tab；`GameplayHud` 增加按住显示的详细数值面板，展示生命、等级、经验、击杀、计时、武器数值、拾取范围、幸运、技能资源和技能冷却；`GameplayRunLoop` 只在 `PLAYING` 下刷新 HUD 叠层，松开隐藏，期间不暂停、不进入 UI 栈。
- 同步详细数值面板的本地化、设置面板重绑定、`settings-smoke` / `runtime-smoke` 覆盖、Gameplay Runtime / Settings 模块文档、GDD、ADR #92、AI 导航、知识库索引、项目记忆和 `current_state.json`。
- 按用户要求把地图从无限扩展改为有限边界，并新增可人工调参的 PCG 地图系统：新增 `client/data/map_layouts.json`，由 `MapManager` 解释地图尺寸、玩家出生点、安全半径、刷怪边距、PCG 机关规则和人工摆点；玩家移动和刷怪位置受有限 bounds 约束。
- 新增 FEA-12 测试机关 `hazard_fea_12_pulse`：`hazards.csv` 管基础数值，`map_layouts.json` 同时放 PCG 与人工摆点，`Hazard` 通过 `PoolManager` 生成并用 `Combat.apply_damage()` 造成伤害；`f9-demo-smoke` 覆盖 FEA-12 存在、伤害和保存 roundtrip。
- 扩展 run payload schema version 2：新增 `map` / `hazards` 快照，续局可恢复有限地图、PCG / 人工机关 placement 和机关冷却；`SaveManager` v1 -> v2 迁移补齐旧 run payload 的 `map` / `hazards` 结构。
- 修复 SaveManager 高精度浮点 hash 风险：保存前先 JSON stringify / parse 归一化 payload，再基于归一化 payload 计算 `data_hash` 和落盘，避免真实 run 快照中的高精度 bullet 浮点写入后读回 mismatch。
- 同步 ADR #93、GDD、`client/data/README.md`、Gameplay Runtime / DataLoader / SaveManager / MapManager / HazardSystem 模块文档、AI 导航、测试策略、知识库索引 / `_kb_index.json`、项目记忆和 `current_state.json`；待决策 C 收窄为有限地图边界引导 / 兴趣点表现。

## 验证

- `pre-commit run --files <本轮 SkillSystem 改动文件>`：通过。
- `python tools/godot_bridge.py --project client l1-smoke`：通过，覆盖旋风斩 AOE、魔法消耗、Combat 路由和冷却阻断。
- `python tools/godot_bridge.py --project client runtime-smoke`：通过，覆盖默认角色运行时释放旋风斩；其中损坏 run 存档 JSON 报错为烟测刻意覆盖的失败恢复路径。
- `python tools/godot_bridge.py --project client replay-input-smoke`：通过。
- `python tools/validate_data.py`：通过。
- `python tools/test_data_loader_schema.py`：通过。
- `python tools/sync_contracts.py --check`：通过。
- `python tools/lint_gdscript_rules.py`：通过。
- `python tools/lint_project_rules.py`：通过。
- `python tools/test_project_rules_lint.py`：通过。
- `python tools/lint_semantic_rules.py`：退出码 0，保留 21 条既有 `autoload-bypass-pool-ui` advisory，均不属本轮新增阻塞问题。
- `python tools/test_semantic_rules_lint.py`：通过。
- `python -m json.tool docs/AI记忆/current_state.json`：通过。
- `python -m json.tool docs/_kb_index.json`：通过。
- `python tools/docs_health_check.py`：通过。
- `python tools/godot_bridge.py --project client headless-boot`：通过。
- `python tools/godot_bridge.py --project client runtime-smoke`：通过；其中损坏存档 JSON 报错为烟测刻意覆盖的失败恢复路径。
- `python tools/godot_bridge.py --project client save-smoke`：通过。
- `python tools/godot_bridge.py --project client f9-demo-smoke`：通过。
- `python tools/godot_bridge.py --project client perf-probe`：通过，`avg_frame_ms≈6.85`，`budget_status=pass`。
- `python tools/godot_bridge.py --project client replay-input-smoke`：通过。
- `python tools/godot_bridge.py --project client replay-smoke`：通过。
- 重新录制并回放 `golden_basic_run`、`golden_pause_resume`、`golden_full_death`、`golden_level_up_choice`：均通过。
- `git diff --check -- . ":(exclude)draft/**" ":(exclude)DRAFT/**"`：通过。
- `pre-commit run --files <本轮改动且排除 DRAFT/>`：通过。
- `python -m json.tool docs/AI记忆/current_state.json`：通过（瘦身后）。
- `python tools/docs_health_check.py`：通过（瘦身后）。
- `python tools/lint_gdscript_rules.py`：通过（UI 焦点策略改动后）。
- `python tools/lint_semantic_rules.py`：退出码 0，保留 23 条既有动态节点创建 advisory，未发现本轮新增阻塞问题。
- `python tools/docs_health_check.py`：通过（UI 焦点策略文档同步后）。
- `git diff --check -- . ":(exclude)draft/**" ":(exclude)DRAFT/**"`：通过（UI 焦点策略改动后）。
- `python tools/godot_bridge.py --project client settings-smoke`：通过；invalid settings / broken config 日志为烟测刻意覆盖的恢复路径。
- `python tools/godot_bridge.py --project client runtime-smoke`：通过，覆盖鼠标无焦点和手柄补焦点；损坏 run 存档 JSON 报错为烟测刻意覆盖的失败恢复路径。
- `python tools/godot_bridge.py --project client headless-boot`：通过（UI 焦点策略改动后）。
- `python tools/godot_bridge.py --project client settings-smoke`：首次新增英文适配检查后发现局外成长购买 / 锁定按钮英文文案略微超宽；扩大购买按钮最小宽度后重跑通过。
- `python tools/lint_gdscript_rules.py`：通过（英文 UI 长度基准改动后）。
- `python tools/lint_semantic_rules.py`：退出码 0，保留 23 条既有动态节点创建 advisory，未发现本轮新增阻塞问题。
- `python tools/lint_project_rules.py`：通过。
- `python tools/test_project_rules_lint.py`：通过。
- `python tools/docs_health_check.py`：通过。
- `python -m json.tool docs/AI记忆/current_state.json`：通过。
- `python -m json.tool docs/_kb_index.json`：通过。
- `python tools/godot_bridge.py --project client headless-boot`：通过（英文 UI 长度基准改动后）。
- `git diff --check -- . ":(exclude)draft/**" ":(exclude)DRAFT/**"`：通过。
- `python tools/sync_contracts.py --check`：通过（详细数值面板 action / setting 契约同步后）。
- `python tools/validate_data.py`：通过（新增 `ui_stats_*` / `ui_settings_input_show_stats_panel` 文案后）。
- `python tools/lint_gdscript_rules.py`：通过（详细数值面板改动后；首次发现 smoke 直接读 `get_tree().paused`，已改为通过 `GameState` + `GameClock.tick()` 断言不冻结）。
- `python tools/lint_project_rules.py`：通过（详细数值面板改动后）。
- `python tools/test_project_rules_lint.py`：通过（详细数值面板改动后）。
- `python tools/test_data_loader_schema.py`：通过（详细数值面板改动后）。
- `python tools/lint_semantic_rules.py`：退出码 0，保留 26 条动态节点 / UI advisory；新增 HUD 明细行属于已记录的数据驱动重复 UI，不构成本轮阻塞。
- `python tools/test_semantic_rules_lint.py`：通过（详细数值面板改动后）。
- `python tools/docs_health_check.py`：通过（详细数值面板文档同步后）。
- `python -m json.tool docs/AI记忆/current_state.json; python -m json.tool docs/_kb_index.json`：通过。
- `git diff --check -- . ":(exclude)draft/**" ":(exclude)DRAFT/**"`：通过（详细数值面板改动后）。
- `python tools/godot_bridge.py --project client settings-smoke`：通过，覆盖 `show_stats_panel` 默认 Tab、重绑定到 I、恢复输入默认；invalid settings / broken config 日志为烟测刻意覆盖的恢复路径。
- `python tools/godot_bridge.py --project client runtime-smoke`：通过，覆盖按住 `show_stats_panel` 显示详细数值 HUD、保持 `PLAYING` 且 `GameClock` 继续推进、松开隐藏；损坏 run 存档 JSON 报错为烟测刻意覆盖的失败恢复路径。
- `python tools/godot_bridge.py --project client headless-boot`：通过（详细数值面板改动后）。
- `python tools/validate_data.py`：通过（有限地图 / FEA-12 数据接入后）。
- `python tools/test_data_loader_schema.py`：通过，覆盖 `map_layouts.json` 的 mode / hazard 跨文件引用坏样例。
- `python tools/sync_contracts.py --check`：通过（未新增词表白名单）。
- `python tools/lint_gdscript_rules.py`：通过（MapManager / HazardSystem 接入后）。
- `python tools/godot_bridge.py --project client headless-boot`：通过（有限地图 / 机关场景接入后）。
- `python tools/godot_bridge.py --project client save-smoke`：通过，覆盖 run v1 -> v2 map / hazards 迁移和高精度浮点 JSON 归一化 hash。
- `python tools/godot_bridge.py --project client runtime-smoke`：通过，覆盖有限地图 bounds、MapManager、active hazards 启动和继续游戏后机关恢复；损坏 run 存档 JSON 报错为烟测刻意覆盖的失败恢复路径。
- `python tools/godot_bridge.py --project client f9-demo-smoke`：通过，覆盖 FEA-12 机关存在、造成玩家伤害和保存 roundtrip。
- `python tools/godot_bridge.py --project client l1-smoke`：通过（地图 / 机关接入后）。
- `python tools/godot_bridge.py --project client perf-probe`：通过，`budget_status=pass`，active hazards 纳入实体 / pool 统计。
- `python tools/lint_project_rules.py`：最终通过；曾发现 `map_layouts.json` 的 `player_start.y` / `manual_hazards[].y` 字段文档未被逐字段识别，已拆分 README 字段行修正。
- `python tools/lint_semantic_rules.py`：退出码 0，保留 27 条动态节点 / UI advisory；新增 `HAZARD_SCENE.instantiate()` 属于 `PoolManager` 工厂注册路径，`active_world.add_child()` 属于挂回已取出的池节点，本轮 accept。
- `python tools/test_semantic_rules_lint.py`、`python tools/docs_health_check.py`、`python -c json.load(...)` 校验 `current_state.json` / `_kb_index.json`、`git diff --check -- . ":(exclude)draft/**" ":(exclude)DRAFT/**"`：均通过。
- 四条 checked-in golden replay 因新增 `map_layouts.json` / FEA-12 数据导致 `data_fingerprint` 有意变化，已用 `capture-golden-replay` 重录 `golden_basic_run`、`golden_pause_resume`、`golden_full_death`、`golden_level_up_choice`，并逐条 `replay-runner --replay-file ... --rerun-runtime-summary` 验证通过。
- `python tools/godot_bridge.py --project client replay-smoke`、`replay-runner --rerun-runtime-summary`、`replay-input-smoke`、`rng-audit`：均通过；`rng-audit` 最大相关系数 `0.02290`。
