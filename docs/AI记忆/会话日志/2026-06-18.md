# 2026-06-18 会话日志

## F4 最小可玩闭环首切片

- 用户指令：开始施工 F4。
- 读取并遵守 `AGENTS.md` 快速开工、Codex 规则、F4 工作包、GDD 玩家 / 输入 / 武器 / 敌人 / 刷怪 / 对象池 / 伤害相关段、测试策略 §7、代码文档规范和相关 autoload 模块文档。
- 代码产出：新增 `Combat` autoload 与 `DamageInfo`，新增 F4 runtime / Player / WeaponSystem / Bullet / Enemy / HUD 阶段脚本，启动场景在数据校验通过后挂载 F4 runtime。
- 行为产出：玩家可移动并保持相机居中；默认角色起始武器自动开火；子弹和敌人通过 `PoolManager`；敌人按 `spawn_waves.csv` 生成并追击；子弹和接触伤害走 `Combat.apply_damage()`；HUD 显示生命、击杀、时间，死亡后按暂停键重开。
- 提交前修正：键盘输入拆为 WASD 移动、方向键瞄准；子弹记录每发已命中目标，避免未来 `pierce_count > 0` 时在重叠期间重复命中同一敌人。
- 文档产出：新增 `docs/代码/combat.md`、`docs/代码/f4_min_playable_loop.md`，同步 `client/README.md`、`client/locale/README.md`、`docs/代码/README.md`、`docs/代码/formal_client_boot.md`、`docs/AI导航.md`、`docs/AI知识库索引.md` 与 `docs/_kb_index.json`。
- 验证记录：已通过 `python tools/lint_gdscript_rules.py`、`python tools/validate_data.py`、`python tools/lint_project_rules.py`、`python tools/sync_contracts.py --check`、`python tools/test_data_loader_schema.py`、`python tools/test_project_rules_lint.py`、`python tools/test_semantic_rules_lint.py`、`python -m json.tool docs/AI记忆/current_state.json`、`python -m json.tool docs/_kb_index.json`、`python tools/docs_health_check.py`、`python tools/godot_bridge.py --project client export-tree`、`python tools/godot_bridge.py --project client headless-boot` 与 `git diff --check -- . ":(exclude)draft/**" ":(exclude)DRAFT/**"`；`python tools/lint_semantic_rules.py` 报 11 个 `add_child` / `reparent` advisory，均为 F4 场景 / HUD 组装和 PoolManager 获取后 reparent，接受为当前脚本装配阶段的非阻塞例外，后续正式场景 / UI 模块化时再收敛。

## F4 headless runtime smoke

- 用户指令：继续推进 F4。
- 代码产出：新增 `client/tools/f4_runtime_smoke.gd`，并给 `tools/godot_bridge.py` 增加 `f4-smoke` 命令；`FormalClientBoot` 在收到 `--f4-smoke` 用户参数时挂载 smoke runner。
- 行为覆盖：自动断言 F4RunLoop 挂载、GameState 进入 PLAYING、InputMap 的 WASD / 方向键分离、玩家移动、方向键瞄准不移动、子弹池获取、敌人刷出、Combat 击杀敌人、Combat 击杀玩家并进入 GAME_OVER。
- 文档同步：更新 `client/README.md`、`docs/代码/f4_min_playable_loop.md`、`docs/AI协作/引擎集成.md`、`docs/AI协作/README.md`、`docs/AI导航.md`、`docs/AI记忆/current_state.json` 与项目记忆。

## F4 手动试玩首轮修正

- 用户反馈：看不清角色面朝方向；角色和敌人缺受击反馈；游戏结束后计时仍走；没有背景参照导致移动感弱；击杀三四十个敌人后刷怪停止。
- 代码 / 数据修正：新增 `F4Background` 世界网格背景；玩家绘制瞄准方向指示器；玩家和敌人受击短暂闪白；`GameClock` 在 `GAME_OVER` 冻结；`spawn_waves.csv` 首波延长到 600 秒并提高预算。
- 回归覆盖：`f4-smoke` 追加断言 `F4Background` 挂载和 `GAME_OVER` 后 `GameClock` 冻结。
- 文档同步：更新 `client/data/README.md`、`docs/代码/f4_min_playable_loop.md`、`docs/代码/game_clock.md`、`docs/AI导航.md`、`docs/AI记忆/current_state.json` 与项目记忆。

## F4 接触伤害无敌窗口修正

- 用户反馈：站着不动时同一只敌人不会再次伤害玩家，但新敌人会伤害玩家，说明接触伤害被错误地做成了敌人侧一次性判定。
- 代码 / 数据修正：移除 `F4Enemy` 的 `_dealt_contact`；新增 stat `damage_invulnerability_duration`，默认 `player.json` 为 0.7 秒；`F4Player.receive_damage()` 在无敌期返回 `reason=invulnerable` 且不扣生命，无敌结束后同一来源可再次造成伤害。
- 回归覆盖：`f4-smoke` 追加玩家侧无敌窗口断言：首次接触伤害生效、同一来源在无敌期被挡、无敌结束后同一来源再次生效。
- 文档同步：更新 `docs/词表与契约.md`、`client/data/README.md`、`docs/代码/f4_min_playable_loop.md`、`docs/AI导航.md`、`docs/AI记忆/current_state.json` 与项目记忆。

## F4 敌人中心分离修正

- 用户反馈：敌人与敌人可以像《吸血鬼幸存者》一样视觉重叠，但不应该完全重叠；每个怪中心应有一个小的不可重叠区域。
- 代码 / 数据修正：`enemies.csv` 新增 `separation_radius`；`DataLoader` 与 `tools/validate_data.py` 校验该字段；`F4Enemy` 在移动后按中心半径做小幅排斥，默认追击敌人为 9px，低于 `hit_radius` 以保留视觉重叠。
- 回归覆盖：`f4-smoke` 追加两个敌人在同一点生成后会分离到中心间距阈值附近的断言，继续覆盖玩家侧接触伤害无敌、刷怪、伤害和 GAME_OVER 时间冻结。
- 文档同步：更新 `client/data/README.md`、`docs/代码/f4_min_playable_loop.md`、`docs/游戏设计文档.md`、`docs/AI导航.md`、`docs/AI记忆/current_state.json` 与项目记忆。

## F4 经验与升级三选一首切片

- 用户指令：试玩反馈已无明显问题后继续推进下一步；按优先级进入 F4 经验掉落与升级选择闭环。
- 代码 / 数据修正：新增 `F4PickupOrb` 池化经验球与 `F4LevelUpPanel` 阶段面板；`F4RunLoop` 接入敌人死亡掉经验、拾取累计经验、`growth.csv` 阈值升级、`growth_pools.json` 候选抽取、`GameState.LEVEL_UP` 暂停和 UIManager 挂载面板；`F4Player` / `F4WeaponSystem` 支持运行时 `stat_modifier`，HUD 显示等级与经验。
- 数据 / 文案：新增 stat `pickup_orb_speed` 并同步生成常量；`player.json` 新增经验球吸附速度；`growth_pools.json` 候选补 `name_key` / `desc_key`；`strings.csv` 新增升级面板与候选中英文文案。
- 回归覆盖：`f4-smoke` 追加经验球对象池获取、拾取触发 LEVEL_UP、选择升级后回到 PLAYING 且属性即时生效的断言；`tools/test_data_loader_schema.py` 追加缺升级候选 locale key 的坏样例。
- 文档同步：更新 `client/data/README.md`、`docs/词表与契约.md`、`docs/代码/f4_min_playable_loop.md`、`docs/游戏设计文档.md`、`docs/AI导航.md`、`docs/AI记忆/current_state.json` 与项目记忆。

## F4 标题 / 失败流、升级反馈与第二敌人

- 用户指令：暂缓人工可调的数值调参，优先做“简单开始 / 结束流、升级反馈、更细敌人 / 武器变化”前三项。
- 代码 / 数据修正：新增 `F4TitleMenu` 和 `F4GameOverPanel`，正常启动先显示标题，smoke 模式自动进战斗；失败后可重开或回标题，暂停键仍可快捷重开；升级选择后 HUD 显示获得反馈；`enemies.csv` 新增 `visual_color` 字段和第二敌人 `enemy_swarm`，`spawn_waves.csv` 在 20 秒后混入疾行者。
- 回归覆盖：`f4-smoke` 追加第二敌人池、数据化占位色、升级反馈和 game-over 面板断言。
- 文档同步：更新 `client/README.md`、`client/data/README.md`、`client/locale/README.md`、`docs/代码/formal_client_boot.md`、`docs/代码/f4_min_playable_loop.md`、`docs/AI导航.md` 与 AI 记忆。

## F4 升级按钮暂停点击修正

- 用户反馈：升级界面弹出后按钮无法点击。
- 根因：升级时 `GameState.LEVEL_UP` 会暂停场景树，旧 smoke 直接调用 `choose_index(0)`，没有覆盖真实鼠标点击；升级面板只依赖按钮信号，暂停输入链路下点击没有稳定触发选择。
- 代码修正：`F4LevelUpPanel` 的交互控件显式允许暂停时处理，并在面板自身增加左键释放命中按钮矩形后的选择兜底；选择逻辑加一次性锁，避免按钮信号和兜底路径重复触发。
- 回归覆盖：`f4-smoke` 改为在 `LEVEL_UP` 暂停状态下向第一个升级按钮发送鼠标 press/release，并断言选择后恢复 `PLAYING`、HUD 升级反馈显示、属性即时生效。

## F4 失败面板按钮点击修正

- 用户反馈：结束游戏界面按钮无法点击。
- 根因：失败面板同样是脚本动态组 UI，旧 smoke 只断言面板出现，没有真实点击“重开 / 回标题”；同时重开时旧 `F4RunLoop` 只 `queue_free()`，新运行时同帧加入时可能被旧同名节点残留干扰。
- 代码修正：`F4GameOverPanel` 的交互控件显式允许输入，遮罩改为纯视觉层，面板自身增加按下 / 释放同一按钮才触发的鼠标命中兜底；`FormalClientBoot._clear_f4_runtime()` 在释放旧运行时前先从父节点移除，避免重开时同名节点冲突。
- 回归覆盖：`f4-smoke` 在玩家死亡后真实点击“重开”，断言回到 `PLAYING` 且失败面板关闭；随后再次击杀玩家并真实点击“回标题”，断言回到 `MAIN_MENU` 且标题菜单出现。

## F4 UI 本地化占位符修正

- 用户反馈：UI 显示占位符。
- 根因：`client/locale/strings.csv` 和 `.translation` 文件存在，但 `client/project.godot` 没有在 `[internationalization]` 中注册翻译资源，导致 `tr("ui_*")` 返回原 key。
- 代码修正：在 `client/project.godot` 注册 `strings.zh_CN.translation` 与 `strings.en.translation`。
- 回归覆盖：`f4-smoke` 增加 `tr("ui_start") != "ui_start"` 断言，确认 UI key 能解析为真实文案。

## F4 失败 UI 重叠修正

- 用户反馈：好像有两种结束 UI，重叠了。
- 根因：玩家死亡时 `F4RunLoop` 既调用了 `F4Hud.show_game_over()` 的旧居中失败提示，又通过 `UIManager` 显示新的 `F4GameOverPanel`。
- 代码修正：失败态 UI 职责收口到 `F4GameOverPanel`；`F4RunLoop` 不再触发 HUD 旧失败提示，`F4Hud.show_game_over()` 保持隐藏以防误调用。
- 回归覆盖：`f4-smoke` 在玩家死亡后断言 `F4GameOverPanel` 出现，同时 HUD 旧失败消息保持隐藏。

## F4 玩家中心分离修正

- 用户反馈：人物也应该有一块不可重叠区域；如果这块区域和敌人的不可重叠区域碰到，可以把敌人挤开。
- 代码 / 数据修正：新增 stat `player_separation_radius`，默认 `player.json` / `characters.json` 为 10px；`F4Player` 暴露 `separation_radius()`；`F4Enemy` 继续做敌人-敌人中心排斥，并在碰到玩家分离圈时只推开敌人。
- 回归覆盖：`f4-smoke` 新增玩家-敌人中心分离断言，确认敌人从玩家中心被推开到玩家半径 + 敌人半径附近，并确认推开后仍可造成接触伤害。

## 作者署名记录

- 用户确认作者署名：中文为“伦敦阿农”，英文为 `Anon London`。
- 同步位置：`CREDITS.md`、`client/data/credits.json`、`client/data/README.md` 示例、`docs/AI记忆/项目记忆.md` 与 `docs/AI记忆/current_state.json`。

## F5 暂停 / 保存 / 续局首切片

- 用户确认 F4 当前体验无明显问题后，指示可以开始 F5。
- 代码产出：新增 `F4PauseMenu`，`F4RunLoop` 支持主动暂停、保存并退出、run payload 生成与恢复；`FormalClientBoot` 在标题菜单显示“继续游戏”并加载 `SaveManager` 的 `run` payload；`F4TitleMenu` 增加继续游戏按钮；`RNG` 与 `GameClock` 增加 `snapshot()` / `restore_snapshot()`；玩家、武器、敌人、子弹和经验球补最小快照 / 恢复 API。
- 行为边界：F5 首片只保存 / 恢复当前 F4 局内状态；不做局外成长奖励、完整设置 / 主菜单、run schema 迁移、黄金回放或平衡 sim。RNG seed/state 以字符串保存，避免 JSON 大整数精度变化导致 `SaveManager` 的 `data_hash` 不匹配。
- 回归覆盖：`f4-smoke` 新增暂停 -> 保存并退出 -> 回标题 -> 继续游戏 -> 恢复玩家位置 / 等级 / 经验 / GameClock 的断言，然后继续覆盖死亡、重开和回标题。
- 文档同步：更新 `client/README.md`、`client/locale/README.md`、`docs/代码/f4_min_playable_loop.md`、`docs/代码/formal_client_boot.md`、`docs/代码/save_manager.md`、`docs/代码/rng.md`、`docs/代码/game_clock.md`、`docs/AI导航.md`、`docs/AI记忆/current_state.json` 与项目记忆。
- 验证记录：已通过 `python tools/lint_gdscript_rules.py`、`python tools/validate_data.py`、`python tools/lint_project_rules.py`、`python tools/sync_contracts.py --check`、`python tools/test_data_loader_schema.py`、`python tools/godot_bridge.py --project client headless-boot`、`python tools/godot_bridge.py --project client export-tree`、`python tools/godot_bridge.py --project client f4-smoke`；`python tools/lint_semantic_rules.py` 仍报告 F4/F5 脚本动态组 UI / 节点的非阻塞 advisory，当前按阶段性脚本组装接受。

## F5 SaveManager 存档可靠性切片

- 用户指令：继续推进 F5。
- 评估：F5 首片已能暂停保存和继续，下一步最有价值的是补存档可靠性自动化验证；这能在接入局外成长前降低迁移、备份回退和坏档隔离风险。
- 代码产出：`SaveManager` 的 `run` kind 提升到 version 2，并在 `_ready()` 注册 v1 -> v2 迁移，用于给早期 F5 run payload 补齐缺失的 `schema_version`、状态字典和实体数组；坏档隔离改为用唯一 `.broken` 路径，避免正式档和 `.bak` 同秒损坏时撞名导致第二个坏文件残留。
- 工具产出：新增 `client/tools/save_manager_smoke.gd` 与 `python tools/godot_bridge.py --project client save-smoke`，覆盖 run envelope roundtrip、`.bak` 回退、正式档 + 备份双坏档隔离、v1 -> v2 迁移和对应 signal。
- 文档同步：更新 `README.md`、`client/README.md`、`docs/代码/save_manager.md`、`docs/代码/f4_min_playable_loop.md`、`docs/代码/formal_client_boot.md`、`docs/测试策略.md`、`docs/AI协作/README.md`、`docs/AI协作/引擎集成.md`、`docs/AI导航.md`、`docs/AI知识库索引.md`、ADR #80 与 AI 记忆。
- 验证记录：`python tools/godot_bridge.py --project client save-smoke` 通过，输出 `loaded=4 migrated=1 corrupted=2`；命令会故意破坏测试槽存档，因此日志中出现预期的 JSON parse / SaveManager error 输出，但进程退出码为 0。`python tools/lint_gdscript_rules.py` 通过；`python tools/lint_semantic_rules.py` 仍只报告既有 F4 动态 UI / 节点组装 advisory。

## F5 暂停 / 保存 / 续局手测确认

- 用户反馈：已手动试玩 F5 暂停 / 保存 / 续局流程，未发现问题。
- 状态更新：`docs/AI记忆/current_state.json` 记录该手动验证通过，并把下一步推进重心收口到坏档用户提示与暂停 / 升级 UI 恢复点。

## F5 坏档用户提示切片

- 用户指令：手测无问题后继续推进。
- 代码产出：`FormalClientBoot` 在继续 `run` 存档失败时清理残留 run 文件、回到标题菜单并传入 `ui_run_save_unavailable`；`F4TitleMenu` 新增 `RunSaveNoticeLabel`，用于显示本地化重置提示并隐藏继续按钮。
- 文案 / 测试：`client/locale/strings.csv` 新增中英文坏档提示；`f4-smoke` 新增构造坏 run 存档、点击继续、断言提示显示且继续按钮隐藏的覆盖；`SaveManager.delete()` 对空 slot 目录的清理与文档契约对齐，避免坏档隔离后留下空 slot。
- 文档同步：更新 `README.md`、`client/README.md`、`client/locale/README.md`、`docs/代码/formal_client_boot.md`、`docs/代码/f4_min_playable_loop.md`、`docs/AI导航.md` 与 AI 记忆。
- 验证记录：`sync_contracts --check`、`validate_data`、`test_data_loader_schema`、`lint_gdscript_rules`、`lint_project_rules`、`test_project_rules_lint`、`lint_semantic_rules`、`test_semantic_rules_lint`、`json.tool current_state`、`docs_health_check`、`headless-boot`、`save-smoke`、`f4-smoke` 与 `pre-commit run --all-files` 均通过；坏档 smoke 中的 JSON parse / SaveManager error 日志为故意破坏存档的预期输出。

## F5 暂停 / 升级 UI 恢复点切片

- 用户指令：继续推进。
- 代码产出：`F4RunLoop.create_run_snapshot()` 新增 `ui_restore`，当前支持 `playing`、`paused`、`level_up`；暂停保存后继续游戏会回到暂停菜单，升级选择面板打开时保存会保留已掷出的候选并续回同一组选择，不重新消耗 `RNG.ui_choice`。
- 回归覆盖：`f4-smoke` 新增升级面板保存 -> 回标题 -> 继续 -> 回同一候选组 -> 点击选择，以及暂停保存 -> 继续 -> 先回暂停菜单 -> 点击继续的断言；`save-smoke` 的 run payload 样例补默认 `ui_restore`。
- 文档同步：更新 `README.md`、`client/README.md`、`docs/代码/f4_min_playable_loop.md`、`docs/代码/formal_client_boot.md`、`docs/代码/save_manager.md`、`docs/正式项目工作规划.md`、`docs/TODO.md`、`docs/AI导航.md` 与 AI 记忆。
- 验证记录：`headless-boot`、`f4-smoke`、`save-smoke` 已通过；坏档 / 备份 smoke 中的 JSON parse / SaveManager error 日志为故意破坏存档的预期输出。后续仍需最终 F5 手动存档 checklist，尚未进入 F6 局外成长奖励。
