# 2026-06-18 会话日志

## F4 最小可玩闭环首切片

- 用户指令：开始施工 F4。
- 读取并遵守 `AGENTS.md` 快速开工、Codex 规则、F4 工作包、GDD 玩家 / 输入 / 武器 / 敌人 / 刷怪 / 对象池 / 伤害相关段、测试策略 §7、代码文档规范和相关 autoload 模块文档。
- 代码产出：新增 `Combat` autoload 与 `DamageInfo`，新增 F4 runtime / Player / WeaponSystem / Bullet / Enemy / HUD 阶段脚本，启动场景在数据校验通过后挂载 F4 runtime。
- 行为产出：玩家可移动并保持相机居中；默认角色起始武器自动开火；子弹和敌人通过 `PoolManager`；敌人按 `spawn_waves.csv` 生成并追击；子弹和接触伤害走 `Combat.apply_damage()`；HUD 显示生命、击杀、时间，死亡后按暂停键重开。
- 提交前修正：键盘输入拆为 WASD 移动、方向键瞄准；子弹记录每发已命中目标，避免未来 `pierce_count > 0` 时在重叠期间重复命中同一敌人。
- 文档产出：新增 `docs/代码/combat.md`、`docs/代码/f4_min_playable_loop.md`，同步 `client/README.md`、`client/locale/README.md`、`docs/代码/README.md`、`docs/代码/formal_client_boot.md`、`docs/AI导航.md`、`docs/AI知识库索引.md` 与 `docs/_kb_index.json`。
- 验证记录：已通过 `python tools/lint_gdscript_rules.py`、`python tools/validate_data.py`、`python tools/lint_project_rules.py`、`python tools/sync_contracts.py --check`、`python tools/test_data_loader_schema.py`、`python tools/test_project_rules_lint.py`、`python tools/test_semantic_rules_lint.py`、`python -m json.tool docs/AI记忆/current_state.json`、`python -m json.tool docs/_kb_index.json`、`python tools/docs_health_check.py`、`python tools/godot_bridge.py --project client export-tree`、`python tools/godot_bridge.py --project client headless-boot` 与 `git diff --check -- . ":(exclude)draft/**" ":(exclude)DRAFT/**"`；`python tools/lint_semantic_rules.py` 报 11 个 `add_child` / `reparent` advisory，均为 F4 场景 / HUD 组装和 PoolManager 获取后 reparent，接受为当前脚本装配阶段的非阻塞例外，后续正式场景 / UI 模块化时再收敛。

## F4 headless runtime smoke

- 用户指令：继续推进 F4。
- 代码产出：新增 `client/tools/f4_runtime_smoke.gd`，并给 `tools/godot_bridge.py` 增加 `f4-smoke` 命令；`FormalClientBoot` 在收到 `--f4-smoke` 用户参数时挂载 smoke runner。
- 行为覆盖：自动断言 F4RunLoop 挂载、GameState 进入 PLAYING、InputMap 的 WASD / 方向键分离、玩家移动、方向键瞄准不移动、子弹池获取、敌人刷出、Combat 击杀敌人、Combat 击杀玩家并进入 GAME_OVER。
- 文档同步：更新 `client/README.md`、`docs/代码/f4_min_playable_loop.md`、`docs/AI协作/引擎集成.md`、`docs/AI协作/README.md`、`docs/AI导航.md`、`docs/AI记忆/current_state.json` 与项目记忆。

## F4 手动试玩首轮修正

- 用户反馈：看不清角色面朝方向；角色和敌人缺受击反馈；游戏结束后计时仍走；没有背景参照导致移动感弱；击杀三四十个敌人后刷怪停止。
- 代码 / 数据修正：新增 `F4Background` 世界网格背景；玩家绘制瞄准方向指示器；玩家和敌人受击短暂闪白；`GameClock` 在 `GAME_OVER` 冻结；`spawn_waves.csv` 首波延长到 600 秒并提高预算。
- 回归覆盖：`f4-smoke` 追加断言 `F4Background` 挂载和 `GAME_OVER` 后 `GameClock` 冻结。
- 文档同步：更新 `client/data/README.md`、`docs/代码/f4_min_playable_loop.md`、`docs/代码/game_clock.md`、`docs/AI导航.md`、`docs/AI记忆/current_state.json` 与项目记忆。

## F4 接触伤害无敌窗口修正

- 用户反馈：站着不动时同一只敌人不会再次伤害玩家，但新敌人会伤害玩家，说明接触伤害被错误地做成了敌人侧一次性判定。
- 代码 / 数据修正：移除 `F4Enemy` 的 `_dealt_contact`；新增 stat `damage_invulnerability_duration`，默认 `player.json` 为 0.7 秒；`F4Player.receive_damage()` 在无敌期返回 `reason=invulnerable` 且不扣生命，无敌结束后同一来源可再次造成伤害。
- 回归覆盖：`f4-smoke` 追加玩家侧无敌窗口断言：首次接触伤害生效、同一来源在无敌期被挡、无敌结束后同一来源再次生效。
- 文档同步：更新 `docs/词表与契约.md`、`client/data/README.md`、`docs/代码/f4_min_playable_loop.md`、`docs/AI导航.md`、`docs/AI记忆/current_state.json` 与项目记忆。

## F4 敌人中心分离修正

- 用户反馈：敌人与敌人可以像《吸血鬼幸存者》一样视觉重叠，但不应该完全重叠；每个怪中心应有一个小的不可重叠区域。
- 代码 / 数据修正：`enemies.csv` 新增 `separation_radius`；`DataLoader` 与 `tools/validate_data.py` 校验该字段；`F4Enemy` 在移动后按中心半径做小幅排斥，默认追击敌人为 9px，低于 `hit_radius` 以保留视觉重叠。
- 回归覆盖：`f4-smoke` 追加两个敌人在同一点生成后会分离到中心间距阈值附近的断言，继续覆盖玩家侧接触伤害无敌、刷怪、伤害和 GAME_OVER 时间冻结。
- 文档同步：更新 `client/data/README.md`、`docs/代码/f4_min_playable_loop.md`、`docs/游戏设计文档.md`、`docs/AI导航.md`、`docs/AI记忆/current_state.json` 与项目记忆。

## F4 经验与升级三选一首切片

- 用户指令：试玩反馈已无明显问题后继续推进下一步；按优先级进入 F4 经验掉落与升级选择闭环。
- 代码 / 数据修正：新增 `F4PickupOrb` 池化经验球与 `F4LevelUpPanel` 阶段面板；`F4RunLoop` 接入敌人死亡掉经验、拾取累计经验、`growth.csv` 阈值升级、`growth_pools.json` 候选抽取、`GameState.LEVEL_UP` 暂停和 UIManager 挂载面板；`F4Player` / `F4WeaponSystem` 支持运行时 `stat_modifier`，HUD 显示等级与经验。
- 数据 / 文案：新增 stat `pickup_orb_speed` 并同步生成常量；`player.json` 新增经验球吸附速度；`growth_pools.json` 候选补 `name_key` / `desc_key`；`strings.csv` 新增升级面板与候选中英文文案。
- 回归覆盖：`f4-smoke` 追加经验球对象池获取、拾取触发 LEVEL_UP、选择升级后回到 PLAYING 且属性即时生效的断言；`tools/test_data_loader_schema.py` 追加缺升级候选 locale key 的坏样例。
- 文档同步：更新 `client/data/README.md`、`docs/词表与契约.md`、`docs/代码/f4_min_playable_loop.md`、`docs/游戏设计文档.md`、`docs/AI导航.md`、`docs/AI记忆/current_state.json` 与项目记忆。

## F4 标题 / 失败流、升级反馈与第二敌人

- 用户指令：暂缓人工可调的数值调参，优先做“简单开始 / 结束流、升级反馈、更细敌人 / 武器变化”前三项。
- 代码 / 数据修正：新增 `F4TitleMenu` 和 `F4GameOverPanel`，正常启动先显示标题，smoke 模式自动进战斗；失败后可重开或回标题，暂停键仍可快捷重开；升级选择后 HUD 显示获得反馈；`enemies.csv` 新增 `visual_color` 字段和第二敌人 `enemy_swarm`，`spawn_waves.csv` 在 20 秒后混入疾行者。
- 回归覆盖：`f4-smoke` 追加第二敌人池、数据化占位色、升级反馈和 game-over 面板断言。
- 文档同步：更新 `client/README.md`、`client/data/README.md`、`client/locale/README.md`、`docs/代码/formal_client_boot.md`、`docs/代码/f4_min_playable_loop.md`、`docs/AI导航.md` 与 AI 记忆。
