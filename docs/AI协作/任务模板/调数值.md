# 任务模板：调数值（平衡迭代）

> **AI 修改说明**：修改本任务模板前先读 `docs/AI协作/文档维护指南.md`。模板变化通常需要同步 `docs/AI协作/README.md`、`docs/AI协作/上下文预算.md`、相关 subagent / command（如有）、`AGENTS.md` 的高频任务表（如入口变化）。

## 必读
1. 当前平台编码规则入口（见 `AGENTS.md` 第 3 步）第 3 节（数据与逻辑分离）
2. 目标 `client/data/*.json` 的字段说明文档
3. （改后）`tests/replays/golden_*.replay` 的存在情况

## 核心约束
- ✅ 只改 `client/data/` 下文件
- ❌ 绝不改代码常量
- ✅ 大改动后**跑黄金回放**确认回归影响是有意的

## 步骤
1. 找到目标条目 → 改值
2. 跑 pre-commit（schema 校验 + 字段范围）
3. 大改动（如基础属性 / 难度曲线）：用 `Replay.play(golden_*)` 跑黄金回放，对比 diff
4. 如果是有意改动，更新黄金回放（重新录一组）+ 在 commit message 注明影响

## 自检
- [ ] 没碰代码
- [ ] 字段值在合法范围
- [ ] hook 全过
- [ ] 大改动有回放回归证据

## 不需要做的
- 一般不需 ADR（除非是难度曲线哲学级改动）
- 一般不需 AI 记忆同步
