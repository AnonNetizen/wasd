# 词表与契约（约定字符串白名单）

> 本文件是项目所有"约定字符串"的**唯一白名单**。代码中必须以**常量/枚举**引用这里的 id，**禁止裸字符串、禁止编造未登记的 id**。
> 新增 id 的流程：① 在本文件登记 → ② 在逻辑层实现对应原语/处理 → ③ 才能在数据/代码中使用。
> 配套：`AI导航.md`、`游戏设计文档.md`、当前平台编码规则入口（见 `AGENTS.md` 第 3 步）。
>
> **AI 修改说明**：修改本文档前先读 `docs/AI协作/文档维护指南.md`。本文档是约定字符串权威；新增 / 删除 / 改名 id 时，常见联动为 `tools/sync_contracts.py` 生成产物（落地后）、引用该 id 的数据 / 代码、`docs/AI导航.md`、`docs/决策记录.md`、`docs/AI记忆/项目记忆.md`。

---

## 1. 属性 stat（用于 modifiers）
最终属性 = `(基础值 + Σ加法修正) × Π乘法修正`。

| stat id | 含义 | 单位/类型 |
|---------|------|-----------|
| `max_hp` | 最大生命 | int |
| `move_speed` | 移动速度 | px/s |
| `damage_invulnerability_duration` | 受伤后无敌时间 | 秒，float |
| `player_separation_radius` | 玩家中心排斥半径 | px |
| `damage` | 子弹伤害 | float |
| `fire_rate` | 射速（每秒发数） | float |
| `bullet_speed` | 子弹飞行速度 | px/s |
| `bullet_range` | 子弹最大射程 | px |
| `bullet_count` | 同时发射数 | int |
| `pierce_count` | 穿透次数 | int |
| `crit_chance` | 暴击率 | 0~1 |
| `crit_mult` | 暴击倍率 | float |
| `pickup_range` | 拾取范围 | px |
| `pickup_orb_speed` | 经验球吸附速度 | px/s |
| `luck` | 幸运（影响掉落、稀有度、升级候选是否 4 选 1） | float |
| `armor` | 物理防御 | float |
| `resist_fire` / `resist_poison` / `resist_lightning` | 元素抗性 | 0~1 |
| `lifesteal_ratio` | 吸血比例 | 0~1 |

> 修正器格式：`{ "stat": "<stat id>", "type": "add" | "mult", "value": <number> }`

## 2. 效果原语 effect（行为类，逻辑层实现）
逻辑层只实现这些有限原语，遗物/道具通过组合引用。

| effect id | 含义 | 关键参数 params |
|-----------|------|----------------|
| `pierce` | 子弹穿透 | `count` |
| `split` | 命中/消亡时分裂 | `count`, `angle` |
| `homing` | 追踪最近敌人 | `turn_rate` |
| `ignite` | 点燃（持续伤害） | `dps`, `duration` |
| `chain` | 连锁闪电 | `jumps`, `range` |
| `knockback` | 击退 | `force` |
| `explode` | 命中爆炸（范围伤害） | `radius`, `damage` |
| `lifesteal` | 吸血 | `ratio` |

## 3. 行为事件 behavior.event（触发时机）
代码引用走 `client/scripts/contracts/events.gd`。

| event id | 触发时机 |
|----------|---------|
| `on_fire` | 开火时 |
| `on_hit` | 子弹命中敌人 |
| `on_kill` | 击杀敌人 |
| `on_player_hit` | 玩家受伤 |
| `on_level_up` | 升级 |
| `on_pickup` | 拾取物品 |
| `on_tick` | 周期性（每秒/每帧节流） |

> 行为格式：`{ "event": "<event id>", "effect": "<effect id>", "params": { ... } }`

## 4. 埋点事件 analytics event（见设计文档 9.6）
代码引用走 `client/scripts/contracts/analytics_events.gd`。

| event_name | 触发时机 | 关键字段 |
|------------|---------|---------|
| `run_start` | 开局 | 角色、起始遗物、版本、locale |
| `run_end` | 一局结束 | 存活时长、击杀数、原因、最终等级 |
| `player_death` | 玩家死亡 | 位置、时间、击杀数、波次、死因 |
| `level_up` | 升级 | 等级、候选数量（3/4）、候选选项、选择、`luck` 快照 |
| `relic_pick` | 获得遗物 | 遗物 id、已有组合 |
| `item_use` | 使用主动/消耗品 | 道具 id、剩余充能 |
| `wave_milestone` | 难度/波次节点 | 波次、密度、DPS/承伤 |
| `game_pause` | 暂停游戏 | 时间、波次（可选） |
| `game_resume` | 继续游戏 | 暂停时长（可选） |
| `replay_recorded` | 一局结束保存回放 | 文件名、seed、时长、版本指纹 |
| `replay_replay_diff` | 回放对照模式发现差异 | 回放文件、首个 diff 帧、字段 |
| `state_changed` | `GameState` 切换 | from、to、context（可选） |
| `save_written` | 存档写入成功 | slot、kind、version、路径 |
| `save_loaded` | 存档读取 | slot、kind、version、迁移链 |
| `save_deleted` | 存档删除 | slot、kind、原因 |
| `save_migrated` | 存档自动迁移 | slot、kind、from_version、to_version、备份路径 |
| `save_corrupted` | 存档损坏 | slot、kind、错误信息 |
| `run_suspended` | 保存当前局并退出 | slot、run_time、seed、玩家等级 |
| `run_resumed` | 从 run 存档续局 | slot、run_time、seed、恢复状态 |
| `pool_overflow` | 对象池水位告警 | pool_id、当前数 / 容量 |
| `meta_currency_earned` | 结算获得局外货币 | currency_id、amount、来源明细 |
| `meta_upgrade_purchased` | 购买局外升级 | upgrade_id、level、cost、余额 |
| `meta_unlock_granted` | 授予局外解锁 | unlock_id、kind、来源 |
| `account_level_up` | 账号等级提升 | old_level、new_level、奖励 unlock_ids |

## 5. 设置 key（见设计文档 9.5）
| key | 类型 | 默认 | 取值范围 |
|-----|------|------|---------|
| `general.locale` | string | `zh_CN` | `zh_CN` / `en` /（扩展） |
| `video.fullscreen` | bool | false | — |
| `video.vsync` | bool | true | — |
| `audio.master` | float | 1.0 | 0~1 |
| `audio.music` | float | 0.8 | 0~1 |
| `audio.sfx` | float | 0.9 | 0~1 |
| `gameplay.fire_on_release` | bool | false | 松开瞄准输入是否停火 |
| `gameplay.aim_mode` | string | `mouse` | `mouse` / `4dir` / `auto` |
| `gameplay.screen_shake` | bool | true | — |
| `gameplay.pause_on_focus_loss` | bool | true | 窗口失焦时自动暂停 |
| `gameplay.record_replays` | bool | true | 是否自动录制每一局回放 |
| `input.move_up` / `input.move_down` / `input.move_left` / `input.move_right` | string | `W` / `S` / `A` / `D` | 键盘主绑定；写入对应 InputMap action，手柄绑定保留在 action 事件中 |
| `input.aim_up` / `input.aim_down` / `input.aim_left` / `input.aim_right` | string | `Up` / `Down` / `Left` / `Right` | 键盘主绑定；写入对应 InputMap action，手柄绑定保留在 action 事件中 |
| `input.use_active_item` | string | `Space` | 主动道具键盘主绑定 |
| `input.pause` | string | `Escape` | 暂停键盘主绑定 |
| `input.ui_confirm` | string | `Enter` | UI 确认键盘主绑定 |
| `input.ui_back` | string | `Escape` | UI 返回键盘主绑定 |
| `privacy.analytics_enabled` | bool | true | 数据收集开关 |

## 6. 本地化 key 命名规范（见设计文档 9.4）
- 文案配置与人工翻译流程见 `client/locale/README.md`；所有玩家可见译文集中写入 `client/locale/strings.csv`。
- 当前必填语言为 `zh_CN` 与 `en`；新增文案时 AI 必须自动补齐另一语言首版译文，人工最终复核。
- 统一蛇形小写：`<域>_<对象>_<字段>`。
- 域前缀约定：
  - `ui_`：界面文本（`ui_settings` / `ui_resume` / `ui_pause` / `ui_quit`）
  - `character_`：角色（`character_default_name` / `character_default_desc`）
  - `weapon_`：武器（`weapon_basic_blaster_name` / `weapon_basic_blaster_desc`）
  - `relic_`：遗物（`relic_sharp_rounds_name` / `relic_sharp_rounds_desc`）
  - `item_`：道具
  - `skill_`：技能（名称、描述、提示）
  - `enemy_`：敌人名
  - `hazard_`：机关 / 危险物名
  - `hint_`：提示/教程
  - `meta_`：局外成长（货币、账号等级、永久升级、解锁项）
- 名称用 `_name`，描述用 `_desc`；动态数值用占位符 `{value}`，禁止字符串拼接。

## 7. 输入动作 action（InputMap，可在设置重绑定）
代码中只引用这些 action id（`Input.is_action_pressed("<id>")` 等），**禁止硬编码物理按键、手柄按钮或手柄轴**；重绑定通过 `Settings` 写入 InputMap。同一 action 可以同时有键盘、手柄按钮和手柄轴绑定，玩法代码不得按设备写分支。

| action id | 含义 | 默认输入 |
|-----------|------|----------|
| `move_up` / `move_down` / `move_left` / `move_right` | 移动（8 方向由组合得出） | W / S / A / D；手柄左摇杆 |
| `aim_up` / `aim_down` / `aim_left` / `aim_right` | 射击瞄准兜底（默认鼠标瞄准；这些 action 供方向键、手柄右摇杆 / D-pad 和回放首片使用） | ↑ / ↓ / ← / →；手柄右摇杆 / D-pad |
| `use_active_item` | 释放主动道具 | Space；手柄 A / South |
| `pause` | 暂停 / 继续（切换） | Esc；手柄 Start / Menu |
| `ui_confirm` | UI 确认 / 激活当前焦点 | Enter；手柄 A / South |
| `ui_back` | UI 返回 / 关闭栈顶面板 | Esc；手柄 B / East |
| `debug_toggle_console` | 调试：切换 GM 指令控制台（开发期） | F1 / ` |
| `debug_submit_command` | 调试：提交 GM 指令（开发期） | Enter |
| `debug_close_console` | 调试：关闭 GM 指令控制台（开发期） | Esc |
| `debug_toggle_replay` | 调试：切换回放视图（开发期） | F9 |

> 暂停统一走 `GameState`（GDD 9.12）；暂停菜单等需暂停时仍响应的节点设 `process_mode = PROCESS_MODE_ALWAYS`。
> 调试用 action 仅在 debug build 启用，发布版禁用。

## 8. 对象池 id（见设计文档 9.13）
代码引用走 `client/scripts/contracts/pool_ids.gd`（自动生成）。

| pool id | 含义 |
|---------|------|
| `bullet_basic` | 基础子弹 |
| `bullet_pierce` | 穿透子弹（如有差异化场景） |
| `enemy_chaser` / `enemy_ranged` / `enemy_swarm` | 各类敌人 |
| `hazard_spike` | 尖刺机关 |
| `damage_number` | 飘字 |
| `hit_spark` | 命中粒子 |
| `pickup_orb` | 经验/金币掉落 |

## 9. 伤害类型 damage_type（见设计文档 9.15）
代码引用走 `client/scripts/contracts/damage_types.gd`。

| id | 含义 |
|----|------|
| `physical` | 物理（默认） |
| `fire` / `poison` / `lightning` | 元素 |
| `true` | 真实伤害（无视防御与抗性） |

`DamageInfo.flags`（位掩码）：`is_crit` / `is_dot` / `pierce_armor` / `cannot_kill` / `is_self_damage`。

## 9-A. 状态效果 status_effect（见设计文档 9.15.2）
代码引用走 `client/scripts/contracts/status_effects.gd`。

| id | 含义 | 典型字段 |
|----|------|---------|
| `burn` | 燃烧（DoT） | `dps`, `duration` |
| `poison` | 中毒（DoT） | `dps`, `duration` |
| `bleed` | 流血（DoT） | `dps`, `duration` |
| `freeze` | 冰冻（控制） | `duration` |
| `slow` | 减速 | `magnitude`, `duration` |
| `mark` | 标记（被标记敌人受额外伤害） | `magnitude`, `duration` |

`stack_rule` 合法值：`REPLACE` / `REFRESH` / `ADD_DURATION` / `INDEPENDENT` / `MAX_MAGNITUDE`。

## 10. 音频 id audio_id（见设计文档 9.17）
代码引用走 `client/scripts/contracts/audio_ids.gd`。

| 域前缀 | 含义 | 示例 |
|--------|------|------|
| `sfx_player_*` | 玩家音效 | `sfx_player_shoot` / `sfx_player_hurt` |
| `sfx_enemy_*` | 敌人音效 | `sfx_enemy_hit` / `sfx_enemy_die` |
| `sfx_pickup_*` | 拾取音效 | `sfx_pickup_orb` |
| `sfx_ui_*` | UI 反馈 | `sfx_ui_click` / `sfx_ui_levelup` |
| `music_*` | BGM | `music_run_loop` / `music_boss` |
| `voice_*`（预留） | 配音（按 locale） | `voice_intro` |

## 11. RNG 子流 rng_stream（见设计文档 9.18）
代码引用走 `client/scripts/contracts/rng_streams.gd`。

| stream id | 用途 |
|-----------|------|
| `spawn` | 刷怪位置 / 数量 |
| `drop` | 掉落 / 拾取 |
| `combat` | 暴击 / 命中浮动 / 闪避 |
| `ui_choice` | 升级候选数量判定与候选池抽取 |
| `world` | 地图 / 机关撒布 |
| `meta` | 元进度（跨局，独立 seed） |

> 各子流由主 seed + stream id 哈希派生；调用形如 `RNG.spawn.randi()`。

## 12. 内容扩展 id（角色 / 能力 / 标签，见设计文档 3.4 / 6.5）
未来角色、道具、遗物允许突破默认玩法限制，但必须以登记过的 id 声明，禁止在业务代码中散落 `character_id` / `capability` / `tag` 裸字符串。

### 12.1 角色 id character_id
代码引用走 `client/scripts/contracts/character_ids.gd`。

| id | 含义 |
|----|------|
| `character_default` | 默认角色（键盘 + 鼠标 / 手柄默认输入、鼠标瞄准、左右朝向、自动开火） |

### 12.2 能力 capability_id
代码引用走 `client/scripts/contracts/capabilities.gd`。Capability 表示“该内容被允许改写哪类默认规则”，具体效果仍需由 effect / behavior / strategy 实现。

| id | 允许突破的默认规则 |
|----|------------------|
| `cap_aim_mode_override` | 改写瞄准模型（如鼠标瞄准 → 4 方向 / 自动瞄准） |
| `cap_fire_mode_override` | 改写开火模型（如自动开火 → 蓄力 / 手动 / 条件触发） |
| `cap_movement_model_override` | 改写移动模型（如冲刺、滑行、惯性、传送） |
| `cap_inventory_slot_override` | 改写栏位数量或携带限制（如额外主动栏） |
| `cap_camera_policy_override` | 改写默认摄像机策略（必须仍通过统一 Camera / GameState 管线） |

### 12.3 内容标签 content_tag
代码引用走 `client/scripts/contracts/content_tags.gd`。

| id | 含义 |
|----|------|
| `tag_character` | 角色内容 |
| `tag_enemy` | 敌人内容 |
| `tag_enemy_prey` | 生态 AI 中的猎物 / 群居弱小敌人 |
| `tag_enemy_predator` | 生态 AI 中的捕食者敌人 |
| `tag_enemy_territorial` | 生态 AI 中的守巢 / 领地型敌人 |
| `tag_hazard` | 机关 / 危险物内容 |
| `tag_relic` | 被动遗物 |
| `tag_active_item` | 主动道具 |
| `tag_consumable` | 消耗品 |
| `tag_skill` | 技能 |
| `tag_limit_break` | 明确突破默认规则的内容，必须带 capability 与测试说明 |

## 12-A. 游戏模式 id game_mode
游戏模式配置来自 `client/data/game_modes.json`，只组合资源池、权重、禁用列表、参与者 / 队伍和轻量覆盖，不实现模式运行时，也不允许业务代码按 mode id 写专属内容分支。

| id | 含义 |
|----|------|
| `mode_standard_survival` | 标准生存模式；当前完整项目默认模式样例 |

## 12-B. 敌人 AI 动作 enemy_ai_action
敌人生态 AI 的运行时动作原语。敌人行为应通过 `client/data/enemy_ai_profiles.json` 组合这些动作与权重，禁止按敌人 id 写一次性分支。

| id | 含义 |
|----|------|
| `ai_action_approach_target` | 向当前目标接近并尝试接触伤害 |
| `ai_action_flee_threat` | 远离当前威胁目标 |
| `ai_action_orbit_target` | 在目标附近绕行，制造侧向压力 |
| `ai_action_charge_target` | 对目标进行蓄力冲锋 |
| `ai_action_guard_home` | 回到出生点 / 领地中心并守住周边 |

## 12-C. 技能 id skill_id
技能配置来自 `client/data/skills.json`。技能不绑定英雄；角色、主动道具、敌人 AI 或事件系统都可以引用同一 skill id。

| id | 含义 |
|----|------|
| `skill_whirlwind_slash` | 旋风斩：围绕释放者对附近敌人造成一次范围物理伤害 |

## 12-D. 技能资源 skill_resource
技能消耗资源的 id。角色或其他释放者声明自己拥有的资源池，技能只声明消耗哪种资源，避免把“魔法”写死成唯一资源。

| id | 含义 |
|----|------|
| `mana` | 魔法值；当前默认角色用于释放主动技能 |

## 12-E. 技能目标选择 skill_targeting
目标选择由 SkillSystem 解释，后续新增策略先登记再实现。

| id | 含义 |
|----|------|
| `aoe_enemies_around_caster` | 以释放者为圆心，选择半径内敌人 |
| `target_enemy` | 指向单个敌人 |
| `target_ally` | 指向单个友方 |

## 12-F. 技能效果 skill_effect
技能效果原语由 SkillSystem 解释；需要复用到道具 / 敌人时仍引用同一原语，不按技能 id 写分支。

| id | 含义 | 关键参数 params |
|----|------|----------------|
| `skill_effect_damage` | 对目标造成伤害，结算走 `Combat.apply_damage` | `amount`, `damage_type` |

## 13. 局外成长 id（Meta Progression，见设计文档 7.2）
局外成长配置来自 `client/data/meta_progression.json`，玩家状态存入 `SaveManager` 的 `meta` kind。代码引用走后续生成常量；脚本未就位前仍以本节为唯一白名单。

### 13.1 局外货币 currency_id
代码引用走 `client/scripts/contracts/meta_currencies.gd`。

| id | 含义 |
|----|------|
| `meta_essence` | 默认局外货币，用于购买永久升级轨道 |

### 13.2 局外升级 upgrade_id
代码引用走 `client/scripts/contracts/meta_upgrades.gd`。

| id | 含义 |
|----|------|
| `meta_upgrade_max_hp` | 永久提升最大生命 |
| `meta_upgrade_damage` | 永久提升基础伤害 |
| `meta_upgrade_fire_rate` | 永久提升基础射速 |
| `meta_upgrade_move_speed` | 永久提升移动速度 |
| `meta_upgrade_pickup_range` | 永久提升拾取范围 |
| `meta_upgrade_luck` | 永久提升幸运值 |
| `meta_upgrade_starter_choice` | 解锁更多开局遗物候选 |

### 13.3 解锁类型 unlock_kind
代码引用走 `client/scripts/contracts/meta_unlocks.gd`。

| id | 含义 |
|----|------|
| `character` | 角色解锁 |
| `relic_pool` | 遗物池解锁 |
| `mode` | 模式 / 玩法入口解锁 |
| `difficulty_tier` | 难度阶层解锁 |
| `run_start_option` | 开局选择项解锁 |

### 13.4 解锁项 unlock_id
代码引用走 `client/scripts/contracts/meta_unlocks.gd`。

| id | 含义 |
|----|------|
| `unlock_character_default` | 默认角色解锁 |
| `unlock_relic_pool_basic` | 基础遗物池解锁 |
| `unlock_challenge_room` | 挑战房间解锁 |
| `unlock_difficulty_tier_2` | 难度阶层 II 解锁 |
| `unlock_starting_relic_choice_1` | 开局遗物候选 +1 |
| `unlock_starting_relic_choice_2` | 开局遗物候选 +2 |
| `unlock_starting_relic_choice_3` | 开局遗物候选 +3 |

## 14. 存档种类 save_kind（见设计文档 9.16）
代码引用走 `client/scripts/contracts/save_kinds.gd`。

| id | 含义 |
|----|------|
| `meta` | 局外成长长期档案：货币、账号等级、永久升级、解锁、累计统计 |
| `run` | 当前一局续局档案：暂停保存退出后加载恢复 |
| `replay_index` | 回放索引档案：具体回放文件仍由 `Replay` 管理 |

---

> 维护要求：任何新增 stat/effect/event/analytics event/setting/locale 前缀 / pool/damage_type/status/audio/rng_stream/character/capability/tag/game mode/enemy AI action/meta currency/meta upgrade/meta unlock/save kind id，**先在此登记**再使用；删除或改名需同步检查引用与数据文件。
> **代码常量自动生成**：上述各节由 `tools/sync_contracts.py` 生成 `client/data/_contracts.json` 与 `client/scripts/contracts/*.gd`（详见 GDD 9.19、规则 15）。
