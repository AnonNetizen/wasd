# 决策记录（ADR）

> 轻量架构决策记录：每条写清**决策 + 理由**，让后续 agent 了解既定约束的由来，避免误改。
> 新决策追加到表格末尾，保留历史。
>
> **AI 修改说明**：修改本文档前先读 `docs/AI协作/文档维护指南.md`。本文档是 ADR 权威；新增 ADR 时必须使用递增编号，并同步 `docs/AI记忆/项目记忆.md`、当日会话日志，必要时同步 GDD、规则、AI导航、词表或测试策略。

| # | 日期 | 决策 | 理由 |
|---|------|------|------|
| 1 | 2026-06 | 初始类型定位为俯视角弹幕生存（以撒 + 吸血鬼幸存者） | 明确早期核心玩法与灵感来源；当前类型定位已由 ADR #98 更新 |
| 2 | 2026-06 | 引擎确定 **Godot 4.6.3 + GDScript** | 开源轻量、资源文本化、AI 易读写、2D 节点契合需求 |
| 3 | 2026-06 | **玩家恒居屏幕中央**，相机挂玩家下，关 `limit` 与 `drag margin` | 复刻吸血鬼幸存者世界滚动手感，保证严格居中 |
| 4 | 2026-06 | 移动 WASD 8 方向；**射击瞄准方向键 4 方向（上下左右）**；全自动开火 | 操作设计确定，移动与瞄准解耦 |
| 5 | 2026-06 | 遗物/道具采用 **modifiers + behaviors 数据驱动**，逻辑层只实现有限效果原语 | 新增内容=加数据，避免硬编码分支爆炸，利于 AI 量产 |
| 6 | 2026-06 | 数值全部外置到 `res://data/`，支持热重载 + 字段说明；具体格式后由 ADR #63 扩展为平表 CSV / 复杂 JSON | 数据与逻辑分离，零代码调参 |
| 7 | 2026-06 | 框架级**多语言本地化**：禁止硬编码文本，一律 `tr("key")` | 后期补本地化成本极高，框架阶段即杜绝硬文本 |
| 8 | 2026-06 | 框架级**设置系统**：`Settings` autoload + 信号即时生效 + `user://settings.cfg` | 统一玩家偏好通路，可扩展、免重启 |
| 9 | 2026-06 | 框架级**数据埋点**：`Analytics` autoload 统一 `track_event` 接口 | 关键节点预留钩子，避免后期补埋点大改业务代码 |
| 10 | 2026-06 | 采纳原建议 E~I（数值格式/遗物架构/平衡数据化/美术规范先行/元进度） | 已落实进设计文档；A 已由 ADR #109 采纳，C 已由 ADR #127 采纳，D~E 仍待决策 |
| 11 | 2026-06 | 建立 **AI 友好工程**：项目索引、词表契约、数据校验、黄金样例、类型化 GDScript、模板、ADR、日志、README | 提升 AI 续写/生成质量，消灭裸字符串与定位成本 |
| 12 | 2026-06 | 设立**元规则**：新规则归档进规则文件、文档与实现保持同步 | 形成"改动→规则归档+文档同步"闭环 |
| 13 | 2026-06 | 加入**暂停功能**：统一 `get_tree().paused` + 菜单 `process_mode=ALWAYS`，暂停键 InputMap action `pause`（可重绑定） | 提供继续/设置/退出菜单，状态控制集中、不散落硬编码 |
| 14 | 2026-06-12 | 仓库重组为 **`docs/` + `client/`（Godot 项目根）+ `server/`（联机/后端预留占位）** 三段结构；工具链文件保留在仓库根 | 文档与实现物理隔离；`server/` 暂占位，启动联机时再补设计；不破坏 Godot 项目结构（`client/` 即 `res://`） |
| 15 | 2026-06-12 | 建立 **AI 协作记忆体系**：`docs/AI记忆/项目记忆.md`（主索引）+ `会话日志/YYYY-MM-DD.md`；维护方式为 AI 主动更新（选项 a），主索引保持精简、细节归会话日志 | 跨会话 / 跨机器 clone 后能直接续接对话，无需翻聊天记录；触发条件与节制原则写在主索引第 9 节 |
| 16 | 2026-06-12 | 引入**录制回放系统**为第四条横向基础设施（GDD 9.9）：`Replay` autoload 录 `seed + 输入序列`；强制确定性约束（随机走 `RNG`、时间走 `GameClock`）；维护黄金回放用于 CI 回归 | 构筑、掉落和数值平衡迭代必需回归安全网；确定性必须在框架阶段立住，否则后期补无解 |
| 17 | 2026-06-12 | **平衡测试接口**预留（GDD 9.10）：`AIPlayer` 与 `Spawner`/`MapManager`/`RNG` 都接受外部 seed；MVP 前只锁接口形态，不实现 | 中后期商业化必须有自动化平衡；现在做空中楼阁不划算，但接口走偏后不可回头 |
| 18 | 2026-06-12 | 引入 **AI 协作工程**为第五条横向基础设施（GDD 9.11 + 规则 14-A）：`docs/AI协作/`（任务模板 + 上下文预算 + 角色分工 + 引擎集成 + 实时验证回路）；新增 `docs/AI导航.md` 5.2 系统依赖图；CICD 阶段 4 加 4.L 健康度 / 4.M 回放回归 / 4.N 平衡 sim | 把"AI 怎么协作"工程化，对齐业界 AI 工作流（实时反馈、引擎接入、角色分工），让项目跨人/跨 agent/跨时间可维护 |
| 19 | 2026-06-12 | **AI 记忆自动瘦身**：`项目记忆.md` 写入前自动检查行数；临近 200 行立即按「日 → 周 → 月」层级聚合旧条目；同位置累积内容用覆盖；瘦身本身不算重大变更（不追加 ADR、仅会话日志记一笔） | 把"主索引精简"从约定升级为 AI 自动行为，避免用户每次提醒；规则 14-B 与项目记忆第 9 节同步落地 |
| 20 | 2026-06-12 | 采纳 **J. RNG**（GDD 9.18.1，规则 12-A 扩展）：autoload + 子流（spawn/drop/combat/ui_choice/world/meta），主 seed + 子流派生；`luck` 接 `weighted_pick` | 后期补种子化几乎要改所有随机调用；分流避免局部偏移影响全局序列 |
| 21 | 2026-06-12 | 采纳 **K. GameState**（GDD 9.12，规则 12-C）：autoload 状态机统一全局流程；PauseMenu/Replay/Analytics 改为状态消费者；禁止业务直接读写 `get_tree().paused` | 散落的流程判断在中后期会爆；状态机做协调中枢 |
| 22 | 2026-06-12 | 采纳 **L. PoolManager**（GDD 9.13，规则 12-C）：autoload 通用对象池，池类型 id 进词表第 8 节；被池化节点实现 `_pool_reset` | 不止子弹，敌人/掉落/飘字/特效都需池化；统一接口避免各系统重写池逻辑 |
| 23 | 2026-06-12 | 采纳 **M. UIManager**（GDD 9.14，规则 12-C）：autoload UI 栈 push/pop，UI 场景用 `@export modal/pauses_game/music_duck` 元数据；禁止业务直接 add_child UI | UI 弹窗的层级、暂停联动、焦点必须收口；本地化刷新由 UIManager 触发 |
| 24 | 2026-06-12 | 采纳 **N. 伤害管线 + StatusEffect**（GDD 9.15，规则 12-D）：单入口 `Combat.apply_damage(DamageInfo)`；持续效果 `StatusEffect` Resource + Component，明确 `stack_rule`；effect 原语薄包装 | "怎么算"和"怎么叠"必须收口；新效果原语只是注入新 StatusEffect，不再各自管 tick |
| 25 | 2026-06-12 | 采纳 **O. SaveManager**（GDD 9.16，规则 12-D）：autoload + 强制 `version` 头字段 + 迁移链 + 损坏备份；与 `Settings` 职责边界明确（玩家偏好走 Settings，游戏内进度走 SaveManager）。此决策已由 ADR #48 扩展为 `meta` / `run` 双档案、标准头字段、原子写入、备份回退和损坏隔离 | 存档第一天就必须有版本号；事后补几乎无解 |
| 26 | 2026-06-12 | 采纳 **P. GameClock**（GDD 9.18.2，规则 12-A 扩展）：autoload 统一时间源，受 `GameState` 暂停冻结；`delta_scaled` 与 `set_time_scale` 不依赖 `Engine.time_scale`；可注入假时钟 | 玩法时间必须可被暂停冻结、可被慢动作缩放、可被测试注入；裸 `Time.get_ticks_msec()` 在暂停/缩放下出 bug |
| 27 | 2026-06-12 | 采纳 **Q. AudioManager**（GDD 9.17，规则 12-D）：autoload `play_sfx/play_music`，与 `PoolManager` 集成；音频 id 进词表第 10 节；暂停 ducking；Bus 路由启动时校验 | 设置已有音量总线但缺统一播放接口；散落 `.play()` 导致 id 漂移与节流困难 |
| 28 | 2026-06-12 | 采纳 **R. 词表→代码常量单一来源**（GDD 9.19，规则 15 扩展）：方案 (b) 脚本生成——`docs/词表与契约.md` → `client/data/_contracts.json` → `client/scripts/contracts/*.gd`；pre-commit 强制同步 | 人工同步迟早漂移；脚本生成 + hook 兜底 = 漂移不可能；md 改完什么都不用做 |
| 29 | 2026-06-12 | 建立 **5 层测试金字塔 + 里程碑级要求**（`docs/测试策略.md`，规则 14-C）：L0 静态/契约 → L1 GUT 单测 → L2 headless → L3 黄金回放 → L4 平衡 sim → L5 手动回归；横向 autoload + Combat + ModifierEngine 行覆盖率 ≥80%；4 个必备黄金样例；性能预算 8 项；M1~M7 测试矩阵；AI 改完按"测试义务表"履责 | 之前测试约定散落在 GDD 9.9/9.10 + CICD 2.G/2.H/4.M/4.N + 5 处；缺总文档导致每次接手要拼读、新人不知道何时该测什么；测试义务无明文 = AI 容易跳过 |
| 30 | 2026-06-12 | 启用 codebuddy 平台 **首批 3 subagents + 5 slash commands**：agents 含 `balancer` / `contract-validator` / `data-author`（角色分工落地），commands 含 `/sync-contracts` / `/new-relic` / `/run-replay-regression` / `/health-check` / `/update-memory`；登记进 `AGENTS.md` 与 `docs/AI协作/README.md`；后续由 ADR #49 扩展创意 / 策略 subagents | 把 `docs/AI协作/角色分工.md` 的设计从文档变为可执行实体；高频操作（同步契约、加遗物、跑回归、健康度、记忆兜底）固化为命令，省 token 且降低 AI 走偏概率；主对话上下文不被专业任务污染 |
| 31 | 2026-06-13 | 调整 **`.codebuddy/` 与 `.codex/` 多平台适配规则**（规则 21）：两者共享核心项目约束，但不再要求文件内容、目录结构或工具字段完全镜像；允许按不同 AI agent / 平台优化 frontmatter、工具名、prompt 与 command 包装 | 不同 AI 工具有不同工具名和调度习惯，字节级镜像会压制 Codex 等平台的专属优化；真正需要强制的是核心规则语义一致，而不是文件哈希一致 |
| 32 | 2026-06-13 | 建立 **通用 AI 入口 + 平台入口适配**：`AGENTS.md` 作为所有 agent 的通用入口，新增 `CODEX.md` / `OPENCODE.md` 作为 Codex 与 OpenCode 轻量入口，并将规则 21 扩展到平台入口文件 | 避免项目说明默认绑定 CodeBuddy；不同 agent 可先读自己的入口，但必须回到同一套项目核心规则、记忆、词表与测试义务 |
| 33 | 2026-06-13 | 落地 **`.opencode/` OpenCode 平台配置**：新增 `opencode.json`、3 个 agents、5 个 commands 与 OpenCode 规则入口，并要求 `.codebuddy/` / `.codex/` / `.opencode/` 三个平台核心语义同步 | OpenCode 有严格 config schema 与独立 agent/command 加载方式；单靠 `OPENCODE.md` 不足以提供平台原生体验，需独立配置但不能分叉项目规则 |
| 34 | 2026-06-13 | 确立 **扩展优先 / 破限内容架构**：未来角色、遗物、道具可以突破默认玩法限制，但必须通过数据声明的 capability、content tag、effect / behavior primitive 或可复用 strategy 实现 | 项目计划长期加入大量突破既定限制的内容；把默认玩法规则当硬编码上限会导致后期返工，声明式扩展能保持内容量增长时仍可维护 |
| 35 | 2026-06-13 | 建立 **代码-文档同步规范**：长期代码模块、公共 API、signal、数据 schema、依赖方向、扩展点或测试义务变化时，同步详细 `docs/代码/` 模块文档；不要求每个小 helper 单独成文档 | 项目后续将由多 agent 长期扩展；模块级文档能防止代码与设计漂移，同时避免逐文件文档导致维护成本失控 |
| 36 | 2026-06-13 | 确立 **AI 默认中文沟通**：AI 面向用户的回复、计划、总结、提问与变更说明默认中文；仅在用户明确要求、引用原文或目标文件 / 对外文本需要时使用其他语言 | 项目主体文档与协作语境为中文；统一沟通语言可降低跨 agent 续接成本，同时保留代码、API、日志等原文准确性 |
| 37 | 2026-06-13 | 将 **`draft/` / `DRAFT/` 设为人工草稿禁区**：AI 默认不得读取、搜索、修改、整理、格式化、总结或引用该目录；只有用户明确点名授权时才可进入，且无需每次主动汇报遵守情况 | `draft/` 承载用户未整理的人工草稿，应保护其私密性与未定稿状态，避免 AI 批量搜索或自动整理时误读、误改、误引用 |
| 38 | 2026-06-13 | 建立 **`MinimumViableProduct/` MVP 隔离实验区**：MVP 文档与客户端代码都放在该目录内；MVP 初始规则为玩家不移动、方向键四方向射击、敌人只从四方向刷新 | 用户要用最小可行产品验证工作流和想法；隔离目录能快速试错并沉淀经验，同时避免临时实现污染完整项目架构 |
| 39 | 2026-06-13 | 正式支持 **手柄输入**：完整项目默认键盘 WASD / 手柄左摇杆移动，键盘方向键 / 手柄右摇杆或 D-pad 4 方向瞄准，手柄 A / South 主动道具，Start / Menu 暂停；玩法代码只消费 InputMap action | MVP 手柄试验证明设备差异可收口在绑定层；正式项目需要从框架阶段支持手柄，避免后期把按钮/摇杆硬编码进业务逻辑 |
| 40 | 2026-06-13 | 完整项目代码文档必须采用 **详细模块文档**：AI 可按模板自动创建/维护，但不得用自动抽取的简短摘要替代；模块文档需覆盖代码地图、场景 / 节点结构、运行流程、API、signal、数据、依赖、扩展点、常见改动、故障排查、测试义务与迁移说明 | 用户明确要求完整项目文档要“详细的、成模块的”；项目会长期由人和 AI 共同维护，简陋摘要无法支撑后续扩展和排障 |
| 41 | 2026-06-13 | 将 `docs/` 升级为 **AI 知识库**：新增人工索引 `docs/AI知识库索引.md`、机器索引 `docs/_kb_index.json`、术语表 `docs/术语表.md`、健康检查说明与 `tools/docs_health_check.py` | 后续 AI 需要稳定检索、判断权威层级、追踪 ADR 影响和自动发现文档漂移；结构化索引与健康检查能把文档从“集合”升级为“可校验知识库” |
| 42 | 2026-06-13 | 强化 **AI 知识库 v2**：`docs/_kb_index.json` 增加 owner / review / canonical / must-read 元数据，`tools/docs_health_check.py` 扩展链接、覆盖、`related_code`、ADR 矩阵与孤儿 / 同步风险报告，并把三平台 `/health-check` 接入该脚本 | 仅有索引骨架还不足以防文档漂移；把任务路由、元数据和健康检查纳入同一闭环，能让 AI 在动手前更快定位权威文件，并在改后自动发现漏同步风险 |
| 43 | 2026-06-13 | 完整项目采用 **经验与升级选择系统**：启用成长池的模式升级时默认 3 选 1；`luck` 通过 `RNG.ui_choice` 概率性把本次候选提升为 4 选 1；升级选项池内容另列待决策。ADR #120 后默认标准模式暂不启用该系统 | 用户明确要求参考《吸血鬼幸存者》的经验升级体验；把候选数量和幸运加成先定为框架规则，可支撑回放确定性和平衡测试，选项池保留数据驱动空间避免过早写死 |
| 44 | 2026-06-13 | AI 记忆改为 **三层结构**：`项目记忆.md` 只做长期索引，`current_state.json` 保存机器可读当前状态，`会话日志/` 保存每日细节；`docs_health_check` 负责校验当前状态与 ADR / 待决策一致 | 原主记忆混合长期事实、短期任务和流水摘要，AI 可读但不够稳定；三层拆分能降低重复、提升可校验性，并让新会话更快拿到“现在该做什么” |
| 45 | 2026-06-13 | 完整项目建立 **人工可维护的数值 / 文案配置手册**：`client/data/README.md` 说明数值文件、字段、单位、范围和调参流程；`client/locale/README.md` 说明 `strings.csv`、语言列、key 命名和占位符规则 | 用户明确要求数值与文案都独立出来、易于人工调整，并且文案便于多语言维护；手册化能让人不读代码也能立即上手配置 |
| 46 | 2026-06-14 | 完整项目采用 **深局外成长**：`meta_progression.json` 数据化局外货币、账号等级、永久升级轨道和内容解锁，状态持久化走 `SaveManager` 的 `meta` kind | 用户明确要求游戏需要较深的局外成长；数据化能保证长期成长、文案、多语言、存档迁移和后续平衡测试都可维护 |
| 47 | 2026-06-14 | 启用 **最小 Stage 1 CI**：`.github/workflows/docs-check.yml` 只跑 JSON、CSV、文档健康检查和 whitespace diff | 当前仍处文档 / 数据框架阶段；先用低成本门禁防漂移，避免过早引入 Godot、GUT、回放和平衡 sim 的复杂度 |
| 48 | 2026-06-14 | 完整项目需要 **强玩家存档**：`SaveManager` 必须同时支持 `meta` 局外成长长期档案与 `run` 局内暂停退出续局档案，并采用版本迁移、原子写入、备份回退和损坏隔离 | 用户明确要求支持局内暂停退出后加载和局外成长；这两类存档是长期留存与玩家信任的基础，不能后期临时补 |
| 49 | 2026-06-14 | 新增 **7 个创意 / 策略 subagents**：`game-designer`、`numeric-designer`、`ip-designer`、`copywriter-packager`、`ui-art-designer`、`game-art-designer`、`marketing-strategist`，三平台同名配置 | 用户需要专业角色分别辅助玩法评估、数值、IP、文案、UI 美术、游戏美术和宣发；把它们做成 advisory subagents 可降低主对话污染并提升设计质量 |
| 50 | 2026-06-14 | 新增 **沟通审查与需求评估规则**：用户问有没有问题 / 风险时基于事实回答，没问题就明确说没问题；用户提出需求后先反馈落地前景、性价比、复杂度和主要风险 | 用户明确要求避免 AI 为显得有用而硬找问题，也要把需求隐患前置暴露；这能降低无效优化和后期返工风险 |
| 51 | 2026-06-14 | 落地 **数据校验 / 契约同步 / 轻量 Godot Bridge**：`tools/sync_contracts.py` 生成 `_contracts.json` 与 GDScript 常量，`tools/validate_data.py` 校验已落地数据与 locale，`tools/godot_bridge.py` 支持 MVP 场景树导出和 headless boot | 数据驱动项目最怕词表、数据、文案和场景状态漂移；先用轻量脚本形成可验证闭环，比过早引入重型 MCP / schema 工具更稳 |
| 52 | 2026-06-14 | 确立 **AI Git 提交策略**：大更改完成后 AI 默认自动 commit，细微改动不提交；提交前必须检查 status / diff / log、跑验证，并且只 stage 本次任务文件 | 用户希望大改可独立回滚且无需反复提醒提交；同时避免把用户脏改动、`draft/` / `DRAFT/` 或临时文件误带进提交 |
| 53 | 2026-06-14 | 安装 **项目级 OpenCode skills**：新增 Godot/GDScript、场景验证、文档同步、安全提交、事实 review、AI 资源筛选和 MCP 评估 7 个 `.opencode/skills/*/SKILL.md`，并记录外部资源评估清单 | Agent Skills 已成为多工具开放格式；把成熟资源模式本地化为按需加载 skill，能减少常驻上下文、提高重复流程可靠性，并避免盲目安装高权限外部 MCP / plugin |
| 54 | 2026-06-14 | 新增 **上下文压缩后的任务恢复规则**：发生上下文总结 / 压缩 / 恢复后，AI 必须先以用户最后明确指令对齐当前任务，摘要与 `Next Steps` 只作候选参考 | 用户指出压缩后容易把历史下一步误当当前要求；该规则能降低越权落地、跑偏执行和误提交风险 |
| 55 | 2026-06-14 | 将 `GodotPrompter`、`headless-godot-skill-kit`、`Claude-Code-Game-Studios` 作为 **外部 AI 资源整包 submodule** 隔离安装到 `.opencode/vendor/ai-resources/`，不加入 `skills.paths`、不自动启用 | 用户希望先把三个包装进项目里审阅；submodule 隔离能保留完整上游内容与许可证，同时避免 hooks、权限和大量 skills 默认生效 |
| 56 | 2026-06-14 | 按上游 README **正式安装外部 AI 工具但排除模板 / 示例**：`GodotPrompter` 通过 OpenCode plugin 启用，`headless-godot` 装入 `.agents/skills/` 并加入 OpenCode `skills.paths`，`Claude-Code-Game-Studios` 的 `.claude` agents / skills / hooks / rules 装入项目 | 用户明确要求把三个包里的 AI 工具装进项目，同时不要安装模板等杂项；分平台安装能启用工具能力，又避免 starter project、示例和生产状态污染仓库 |
| 57 | 2026-06-14 | 为 CCGS 新增 **跨 agent 复用适配层**：`.agents/skills/ccgs-game-studio` 负责让 OpenCode / Codex / CodeBuddy / 通用 agent 按需读取 `.claude/agents` 与 `.claude/skills`，不批量注册 49 个外部 agent | 用户希望 CCGS 可被其他 agent 使用；适配层能复用工作流资产，又避免上下文常驻膨胀、平台配置爆炸和外部模板 / hooks 越过项目规则 |
| 58 | 2026-06-14 | 将外部 AI 三库活跃层**收敛为 `game-ai-reference`**：移除 GodotPrompter plugin、`.agents/skills/headless-godot`、`.agents/skills/ccgs-game-studio` 与活跃 `.claude/`，仅保留 `.opencode/vendor/ai-resources/` 作为上游来源 | 直接启用三套外部工具会放大上下文、重复规则和平台冲突；轻量适配层能按需复用有价值参考，同时保证项目 GDD / ADR / 规则始终优先 |
| 59 | 2026-06-15 | 将项目级 skills **统一收敛到 `.opencode/skills/` 并删除 vendor/reference 层**：吸收外部库中有用的 Godot、headless 验证与试玩复盘流程，移除 `.agents/skills/game-ai-reference`、`.opencode/vendor/ai-resources/` submodules 与 `.gitmodules` | 用户要求 skill 不重复、不冲突且能直接使用；把外部经验本地化进项目自有 skill 能减少跳转、避免外部规则冲突和整包维护成本 |
| 60 | 2026-06-15 | 项目级 skills 改为 **三平台同步安装**：同名 `SKILL.md` 同步放在 `.codebuddy/skills/`、`.codex/skills/` 与 `.opencode/skills/`，Codex / CodeBuddy 读取各自目录，OpenCode 继续由 `opencode.json` 加载 | 用户指出当前执行者是 Codex，不能只把能力收进 OpenCode；三平台同源能保证 skill 直接可用且不需要跨平台 reference 跳转 |
| 61 | 2026-06-15 | 建立 **GM 指令与调试工具构建隔离规则**：调试控制台、GM 命令和开发 overlay 只在 debug/dev_tools 构建启用，release preset 不启用 `dev_tools` 且应排除调试资源；MVP 先用 `debug_tools.gd` 验证命令流程 | 用户需要 GM 指令和调试工具辅助调参、试玩复盘与问题复现；构建隔离和正式 API 调用边界能避免调试能力泄露到正式版或污染玩法系统 |
| 62 | 2026-06-15 | 建立 **正式项目阶段工作规划**：MVP 验证和 AI 工作框架完成后，完整项目按 F1~F9 从工程骨架、autoload、数据契约、可玩闭环、存档成长、UI、本地化、回放测试到内容打磨推进 | 用户确认 MVP 已验证、AI 工作框架已搭建完成；正式项目需要稳定路线图供后续 AI 选择任务、控制范围并按阶段验证 |
| 63 | 2026-06-15 | 数值配置格式采用 **平表数值优先 CSV、复杂配置优先 JSON**：`DataLoader` 后续需同时支持 CSV / JSON，并由 `client/data/README.md` 说明每个数据文件的格式选择 | 用户明确指出 CSV 更方便人工阅读和批量调参；格式分流能兼顾人工编辑体验、AI 校验与复杂配置表达能力 |
| 64 | 2026-06-15 | 本地化首批语言固定为 **`zh_CN` + `en`**，新增玩家可见文案由 AI 自动补齐另一语言首版译文，人工最终复核 | 用户要求先做中英文两种语言并由 AI 自动翻译；该策略能降低早期本地化维护成本，同时保留人工校对避免译文失真 |
| 65 | 2026-06-16 | 确立 **多模式资源复用原则**：角色、遗物、道具、敌人、成长奖励等资源本体默认模式无关，模式只通过数据化资源池、权重、条件、禁用列表和轻量覆盖组合资源 | 用户明确提出后续可能有多种模式，资源不能强耦合；提前规定资源与模式边界可避免复制内容、硬编码 `mode_id` 分支和后期大规模返工 |
| 66 | 2026-06-16 | 确立 **未来多人 PvE / PvP 预留边界**：当前不实现多人，但输入、伤害、阵营、模式、回放、存档和埋点避免写死单玩家假设，保留 actor / participant / team / intent 扩展空间 | 用户明确提出未来可能做多人 PvE 甚至 PvP；轻量预留能降低后期重构风险，同时避免现在过早进入联网、同步、服务器权威和反作弊的大坑 |
| 67 | 2026-06-16 | 建立 **快速开工 + 按需读取** 上下文分层流程：日常接手先读 `docs/AI协作/快速开工.md`、`current_state.json`、当前平台规则入口与任务相关导航段，完整长期文档只在对应任务触发时读取；`current_state.json.last_verified` 只保留最近热路径验证 | 降低 AI token 消耗，把冷信息从默认上下文移出，同时保留权威文档、红线、长期记忆和文档同步责任不变 |
| 68 | 2026-06-16 | 正式项目阶段任务采用 **短工作包 + 模块文档优先**：反复推进的阶段工作收录到 `docs/AI协作/工作包/`，代码阶段默认读模块文档和目标源码，README / CONTRIBUTING / 工具适配指南不复制完整规则正文 | 继续降低 AI token 消耗，让任务入口更短、更稳定，同时把规则权威集中在 `AGENTS.md` 和平台规则，减少多处同义漂移 |
| 69 | 2026-06-16 | 大型代码改动提交前自动追加 **事实型 code review**：跨多文件功能、代码模块、数据 schema、工具、CI 或复杂行为改动完成后，AI 必须在提交前用 `code-review-factual` / Reviewer 口径检查 bug、回归风险和缺测试；拼写、单行、小范围文档、只读诊断和临时验证不触发正式 review | 把质量守门放在真正有风险的改动后面，同时避免小改动为形式化 review 消耗额外 token |
| 70 | 2026-06-17 | DataLoader schema 回归测试进入 **Stage 1 CI**：`tools/test_data_loader_schema.py` 用临时数据副本断言黄金数据通过，未登记 id、缺 locale key、类型 / 范围错误和跨文件引用错误必须 fail-fast | F3 已进入数据契约闭环阶段，仅跑正向数据校验不足以防 schema 退化；在 GUT runner 完整接入前，轻量 Python 回归测试能低成本守住 DataLoader-facing 错误格式与边界 |
| 71 | 2026-06-17 | 建立 **双致谢清单**：根目录 `CREDITS.md` 作为代码库级人类清单，`client/data/credits.json` 作为游戏内 Credits UI 数据源，并由 DataLoader / `validate_data.py` 校验 | 致谢、第三方来源和许可证 notice 需要从开发期开始可追踪；双清单能兼顾发行合规、人类维护和未来游戏内展示 |
| 72 | 2026-06-17 | `affaan-m/ECC` 作为 **agent-harness 工作流参考** 被部分吸收：context budget、skill stocktake、strategic compact、verification loop 和 GateGuard / harness-audit 思路改写进项目自有 `ai-resource-curator`，但不安装 ECC 整包、hooks、MCP、GitHub App、npm plugin、dashboard 或批量 agents / skills / commands | ECC 的有用价值在流程审计和安全边界，不在直接叠加一套外部 harness；项目已有三平台入口和规则体系，继续保持低上下文、低权限、项目自有的 AI 协作层 |
| 73 | 2026-06-17 | 对 `affaan-m/ECC` 完成 **README 与全工具面吸收清单**：外部大型 AI 工具仓库必须先记录来源快照、逐项盘点 agents / commands / skills / hooks / scripts / MCP / 平台适配，再把可复用流程合并进项目自有 skill / 文档；默认不新增常驻 command、subagent、vendor tree 或外部 runtime | 用户要求逐个查看 ECC 工具并吸收有用部分；把全量筛选结果沉淀为 `docs/AI协作/ECC工具吸收清单.md` 可避免重复大仓扫描，也能保留有用流程而不扩大权限、上下文和维护面 |
| 74 | 2026-06-17 | 新写 / 修改的 GDScript 采用 **Godot 4.6 官方 GDScript style guide** 作为基础代码风格；本项目更严格的类型化、数据驱动、autoload、词表常量和文档同步规则优先；不因此批量重排未触碰脚本 | 官方规范降低人类与 AI 协作摩擦并贴近引擎生态；限制批量格式化可避免无关 diff 与回归风险 |
| 75 | 2026-06-17 | **删除 MVP 隔离实验区目录**：`MinimumViableProduct/` 在用户确认测试完成后从仓库移除；后续只保留历史 ADR / 会话记录和可迁移经验，正式实现只在 `client/` 推进 | MVP 已完成验证使命，继续保留会增加维护面、健康检查噪音和错误入口；删除目录并把工具默认目标切到正式 `client/`，能让后续工作聚焦完整项目 |
| 76 | 2026-06-17 | 第一档 GDScript 项目 lint 进入 **Stage 1 CI**：`tools/lint_gdscript_rules.py` 自动检查官方段落顺序、危险 `:=`、中文硬编码字符串，以及裸随机 / 时间 / 暂停 API | 把低误报的代码规范和项目红线从 LLM 人工阅读前移到脚本门禁，降低审查上下文成本；完整 gdtoolkit、headless 与人工 review 仍按后续阶段补齐 |
| 77 | 2026-06-17 | 第二档项目规则 lint 进入 **Stage 1 CI**：`tools/lint_project_rules.py` 自动检查数据字段手册覆盖、locale `zh_CN` / `en` 双语，以及 release preset debug/dev_tools 禁入边界 | 把“新增字段要写手册、文案要双语、release 不能带调试工具”从人工提醒变成可执行门禁；id 词表和生成文件漂移继续由 `validate_data.py` 与 `sync_contracts.py --check` 作为权威 |
| 78 | 2026-06-17 | 第三档语义 advisory lint 进入 **Stage 1 CI**：`tools/lint_semantic_rules.py` 默认非阻塞，提示特殊 id 分支、业务脚本绕过 autoload、缺类型签名、长期脚本缺 `# Doc:` 与未知 contract 常量 | 这些检查价值高但可能误报，先作为 CI 日志和 health-check 线索降低 LLM 阅读量；坏样例回归由 `tools/test_semantic_rules_lint.py` 确保规则不退化 |
| 79 | 2026-06-17 | 建立 **本地 pre-commit 与工具先行代码审核流程**：`.pre-commit-config.yaml` 复用 Stage 1 lint / test / docs gate，`docs/AI协作/代码审核流程.md` 要求 Reviewer 先看工具输出，再审当前 diff 与残余风险 | 用户要求降低 LLM 阅读量；把“脚本规则由脚本守门、LLM 只补语义和回归风险”的口径固化成 hook 与 SOP，可避免每次 review 重扫已脚本化规则 |
| 80 | 2026-06-18 | F5 `run` 存档进入 **version 2 + SaveManager smoke**：`SaveManager` 注册 `run` v1 -> v2 迁移，`save-smoke` 覆盖 run roundtrip、`.bak` 回退、双坏档隔离和迁移链 | 暂停退出续局属于强玩家存档核心需求；在接入局外成长前先把迁移、备份回退和坏档隔离自动化验证起来，能降低长期存档信任风险 |
| 81 | 2026-06-20 | F8 回放确定性基线收紧：`RNG` 子流 seed 派生改用项目内稳定字符串算法，升级候选入选后按 id 稳定排序，并新增 `golden_level_up_choice.replay` 覆盖升级候选 decision 记录与选择应用 | 新增升级选择 golden 暴露出跨 Godot 进程的候选顺序漂移；回放回归必须同 seed 跨进程稳定，且选择第 N 项不能被无设计价值的随机显示顺序影响 |
| 82 | 2026-06-20 | 默认瞄准从 **4 方向** 改为 **鼠标方向瞄准**，方向键 / 手柄右摇杆 / D-pad 作为兜底输入；玩家和敌人占位表现只做左 / 右两种朝向 | 开放地图中鼠标瞄准更贴近《挺进地牢》《土豆兄弟》类俯视角手感，能减少斜向空打；左右朝向保持表现成本低且适合早期 Demo |
| 83 | 2026-06-20 | 预留 **本地数据包式 Mod 接口**：新增 `ModLoader` autoload 扫描 `user://mods/<mod_id>/mod.json`，只支持声明式 JSON / CSV append patch 与少量动态契约扩展；创意工坊暂不接入，未来仅作为分发层 | 现在先给玩家 mod 留稳定、可校验、安全的内容入口，避免后期重写 DataLoader / 回放 / 存档边界；不执行玩家脚本和不接平台 SDK 可把安全、审核和维护风险压到最低 |
| 84 | 2026-06-20 | 预留 **平台服务接口，Steam 优先**：新增 `PlatformServices` autoload，统一承接 Steam 成就、统计、富状态 / 状态显示、overlay、Lobby / 联机入口和用户身份；当前只提供 `none` 空后端，不接 Steamworks SDK，其他平台后续走 provider adapter | Steam API 会横切 UI、存档、成就、联机和发布配置；先统一业务调用面能避免后期到处硬接 Steam，同时保证非 Steam / 离线状态仍可启动和游玩 |
| 85 | 2026-06-20 | 项目引擎基线从 **Godot 4.6.3** 迁移到 **Godot 4.7**：`client/project.godot` 的 `config/features` 改为 `4.7`，三平台规则 / skills / README / GDD / AI 导航改用 4.7 口径；按官方 4.7 迁移指南扫描高风险 API，当前未发现需改写的 4.6-only 调用 | Godot 4.7 已发布稳定版，官方迁移说明显示本项目使用的 2D / typed GDScript 路径大多兼容；先统一工程和协作工具基线，能在后续 F9 内容扩展前降低版本漂移与验证不一致风险 |
| 86 | 2026-06-20 | RNG 子流 seed 派生升级为**带域隔离的 SHA-256 mixer**，并新增 `rng-audit` headless 相关性审计；修改 seed 派生、子流集合或底层 RNG 时必须跑 `rng-audit` 和 checked-in golden replay runner | STS2 式问题说明“不同 seed”不等于“不同子流独立”；本项目后续会有种子分享、稀有奖励、事件选择和回放传播，必须把跨子流可预测性提前纳入门禁 |
| 87 | 2026-06-20 | 建立 **Claude Code 平台原生配置 `.claude/`**：仓库根新增项目自有的 `.claude/agents/`（10 个 subagent）、`.claude/commands/`（5 个 slash command）、`.claude/skills/`（9 个项目级 skill）、`.claude/rules/game-coding-rules.md` 与 `.claude/settings.json`（仅含 `draft/` / `DRAFT/` deny，权限 allow 白名单按需另行授权）；与 `.codebuddy/` / `.codex/` / `.opencode/` 同源、核心语义一致，项目级 skills 同步面由三平台扩为四平台。本决策只新增项目自有 Claude 原生配置，**不**复活 ADR #56~#59 / #72 拒绝的外部 `.claude/` 整包（CCGS / ECC 的 hooks、外部 agents、settings、vendor），不引入活跃 hooks | Claude Code 此前只有轻量 `CLAUDE.md` 入口，缺少与其他三平台对位的原生 subagent / command / skill / 权限能力；用户要求把 Claude 对齐为一等平台。复用现有同源配置可让 Claude 原生调度既有角色与命令，并在 harness 层用 settings deny 强制草稿禁区，同时遵守规则 21“语义一致、不要求字节级镜像” |
| 88 | 2026-06-22 | IP 方向定为 **《破巢者》**（英文暂定 `Nestbreakers`），并独立维护 `docs/IP设定.md`：未知原因导致其他宇宙与本宇宙的通道突然打开，原本拥有银河系星际文明的人类被打得七零八落，只剩首都星域相对稳定；玩家选择多英雄中的一名主动突入敌方“巢”，在怪潮中夺取遗物、升级构筑并尝试打穿巢核、切断通道或削弱敌方源头；“巢”泛指敌方核心据点 / 生产源头 / 通道锚点 / 意志中枢，不限定为虫巢 | 这个包装比抽象档案或泛化敌域更直觉，能让玩家立刻理解“一个英雄杀进敌方老家”的目标；跨宇宙战争和银河文明残局能支撑多英雄、多敌方主题与长期内容扩展，独立 IP 文档能承载英雄、敌方势力、遗物命名、局外包装和宣发基调，避免 GDD 概述臃肿 |
| 89 | 2026-06-22 | 敌人 AI 采用 **数据驱动生态 profile + Utility/FSM/Steering 分工**：`enemies.csv` 只声明基础数值、生态 tag 和 `ai_profile_id`，`enemy_ai_profiles.json` 声明感知、目标权重、动作列表和动作参数；运行时用 Utility 选动作，用 FSM 执行冲锋等阶段动作，用 Steering 移动，所有伤害仍走 `Combat` | 《雨世界》式怪物生态需要敌人能在玩家、其他怪物和领地之间选择目标，但全行为树会让早期调参和扩展成本过高；profile 分工让策划可调、代码可扩展，并避免按敌人 id 写分支 |
| 90 | 2026-06-22 | 主动技能采用 **技能定义与英雄解耦 + 可扩展资源池 + targeting/effect primitive**：技能本体进入 `client/data/skills.json`，角色只通过 `starting_loadout.skill_ids` 引用技能并声明 `skill_resources`；首片 `skill_whirlwind_slash` 消耗 `mana`，用 `aoe_enemies_around_caster` 和 `skill_effect_damage` 经 `Combat` 对周围敌人造成 AOE 伤害 | 用户明确要求技能可复用、可扩展，并为未来非魔法资源英雄预留空间；把目标筛选、资源消耗和效果解释抽成数据化 primitive，可避免技能绑定英雄或按技能 id 写一次性分支 |
| 91 | 2026-06-22 | 多语言 UI 尺寸以英文 `en` 文案长度作为设计和验收基准；新增 / 修改玩家可见 UI 文案或布局时必须切英文检查不截断、不溢出、不遮挡，并由 `settings-smoke` 覆盖当前可见按钮类控件的英文宽度适配 | 中文信息密度高，用中文验收会掩盖英文和其他拉丁语言的长度风险；早期以英文基准设计按钮、面板和 HUD，能降低本地化返工与 UI 破版风险 |
| 92 | 2026-06-22 | 详细数值面板采用 **按住 `show_stats_panel` action 显示、松开隐藏、不暂停** 的 HUD 叠层；默认键盘主绑定为 Tab，并在设置面板可重绑定 | 试玩和调试需要随时查看生命、等级、武器、拾取、幸运和技能资源等当前数值；做成非模态 HUD 层不会打断战斗节奏，走 InputMap / Settings 又避免硬编码物理键 |
| 93 | 2026-06-22 | 地图从无限扩展改为 **有限边界 + 可人工调参 PCG**：`map_layouts.json` 声明模式地图尺寸、玩家出生点、安全半径、刷怪边距、PCG 机关规则和人工摆放点；`MapManager` 统一生成 / 保存地图与机关位置，机关运行时复用 `hazards.csv`、`PoolManager` 和 `Combat` | 有限地图更利于 Demo 节奏、性能预算和手工关卡打磨；把 PCG 规则与 manual overrides 放进数据能支持 AI 批量生成、人工修正、暂停续局和回放诊断 |
| 94 | 2026-06-23 | 主动技能系统升级为 **项目版轻量 GAS 首片**：保留 `skills.json` / `SkillSystem` 调用面，新增 `ability_tags`、`activation.required_tags` / `blocked_tags` / `granted_tags`、owned tag 计数和 run 快照恢复；当前只吸收 ability / effect / tag / cost / cooldown / targeting 思想，不照搬 Unreal 网络预测、replication 或 Blueprint 类型系统 | 项目后续会有大量角色、主动道具、遗物触发、状态和破限能力；轻量 GAS 能把“能不能释放、消耗什么、打谁、产生什么效果”收束到同一条数据驱动管线，同时避免过早引入不适合当前单机阶段的大框架成本 |
| 95 | 2026-06-23 | 状态效果首片接入项目版轻量 GAS：新增 `StatusEffect` Resource 与 `StatusEffectComponent`，`skill_effect_apply_status` 可施加 `silence` 并授予 `ability_tag_silenced`，状态按 `GameClock` 过期并进入 SkillSystem run 快照 | 沉默、姿态、破限、DoT 和未来 modifier 都需要统一生命周期与叠加规则；把状态 tag 来源收束到 `StatusEffectComponent`，可避免各系统手动计时、tag 泄漏和续局双计数 |
| 96 | 2026-06-23 | `Player` 与 `Enemy` 正式接入 `StatusEffectComponent`：实体暴露 `apply_status_effect()` 与 owned ability tag 查询，玩家 / 敌人 run 快照保存状态效果与状态授予 tag，Enemy 在 `configure()` / `_pool_release()` / `_pool_reset()` 清理状态 | 状态效果必须作用到真实实体才能支撑沉默、控制、DoT、减速和 on-hit primitive；把状态生命周期放在实体边界并在对象池复用时清理，可避免状态泄漏、续局丢状态和后续效果各自手写计时 |
| 97 | 2026-06-23 | burn 作为首个真实 DoT 状态落地：`StatusEffect` 保存 `damage_type`、`tick_remaining` 与队伍归因，`StatusEffectComponent` 在 `PLAYING` 下按 `GameClock` tick 并通过 `Combat.apply_damage()` 结算 `fire + is_dot` 伤害 | DoT 需要和沉默 / 减速共享同一状态生命周期、叠加、暂停冻结和续局恢复；把 tick 放在状态组件且伤害仍走 Combat，可避免每个效果原语各写计时器或绕过统一伤害入口 |
| 98 | 2026-06-23 | 项目类型定位改为 **俯视角刷宝动作生存**：核心参考《星际战甲》与《暗黑》的装备 / 词条 / 长期养成和刷战利品循环，保留局内随机奖励、单局构筑和生存压力，但不再沿用旧类型定位 | 用户明确要求游戏改成刷子游戏；新定位能把长期掉落追求、装备词条、局外成长和内容复刷放到核心，同时避免后续设计继续按旧类型口径推进 |
| 99 | 2026-06-23 | 玩家表现先采用 **2.5D 视觉层**：`Player` 继续用 `CharacterBody2D`、`Camera2D`、2D 碰撞和 2D 地图平面移动，视觉上通过 `Player3DVisual` 的 SubViewport 渲染低模 3D 胶囊角色再贴回 2D 世界 | 这是接近《暗黑》斜俯视质感的低风险第一步；保留现有 2D 玩法系统、回放、存档、Combat、EnemyAI 和地图边界，避免直接重构成完整 3D 场景带来的成本和遮挡风险 |
| 100 | 2026-06-23 | 当前正式视角口径改为 **固定斜俯视 2.5D 刷宝动作生存**：显示层向暗黑式 3/4 可读性靠拢，移动、碰撞、刷怪、地图边界和回放仍保持 2D 平面；具体相机实现边界由 ADR #101 修正 | 用户判断纯俯视角不适合当前项目；固定斜俯视更接近《暗黑》类刷宝动作的可读性和角色存在感，同时保留 2D 系统的低成本、确定性和现有实现稳定性 |
| 101 | 2026-06-23 | 固定斜俯视首片**不使用 `Camera2D` 滚转画面**：`CenteredCamera` 保持屏幕水平和玩家居中；`Player3DVisual` 用更明显的 3/4 俯看 `Camera3D` 角度提供角色立体感；后续非等比缩放边界由 ADR #102 修正 | 直接旋转 2D 摄像机会把整个屏幕坐标系拧歪，既不像稳定斜俯视，也会伤害地图、UI 和输入方向的直觉 |
| 102 | 2026-06-23 | 固定斜俯视显示改为**哈迪斯式等比屏幕尺度**：`Camera2D` 不旋转、不非等比缩放，世界横纵单位映射到屏幕保持同尺度；斜俯视观感由地面美术、障碍物、Y-sort / 遮挡、房间 / 舞台构图和 2.5D 角色 / 敌人视觉层承担 | 用 `Camera2D.zoom.y` 压缩整张世界会让横向和纵向屏幕移动速度不一致，造成观感与数学脱节；哈迪斯式舞台表达能保留 2D 玩法确定性，同时让画面读起来像斜俯视 |
| 103 | 2026-06-23 | 通用机关改为**哈迪斯式菱形危险地块**：`Hazard` 的占位表现和触发判定都使用菱形范围；当时字段为 `hazards.csv.radius`，该字段口径已由 ADR #105 更新为 `radius_tiles` | 机关如果只画成菱形但仍用圆形范围判定，会让危险区域读法和实际数学脱节；把视觉和触发区域统一成菱形，能延续斜俯视舞台语言并降低玩家误判 |
| 104 | 2026-06-23 | 固定斜俯视资产处理采用**地面范围读菱形、非地面资产自由轮廓 + 落地点 / 阴影 / 遮挡统一**：AI 生成或替换资产时必须说明 footprint、anchor、shadow、sort layer 与真实判定形状 | 把所有资产都做成菱形会让角色、敌人、拾取物和障碍物失去辨识度；固定斜俯视的关键是把资产锚定到同一个地面平面，而不是把所有轮廓强制变成菱形 |
| 105 | 2026-06-23 | 地图采用**量化菱形格度量**：`map_layouts.json.grid.cell_width/cell_height` 定义地图格，bounds 是菱形外接框，必须是格尺寸奇数倍且满足 `height = width * cell_height / cell_width`；玩家出生点 / 人工机关 / PCG 机关中心吸附到格点；`hazards.csv.radius` 改为正整数 `radius_tiles`，机关菱形尺寸按格子的整数倍生成 | 旧实现把背景斜线、机关绘制和触发数学分成多套口径，导致机关看起来像正方形且无法量化占格；统一格度量后，地图、摆点、视觉和判定可被同一套坐标解释 |
| 106 | 2026-06-23 | 机关锚点按 `radius_tiles` 奇偶吸附：奇数尺寸中心落在菱形格心，偶数尺寸中心落在网格顶点，人工摆点和 PCG 都使用同一规则 | 只把所有机关中心吸附到格心会让偶数尺寸机关的边缘穿过格心而不是贴住网格线；按奇偶选择锚点才能让所有整数占格机关真正对齐地图格 |
| 107 | 2026-06-23 | 玩家出生安全区的**可见标记改为贴格菱形**：`safe_radius` 仍作为 PCG 避让距离下限，`MapManager` 视觉上把出生区画成按 `grid.cell_width/cell_height` 斜率并吸附到格线的菱形 | 出生区属于贴地范围提示，继续用正圆会和固定斜俯视、量化菱形地图格冲突；视觉改成格线菱形能保持舞台读法一致，同时不改变现有 PCG 距离规则 |
| 108 | 2026-06-24 | 项目维护者授权 **subagent 默认主动调度**：支持 subagent 的平台在复杂、专业或可并行任务中可直接启用对应项目 subagent；不支持或受外层工具策略限制时读取同名 agent `.md` 作为 prompt 模板 | 用户希望未来新对话也不必反复授权；把授权写入通用入口、平台入口和工具适配说明，可降低协作摩擦，同时保留小任务不强拆和无原生调度时的降级路径 |
| 109 | 2026-06-24 | 核心玩法改为 **固定斜俯视 2.5D 射击刷宝生存**：默认武器必须按住 `fire` action 才持续射击，技能保留但首批内容定位为武器模组 / 射击强化 / 战术开火能力；默认内容移除旋风斩、点燃斩和燃烧法术包装，首片改为 `skill_overdrive_rounds` 临时强化射速与弹速 | 用户明确要求游戏核心从动作技能转向射击，并点名不要当前旋风斩和燃烧类技能；按住开火能强化操作身份，射击强化技能能保留构筑深度，同时复用已有轻量 GAS、状态和 modifier 管线而不是重写系统 |
| 110 | 2026-06-24 | AI 协作新增 **需求不明先问** 规则：当需求、术语、验收标准、授权边界或上下文含义无法可靠确认时，AI 必须先问一个简短澄清问题；禁止为了推进而自行脑补或替用户做高风险假设 | 用户明确要求 AI 有不明白的地方直接问、不要随便猜；把澄清优先写入通用入口、平台入口和规则文件，能减少误实现、越权提交和返工 |
| 111 | 2026-06-24 | 《破巢者》首个 IP 美术风格采用 **钙化活体建筑 + 阵营色隔离**：敌巢 / 虫族使用骨白、蜡黄、干肉粉、深红、黑紫和少量毒蓝，青 / 红 / 白归属虫族与敌巢；玩家和玩家子弹默认避开青 / 红 / 白，敌方远程攻击可用红色，宝箱与地图兴趣点按功能色区分 | 用户确认生成概念图方向后要求沉淀美术文档；阵营色隔离能让虫巢、玩家、敌方远程攻击、奖励和地图兴趣点在固定斜俯视战斗中保持读图清晰，同时让“巢”更像外宇宙生物工业而非传统湿肉虫巢 |
| 112 | 2026-06-24 | 采用 **敌巢战区导演（WarzoneDirector）首片**：新增 `warzone_directors.json` 和 `WarzoneDirector`，用固定 phase、巢变异主题、生态 encounter 与兴趣点组合组织当前模式 wave；首片只按 `GameClock` 时间 gating `spawn_waves.csv`，不读取玩家状态、不做隐藏 DDA、不接运行时 LLM、不提升 run 存档 schema | 用户希望采用 AI 辅助设计的节奏导演、巢变异、地图兴趣点和敌人生态导演，但明确排除读玩家状态；把导演收束为可校验数据解释层，能提升战区主题和调参能力，同时避开动态难度信任风险、回放漂移和存档复杂度 |
| 113 | 2026-06-25 | F10 兴趣点接入 `MapManager` 初始机关生成：`WarzoneDirector` 只过滤当前 layout 的 `interest_points`，`MapManager` 使用通用 PCG / 菱形锚点规则生成 `source="director"` hazards，并保留 `interest_point_id`；不读取玩家状态、不按 `poi_id` / `hazard_id` 特判、不提升 run 存档 schema | 让战区主题从 debug summary 进入玩家可感知地图，同时复用既有数据校验、`RNG.world`、机关锚点、run snapshot 和 HazardSystem 机制，降低 replay / save 风险并保持非隐藏 DDA 边界 |
| 114 | 2026-06-25 | 普通新局 / 重开会通过 `RNG.set_random_run_seed()` 生成新的 run seed；继续游戏从 run snapshot 恢复，replay / smoke / golden 工具仍显式设置固定 seed 或直接启动 `_start_gameplay_run()` 保持可复现 | 玩家正常游玩需要每局地图 / 刷怪 / 掉落序列不同；把随机主 seed 生成收口到 RNG autoload，并只在人工新局入口调用，可兼顾玩家新鲜感、暂停续局、回放复现和测试稳定性 |
| 115 | 2026-06-25 | 跨局成长从 F6 永久升级轨道改为参考《星际战甲》的装备 Mod 系统：玩家配置英雄与武器两套 Mod，Mod 可掉落、升级和分解；旧 `MetaProgressionSystem` / `meta_progression.json` 进入 legacy 迁移范围，不再作为未来下一局属性来源 | 用户明确要求抄《星际战甲》Mod 系统并删除现有局外成长；装备 Mod 比永久属性轨道更符合刷宝长期追求和构筑取舍，同时保留 `SaveManager` 的 `meta` kind 可降低存档迁移风险 |
| 116 | 2026-06-25 | 旧 `purchased_upgrades` 按已购等级的历史花费一次性折算为 `gear_mod_dust`：`GearModSystem` 读取 `meta_progression.json.upgrade_tracks[].costs` 累加尚未补偿的等级成本，写入 `meta.gear_mods.legacy_migration.purchased_upgrades_compensation` 记录已补偿等级并保留旧字段 | 旧永久升级已不再影响下一局，按历史花费补成 Gear Mod 升级资源能保护老档价值，同时用已补偿等级记录支持增量迁移并避免重复领取 |
| 117 | 2026-06-25 | 旧局外成长运行时退役：删除 `MetaProgressionSystem` autoload、`MetaProgressionPanel`、标题旧升级入口、死亡旧货币 / 账号经验结算和 `meta-smoke`；保留 `meta_progression.json` 作为旧 `purchased_upgrades` 成本补偿表，由 `GearModSystem` 直接读写 `SaveManager` 的 `meta.gear_mods` | Gear Mod 已接管跨局成长并完成旧购买补偿，继续保留旧 UI / 结算会制造双成长路径；保留历史成本数据可保护老档价值并降低存档兼容风险 |
| 118 | 2026-06-25 | 旧局外成长测试档不再迁移：删除 `client/data/meta_progression.json`、旧 `meta_*` 契约常量、旧 `ui_meta` / `meta_*` 文案、旧 `purchased_upgrades` -> `gear_mod_dust` 补偿逻辑和相关 schema / smoke 覆盖；不符合当前 `meta.gear_mods` 的内部测试档直接重置或隔离 | 项目尚未上线，旧档只有内部测试价值；继续维护旧迁移会拖慢 F11 装备 Mod 迭代并制造误导，保留 `SaveManager` 的可靠读写和坏档隔离即可 |
| 119 | 2026-06-25 | 本地 `staged-whitespace-check` 对 staged 文件的 EOF 多空行自动修复后再检查；其他 whitespace 错误仍作为硬失败 | EOF 多空行不影响运行但频繁打断提交；自动修复能保留整洁 diff，同时避免低价值格式问题消耗人工和 AI 时间 |
| 120 | 2026-06-26 | 默认标准模式改为暗黑式 8-12 分钟短刷图循环，局内经验升级 3 选 1 暂不在默认模式启用；F4 GrowthSystem 保留为未来非默认模式能力 | 当前核心是刷装备 Mod 和高频重开验证装配，短循环比 25 分钟长生存更符合刷子游戏；默认模式移除局内 3 选 1 能避免与 Gear Mod 长期成长主轴抢焦点 |
| 121 | 2026-06-27 | AI 上下文预算新增 S / M / L / XL 任务复杂度分级：先判流程深度，再按任务类型读取文件 | 用户确认需要区分不同复杂度任务的执行方式；复杂度分级能避免小任务被大流程拖慢，也避免跨模块 / 决策级任务欠规划、漏验证或漏文档同步 |
| 122 | 2026-06-27 | 默认短刷图战利品采用 **暂存 → 完成 / 撤离结算**：Gear Mod、dust 等本局掉落先进入 `run.pending_loot`，只有击破小巢核或未来撤离成功才写入 `meta.gear_mods`；死亡、重开、回标题或放弃会丢失未结算战利品，保存退出只保存本局暂存并继续 | 用户明确要求不要“已拿到就带回”，参考《星际战甲》撤离口径和暗黑式有限携带压力；把结算写入集中到成功出口能让死亡 / 撤离 / 完成有清晰风险收益，也防止重复结算刷局外奖励 |
| 123 | 2026-06-27 | 默认短刷图小巢核击破后不再直接完成本局，而是开启撤离区；玩家进入撤离区并完成短读条后，才提交 `run.pending_loot`、删除 `run` 存档并显示完成面板。撤离区当前形状已由 ADR #125 修正为矩形俯视格 | 这让“击破核心”和“带回战利品”成为两个连续但可读的动作，符合《星际战甲》式撤离风险收益，也给后续扩展多个出口、贪局加压和完整结算清单留下空间 |
| 124 | 2026-06-27 | 当前正式视角改回 **俯视角 2D**：删除正式玩家场景的 `Player3DVisual` / `SubViewport + Camera3D` 正交视觉层，玩家占位由 `Player._draw()` 按完整 `aim_direction` 绘制方向标记。地图格、边界和地面范围口径随后由 ADR #125 修正为矩形俯视格 | 用户明确要求改回俯视角、不要继续使用模拟正交视角；纯 2D 表现更贴合当前射击读图、瞄准和碰撞预期，也降低后续美术 / 测试 / 回放维护成本 |
| 125 | 2026-06-27 | 俯视角地面语言改为 **矩形 / 方形 2D 网格**：`map_layouts.json.grid.cell_width/cell_height` 表示水平 / 垂直格宽高，默认 `160 x 160`；`MapManager` 使用矩形边界、矩形出生安全区和矩形 clamp；`Hazard`、兴趣点目标、缓存箱和撤离区都用矩形俯视 footprint，不再用菱形地图格 / 菱形机关模拟等距或正交视角 | 用户指出只删玩家 3D 层不够，地图格子仍像模拟正交视角；把地面网格、边界和范围判定一起改成矩形，才能让画面、碰撞、PCG、smoke 和数据校验真正回到俯视角 |
| 126 | 2026-06-28 | 敌人 AI 新增通用 **远程攻击 action**：`ai_action_ranged_attack` 通过 `enemy_ai_profiles.json.movement.ranged_*` 字段保持距离、发射池化投射物并经 `Combat.apply_damage()` 伤害玩家；首个默认内容为 5 分钟进入的 `enemy_spitter` 喷棘者 | 远程敌人能补足 F12 短刷图的走位压力；把远程攻击做成 profile action 和投射参数，而不是按敌人 id 特判，可复用、可调参，并保持对象池、Combat、回放 / 存档边界一致 |
| 127 | 2026-06-28 | 默认短刷图下一阶段转向 **手工房间串联**：每个房间用 Godot `.tscn` + marker 手工制作，首片只做线性房间序列、清房开门和进入下一房间；暂不自研完整关卡编辑器 UI，也不继续用 PCG 作为默认房间内容来源 | 用户希望改成类似《Enter the Gungeon》的一个个房间与出入口，并手工制作房间；房间制能更清楚地解决有限地图目标引导问题，让敌人生成点、陷阱和门的节奏可被人工打磨，同时保留现有 Gear Mod 暂存 / 撤离结算规则 |
| 128 | 2026-06-28 | **F13 手工房间制短刷图首片落地（运行时）**：新增 `RoomManager`（`Node2D`，房间 carrier 模式下由 `GameplayRunLoop` 挂在 `ActiveWorld` 下并驱动）、`rooms.json` / `room_sequences.json`、`RoomRoot` + 四类房间 marker、两个演示房间 `.tscn`，实现「数据序列加载房间 → 清房开门 → 触碰出口切房 / 完成」与暂停存档续局恢复；门 `direction` / `unlock_rule` 和房间 `clear_condition` 进词表 §15-A/B/C 并生成常量；run payload schema v2→v3 新增 `room` 块并配 `_migrate_run_v2_to_v3`；房间 carrier 首片为 opt-in（`debug_enable_room_carrier()` 或存档含 `room` 块），默认 open-warzone 路径不变，对象池生成 / `Combat` / 战利品仍走 `GameplayRunLoop`；新增 `room-switch-smoke`，四条黄金回放因 `data_fingerprint`（新增契约 + schema_counts）变化重录、行为 summary 未变 | 用户点名推进 F13 并要求先出 plan 再写代码；把房间制做成数据 + marker + `RoomManager` 协调层而非按 id 特判，能复用 F12 的池化 / 暂存 / 撤离结算 / 矩形格几何，首片 opt-in 让 F12 smoke 与黄金回放稳定，把「房间设为默认可玩模式」留作需要重写 F12 smoke / 重录黄金的后续切片 |
| 129 | 2026-07-11 | **Steamworks Slime Lab 切换为专属 Steam 应用身份**：`output/steamworks_lab` 使用 App ID `4955670`，运行时校验初始化结果、Steam runtime App ID 与 Lobby marker / protocol version，发布版可经 Steam 客户端重启，初始化 / 连接失败回到离线 transport；该 Lab 仍是独立应用，不代表正式 `client` 已完成 Steamworks / `PlatformServices` provider 接入 | 专属 App ID 会改变 Lobby 隔离、账号许可证、邀请与后续 Depot 发布身份；明确独立 Lab 与正式客户端边界，并保留单机 / 本地 ENet 降级，可避免把源码接入误报为正式客户端或 Steam 后台已发布 |
| 130 | 2026-07-11 | **Windows / PowerShell 环境采用稳定执行规范**：固定字符串优先 `rg -F`、全部 `rg` 选项置于 `--` 前、路径走 `-LiteralPath`、cmdlet 与原生程序分别处理错误、合法非零退出码先归一化再进入并行 / fail-fast；`git diff --no-index=1` 仅在输入已校验为文件后表示差异；以规则 + 可复制模板落地而不新增包装器 | 多层转义、尖括号占位符、误判工具退出码或把缺失输入归一化为成功都会制造假结果；把 shell 语义固化到所有平台入口可降低跨 agent 漂移，同时避免为简单命令引入新的维护工具 |
| 131 | 2026-07-11 | **正式客户端当前只适配固定 16:9 分辨率**：默认逻辑画布为 1920×1080，设置只允许经过验证的 16:9 固定预设；非 16:9 屏幕等比缩放并补上下或左右黑边，不拉伸、不裁切、不扩大玩法视野；其他比例留作未来按独立固定预设接入的优化项 | 先锁定单一画布能降低 UI、瞄准、截图和玩法视野的适配成本；黑边可保证不同屏幕不破坏当前构图，而把新增比例改成显式验收的后续优化，避免现在为连续响应式布局投入过高成本 |
| 132 | 2026-07-12 | **Steamworks Slime Lab 的 Windows Steam 工具链锁定为 Godot 4.7 + GodotSteam 4.20 + Steamworks SDK 1.64 官方 module editor / templates**：二进制只安装到本地 `.toolchain/`，仓库提交下载地址 / SHA-256 版本锁、统一 setup / verify / export-release 工具和 Windows export preset；App ID 权威迁移到 `steam/initialization/app_data/app_id=4955670`，运行时显式调用 `steamInitEx(4955670, false)`；SteamPipe / Depot / 双账号验证仍保持外部步骤 | 固定官方 module 组合可同时提供 `Steam` singleton 与 `SteamMultiplayerPeer`，避免混用已停更的第三方 peer；不提交约 377 MB 外部二进制仍能保持仓库轻量，而版本锁、校验和统一导出工具让新机器可复现生成包含 exe / PCK / `steam_api64.dll` / 第三方许可声明且排除开发文件的 Windows release |
| 133 | 2026-07-12 | **Steamworks Slime Lab 改用 GodotSteam 4.20 官方 GDExtension，并废止 ADR #132 的 module editor / templates 选型**：4.20 的 `gdextension` 分支已合并并注册 `SteamMultiplayerPeer`，插件安装到忽略的 `addons/godotsteam/`，开发 / 导出使用普通 Godot 4.7 和标准 templates；统一工具继续锁定插件 URL / SHA-256、验证 singleton / Peer 并门禁 Windows release | 退役的独立 MultiplayerPeer 仓库已合并回主仓库，继续使用 module 不再是保留 Godot RPC 的必要条件；GDExtension 可减少引擎分叉与编辑器混淆。Steam 商店版 Godot 自带同名 `steam_api64.dll` 会与插件冲突，因此 setup 只准备同一普通 Godot 二进制的干净本地副本，不引入 GodotSteam 定制引擎 |
| 134 | 2026-07-15 | **Steamworks Slime Lab 停止创建 `.toolchain/`，直接使用 `--godot` / `GODOT_PATH` 指向的无 DLL 冲突普通 Godot 4.7**：setup 只在系统临时目录下载 / 校验 / 解压 GodotSteam 并安装到忽略的 `addons/godotsteam/`，verify 直接验证所选 editor，export-release 按 editor 模式使用标准用户目录或 self-contained `editor_data/` 中与版本精确匹配的 Windows templates；插件版本锁、release 门禁和 Steam 外部验收边界不变 | 继续复制 editor / templates 会制造重复占用与额外状态；环境变量入口与两种 Godot 标准模板位置可兼顾本机直接使用、便携安装和跨机器可移植性，临时下载与精确模板校验仍维持可验证发布链 |
| 135 | 2026-07-15 | **Steamworks Slime Lab 的玩家可见本地多人改为单进程同屏合作**：P1 固定键鼠，独立手柄自动分配 P2–P4；同屏直接运行权威战斗并支持逐槽输入、四卡 HUD、顺序强化、表情控制权和断线冻结 / 最低缺失槽重绑。Steam Lobby 继续一台设备一名玩家，RPC / 快照协议不变；ENet Host/Join 从玩家 UI 移除，仅保留内部双进程协议回归 | 本地游玩不应要求玩家双开进程、填写地址或端口；把设备路由与网络会话分离能提供符合直觉的沙发合作体验，同时不扩大 Steam 混合同屏、协议迁移或 host 模型风险 |
| 136 | 2026-07-15 | **Steamworks Slime Lab 清理残存 smoke 并升级快照 wire 协议**：世界矩形 smoke 按实际 viewport 计算居中设计原点；最长存活纪录仅在显式写入、flush、关闭成功后更新，失败回滚内存值；网络快照保持上层 `Dictionary` schema / signal 不变，底层改为 FastLZ 压缩、最多 900 字节的 unreliable 分片和 64 位递增序号，并将 Lobby `lab_version` 升至 `2` | 居中扩展画布是正确产品行为，不应被绝对坐标测试误报；存档成功必须代表真实落盘；单包 Dictionary 快照已超过 ENet MTU，受限分片与严格重组校验可消除超 MTU 丢包风险，而 wire RPC 不兼容旧版所以必须拒绝 v1/v2 混连 |
| 137 | 2026-07-15 | **Steamworks Slime Lab 将最长存活纪录拆为单人和多人两类**：`PlayMode.SINGLE` 只写单人纪录，本地同屏与 Steam Host / Client 合并写多人纪录；`user://save.cfg` 使用 records schema v2 的两个独立字段，旧混合纪录清空；Records 面板同时显示两行 | 单人和多人难度及协作条件不同，混用一个最长时间会让纪录失去比较意义；同屏与 Steam 统一归入多人可保持界面和存档简单，而清空无法可靠归类的旧值能避免错误继承 |
| 138 | 2026-07-16 | **Steamworks Slime Lab 自动 smoke 统一由仓库 Python runner 编排并隔离玩家数据**：`tools/steamworks_lab_toolchain.py smoke` 负责 Godot 4.7 / Windows console 解析、每进程独立 `user://` 环境、精确成功与错误标志、battle 默认五轮、动态 ENet 端口 / `READY` / 轻量 client 副本、超时清理，并保护真实设置 / 存档与源码 `steam_appid.txt=4955670`；测试 fixture 必须在 `_ready()` 前注入，禁止备份真实玩家文件或临时手写双进程命令 | 之前反复出现 shell 自动变量冲突、退出码误判、成功标志不统一、静态端口 / 缓存锁、真实 `user://` 备份恢复和源码 App ID 被误删等编排问题；把行为固化为可单测 runner、pre-commit 门禁、四平台 skill 与权威文档，可让同仓库后续对话复用同一安全路径 |
| 139 | 2026-07-17 | **Steamworks Slime Lab 仅在 `PlayMode.SINGLE` 增加 AI 队友大招**：P1 子弹命中敌人 / Boss 获得 1 点充能，普通击杀额外 5 点、Boss 击杀额外 20 点，100 点满后按 `E` 召唤持续 10 秒且不可受伤、不吸引火力、自动跟随 / 射击的 AI；玩家须松开再按住 `E` 发起合体，AI 自动同意，合体计入持续时间且每次召唤最多一次；多人、网络、存档与正式 `client/` 不变 | 单人模式原本无法体验合体协作；用战斗表现积攒的短时 AI 援军可提供可控爆发与单人合体入口，而限制为临时、非玩家实体并隔离多人 / wire / 存档，可避免扩大既有协议和持久化边界 |
| 140 | 2026-07-17 | **Steamworks Slime Lab 单人 AI 队友改为确定性自主游击 / 预判闪避**：AI 按敌弹预测轨迹以及普通敌人、Boss、障碍物位置避让并保持游击射击，常规距 P1 使用 210 px 硬限、超过 220 px 触发复位；玩家按 `E` 发起合体后，AI 高速归队至 P1 的 92 px 范围并停靠 0.8 秒再自动同意合体；AI 的不可受伤 / 不吸引火力边界不变，网络、存档和正式 `client/` 不变 | 贴身跟随缺少自主队友的走位与躲避表现；确定性威胁评估和双阈值约束既能强化游击感，又能让 smoke 可复现并避免 AI 脱离玩家，而延迟停靠可让合体归队过程清晰可读 |
| 141 | 2026-07-18 | **项目当前开发、验证和推荐编辑器基线从 Godot 4.7 精确到 Godot 4.7.1 stable**：正式 `client/`、Test Lab 与 Steamworks Lab 统一使用 4.7.1；`project.godot` 的 `config/features="4.7"`、官方 `/en/4.7/` 文档路径和 Steam 工具链 `godot_version="4.7"` minor 系列兼容检查保持不变，不修改运行时代码、工具代码或数据 schema | 当前配置编辑器与匹配 export templates 已是官方 4.7.1，精确记录补丁基线可减少跨机器和 AI 协作时的版本漂移；保留 Godot 的 minor-series 特性标识、官方文档路径和工具补丁兼容语义可避免为无 API 变化的版本更新制造无意义代码改动 |
| 142 | 2026-07-20 | **F13 默认关卡方向由线性手工房间改为 9×9 模块连续大地图**：世界固定 9×9 模块、每模块 11×11 格、单格默认 160 px；关键槽位固定，普通模块从人工批准的 AI JSON 模板池按 `RNG.world` 与 run seed 组合，运行时只维持玩家周围 3×3 模块活跃，以模块级迷雾、核心目标和撤离收束 8–12 分钟短局；ADR #127 的默认方向被本决策取代，ADR #128 仅保留历史，旧线性 RoomManager 在模块世界切默认后移除 | 双层模块 / 格子坐标能同时提供连续探索、确定性组合、AI 内容量产、存档定位与流式性能边界；统一 JSON 让当前 AI 生成和未来人工编辑器共享同一 schema，而固定关键骨架与批准门禁避免全随机地图失去路线节奏和内容质量 |
| 143 | 2026-07-21 | **性能测试改为仅由用户当次明确触发**：`startup-probe`、`perf-probe`、Profiler 和同类性能采样不属于日常修改、提交前检查、常规回归、默认 CI 或 AI 交付义务；现有 probe 保留为按需工具，用户明确要求后才运行并按既有预算判断结果 | 性能采样耗时且不需要在每次实现或回归中重复执行；保留工具可避免未来需要时重建测试能力，同时把是否付出性能验证成本的决定交还用户 |
| 144 | 2026-07-21 | **删除敌人种间交互生态**：敌人只把玩家作为战斗目标，删除猎食 / 逃跑关系、生态 tag、逃跑 action、敌方互伤和 `WarzoneDirector` 的 encounter 元数据；保留五种敌人的追击、快速近战、冲锋、守家、远程行为与无伤害中心分离。`enemy_ai_profiles.json` 和 `warzone_directors.json` 升至 schema v2，run schema 保持 v4，旧快照中的已删除动作在恢复时清空并重新决策；本决策废止 ADR #89 的种间生态部分和 ADR #112 的 encounter 元数据部分 | 模块 JSON 已负责敌人组合与关卡内容，种间生态增加扫描、契约和回归成本，却不能稳定形成玩家可读的收益；固定对玩家目标能保留敌人差异并简化行为边界，同时避免敌人内斗和误伤 |
| 145 | 2026-07-21 | **F14 敌人寻路与感知采用共享流场 + 混合感知**：F13 保持已完成并只提供完整 99×99 静态地形；`ModuleNavigationField` 在玩家跨格时运行确定性八方向 Dijkstra，守家 / 最后已知位置按决策 tick 使用同一 mask 的 `AStarGrid2D`。敌人按 360° 地形视线、路径距离、1.5 秒最后已知位置记忆依次感知，畅通时直追、受阻时绕路；冲锋要求当前感知与清晰走廊，远程攻击要求当前地形视线。`enemy_ai_profiles.json` 升 schema v3，run 保持 v4，开放战区无导航提供者时保留直线兜底 | 模块世界已有稳定格子地形，复用它构建一个全局共享距离场，能避免每个敌人各自持续寻路，也能让“隔墙是否知道玩家”和实际可达距离使用同一套几何；短期记忆与攻击视线门禁避免无限追踪、顶墙和穿墙起手，同时保持现有五类敌人及 ADR #144 的玩家唯一目标边界 |
| 146 | 2026-07-21 | **F14 活动目标共享流场改为感知范围驱动的局部有界重建**：完整 99×99 walkability mask、地形视线和守家 / 记忆 `AStarGrid2D` 保持全图；`GameplayRunLoop` 取已加载 EnemyAI profile 的最大 `sight_radius`，按 `ceil(radius / cell_size) + 2` 计算流场半径，当前为 8 格。玩家跨格时只在最多 17×17 = 289 格的 Chebyshev 窗口内同步运行确定性八方向 Dijkstra，只清理上次实际触达格，并用并行数值数组维护堆；窗口外活动流场查询不可达，全图 AStar 查询不受影响。本决策只修正 ADR #145 的活动流场覆盖范围，run v4 与 EnemyAI schema v3 不变 | 玩家每跨 160 px 就在主线程重算完整 9,801 格并为堆节点创建字典，造成规律性停顿；当前视觉 / 路径感知分别不超过 860 / 560 px，半径 8 已覆盖有效感知并预留两格绕障缓冲，将单次访问上限收敛到全图的约 3%，同时保持同步确定性和回放时序 |
| 147 | 2026-07-21 | **正式客户端固定版本入库并共享启用 `@icons 1.4.0` 与 `Script-IDE 2.2.3`**：只 vendoring 官方发布包的 addon 子目录，保留 MIT 许可与上游署名；仓库内代码作为维护型 fork 遵守项目类型化 GDScript 与 lint，不设目录豁免，版本升级必须人工核对发布包哈希、审查差异并迁移本地补丁 | 两个插件能提升图标检索和脚本导航效率；固定源码与手工升级可让所有开发环境复现同一编辑器能力，并避免自动更新、未经审查的上游变化和第三方代码绕过项目门禁 |
| 148 | 2026-07-22 | **正式玩家摄像机迁移到固定版本 `Phantom Camera 0.11.0.3` 维护型 fork**：从官方发布包仅入库 `addons/phantom_camera/`、保留 MIT 许可并排除 examples；`Player` 子场景内以 GLUED PCam 保持严格居中、水平与等比缩放，玩家有效受伤按 `camera_feedback.json` 触发可关闭位移震屏，噪声走独立 `RNG.camera_fx`；项目固定注册 `PhantomCameraManager`、禁用插件自动更新 / autoload 改写，并显式保持 `Engine.physics_jitter_fix=0.5` | Phantom Camera 为未来的 tween、切镜和多虚拟相机提供统一框架；首片只替换玩家跟随与受伤反馈，保留原有严格居中手感、数据驱动 / Settings / 回放确定性边界，同时避免上游全局引擎设置和自动升级破坏可复现性 |
| 149 | 2026-07-22 | **玩家和敌方子弹默认都受地形阻挡，墙体穿透与伤害目标穿透保持独立**：`Bullet` 用 `hit_radius` 圆形查询做首帧重叠和逐物理帧扫掠，命中模块墙体后移动到安全比例并由 `PoolManager` 回收；`pierce_count` 只控制额外伤害目标，新增 `wall_pierce` 以 `0` / `>0` 表达全地形阻挡 / 忽略，并在发射时快照。run 保持 v4，旧子弹快照缺字段时按不能穿墙恢复 | 默认穿墙会破坏模块地形、掩体和敌弹走位；把两种穿透能力拆开可避免 Buff 语义耦合，同时保留未来通过既有 modifier 管线增加穿墙能力的低成本扩展点 |
| 150 | 2026-07-22 | **正式客户端未来采用 GodotSteam + Talo 的供应商组合且不开发自有通用后端**：GodotSteam 只在 `PlatformServices` 背后承担 Steam 身份 / 成就 / 富状态 / Overlay / Lobby 等平台能力，Talo 只在规划中的 `OnlineServices` 背后承担跨平台身份、排行榜 / 统计、Live Config、事件上传和轻量社交；当前不安装两者、不批准具体在线玩法，Talo Cloud 与官方自托管发行版留到首个功能实施前决策 | 自有门面保留业务与供应商解耦，成熟官方服务避免重复建设身份、排行榜、统计和运维体系；把安装、托管方式和具体功能放到明确触发的后续阶段，可保持当前单机离线、存档、回放和发布链稳定 |
| 151 | 2026-07-22 | **正式输入迁移到固定版本 `G.U.I.D.E 0.14.0` 维护型 fork，并以项目 `InputService` 作为唯一业务边界**：GUIDE 负责物理输入、mapping context、重绑定和提示；`InputService` 负责生成 action 契约、`move` / `aim` `Vector2` intent、context 生命周期、Godot UI 窄桥、设备提示和回放覆盖。业务不得直调 GUIDE / `Input` / `InputMap`；设置配置升 v2 并把旧 `input.*` 迁移到 `user://input_bindings.tres`，Replay file / recording schema 升 v2 且内存兼容 v1 | GUIDE 提供现有轻量 InputMap 方案欠缺的多设备映射、完整捕获、冲突检测和按键图标；项目门面可隔离第三方 API，统一本地玩家、回放、未来 AI / 网络 intent，并通过固定发布包、显式 autoload、本地补丁和无 lint 豁免维持可复现性与可维护性 |
| 152 | 2026-07-22 | **在公开兼容窗口前关闭旧输入格式兼容，只保留 GUIDE 当前绑定与 Replay v2**：Settings v1 继续保留并重写合法普通偏好，但忽略旧 `input.*` 且不再交给 `InputService`；Replay 只接受 file / recording schema v2，v1 与未来未知版本明确拒绝且不改写；八个旧方向 action 从词表和生成契约移除，同名 `input.*` GUIDE binding id 保持稳定。本 ADR 只取代 ADR #151 的 Settings / Replay v1 兼容条款 | 项目尚未公开发行，无需长期背负一次性迁移与旧方向 action；在当前阶段删除兼容分支能缩小输入契约、减少歧义，同时不影响现有 `input_bindings.tres`、普通设置或四条 v2 黄金回放的玩法语义 |
| 153 | 2026-07-23 | **模块制作改为 Godot `.tscn` 编辑主源 + 确定性 JSON/TRES 烘焙 + 运行时预烘焙资源**：16 个模块固定三层 `TileMapLayer` + `Placements`，socket 从边缘 floor 推导；`module_templates.json` 保持审核策略主源，`modules/*.json` 路径不变但成为禁止手改生成物；场景变更把 approved 降为 candidate，显式批准后才回默认池。运行时仍保留 9×9 世界、11×11/160 px、九 chunk、对象池、JSON map hash、导航、run v4 和回放契约。本 ADR 取代 ADR #142 的“JSON 是模块制作主源 / 不做人工可视化编辑”部分 | 运行时代码拼装阻碍人工微调并重复构建 TileMap / 碰撞；把布局与表现放回 Godot 编辑器、把确定性玩法语义和预计算表现资源交给 bake，可同时保持存档 / 回放稳定边界与 3×3 流式上限 |
| 154 | 2026-07-23 | **模块 JSON schema v2 恢复为布局与表现的唯一制作主源，Godot 内部 `Module JSON` 中央主编辑区只可视化编辑 JSON，并建立严格 `JSON → 生成型 TSCN → 运行时实例化` 单向管线**：稳定 tile id 由独立目录映射 Godot TileSet；socket 从边缘 floor 派生；gameplay approval hash 与完整 bake hash 分离。每个模块只生成一份规范 0° TSCN，内含三层 TileMap、合并碰撞、四边封锁和规范 placement 快照；`module-bake-check` 从源数据重建规范指纹而不信任 TSCN metadata，并拒绝遗留方向文件。运行时按 module id 预加载唯一 `PackedScene`，九个 chunk 只挂载缓存实例，再对根节点应用允许的正交旋转、方形枢轴补偿和封边方向反映射；schema v1 等价 gameplay projection 保持旧 map hash、run v4、导航、存档和回放契约。本 ADR 取代 ADR #153 的“`.tscn` 为制作主源 / JSON+TRES 为生成物”部分，并取代 ADR #142 的“不做模块可视化编辑器”边界 | JSON 更适合作为人工与 AI 协作、diff、审核和冲突检测的稳定权威；可视化主编辑区保留人工微调能力且不挤占 FileSystem / Debugger 停靠区，单向构建消除双主源和反向转换歧义。TileMap / 碰撞构建仍在编辑期完成，而复用一个方形模块场景的根节点旋转可把生成物从 61 份收敛为 16 份，不引入运行时烘焙 |
| 155 | 2026-07-23 | **角色与敌人采用数据绑定的专属继承场景，TSCN 管静态表现与稳定节点结构，JSON / CSV 继续管理玩法数值；每种敌人使用独立对象池**：新增 `PlayerBase` / `EnemyBase` 及当前 1 个角色、5 个敌人的真实 Godot 继承场景。`characters.json` 升 schema v2 并新增 `scene_path`；`enemies.csv` 新增 `scene_path` / `pool_prewarm`、删除 `visual_color`，五个 `pool_id` 与敌人 id 一一对应且移除 `enemy_ranged`。不同内容 id 可以复用同一专属 TSCN，但敌人池不得复用。`GameplayRunLoop` 在开局时缓存唯一 actor `PackedScene`，通过 `PlayerHost` 挂载所选角色并按敌人数据动态注册 / 预热五个池；新局默认 `character_default`，续局按 run v4 快照角色恢复，未知角色或无效场景 fail closed。地图、EnemyAI、Combat、run v4 与存档结构不变 | 通用运行时场景无法让不同角色 / 敌人独立手调静态轮廓、颜色和未来动画节点；基础场景继承可复用共享组件，数据 `scene_path` 又允许内容复用同一外观。把玩法数值留在数据、静态表现留在 TSCN，并为敌人隔离池，能同时保留数据驱动、Godot 场景复用与对象池类型安全 |
| 156 | 2026-07-23 | **玩家摄像机 Rig 是对局级场景节点，不属于角色场景**：`GameplayCameraController` 从 `PlayerBase` 移到 `GameplayRunLoop/ActiveWorld`，与 `PlayerHost` 并列且每局只保留一份；RunLoop 在默认角色创建或 run v4 角色恢复后，把 `PlayerCamera.follow_target` 重新绑定到当前 Player。角色基础 / 专属 TSCN 只拥有角色自身组件；缺失或类型错误的 Rig 会阻止启动，续局走既有 `restore_failed`。Phantom Camera 的 GLUED 跟随、严格居中、priority/layer、`teleport_position()`、震屏设置和 `RNG.camera_fx` 全部不变。本 ADR 只取代 ADR #148 的“相机位于 Player 子场景”部分，插件选型与相机行为决策继续有效 | 对局摄像机的生命周期长于可替换角色，把 Rig 固定在 RunLoop 可避免角色切换、重建或续局时复制相机结构，同时让角色继承场景保持单一职责；逻辑 follow target 仍能获得与父子挂载相同的严格跟随行为 |
| 157 | 2026-07-23 | **开始游戏、继续游戏和重开一局统一使用玩家加载流程**：`FormalClientBoot` 立即进入既有 `GameState.LOADING`，由 `UIManager` 显示全屏暗色 `LoadingScreen`、场景内旋转图形和本地化“正在加载…”，至少渲染一帧后再开始重任务；`GameplayRunLoop` 新增准备 / 激活边界，玩家入口用 Godot `ResourceLoader` 线程接口读取 actor 与模块 `PackedScene`，对象池预热、初始模块挂载和续局恢复在主线程分批让帧。准备完成后先移除加载界面再进入 `PLAYING`，失败清理半成品和对象池后回标题；headless、replay、golden 与既有 smoke 保留同步准备。不创建自管 `Thread`，不显示阶段 / 百分比 / 技术信息 / 取消按钮，不人为延时，也不处理应用冷启动 | 玩家当前在开始、继续和重开后的数秒准备期看起来像卡死；统一遮罩和明确激活边界能提供即时反馈、阻止重复请求与提前输入，同时复用 Godot 资源线程和主线程分帧构建，避免扩大线程安全、存档、RNG、地图 hash、回放与工具确定性边界 |
| 158 | 2026-07-24 | **建立数据驱动的统一视觉效果库并全量接入现有内容**：`visual_effects.json` / `presentation_profiles.json` 管理效果与语义绑定，`VisualEffects` 只注册解析，每局 `VfxHost` / `GameplayFeedbackController` 管空间、生命周期、池与跨系统反馈；Player / Enemy 基类固定 Presentation 与六个挂点，现有命中、飘字、受击、退场、技能、临时强化、状态、武器、拾取和机关全部走 cue/profile。`UIManager` 升级为 ENTERING / ACTIVE / EXITING / REMOVED 异步栈并接入共享 UI 动效、正式 ConfirmationModal、焦点恢复与 Loading 移除等待；设置新增 VFX 质量、reduced motion 和闪屏。editor-only `VFX Library` 统一检索、预览、Inspector 选择、内容绑定、模板向导与校验，release 排除。正式效果只用 AnimationPlayer / AnimationTree、Tween / Curve、序列帧、2D 粒子、CanvasItem Shader 和精选程序几何复合模板；不引入离线 3D、AI / DCC 预渲染或节点式 VFX Graph，命中停顿首版不驱动 `GameClock` | 统一数据、运行时、场景挂点和编辑器入口能让 UI / 战斗 / 内容表现复用且可选择，避免业务脚本继续散落颜色计时、Tween、直接实例化和手抄字符串；把程序几何限制为带 Shader / 动画 / 粒子的精选模板，同时把质量、无障碍、对象池与回放 RNG 边界纳入校验，可在不重做现有布局和角色整体美术的前提下提升反馈并保持可维护性 |
| 159 | 2026-07-24 | **在正式 `client/` 内建立 debug/dev_tools 专用“开发者测试岛”**：新增继承 `GameplayRunLoop` 的内部测试用途和场景化三区，复用真实 Player、WeaponSystem、SkillSystem、EnemyAI、Combat、PoolManager、VFX 与 Gear Mod modifier；提供入场前数据驱动配装、固定高生命训练靶、正常 AI、暂停控制面板、无敌 / 免费技能 / 刷新 / 传送 / 重置、死亡自动复位和 5 秒滚动 DPS。开发配置独立写入 `user://debug_test_arena.cfg` schema v1，无效内容回退并诊断；不读取正式背包 / 货币，不保存 / 删除 `meta` 或 `run`，不结算奖励，进入时临时关闭 Replay / Analytics、退出恢复。标题与 `--debug-test-arena` 受 debug/dev_tools guard，调试场景 / 脚本 / smoke 全部动态加载并由 release preset 排除；本用途不新增正式 game mode、save kind、pool id 或 input action | 单个可重复、使用正式系统链路的开发场地能显著降低武器、技能、敌人、Mod、伤害和表现调试成本；把它做成内部 RunLoop 用途并同时隔离存档、回放、埋点和发布资源，可避免测试便利污染玩家进度、正式流程、确定性基线或发行包 |
| 160 | 2026-07-24 | **开发者测试岛只作为独立 debug scene 启动**：`debug_test_arena.tscn` 作为 host 每次先显示配装，再在入树前配置并创建内部 `debug_test_arena_run.tscn`；返回配装销毁并重建 runtime，“退出测试”恢复 Replay / Analytics 并结束当前场景进程。正式标题、`FormalClientBoot` 和正式 `--debug-test-arena` 入口全部移除，项目 lint 强制正式入口零引用，release 资源排除保持不变。本 ADR 取代 ADR #159 的标题 / 正式 CLI / FormalClientBoot 编排部分，其余真实战斗复用、配置、存档、服务和发布隔离决策继续有效 | 测试岛是开发者按需单独运行的工具，不属于玩家启动流程；独立 host 能保留入树前配置、配装重建和服务恢复，同时消除正式菜单、启动脚本与测试资源之间的耦合 |
| 161 | 2026-07-27 | **角色升级为“主英雄 + 子英雄”四槽组合，并以七元素、防御层、能量和基础冲刺取代旧单角色 / 伤害类型基线**：主英雄提供场景、属性、被动和技能 1/2，子英雄只提供强调色和技能 3/4；首版冷静/愤怒禁止同英雄开局，四槽按 `skill_1`～`skill_4` 独立冷却。`element_id` 完全取代 `damage_type` / 物理 / 真实 / 穿甲；玩家采用超盾→普通盾→元素被动+护甲生命层、盾门与独立敌人接触间隔。Meta/Run/Replay 分别升 v2/v5/v3，旧 Run v4 与 Replay v2 明确拒绝，开局选择、HUD、调试岛和四条黄金回放同步新基线，局内换人只留接口 | 主／子组合能用少量英雄形成可读的技能搭配空间，而统一槽位、元素、状态修饰器和防御资源能避免按英雄特判；在公开兼容窗口前一次性删除旧伤害与单角色契约，可减少长期迁移和双轨维护成本 |
| 162 | 2026-07-27 | **玩家描述中的可调数值统一改为配置驱动的命名占位符**：技能 / 被动先取得完整本地化句子模板，再由 `SkillDescriptionFormatter` 从 `skills.json` / `hero_passives.json` 注入数值；技能实际效果与描述共用 `SkillValueResolver` 的强度、范围、效率和持续时间缩放。`*_desc` 不再重复写死伤害、消耗、冷却、范围、持续时间、概率、层数或倍率，数据校验拒绝配置无法提供的 token；格式化不得按内容 id 特判 | 数值平衡会频繁调整，译文再保存一份数字必然产生漂移；让配置成为单一数值来源可使调参自动更新中英文描述，并保证界面承诺与实际结算使用同一缩放公式 |
| 163 | 2026-07-27 | **静域屏障按敌方投射物是否跨越圆周边界判定拦截**：外→内与内→外命中屏障，内→内及不穿圆的外→外放行；敌弹首帧从射手开火位置扫掠到弹体，枪口偏移不能跳过边界，玩家弹继续完全忽略屏障 | 该语义既让盾外火力无法打入、盾内火力无法打出，又允许进入盾内的敌人正常攻击盾内目标；以边界跨越而非“弹体是否在圆内”判定可保持行为直观并封住枪口越界漏洞 |
| 164 | 2026-07-27 | **F13 正式普通槽改为全开放平地，模块敌人改为首次实际进入时从有效空地固化生成计划**：新增 11×11 全 floor、无 placement 的 `module_flat_ground`，正式模板池及非固定 fallback 槽只使用该模板，起点 / 目标 / 撤离固定槽保留；模块 schema 升 v3 并彻底删除 `module_place_enemy_spawn`，世界 schema 升 v2，以 `first_visit_enemy_spawn` 数据配置 4–6 个敌人、1.5 秒地面预警和按局内时间解锁的敌种权重。相邻非封锁模块由“整条 socket 完全一致”改为“旋转后至少一个共同开放边缘格”，仍以完整 99×99 flood-fill 和世界外圈封边守住全局可达性。首次进入非起点槽时按行列稳定顺序从旋转 / 封边后的空 floor 且排除 gameplay placement footprint 的格心用 `RNG.spawn` 无放回抽取，并立即把敌种、位置与剩余预警写入 Run v5 槽位；邻域预加载不触发、暂停 / 卸载冻结、返回不重抽、清空不刷新，Replay 保持 v3 且不提供旧基线兼容 | 敌人摆点属于可重复推导的遭遇逻辑而不是模块静态内容；从真实可行走空地生成可复现计划能让纯平地模块无需维护候选 marker，同时把预警、保存恢复和流式生命周期收束到槽位状态。当前只投放平地模板可以先验证大地图移动与遭遇节奏，又不删除既有模块或技术首片资产 |
| 165 | 2026-07-27 | **武器使用共享 `recoil` 同时驱动固定单发扩散、碰撞安全反向冲量和独立动态震屏**：`weapons.json` schema v3 为每把武器声明 `recoil` 与完整锥角 `spread_angle_max`，根级 `recoil_model` 统一解析公式、时长、速度和 180° 运行时硬上限；每颗弹丸从中心枪口用 `RNG.combat` 独立抽取方向，零扩散仍消耗随机数，多弹丸一次扳机只触发一次后移和震屏。后移与移动叠加并走 `CharacterBody2D` 碰撞 / 世界边界，暂停和 Run v5 保存当前冲量，冲刺只抑制后移；鼠标按固定屏幕中心瞄准，武器震屏使用独立 Phantom Camera emitter，关闭震屏不改变玩法。新增后坐阻尼与扩散稳定两张数据驱动 Gear Mod；Replay 输入格式不变并保持 v3，弹道基线变化直接重录黄金回放 | 一个共享、可配置的武器手感轴能让扩散、身体反馈与镜头反馈保持一致，独立扩散上限 Mod 又保留精度构筑空间；固定 RNG 消耗、无相机噪声瞄准和完整冲量快照保证设置切换、暂停续局与回放仍可确定复现 |
| 166 | 2026-07-28 | **新增模式级 `DifficultyProgression`，以独立威胁时间持续且无上限地强化后续出生敌人的生命与伤害，并提供玩家可见难度标记器**：`difficulty_profiles.json` schema v1 由 `game_modes.json` schema v2 的 `difficulty_profile_id` 引用；标准曲线每 90 秒同时连续增长与阶段跃升，生命使用完整 coefficient，伤害只取增长量的 48%，不改移速、攻击间隔、弹速、AI、奖励、敌人数或预算。模块世界中玩家实际位于 `module_role_start` 时威胁时间暂停，武器与四技能锁定，但移动、冲刺、交互、回复、冷却、敌人与敌弹继续；开放战区立即计时，开发者测试岛默认关闭。敌种 `unlock_time`、模块首次遭遇和旧导演 wave gating 改读威胁时间；敌人在真正生成时快照生命 / 伤害倍率，既有敌人不重算，卸载 / 续局精确恢复。HUD 在小地图下显示 `MM:SS`、等级、九段名称与进度，起点房显示暂停 / 锁定；详细面板、结算、埋点和 Replay v3 摘要使用威胁时间。Run 升至 v6，旧 Run v5 因无法还原起点停留时间与敌人出生倍率而明确拒绝并只删除 run、保留 Meta | 独立时钟能让玩家安全整理初始配置而不偷跑难度，同时不破坏移动、冷却、状态与回放的既有 `GameClock` 语义；出生时快照保证阶段变化可预测、流式恢复精确，也为后续敌人组合与遭遇预算扩展留下稳定入口 |
| 167 | 2026-07-28 | **玩家镜头改为瞄准方向引导，不再始终严格居中**：保留 Phantom Camera `GLUED` 跟随、对局级 Rig、水平等比画面、无相机边界 / drag 和既有震屏；`camera_feedback.json` 升 schema v3 并新增必填 `aim_look`。开局首次有效瞄准前居中；鼠标以光标到玩家实际屏幕位置的向量应用 32 px 死区、30% 比例和 240 px 上限，键盘、手柄与 Replay 使用归一化最终 aim 的 240 px 最大偏移并在松开后保持方向；镜头用 0.18 秒时间常数指数平滑，暂停冻结。稳定引导偏移与 `Camera2D.offset` 震屏噪声分离，开关震屏不得改变引导、弹道或玩法位置；`gameplay.reduced_motion` 当前不影响相机。本 ADR 只取代 ADR #3 / #148 / #156 / #165 中“严格居中”和“鼠标按固定屏幕中心瞄准”的部分，插件选型、Rig 归属、水平等比、震屏与确定性边界继续有效；不新增设置项，Replay v3、Run v6、震屏 profile 数与数据指纹结构不变 | 向瞄准方向让出画面能提高主动射击时的前方信息量；在项目适配层复用现有 Rig、数据与最终 aim，并严格隔离震屏，可用较低实现成本改善观察和瞄准手感，同时保持存档、回放和插件维护边界稳定 |
| 168 | 2026-07-28 | **完整退役“减少动态效果”，所有玩家统一使用正常动态表现**：删除 `accessibility.reduced_motion` 设置、文案、面板控件、运行时策略、UI / 角色 / 飘字 / 难度标记分支、VFX 编辑器预览与两个低动态专用效果；Settings 升 v3，读取 v1/v2 时保留其他合法偏好并清理旧键；`visual_effects.json` 升 schema v2 并拒绝遗留 `reduced_motion` 字段，同时删除 `vfx_motion_policies` 词表与生成契约。`accessibility.screen_flashes`、`video.vfx_quality` 和 `gameplay.screen_shake` 继续独立生效；Run v6、Replay v3 及数据指纹 wire format 不变，但 catalog / contract / locale 变化会更新指纹值。本 ADR 取代 ADR #158 中 reduced-motion 设置与表现策略部分，并关闭 ADR #167 的待删除事项 | 玩家已明确选择不再维护低动态模式；完整删除数据、运行时和编辑器双轨可避免隐藏开关、死资源与文档口径继续漂移，同时用 v3 设置迁移保留现有用户的其他偏好 |
| 169 | 2026-07-28 | **完整删除局内 XP，改为金币驱动等级成长，并把升级选择拆为调用方触发的通用奖励选择**：`GoldProgression` 同时维护可消费 `gold_balance` 与只增不减的 `gold_earned_total`，等级只由累计金币推导；Lv.1→2 需要 100，后续每段 `ceil(previous × 13 / 10)`，无等级上限。玩家归因击杀按 `enemies.csv.gold_reward` 必定生成一个池化 `gold_orb`，不消耗掉落 RNG；消费不降级，一笔跨多级只提示最终等级且不暂停。`RewardChoiceController` 只接受 `PLAYING` 下调用方提供的登记 pool / trigger 与 2–5 候选数，完整预检后用 `RNG.ui_choice` 按稳定 id 加权无放回抽取，进入 `REWARD_CHOICE` 并要求选择；暂停菜单可覆盖，续局恢复原候选而不重抽，`luck` 暂不影响数量、权重或结果。Run 升 v7，v6 的 XP / 旧选择状态明确不兼容并只删除 run、保留 Meta v2；Replay 保持 v3、四条黄金回放重录。本 ADR 取代 ADR #20 中“luck 接候选抽取”、ADR #43 的 XP / 自动升级选择规则，以及 ADR #120 中“保留旧成长池给未来模式”的部分 | 单一金币资源能把击杀、消费与可见成长连接起来，同时用累计量隔离消费带来的降级问题；把奖励选择从自动升级事件中解耦，可供事件、交互和未来系统复用，并让候选数量、暂停与恢复边界由调用方明确控制 |
| 170 | 2026-07-28 | **完整删除敌人通用接触伤害，五种现有敌人统一改为数据驱动的显式攻击**：爆猎者进入 0.65 秒爆炸前摇后立即无敌、关闭实体阻挡并必然在 96 px 内造成 120 中性伤害；只有前摇前的玩家击杀才计击杀并掉落，实际爆炸的爆猎者均无奖励。爆炸按冻结目标快照与 `runtime_spawn_serial` 稳定顺序伤害玩家及视线内敌人；致死未锁定爆猎者会进入完整新一代前摇，同帧不递归。群袭者使用锁向扇区近战，伏击者和壁垒者使用前摇—释放冲撞、线段扫掠与撞墙终止，壁垒者命中后停止并施加独立玩家击退，喷吐者只保留远程投射物。`enemy_ai_profiles.json` 升 schema v4，攻击参数只允许位于攻击 action 的必填 `attack`，`enemies.csv` 删除旧 contact 字段；攻击计时走 `GameClock`，难度只缩放伤害。Run 升 v8 并保存攻击阶段、剩余时间、锁向、命中位、armed、生成序号和玩家击退；v7 Run 明确不兼容，Meta v2 与 Replay v3 wire format 保持。本 ADR 取代 ADR #161 的通用接触伤害 / 独立接触间隔，以及 ADR #166 的接触伤害倍率口径 | 在高密度刷怪下，身体重叠直接扣血缺少可读反制且会把移动 AI 与伤害语义耦合；显式前摇、方向、范围和提交阶段让危险可观察、可闪避、可测试，同时以稳定结算顺序保持连锁奖励 RNG、存档和回放确定性 |
| 171 | 2026-07-29 | **将 `enemy_spitter` 改造为最常见的突击枪手，同时保留全部内部 id、池和场景路径**：玩家可见名称改为“突击枪手 / Assault Gunner”；在 600 px 射程、280 px 保持距离内先锁定玩家当时方向并停止移动，显示 0.32 秒直线预警，再沿固定方向以 0.12 秒间隔点射 4 发，整轮完成后冷却 0.95 秒。每弹 18 中性伤害、440 px/s、720 px 射程、5 px 半径、1.8 秒寿命；起手要求视线，提交后不追踪且墙体继续挡弹，难度只缩放伤害。`enemy_ai_profiles.json` 升 schema v5，远程 attack 必填 `windup/burst_count/shot_interval`；Run 保持 v8，只增量保存 `burst_shots_remaining`，旧 v8 缺字段按空闲恢复，非法点射快照清空并进入一次冷却。新增 `presentation_enemy_rifle`，每轮只发一次 windup cue、每弹发一次 commit cue；共享 `bullet_basic` 根据队伍切换并完整重置玩家黄弹与敌方红弹 / 拖尾，预热 64、容量仍 192。模块与模式完整敌池权重统一为 `100/55/30/20/15`，突击枪手从 0 秒解锁并占 `100/220≈45.5%`；开放战区改为 0 秒、1.35 秒间隔、16 上限并等量下调其他波次，敌人预热重分配为 `12/6/4/3/3`、总数仍 28。金币降为 3，“弹道稳定 Mod”单体掉率降为 0.2%。本 ADR 更新 ADR #126 的当前远程敌人内容与 ADR #170 中“喷吐者仅远程”的现行口径；两条历史原文保留 | 现有远程怪只偶发单发投射物，与弹幕地狱的持续走位压力关系弱；固定锁向预警和短点射能形成可读、可交叉的枪线，把最常见基础怪改为远程压力源，同时通过权重、金币和掉率联动避免抬高成长经济，并保持对象池、Combat、Run v8 与 Replay v3 边界稳定 |
| 172 | 2026-07-29 | **突击枪手保留 0.32 秒锁向停步前摇，但取消提前直线弹道并改为枪口局部红色蓄光；敌弹降至 280 px/s，并以 2.6 秒寿命继续受 720 px 射程先行限制**：`presentation_enemy_rifle.enemy_attack_telegraph` 在 `muzzle` 挂点复用 `weapon_muzzle_flash_default`，使用 `#ff291fff` tint 与 0.35 缩放；远程 windup context 不再提供轨迹 `scale`。四发点射、0.12 秒间隔、0.95 秒冷却、起手视线、锁向后不追踪、墙体阻挡、难度伤害缩放、共享子弹池、出现率与经济全部保持。EnemyAI schema v5、Run v8 与 Replay v3 均不升级。本 ADR 只取代 ADR #171 的直线预警、440 px/s 弹速与 1.8 秒寿命口径，其余决策继续有效 | 最高频远程怪同时绘制长直线会在多枪手交叉时制造过多屏幕引导；保留局部枪口起手提示并降低弹速，可维持锁向点射节奏，同时把反制重点转回可观察、可穿行的慢速红弹 |
| 173 | 2026-07-29 | **新增数据驱动的世界事件系统和五类可摆放交互模块**：`world_events.json` schema v1 定义防御、生存、占点、金币祭坛与血量祭坛；模块 schema v4 新增严格 `{world_event_id}` placement，模块世界 schema v3 每层从五类等权无放回选择三类，各放一次。持续事件由交互启动、全局同时只允许一个，激活模块脱离 3×3 流式范围后继续固定模拟；祭坛不占互斥名额。事件激活时用独立 `RNG.world_event` 按当前解锁敌池、权重和难度固化波次及隐藏奖励；仅防御事件敌人以 1200 HP 专用目标为主目标，仍可伤害路径上的玩家，普通环境敌人不能伤害目标。完成奖励为 70% 即时金币 / 30% 普通 Mod，Mod 只进 `pending_loot`；金币坛递增付费且两次成功 Mod 不重复，血坛通过原子接口按超量盾→盾→生命献祭且至少保留 1 生命。Run 升 v9，保存固定模块、事件状态、目标、计时、占点双进度、波次计划、事件敌人归属、隐藏奖励和祭坛事务进度；旧 Run v8 明确不兼容并只删除 run、保留 Meta v2，Replay 保持 v3并重录黄金基线。ADR #143 的性能 probe 按需规则继续有效 | 可复用、可摆放的交互事件能为纯平原模块提供短期目标、风险与奖励选择；独立 Controller、固定波次计划、专属目标上下文和完整 Run v9 快照把后台模拟、战斗目标切换与事务幂等限制在可测试边界内，避免把复杂状态塞进兴趣点或破坏环境敌人语义 |
| 174 | 2026-07-29 | **人工检查与人工验收项只由人工执行**：任务说明、计划、ADR、GDD、工作包、测试策略、checklist 或验收项凡明确标为人工检查 / 人工验收 / 手动验收 / L5 / 真实设备 / 视觉、听觉或手感验收，AI 不得执行，不得以 GUI、浏览器、Computer Use、模拟输入、截图或录屏替代，也不得自行宣称通过。AI 仍须完成其余自动化验证，只为人工项交付前置条件、步骤、预期现象和已知风险，并标记“待人工验收”；人工回传结果后可由 AI 整理、分析和修复，但验收结论归属于人工，修复后仍交回人工复验。普通“手动运行命令”不自动属于人工验收 | 人类体验、真实设备和主观可读性无法由 AI 工具自证；明确执行归属可以防止自动化观察越权替代人工签字，同时保留完整的机器验证责任 |
| 175 | 2026-07-29 | **敌人金币改为生成时锁定的数据驱动乘法公式，并建立难度 profile 系数接口**：最终金币为 `round(10 × difficulty_coefficient × gold_value_multiplier × reward_specialization_multiplier × (1 + 0.10 × spawn_tier) × RNG.economy[0.9,1.1])`，正数结果至少为 1 并在安全整数上限饱和。`enemy_rewards.json` schema v1 管全局系数，`enemies.csv.gold_value_multiplier` 管怪物价值；独立 `RNG.economy` 只在成功取得敌人池实体后抽取一次，完整计算明细随 Enemy 与 Run v10 保存，死亡不重算。`difficulty_profiles.json` 升 v2，profile 增加双语名称与 `difficulty_coefficient`；该系数同时缩放威胁时间推进和金币公式，当前仅提供标准 `1.0`，启动 / Loading / 重开 / 续局贯通入树前 profile 接口但不增加选择 UI。旧 Run v9 因缺少奖励快照和 economy RNG state 明确不兼容，只清 run、保留 Meta v2；Replay 保持 v3，游戏版本升 `v1.9` | 把敌人价值、难度、特殊化、生成阶段和小幅波动拆成独立倍率，能提高现有金币产出并为未来精英 / 难度选择提供稳定扩展点；生成时锁定和独立随机流避免死亡时重抽、跨阶段漂移或扰动其他掉落与战斗序列 |
| 176 | 2026-07-29 | **完整删除 VFX 质量设置和质量变体接口**：移除 `video.vfx_quality` 设置契约、默认值、枚举校验、面板控件、双语文案及 VFX 编辑器质量预览；Settings 升 v4，读取 v1–v3 时保留其他合法偏好并从重写配置中清除旧质量 key。`visual_effects.json` 升 schema v3，删除并明确拒绝 `quality_variants`，同时删除 `vfx_qualities` 契约、`VisualEffects.resolved_effect()` 和编辑器质量解析 / metadata；`VfxHost` 直接读取唯一正式 effect。闪屏与震屏设置继续独立，Run v10、Replay v3 与游戏版本 v1.9 均不升级。本 ADR 取代 ADR #158 / #168 中 VFX 质量档位与降级接口部分 | 现有全部质量变体均为空，三档设置没有任何实际表现差异；项目不计划以 VFX 降档解决性能问题，保留空设置、schema 字段和编辑器分支只会增加维护、测试和玩家理解成本 |
| 177 | 2026-07-29 | **枪械加入弹匣、备弹、总弹药上限、换弹与敌人弹药掉落，同时重排默认战斗键位**：`weapons.json` 升 schema v4，每把武器必填 `ammo`；基础枪为 30 发弹匣、150 发开局备弹、240 发总上限和 1.2 秒保留余弹换弹。弹匣打空后持续按住不会自动换弹，必须松开再按开火或按 `reload`；R 与开火同 tick 时换弹优先。弹匣和备弹都为零时，重新按开火可无限降级射击，射速 / 弹速各为 50%，寿命反向补偿以保持射程。敌人玩家归因且允许奖励时使用独立 `RNG.ammo` 按连续未掉次数执行 `8/23/38/53/68/83/98/100%`，成功掉落当前武器一个完整弹匣；满弹不抽取也不推进计数。新增池化 `ammo_magazine`，空弹拾取优先直接装匣，溢出丢弃。默认键位改为技能 1–4 使用顶排 `1/2/3/4`，换弹 `R` / 手柄 East，交互 `E` / 手柄 West；绑定 schema 升 v2，旧 v1 自定义绑定回退默认。Run 升 v11，保存弹匣 / 备弹 / 换弹、掉率计数、`RNG.ammo` 和场上弹匣；旧 Run v10 明确不兼容并只删除 run、保留 Meta v2。Replay 保持 v3，但摘要增加弹药状态并重录四条黄金回放；游戏版本升 `v1.10` | 有限弹匣和主动换弹为持续射击加入可读节奏，敌人弹药掉落让推进与补给形成闭环；零弹降级射击避免彻底失去攻击能力，独立随机流、生成提交点和完整快照则保持对象池失败、续局与回放确定性 |
| 178 | 2026-07-30 | **项目 IP 改为代号 `WASD` 下的“集体无意识清理”设定**：完整清理智能由主 / 副智能碎片组合，进入人类集体无意识的一张 9×9 意识层，清理万物形成的心象，击破意识核后撤离；不解释意识层来源、AI 立场和清理结果。每层选择一个只作用于环境装饰方块及色阶的代表色，敌我颜色、弹幕警示色和交互功能色保持稳定；心象必须依靠轮廓、运动、前摇和攻击形状区分。本决策取代 ADR #88 与 #111 的当前世界观和美术结论，旧原文只作为历史保留；正式中英文标题尚未决定，当前统一使用项目代号 `WASD` | 新设定能直接承接现有主 / 副组合、射击、模块世界、意识核目标与撤离循环，同时允许物件、记忆、情绪、语言、习惯等任意内容转化为可扩展敌人与层主题；本轮只重写长期文档，不改变运行时、玩家文案、数据、素材或版本 |
| 179 | 2026-07-30 | **将 ADR #178 的集体无意识设定迁移到玩家可见文案，并明确 AI 的行动目标是清理集体无意识**：正式客户端使用清理智能、主 / 副智能碎片、心象、意识层与意识核；单层目标表述为“清理意识核后撤离”，运行时仍以意识核生命归零开启撤离，不解释删除、整理或转化。第九级威胁显示为“失序 / Disarray”，击败数量显示为“清理 / Cleared”；“标准生存 / Standard Survival”、五只心象个体名、世界事件、七元素占位和开发者测试岛文案保持。标题页只显示项目代号 `WASD`，副标题暂时隐藏。现有 locale key、`character_*` / `ui_hero_*` / `hero_*` / `enemy_*` 内部标识、数据引用、schema、Meta v2、Run v11、Replay v3 与游戏 v1.10 均不改变 | 统一玩家显示与当前 IP 能消除“英雄 / 敌人 / 巢灾”等旧包装残留；保留内部标识、直观射击术语和玩法判定，可在不制造迁移与确定性风险的前提下完成可逆的内容换壳 |
| 180 | 2026-08-03 | **新增 Godot editor-only“数据配表”中央主界面**：用声明式目录统一编辑普通 `client/data` JSON、全部数据 CSV 与 `strings.csv`，提供表格、递归属性、TSV 粘贴、CRUD、Undo/Redo、双语文案联动、技能占位符预览和 1–8 层全局字面搜索；`_contracts.json`、模块 JSON/注册表/图块目录、VFX catalog/profile 明确排除，`module_worlds.json` 继续纳入。未保存修改进入 `user://data_table_editor`，项目保存以 hash、备份、确定性序列化、受控内容契约登记、独立 headless `DataLoader.validate_project_data()` 和全文件回滚组成事务；数据配表与模块 baker 共用 `ProjectDataValidationBridge`。新增数据源若未登记为可编辑、专用或生成来源，smoke 失败；release 排除插件与 editor-only 脚本 | 普通 JSON、平表和双语文案原本分散编辑，层级也不统一；声明式目录和递归兜底能在不强迫所有 JSON 同构的前提下提供一站式配表，而专用模块/VFX 工具继续维护自身强语义工作流。草稿与验证事务可避免未完成或外部冲突数据直接污染项目文件，受控契约白名单则防止策划工具误造代码原语 |
| 181 | 2026-08-04 | **正式共享 `bullet_basic` 改为四控制节点史莱姆圆弹，并统一敌我基础尺寸与速度**：场景只保留一个 `BulletSlimeVisual`，固定保存东 / 南 / 西 / 北四个边缘节点，以向心 Catmull-Rom 一次生成 64 点圆润边界到无材质 `Polygon2D Body + Line2D Rim`；玩家使用 `#DCEAF2 / #F6FBFF`，敌人使用 `#E62935 / #FF5963`，双方完全同形且视觉 / 判定半径均为 12 px、基础速度均为 350 px/s，不使用 Shader、高光、黑边、外发光、拖尾或敌方视觉副本。玩家射程 / 寿命 / 枪口距离为 650 px / 1.9 秒 / 24 px，敌人为 720 px / 2.1 秒 / 24 px；零弹 50% 降级为 175 px/s 与 3.8 秒，仍保持 650 px 射程。`Bullet.configure()`、快照与 PoolManager 外部接口、伤害、射速、四连发节奏、枪口蓄光、独立命中特效、Meta v2、Run v11、Replay v3、数据 schema 和游戏版本不变；通用 RibbonTrail 保留给其他效果，VFX baker 不再补回子弹拖尾并以语义比较避免随机资源 ID 造成无效改写。本 ADR 取代 ADR #172 的敌弹速度、半径、寿命和枪口距离当前口径，其余内容继续有效 | 用户已在 Test Lab 选定四节点史莱姆边圆球并要求落地正式游戏；统一大轮廓与阵营色可提高弹体辨识度，平色内外层保持简洁，单视觉节点与无拖尾边界也降低对象池串色、动态节点增长和判定误读风险 |
| 182 | 2026-08-04 | **取消图片、音频和字体按常见扩展名全局进入 Git LFS，并把当前 199 个、共 54.44 MiB 的 LFS 文件迁回普通 Git blob**：`.gitattributes` 继续用 `binary` 抑制文本 diff，但删除 `*.png`、`*.jpg`、音频和字体等全局 `filter=lfs` 规则；迁移只重新规范化变更前取得的精确当前文件清单，不重写历史、不强推、不删除远端旧 LFS 对象。旧提交继续保留 LFS 指针，检出对应历史版本时可能仍需 Git LFS；未来只有经单独决策的明确大型资产路径才可启用 LFS，禁止再次按常见扩展名一刀切 | 当前资产很少修改，最大单文件约 3.31 MiB；全局 LFS 对这批小型稳定资产的收益有限，却引入对象上传、配额和 pre-push 钩子失败等额外维护成本。保留历史、只迁当前快照能以约 54 MiB 的可接受 Git 增量换取更直接可靠的日常 clone / commit / push 流程 |
| 183 | 2026-08-05 | **将 Test Lab 已人工确认的双涡旋史莱姆接入正式 Player，并把碎片配色收敛为 primary-only**：`player.json` 升 schema v4，新增 `body.radius=25`，视觉、圆形碰撞、`hit_radius()`、地图边界内缩和 `player_separation_radius` 统一 25 px；正式基础武器、`VfxAnchors/Forward/Muzzle` 与常驻朝向短束统一 38 px，敌人枪口仍为 24 px，本条仅取代 ADR #181 的玩家枪口距离。`PlayerSlimeVisual` 使用 20 个持久控制点和每角 5 次有界二次曲线采样形成 100 点边界，移动 / 冲刺与连续 5 点开火冲击只驱动表现，不改变玩法、RNG、碰撞或存档；3 px 外轮廓与短束使用主碎片原始主色，1 px 湿润边使用其提亮派生值。`characters.json` 升 schema v4，每个 palette 必须且只能含 `primary`，冷静为 `#68BCDD`、愤怒为 `#ED2F72`；Resolver 只输出 `main_primary` / `sub_primary`，Shader 双涡旋分别使用双方主色，HUD 名称与槽 1/2 使用主色、槽 3/4 使用副色、能量保持白色。`ActorPresentationController` 优先调用可选 Shader 表现接口，旧 Polygon 敌人继续回退；Run v11、Replay v3、Meta v2 和游戏 v1.10 不变，视觉状态不序列化 | 已验收原型证明双涡旋能在不制作 N×N 场景的情况下同时表达主、副碎片；统一半径与 38 px 发射间隙消除视觉 / 命中 / 枪口错位，有界求解器与 scene-authored 实例资源避免尖刺、节点增长和跨实例串色，primary-only 则删除无实际价值的 secondary / accent 复杂度 |
| 184 | 2026-08-05 | **完全删除正式游戏的枪口闪光及其共享路径**：从 `visual_effects.json` 删除枪口闪光 catalog 条目，从词表 / 生成契约删除专用 VFX 池；`presentation_enemy_rifle` 不再绑定前摇或逐发 impact 视觉，0.32 秒锁向停步前摇、四发点射、伤害、弹速、射程、枪口距离和续局语义保持；`presentation_weapon_default.weapon_fire.effects` 固定为空，只保留 `weapon_recoil_shake`。正式 `Muzzle` 挂点继续服务投射物与通用挂点契约，玩家的 38 px 常驻 `FacingBeam` 和连续五点史莱姆开火形变继续作为角色视觉存在，不属于枪口闪光。Run v11、Replay v3、Meta v2 与游戏 v1.10 不升级。本条取代 ADR #172 的局部枪口蓄光、ADR #181 所述蓄光保持不变，以及任何默认玩家 / 敌人枪口闪光口径 | 用户明确认为该效果破坏角色视觉并要求所有共用敌人一起删除；同时删除 catalog、profile 与对象池可避免死数据和后续误复用，而保留纯玩法信号、计时与镜头后坐可把改动严格限制在枪口视觉层 |

---

## 待决策（详见 `修改建议.md`）
- **D** 子弹手感：初期提速 / 弹道宽容
- **E** 通用奖励池扩展内容：遗物 / 属性 / 主动强化 / 回血 / 刷新等取舍
- **S** 正式中英文标题：当前只使用项目代号 `WASD`，标题页暂不显示副标题（ADR #179）

> 原 J~R 已通过 ADR #20~#28 全部采纳，从 `修改建议.md` 待决策区移走（保留归档）。
