# 决策记录（ADR）

> 轻量架构决策记录：每条写清**决策 + 理由**，让后续 agent 了解既定约束的由来，避免误改。
> 新决策追加到表格末尾，保留历史。

| # | 日期 | 决策 | 理由 |
|---|------|------|------|
| 1 | 2026-06 | 类型定位为俯视角 Roguelike 弹幕生存（以撒 + 吸血鬼幸存者） | 明确核心玩法与灵感来源 |
| 2 | 2026-06 | 引擎确定 **Godot 4.6.3 + GDScript** | 开源轻量、资源文本化、AI 易读写、2D 节点契合需求 |
| 3 | 2026-06 | **玩家恒居屏幕中央**，相机挂玩家下，关 `limit` 与 `drag margin` | 复刻吸血鬼幸存者世界滚动手感，保证严格居中 |
| 4 | 2026-06 | 移动 WASD 8 方向；**射击瞄准方向键 4 方向（上下左右）**；全自动开火 | 操作设计确定，移动与瞄准解耦 |
| 5 | 2026-06 | 遗物/道具采用 **modifiers + behaviors 数据驱动**，逻辑层只实现有限效果原语 | 新增内容=加数据，避免硬编码分支爆炸，利于 AI 量产 |
| 6 | 2026-06 | 数值全部外置到 `res://data/`（JSON），支持热重载 + 字段说明 | 数据与逻辑分离，零代码调参 |
| 7 | 2026-06 | 框架级**多语言本地化**：禁止硬编码文本，一律 `tr("key")` | 后期补本地化成本极高，框架阶段即杜绝硬文本 |
| 8 | 2026-06 | 框架级**设置系统**：`Settings` autoload + 信号即时生效 + `user://settings.cfg` | 统一玩家偏好通路，可扩展、免重启 |
| 9 | 2026-06 | 框架级**数据埋点**：`Analytics` autoload 统一 `track_event` 接口 | 关键节点预留钩子，避免后期补埋点大改业务代码 |
| 10 | 2026-06 | 采纳原建议 E~I（数值格式/遗物架构/平衡数据化/美术规范先行/元进度） | 已落实进设计文档；A~D 仍待决策 |
| 11 | 2026-06 | 建立 **AI 友好工程**：项目索引、词表契约、数据校验、黄金样例、类型化 GDScript、模板、ADR、日志、README | 提升 AI 续写/生成质量，消灭裸字符串与定位成本 |
| 12 | 2026-06 | 设立**元规则**：新规则归档进规则文件、文档与实现保持同步 | 形成"改动→规则归档+文档同步"闭环 |
| 13 | 2026-06 | 加入**暂停功能**：统一 `get_tree().paused` + 菜单 `process_mode=ALWAYS`，暂停键 InputMap action `pause`（可重绑定） | 提供继续/设置/退出菜单，状态控制集中、不散落硬编码 |
| 14 | 2026-06-12 | 仓库重组为 **`docs/` + `client/`（Godot 项目根）+ `server/`（联机/后端预留占位）** 三段结构；工具链文件保留在仓库根 | 文档与实现物理隔离；`server/` 暂占位，启动联机时再补设计；不破坏 Godot 项目结构（`client/` 即 `res://`） |
| 15 | 2026-06-12 | 建立 **AI 协作记忆体系**：`docs/AI记忆/项目记忆.md`（主索引）+ `会话日志/YYYY-MM-DD.md`；维护方式为 AI 主动更新（选项 a），主索引保持精简、细节归会话日志 | 跨会话 / 跨机器 clone 后能直接续接对话，无需翻聊天记录；触发条件与节制原则写在主索引第 9 节 |
| 16 | 2026-06-12 | 引入**录制回放系统**为第四条横向基础设施（GDD 9.9）：`Replay` autoload 录 `seed + 输入序列`；强制确定性约束（随机走 `RNG`、时间走 `GameClock`）；维护黄金回放用于 CI 回归 | Roguelike 平衡迭代必需的回归安全网；确定性必须在框架阶段立住，否则后期补无解 |
| 17 | 2026-06-12 | **平衡测试接口**预留（GDD 9.10）：`AIPlayer` 与 `Spawner`/`MapManager`/`RNG` 都接受外部 seed；MVP 前只锁接口形态，不实现 | 中后期商业化必须有自动化平衡；现在做空中楼阁不划算，但接口走偏后不可回头 |
| 18 | 2026-06-12 | 引入 **AI 协作工程**为第五条横向基础设施（GDD 9.11 + 规则 14-A）：`docs/AI协作/`（任务模板 + 上下文预算 + 角色分工 + 引擎集成 + 实时验证回路）；新增 `docs/AI导航.md` 5.2 系统依赖图；CICD 阶段 4 加 4.L 健康度 / 4.M 回放回归 / 4.N 平衡 sim | 把"AI 怎么协作"工程化，对齐业界 AI 工作流（实时反馈、引擎接入、角色分工），让项目跨人/跨 agent/跨时间可维护 |
| 19 | 2026-06-12 | **AI 记忆自动瘦身**：`项目记忆.md` 写入前自动检查行数；临近 200 行立即按「日 → 周 → 月」层级聚合旧条目；同位置累积内容用覆盖；瘦身本身不算重大变更（不追加 ADR、仅会话日志记一笔） | 把"主索引精简"从约定升级为 AI 自动行为，避免用户每次提醒；规则 14-B 与项目记忆第 9 节同步落地 |

---

## 待决策（详见 `修改建议.md`）
- **A** 松开方向键是否停火（可设置项）
- **B** 瞄准方式是否提供自动瞄准/鼠标瞄准
- **C** 大地图引导：软边界 / 兴趣点
- **D** 子弹手感：初期提速 / 弹道宽容
