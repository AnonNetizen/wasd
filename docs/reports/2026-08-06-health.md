# 2026-08-06 项目健康报告

> **AI 修改说明**：本报告记录 ADR #188 大改后的自动化健康度快照；更新健康结论时必须重新运行对应检查，并同步 `docs/AI知识库索引.md` 与 `docs/_kb_index.json`。

- 时间：2026-08-06（Asia/Hong_Kong）
- 基线提交：`d8ef6edf`（当前工作树包含尚未提交的 ADR #188 实现）
- 范围：Gear Mod 纯局内化、Roguelike 直接通关循环、Meta v3 / Run v13 / Replay v5 / 游戏 v1.12
- 总体结论：绿色；未发现需要阻断提交的文档、契约、数据、lint 或回放健康问题。

## 指标

| 指标 | 当前值 | 目标 | 趋势 |
|------|--------|------|------|
| 裸字符串率 | 0 个未处理项目规则 / 语义 lint 发现；精确分母工具尚未提供 | → 0% | 首次报告，作为基线 |
| 数据驱动覆盖率 | 0 个数据驱动规则违规；`validate_data`、DataLoader schema 与项目规则 lint 全部通过；精确分母工具尚未提供 | → 100% | 首次报告，作为基线 |
| 词表登记率 | contracts 同步检查通过，正式启动加载 51 组契约，未发现未登记 id | = 100% | 首次报告，作为基线 |
| 类型化 GDScript 比例 | 第一档 GDScript lint 0 个问题；精确函数分母工具尚未提供 | → 100% | 首次报告，作为基线 |
| 文档同步度 | `docs_health_check` 0 error | = 0 条死链 | 首次报告，作为基线 |
| AI 知识库健康度 | `_kb_index.json`、任务路由、ADR 矩阵与孤儿文档检查 0 error | 无 error | 首次报告，作为基线 |
| 黄金回放回归通过率 | Replay v5 checked-in golden 4 / 4 通过真实 runtime summary 重放 | = 100% | 100% 基线 |
| ADR / 规则 / 设计同步度 | ADR #188 已同步 GDD v1.55、README、IP、AI 导航、词表、测试策略、模块文档、三层记忆与 Replay 报告；健康检查通过 | = 100% | 100% 基线 |

## 自动化证据

- `pre-commit run --all-files`：12 / 12 hook 通过，包括 contracts、数据、模块 bake、schema、三档 lint 及各自坏样例、Steamworks Lab、文档健康和空白检查。
- `sync_contracts.py --check`、`validate_data.py`、`test_data_loader_schema.py`、三档 lint、`docs_health_check.py`：全部通过；语义 advisory 无项目 warning。
- 四条 Replay v5：全部通过 `replay-runner --rerun-runtime-summary`；数据指纹为 `86434bcc45764303e3db13764a8c607f08b50d8e56c3b4f52baee2c30a86fa22`，模块地图 hash 为 `40d2a0870aa54bc856fdf34002ed6d5dd406ad86be6bcefffd543ae35158552d`。
- `README.md` 与 GDD 当前一致指向 GDD v1.55、游戏 v1.12；`docs/修改建议.md` 当前活动编号 E、S 唯一。

## 风险与边界

- 仓库没有 `tools/health_metrics.py`，因此裸字符串率、数据驱动覆盖率和类型化函数比例只能使用现有 lint 的“0 个未处理发现”做代理值；本报告不把它们虚构为精确统计百分比。
- `module_resource_cache` 与 `module_resource_crossroads` 因奖励语义改变已自动降为 `module_review_candidate`，未被 AI 重新批准；这是待人工玩法复核项，不是自动化健康错误。
- 标题入口、局内掉落 / 升阶反馈、最终构筑、中英文布局和意识核立即通关节奏仍待人工验收；本报告不代替人工结论。
- 按用户要求未运行 performance probe 或 startup probe，也未以单局时长作为门槛。
- Godot headless 退出时仍会输出既有 RID / ObjectDB / resource 清理诊断；所有 runner 均返回通过协议与退出码 0。

## 处理建议

当前没有红色健康项。后续优先补充统一的 `tools/health_metrics.py`，使前三项代理指标可转为可重复的精确统计；两个 candidate 模块只应在人工玩法复核后按现有审批流程重新批准。
