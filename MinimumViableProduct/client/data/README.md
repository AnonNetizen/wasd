# MVP Data

> **AI 修改说明**：修改本文档前先读 `MinimumViableProduct/README.md` 与 `MinimumViableProduct/docs/MVP设计说明.md`。
> 本目录存放 MVP 专用数据配置；不要与根目录完整项目的 `client/data/` 混用。

---

当前 M1 只建立客户端骨架，暂不需要数据文件。M2 / M3 开始实现射击、敌人和刷怪时，可在这里加入 `mvp_config.json`。
