# MVP 代码文档索引

> **AI 修改说明**：修改本文档前先读 `docs/AI协作/文档维护指南.md`、`docs/代码文档规范.md` 与 `MinimumViableProduct/README.md`。
> 本文档是 MVP 隔离实验区的代码文档索引；新增、拆分或删除 MVP 客户端模块文档时，必须同步 `MinimumViableProduct/client/README.md` 与对应脚本的 `# Doc:` 头引用。

---

## 说明

MVP 代码文档放在 `MinimumViableProduct/docs/代码/`，只描述隔离实验区代码契约，不代表完整项目根目录 `client/` 的最终架构。

完整项目正式模块文档仍放在根目录 `docs/代码/`。

## 模块文档

| 模块 | 文档 | 覆盖代码 |
|------|------|----------|
| MVP 客户端闭环 | `mvp_client.md` | `MinimumViableProduct/client/scenes/*.tscn`、`MinimumViableProduct/client/scripts/*.gd` |
