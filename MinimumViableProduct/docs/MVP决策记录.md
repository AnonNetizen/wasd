# MVP 决策记录

> **AI 修改说明**：修改本文档前先读 `MinimumViableProduct/README.md` 与 `MinimumViableProduct/docs/MVP设计说明.md`。
> 本文档只记录 MVP 局部决策；如果某个决策要影响完整项目，必须升级到根目录 `docs/决策记录.md`。

---

| # | 日期 | 决策 | 理由 | 是否考虑迁移到完整项目 |
|---|------|------|------|--------------------------|
| MVP-1 | 2026-06-13 | MVP 放在 `MinimumViableProduct/`，文档与客户端代码都在该目录内 | 隔离最小可行产品，验证工作流和想法，不污染完整项目结构 | 否，作为实验区边界保留 |
| MVP-2 | 2026-06-13 | MVP 玩家不能移动，只能方向键切换四方向射击 | 压缩变量，优先验证射击 / 刷怪 / 复盘工作流 | 仅作为实验约束，不代表完整项目 |
| MVP-3 | 2026-06-13 | MVP 敌人只从上 / 下 / 左 / 右四方向刷新 | 简化刷怪空间，便于快速形成压力和观察射击方向选择 | 可能迁移为早期教学 / 测试场景 |
| MVP-4 | 2026-06-13 | MVP 客户端采用 `MinimumViableProduct/client/` 下独立 Godot 4.6.3 项目 | 可以单独打开、运行和试错，不占用完整项目根目录 `client/` | 否，作为实验区实现边界保留 |
| MVP-5 | 2026-06-13 | M2 方向输入复用 Godot 内置 `ui_up/down/left/right` actions | 先验证四方向射击，不新增自定义输入配置；仍保持 InputMap action 抽象而非硬编码按键 | 可能迁移为“原型先复用内置 action”的工作流经验 |
| MVP-6 | 2026-06-13 | M3 刷怪按上、右、下、左固定循环，不使用随机数 | MVP 当前目标是验证四方向压力和工作流；固定顺序更容易调试和复现问题 | 可能迁移为“原型阶段先确定性再随机化”的经验 |
| MVP-7 | 2026-06-13 | M4 使用 `main.gd` 承担轻量 GameSession，并用内置 `ui_accept` 重开 | MVP 已到可玩闭环，先避免过早拆系统；复盘后再决定是否拆 `game_session.gd` | 可能迁移为“原型闭环优先于架构拆分”的经验 |
| MVP-8 | 2026-06-13 | M4.5 在运行时把键盘方向键、手柄 D-pad、左右摇杆映射到同一组 `ui_*` action | MVP 先验证多输入设备是否能共享四方向瞄准逻辑，不提前引入完整设置系统 | 可迁移为“完整项目手柄支持仍走 InputMap action，设备差异留在绑定层”的经验 |
