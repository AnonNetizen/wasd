# OPENCODE.md —— OpenCode 平台入口

> 本文件只说明 OpenCode 的加载适配；共同规则正文见 `docs/AI协作/项目规则.md`。

开工顺序：

1. 读 `AGENTS.md`。
2. 读 `docs/AI协作/项目规则.md`。
3. 读 `docs/AI协作/快速开工.md`、`docs/AI记忆/current_state.json` 与 `docs/AI导航.md` 相关段落。
4. 按任务读取直接权威文档。

OpenCode 平台文件：

- 配置：`.opencode/opencode.json`
- 规则适配器：`.opencode/rules/game-coding-rules.md`
- Agents：`.opencode/agents/`
- Commands：`.opencode/commands/`
- Skills：`.opencode/skills/`

平台文件可以适配 OpenCode 注册格式，但不能复制、修改或放宽共同规则。
